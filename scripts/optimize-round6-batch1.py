from pathlib import Path
import re


ROOT = Path.cwd()


def replace_section(content: str, heading: str, body: str) -> str:
    pattern = re.compile(rf"(## {re.escape(heading)}\r?\n\r?\n)([\s\S]*?)(?=\r?\n## |$)")
    match = pattern.search(content)
    if not match:
        raise ValueError(f"Section not found: {heading}")
    return pattern.sub(lambda m: f"{m.group(1)}{body.strip()}\n", content)


updates = [
    {
        "file": "content/questions/java/hashmap-thread-unsafe.md",
        "one_liner": """HashMap 只适合单线程或只读场景；一旦多个线程同时 `put`、`resize` 或迁移桶位，就可能出现覆盖、丢数据、读到中间状态，JDK 7 里甚至可能把链表改成环。""",
        "interview": """面试里可以按“写入冲突 -> 扩容迁移 -> 方案替代”这条线回答：

1. `put` 不是原子操作。两个线程同时算到同一个桶位，后写的结果可能覆盖先写的结果。
2. `resize` 会搬迁节点和重连指针，并发执行时容易把链表结构改乱，JDK 7 旧实现里还可能出现环。
3. HashMap 没有锁，也没有为并发写设计的可见性与复合操作保护，读线程可能看到尚未稳定的结构。
4. 真正的解决方案通常是 `ConcurrentHashMap`、只读快照发布，或者外层在更高层次做串行化控制，而不是给 HashMap 临时补一把粗锁。""",
        "followups": """### 为什么很多时候没有报错，但线上还是会出事？

因为 HashMap 的并发问题常常表现为“悄悄写错”而不是立刻抛异常，例如 value 被覆盖、size 统计不准、桶迁移后元素丢失。这类问题比直接报错更难排查，通常要靠压测、对账或最小复现去抓。

### JDK 7 和 JDK 8 在这题上该怎么区分？

JDK 7 需要重点提头插法迁移导致链表成环的经典问题；JDK 8 虽然迁移逻辑改了，但它依旧不是并发 Map，并发写仍然会有覆盖、丢失和中间状态可见的问题。面试里别把“没有成环”误说成“线程安全了”。

### `Collections.synchronizedMap` 和 `ConcurrentHashMap` 怎么选？

前者本质是整张表一把锁，语义简单但并发度低；后者针对并发访问做了更细粒度设计，读写性能和扩容协作都更适合高并发场景。只有在极低并发、快速包一层兼容旧代码时，才会考虑前者。

### 怎么自己证明这题不是只会背？

最有效的方法是写一个小压测：多个线程同时向同一批 key 写入，统计最终条目数、value 覆盖率和 CPU 波动。你要能说出“有时复现不了”恰恰是这题的特点，因为它高度依赖调度时序。""",
        "diagram": """适合画一张并发写入时序图：线程 A 和线程 B 同时命中同一桶位 -> 一个线程先插入，另一个线程覆盖或改写 next 指针 -> 扩容线程开始搬迁 -> 读线程读到中间状态。重点突出三个风险：写覆盖、迁移错乱、旧版成环。""",
    },
    {
        "file": "content/questions/java/hashmap-resize.md",
        "one_liner": """HashMap 扩容不是简单把数组翻倍，而是按新容量重新分布节点；JDK 8 利用“原索引或原索引 + oldCap”的规则拆分链表，减少了重新 hash 的成本。""",
        "interview": """回答这题最好讲成一条完整链路：

1. 触发条件是 `size > threshold`，而 `threshold = capacity * loadFactor`。
2. 真正分配数组通常是懒加载，第一次 `put` 才初始化 `table`。
3. 扩容时容量翻倍，旧桶里的节点要迁移到新数组；JDK 8 根据高位是否为 1，把节点留在原索引或移动到 `index + oldCap`。
4. 扩容本身很重，会触发数组分配、节点搬迁和缓存失效，所以如果预估数量明确，最好提前算好初始容量。""",
        "followups": """### 为什么容量通常要保持 2 的幂？

因为这样可以用 `(n - 1) & hash` 代替取模，性能更稳定；同时扩容翻倍后，节点只需要看某一位是否变化，就能决定去低位链还是高位链，这也是 JDK 8 迁移逻辑能变简单的关键。

### JDK 8 的迁移为什么说“更省”？

旧桶拆分时不必重新完整计算 hash，只看新增的那一位即可；链表还能直接拆成 low/high 两条子链。这不仅减少计算量，也让迁移逻辑更清晰，面试里可以把这点作为和 JDK 7 区分的抓手。

### 什么时候要手动设置初始容量？

批量装载数据、热点缓存预热、一次性导入字典表时都值得提前设置。常见做法是按 `expectedSize / 0.75 + 1` 估算，再交给 `tableSizeFor` 向上取整到 2 的幂，避免刚放进去就触发多次扩容。

### 扩容为什么会拖慢线上请求？

因为它发生在写入线程上，写线程要分配更大的数组并迁移节点；如果表很大或写入高峰叠在一起，接口 RT 会突然抖一下。线上通常通过 GC、热点方法火焰图和吞吐下降去侧面定位。""",
        "diagram": """适合画一张扩容迁移图：旧数组一个桶里挂着多个节点 -> 容量翻倍 -> 按是否命中新高位拆成 low/high 两条链 -> 分别落到原索引和原索引 + oldCap。重点表现“不是全量重算，而是按位拆分”。""",
    },
    {
        "file": "content/questions/java/hashmap-load-factor.md",
        "one_liner": """0.75 不是数学定理，而是 JDK 在空间占用、扩容频率和冲突概率之间做出的工程折中：再小会更浪费，再大链冲突和树化概率都会上升。""",
        "interview": """这题别只背“经验值”，要把它和扩容行为绑起来说：

1. 负载因子决定阈值，公式是 `capacity * loadFactor`。
2. 负载因子小，桶还没怎么用满就扩容，内存浪费但碰撞少。
3. 负载因子大，空间利用率更高，但链表变长、树化概率上升，查询和迁移成本都会变差。
4. 默认 0.75 代表 JDK 认为“大多数通用业务”里，这是时间和空间较平衡的点；优化时通常先调初始容量，而不是先改负载因子。""",
        "followups": """### 为什么不直接把默认值设成 1？

因为那等于尽量把桶塞满再扩容，空间是省了，但碰撞会明显增加。HashMap 的查询效率依赖分布够散，链越长，CPU cache 命中和遍历成本都会变差。

### 什么时候会考虑改负载因子？

极少数对空间特别敏感、并且 key 分布稳定的场景才会考虑；大多数业务更推荐按预估元素量设置初始容量。面试里主动说“生产里改默认负载因子不是第一优先级”，会显得更稳。

### 它和树化阈值有什么关系？

负载因子越大，冲突链变长的概率越高，更容易逼近树化条件；但树化还受桶数组长度限制，不是单纯链长到了就一定变红黑树。这个点能说明你知道 HashMap 在看多个条件，不是单点判断。

### 怎么讲这题才不空？

可以举一个 100 万条数据的缓存例子：如果初始容量太小，会在装载过程中多次扩容；如果乱把负载因子调高，链冲突和 RT 又会上去。把“空间、时间、扩容次数”三个指标一起讲，答案就落地了。""",
        "diagram": """适合画一张负载因子权衡图：横轴是负载因子从低到高，左边标注内存浪费与扩容频繁，右边标注碰撞增多与查询退化，中间 0.75 作为平衡点。再补一条阈值公式和触发扩容箭头。""",
    },
    {
        "file": "content/questions/java/arraylist-linkedlist.md",
        "one_liner": """两者的核心差异不是“一个增删快一个查询快”这么粗，而是数组连续内存和链表节点跳转带来的访问路径、缓存命中率和真实业务成本完全不同。""",
        "interview": """面试里建议直接按“结构 -> 操作 -> 实际选型”来答：

1. ArrayList 基于动态数组，按下标随机访问快，CPU cache 友好。
2. LinkedList 基于双向链表，头尾插删容易，但按索引访问必须走节点。
3. ArrayList 在中间插入删除要搬移元素；LinkedList 虽然改指针便宜，但前提是你已经遍历到目标位置。
4. 真实业务里，大多数场景更常用 ArrayList；只有明确存在头尾频繁操作或需要队列/双端队列语义时，才会考虑 LinkedList。""",
        "followups": """### 为什么很多时候 LinkedList 理论上增删快，实践里却不占优？

因为“找到位置”本身就要遍历，节点又分散在堆上，缓存局部性差。真实系统里内存访问和对象分配成本常常比“改一下指针”更贵，所以面试里不能只背大 O 结论。

### 如果我要实现队列，用哪个更合适？

如果只是 FIFO，优先看 `ArrayDeque`，通常比 LinkedList 更轻；LinkedList 的优势在于它天然实现了 Deque 接口，但每个节点都有额外指针开销。能主动提 ArrayDeque，会比只在两者里二选一更像做过工程的人。

### ArrayList 的扩容要不要顺带讲？

值得提一句。因为很多业务最终选 ArrayList，面试官常会继续问扩容、初始容量和 fail-fast。你可以顺势说“它常用，但也有扩容拷贝成本”，让答案更完整。

### 哪些信号说明你真的应该换成 LinkedList？

比如确实存在大量头尾插删、且元素量不大、又不关心随机访问。这种条件同时满足才更像 LinkedList 的主场；如果只是“可能会增删”，通常还不够。""",
        "diagram": """适合画一张对比图：上半部分是 ArrayList 的连续数组与按索引直达，下半部分是 LinkedList 的节点和前后指针，再从随机访问、中间插删、内存布局三个维度拉对比。重点突出“链表快在修改指针，不快在找位置”。""",
    },
    {
        "file": "content/questions/java/arraylist-fail-fast.md",
        "one_liner": """fail-fast 是迭代器的自我保护机制，不是并发正确性保证；它通过比较 `expectedModCount` 和 `modCount`，在发现结构性修改后尽快抛出异常。""",
        "interview": """讲这题时别只停在异常名上：

1. 集合维护 `modCount`，迭代器创建时保存一份 `expectedModCount`。
2. 遍历过程中如果发现两者不一致，说明集合在迭代器视角外被结构性修改了，就会抛 `ConcurrentModificationException`。
3. 它的目的不是帮你做并发控制，而是尽早暴露错误用法，避免在错误状态下继续遍历。
4. 正确删除通常要用迭代器自己的 `remove()`，或者遍历完成后统一处理。""",
        "followups": """### 为什么说 fail-fast 只是 best effort？

因为它不是靠锁保证的，也不承诺 100% 检出；如果时序、JIT 或遍历路径不同，可能有时抛异常，有时没有。这也是为什么它不能被拿来当并发安全机制。

### 什么叫结构性修改？

像 `add/remove/clear` 这种会影响元素个数或内部结构的操作；而 `set(index, value)` 通常不算结构性修改。这个区别一说出来，面试官就知道你不是只记异常名。

### `subList` 为什么也容易踩坑？

因为它通常共享原列表的 `modCount` 视图。父列表和子列表交叉修改时，很容易在遍历或后续操作中触发异常，这类问题在线上排查时经常比单纯的增强 for 删除更绕。

### 并发读写列表时该怎么做？

如果真有并发访问需求，就该换并发容器、快照方案或更明确的锁策略，而不是指望 fail-fast 报错帮你兜底。面试里这句一说，答案的工程味就上来了。""",
        "diagram": """适合画一张版本检查图：创建迭代器时记录 expectedModCount -> 外部代码 add/remove 导致 modCount 变化 -> 下一次 next/remove 比较版本失败 -> 抛出 ConcurrentModificationException。重点突出“这是检测机制，不是同步机制”。""",
    },
    {
        "file": "content/questions/java/concurrenthashmap.md",
        "one_liner": """JDK 8 的 ConcurrentHashMap 不是“很多段锁”的旧答案了，它更像“空桶 CAS、冲突桶加锁、读多数无锁、扩容可协作”的一套组合拳。""",
        "interview": """这题建议按“读怎么快、写怎么稳、扩容怎么做”来回答：

1. 读路径多数不加锁，依赖 `volatile` 可见性拿到最新桶引用和节点状态。
2. 写入空桶时用 CAS 抢占；遇到非空桶，再锁桶头节点做链表或树节点更新。
3. 冲突链过长会树化，避免热点桶退化成线性查找。
4. 扩容时多个线程可以一起迁移，借助 `ForwardingNode` 标记已迁移桶位，而不是让单线程独自搬完整张表。""",
        "followups": """### 为什么它不允许 `null key/value`？

因为并发场景下 `get(key) == null` 需要明确表示“没有这个 key”，如果允许 null value，就没法区分“key 不存在”还是“存在但 value 为 null”。这是并发语义设计，不只是风格选择。

### `computeIfAbsent` 有什么坑？

回调函数最好保持短小、无阻塞、无副作用。因为它可能在锁保护区附近执行，若里面再做远程调用、再次修改同 map 或抛复杂异常，容易放大锁竞争甚至引出死锁和重入问题。

### `size()` 为什么不能当强一致计数？

高并发下它会通过计数单元近似统计或多次尝试汇总，追求的是性能和可接受精度，而不是事务级绝对一致。如果业务必须精确计数，通常要单独设计计数器或落库方案。

### 它和 `Hashtable` 最本质的区别是什么？

不是“一个新一个旧”这么简单，而是并发控制粒度和读路径完全不同。Hashtable 近似整表大锁，而 ConcurrentHashMap 让无冲突读写尽量并行，这才是它吞吐更高的根因。""",
        "diagram": """适合画一张桶位并发控制图：读线程直接走 volatile table -> 写线程命中空桶 CAS -> 冲突桶锁头节点更新 -> 长链树化 -> 扩容时 ForwardingNode 指向新表并协作迁移。重点突出不同路径使用了不同控制手段。""",
    },
    {
        "file": "content/questions/java/copyonwritearraylist.md",
        "one_liner": """CopyOnWriteArrayList 的关键不是“线程安全”四个字，而是它把读写彻底分路：读看旧快照，写复制新数组，所以只适合读多写少且能接受短暂旧数据的场景。""",
        "interview": """回答时可以抓住三个关键词：快照、复制、写放大。

1. 读操作直接读取当前数组快照，基本不加锁，所以读性能和遍历体验很好。
2. 写操作会先加锁，再复制一份新数组，在新数组上修改后整体替换引用。
3. 这意味着它非常适合监听器列表、规则白名单、配置快照这类“读远多于写”的对象。
4. 代价是每次写入都要复制数组，数据量大或写频繁时，内存和 GC 压力会迅速上升。""",
        "followups": """### 为什么说它读到的可能不是最新值？

因为读线程拿的是某个时刻的数组快照，写线程更新后，旧快照上的迭代器并不会自动切到新数组。这是它换取无锁读取的代价，也是为什么它不适合强实时数据。

### 什么时候不要用它？

高频写、长列表、队列型场景都不合适。比如消息缓冲、订单流转列表、实时排行榜，这些都更关心最新状态或写入效率，放到 CopyOnWriteArrayList 里成本会非常难看。

### 它和 `Collections.synchronizedList` 怎么选？

后者本质是同一份数据加互斥锁，读写都竞争同一把锁；前者让读完全走快照，写再付出复制成本。也就是说，一个是锁竞争重，一个是写放大重，选型要看读写比例。

### 迭代器为什么不能删元素？

因为迭代器持有的是快照视图，不希望你在这个旧视图上再去修改底层结构。能把“快照不可变”这个语义讲出来，比单纯背 API 限制更有味道。""",
        "diagram": """适合画一张快照切换图：旧数组被多个读线程共享 -> 写线程复制成新数组 -> 在新数组上追加/删除 -> 原子替换引用 -> 新读线程看到新数组，旧迭代器仍停留在旧快照。""",
    },
    {
        "file": "content/questions/java/thread-pool-core-parameters.md",
        "one_liner": """线程池参数要按“任务先建线程、再入队、再扩容、最后拒绝”来理解，脱离流转顺序死背参数名，面试一追问就散。""",
        "interview": """面试里可以直接照着 `ThreadPoolExecutor` 的接单流程讲：

1. 运行线程数小于 `corePoolSize` 时，优先创建核心线程执行任务。
2. 到了核心线程数后，新任务先尝试进入 `workQueue`。
3. 队列满了且线程数还没到 `maximumPoolSize`，再创建非核心线程顶上。
4. 线程和队列都吃满后，才进入 `RejectedExecutionHandler`。
5. `keepAliveTime` 控制的是非核心线程空闲多久回收，是否连核心线程都超时回收，还要看 `allowCoreThreadTimeOut`。""",
        "followups": """### 为什么很多人把 `maximumPoolSize` 讲错了？

因为它不是“线程池一直能开到的常态人数”，只有在队列放不下任务时才会继续扩容。如果你用的是无界队列，很多情况下最大线程数几乎形同虚设，这是高频误区。

### CPU 密集和 IO 密集该怎么配？

CPU 密集更关注核心数附近，避免线程过多引发切换；IO 密集可以适当放大，但前提是明确瓶颈在等待而不是 CPU。本质上是根据任务画像配，不是背一个万能公式。

### 线上怎么判断线程池是不是配小了？

常看几个信号：队列长度长期堆积、活跃线程接近上限、拒绝次数上涨、接口 RT 拉长、调用线程被 CallerRuns 拖慢。这类指标一讲，面试官就会觉得你真看过监控。

### 为什么要自定义线程工厂？

至少要给线程起可识别名字，方便日志、jstack、监控平台定位；很多团队还会顺手设置守护线程标记、异常处理器和业务标签。这是很容易落地的工程细节。""",
        "diagram": """适合画一张任务流转图：新任务进入 -> 判断是否小于 corePoolSize -> 否则入 workQueue -> 队列满后再看 maximumPoolSize -> 仍不行则触发 rejection handler。旁边补两条注释：keepAliveTime 只回收空闲非核心线程、无界队列会削弱 maximumPoolSize 的作用。""",
    },
    {
        "file": "content/questions/java/thread-pool-rejection-policy.md",
        "one_liner": """拒绝策略不是背四个类名，而是要说明系统满载时你是抛错、反压、丢新任务还是丢旧任务，以及业务能不能承受这个后果。""",
        "interview": """建议按“默认行为 + 业务取舍”来答：

1. `AbortPolicy` 直接抛 `RejectedExecutionException`，最利于显式失败。
2. `CallerRunsPolicy` 让提交线程自己执行任务，相当于把压力反推给上游。
3. `DiscardPolicy` 静默丢弃，除非业务天然允许丢，否则风险很高。
4. `DiscardOldestPolicy` 会丢队列中最旧的任务，更像为“过期数据不值得做”这类特殊场景准备。
5. 真正上线时，拒绝策略通常要配合监控、日志和自定义降级动作，而不是只靠 JDK 默认值。""",
        "followups": """### 为什么很多业务更偏向显式失败？

因为抛错最容易被调用方感知，也便于监控和补偿。静默丢弃虽然“看起来没报错”，但最容易把数据丢失变成黑洞，后续追查成本反而最高。

### `CallerRunsPolicy` 为什么能形成反压？

提交任务的线程被迫亲自执行，提交速度自然会慢下来。如果上游就是 Web 线程或消息消费线程，这种拖慢会直接传回入口，从而限制继续灌任务的速度。

### 可以无限重试提交吗？

通常不行。线程池已经在告诉你容量打满了，这时无脑重试只会继续放大拥塞。更合理的是记录拒绝次数、快速失败、限流、降级，或者切到备用通道。

### 自定义拒绝策略时最该补什么？

至少补拒绝计数、结构化日志和告警指标；如果任务允许延迟执行，再补缓存队列、补偿表或降级逻辑。能把“失败后怎么办”说出来，这题就不再只是 API 记忆。""",
        "diagram": """适合画一张满载分叉图：任务进入线程池 -> 线程与队列都已满 -> 分成 Abort、CallerRuns、Discard、DiscardOldest 四条支路，每条支路旁边标业务含义和主要风险。重点突出“拒绝本身就是容量保护”。""",
    },
    {
        "file": "content/questions/java/threadlocal.md",
        "one_liner": """ThreadLocal 的本质是“数据挂在线程身上”，不是挂在 ThreadLocal 对象自己身上；线程复用时不清理，最先出事的通常不是 OOM，而是上下文串用。""",
        "interview": """这题面试里可以按“存哪儿、为什么泄漏、怎么规避”三步答：

1. 每个线程对象里都有自己的 `ThreadLocalMap`，value 存在线程里，不是存在 ThreadLocal 实例里。
2. key 是对 ThreadLocal 的弱引用，ThreadLocal 被回收后，key 可能变成 `null`，但 value 还可能挂在线程上。
3. 在线程池里线程寿命很长，如果你只 `set` 不 `remove`，就会出现脏数据串用和对象长期不释放。
4. 规范做法是在 `try/finally` 里清理，并尽量别往里面塞大对象或复杂业务状态。""",
        "followups": """### 为什么说线程池比新建线程更危险？

因为新线程执行完就结束，残留数据有机会随线程一起回收；线程池线程会被反复复用，同一个线程下一个请求还能看到上一个请求留下来的值，这比“可能泄漏”更早、更常见。

### key 是弱引用，为什么 value 还会留住？

弱引用只会让 key 在 GC 后变成 null，但清理 stale entry 依赖后续访问触发；如果线程长期存活又很少触碰对应槽位，value 仍然可能挂着。这正是很多人对“弱引用就不会泄漏”的误解。

### 它适合放哪些东西，不适合放哪些东西？

适合请求级上下文、traceId、租户标识这类“线程内临时元信息”；不适合大对象、跨线程共享状态、需要强一致传递的核心业务数据。面试里把“合适和不合适”都说出来，会更完整。

### 线上怎么排查 ThreadLocal 问题？

常见信号是用户串号、日志 trace 混乱、线程池线程堆积大对象、heap dump 里线程引用链异常长。你能说出这些现象，答案就会从原理题变成排障题。""",
        "diagram": """适合画一张引用关系图：线程对象持有 ThreadLocalMap -> Entry 的 key 是弱引用 ThreadLocal，value 是实际上下文对象 -> 线程池复用线程时，下一个请求复用旧 value -> finally remove 清理。重点突出“值在线程里，不在线程变量对象里”。""",
    },
    {
        "file": "content/questions/java/threadlocal-inheritable.md",
        "one_liner": """InheritableThreadLocal 只能在“创建子线程”那一刻拷贝父线程上下文，碰到线程池复用时，它既不会自动重新继承，也不会自动替你清理旧值。""",
        "interview": """回答时把“新线程”和“线程池线程”分开讲最清楚：

1. 新建子线程时，父线程里的 inheritable 值会复制到子线程。
2. 这个复制动作发生在线程创建阶段，而不是每次提交任务时。
3. 线程池里的工作线程通常很早就创建好了，后续任务不会重新继承父线程最新值，所以很容易串请求、串 trace、串租户。
4. 真要做线程池上下文透传，优先考虑显式参数、任务装饰器或专门的上下文传递方案，而不是直接押注 InheritableThreadLocal。""",
        "followups": """### 为什么很多人明明用了它，在线程池里还是拿不到新值？

因为他们误以为“提交一个任务就等于新建一个线程”。实际上工作线程早就存在了，没有新的继承动作发生，所以你在父线程后续 set 的内容，并不会自动进到池线程。

### 父线程后来又改值，子线程会同步更新吗？

不会。默认语义更像一次性拷贝，而不是绑定联动。把这一点说出来，能很好地区分“继承”与“共享”的概念。

### 和 `TransmittableThreadLocal` 有什么关系？

TTL 本质上是通过包装任务，把提交时的上下文捕获下来，再在执行前后设置和清理，弥补线程池复用场景下 InheritableThreadLocal 的空档。面试里提到这个方向会比较加分。

### 最后的落地建议是什么？

优先用显式参数传关键业务数据；只有在链路日志、MDC、traceId 这类横切上下文里，才慎用上下文透传工具，并且必须配套清理策略。""",
        "diagram": """适合画一张对比图：左边是父线程创建新子线程时复制 inheritable 值；右边是线程池预先创建工作线程，后续多个任务复用同一线程导致旧值残留。重点突出“继承发生在线程创建，不发生在任务提交”。""",
    },
    {
        "file": "content/questions/java/completablefuture.md",
        "one_liner": """CompletableFuture 的价值不在“能异步”，而在“能把并行、串联、合并、异常和超时编排成一条可读链路”，它本质上是异步编排工具，不是线程池替代品。""",
        "interview": """这题可以按“什么时候用、怎么串、要避开什么坑”来回答：

1. 它适合聚合多个独立调用、缩短总等待时间，例如首页并行查用户、订单、优惠券。
2. `thenApply` 适合结果转换，`thenCompose` 适合依赖下一个异步任务，`thenCombine/allOf` 适合合并多个分支。
3. 它能把异常处理、超时控制和结果兜底写在同一条链上，比手写回调或 Future 阻塞等待更清楚。
4. 但它不是无限提速器，线程池隔离、下游容量、上下文透传和超时取消必须一起考虑。""",
        "followups": """### 为什么说它不是“多开几个线程就完了”？

因为真正的复杂点在编排，而不是启动线程本身。多个任务之间是并行、依赖、竞速还是兜底，异常和超时怎么收敛，这些才是 CompletableFuture 的核心价值。

### 默认线程池能不能直接用？

小 demo 可以，线上要谨慎。默认的 `ForkJoinPool.commonPool()` 容易和别的任务共享资源，IO 阻塞任务尤其不适合直接扔进去。能主动说“按任务类型隔离线程池”，答案会成熟很多。

### 哪些场景不适合它？

强事务链路、需要严格顺序保证、调用方本身已经有成熟响应式/事件流模型时，都不一定适合盲目上 CompletableFuture。异步化之后补偿和排障成本也会更高。

### 线上怎么验证异步编排真的带来了收益？

看聚合接口的 RT、线程池活跃数、队列积压、超时率和下游依赖的吞吐。只说“理论上更快”不够，最好能补一句“并行前后指标怎么变化”。""",
        "diagram": """适合画一张异步编排图：请求进入 -> 同时发起用户/订单/优惠券三个 Future -> 各自转换结果 -> allOf 汇总 -> 统一异常/超时收敛 -> 返回聚合响应。重点突出“并行分支”和“收口点”。""",
    },
    {
        "file": "content/questions/java/completablefuture-exception.md",
        "one_liner": """CompletableFuture 的异常不会像同步调用那样直接冒到当前调用栈，必须在编排链上显式决定是记录、转换、兜底还是继续向后传播。""",
        "interview": """这题最好把三个常用 API 的角色分开讲：

1. `exceptionally` 更像失败兜底，出错时给一个替代值。
2. `handle` 不管成功失败都会进，适合统一转换结果。
3. `whenComplete` 更适合做日志、指标、埋点观察，不等于把异常吞掉了。
4. 最后如果用 `get()` 或 `join()` 收口，还要知道一个包 `ExecutionException`，一个包 `CompletionException`。""",
        "followups": """### 为什么很多异步异常上线后才暴露？

因为代码里可能根本没有 `join/get`，或者日志只打在 `whenComplete` 里却没真正处理。异常被留在 Future 链里，业务主线程不一定第一时间感知。

### `allOf` 里一个分支失败会怎样？

整体 future 会以异常完成，但你如果想保留部分成功结果，就要在各分支自己兜底，或者把结果包装成成功/失败对象后再汇总。这是“聚合接口要不要部分降级”的关键设计点。

### 超时和取消该怎么讲？

可以补 `orTimeout`、`completeOnTimeout` 和 `cancel` 的语义差别：一个是超时报错，一个是超时给默认值，一个是主动取消。把这三个点拉开，答案就不再像模板。

### 最佳实践是什么？

在线路末端统一收敛异常，重要分支打指标和结构化日志，线程池按 IO/CPU 分离，必要时加超时和熔断。异步最怕“错了没人知道”，所以观测和收口比语法更重要。""",
        "diagram": """适合画一张异常收敛图：异步任务 A/B/C 并行执行 -> 某个分支抛错 -> exceptionally 做兜底、handle 做统一转换、whenComplete 记录日志 -> 最终 join/get 收口。重点突出“观察不等于恢复”。""",
    },
    {
        "file": "content/questions/java/blockingqueue.md",
        "one_liner": """BlockingQueue 的重点不是“线程安全队列”这五个字，而是它把生产过快和消费过快都变成了可控的阻塞语义，是典型的背压与解耦组件。""",
        "interview": """回答时建议按“阻塞语义 + 实现差异 + 典型场景”来讲：

1. 队列空时，消费者拿不到数据会阻塞；队列满时，生产者放不进去也会阻塞。
2. 这种阻塞语义天然适合生产者-消费者模型，也正是线程池工作队列能削峰填谷的基础。
3. `ArrayBlockingQueue` 是有界数组队列，内存更可控；`LinkedBlockingQueue` 常用于链表队列；`SynchronousQueue` 则根本不存元素，强调直接交接。
4. 真正选型时，边界容量、吞吐和延迟目标要一起看，不是看到 BlockingQueue 就一把梭。""",
        "followups": """### 为什么有界队列比无界队列更值得关注？

因为无界队列会把压力一直往内存里吞，短期看“没有拒绝”，长期看可能把 RT、堆内存和 GC 都拖坏。有界队列更像明确告诉系统：这里能承受的缓冲就这么多。

### `SynchronousQueue` 有什么特殊之处？

它不真正缓存元素，生产和消费必须一一配对交接。在线程池里配合较大的最大线程数时，常表现为“来一个任务就尽量找线程直接接”，这和普通排队队列的行为很不一样。

### `put/take` 和 `offer/poll` 怎么区分？

前者强调阻塞等待，后者更适合超时或非阻塞尝试。面试里能把 API 语义和使用场景绑起来，比只背名字更像实战。

### 线上怎么判断队列成了瓶颈？

看队列长度长期高位、生产端阻塞时间、消费吞吐、任务超时率和下游处理能力。如果只看“有没有元素”，很容易低估队列带来的延迟放大。""",
        "diagram": """适合画一张生产者-消费者图：生产者向有界队列放任务，满了时阻塞；消费者从队列取任务，空了时阻塞。旁边补 ArrayBlockingQueue、LinkedBlockingQueue、SynchronousQueue 三种实现的行为差异。""",
    },
    {
        "file": "content/questions/java/aqs.md",
        "one_liner": """AQS 不是某一种锁，而是一套“共享状态 + 等待队列 + 获取/释放模板方法”的同步器骨架，ReentrantLock、Semaphore、CountDownLatch 都是在它上面长出来的。""",
        "interview": """这题最好按“state、队列、模板方法”三件套来答：

1. `state` 表示同步状态，子类通过 CAS 或特定规则修改它。
2. 获取资源失败的线程会封装成 Node 挂到等待队列里，随后 park 挂起。
3. 资源释放后，AQS 会按规则唤醒后继节点继续竞争。
4. 子类不是重写整个并发框架，而是重点实现 `tryAcquire/tryRelease` 或共享模式下对应方法，所以它非常适合做锁和同步器模板。""",
        "followups": """### 为什么说 AQS 是“框架”而不是“锁”？

因为它并不直接决定你是独占还是共享、可重入还是不可重入、公平还是非公平；这些都是子类在 state 语义和获取释放规则上再定义出来的。把这层抽象讲出来，答案就立住了。

### CLH 队列在这题里要讲到什么程度？

不用把每个字段都背全，但至少要知道它是一个等待线程的双向队列，节点拿不到锁会挂起，前驱释放后再被唤醒。能说出 `waitStatus`、前驱/后继关系，就已经比纯背概念更深一层。

### 公平锁和非公平锁在 AQS 里怎么落地？

核心差别在于获取资源前是否先看队列中有没有前驱节点，例如 `hasQueuedPredecessors()`。这句要是能自然说出来，面试官一般会点头。

### `Condition` 和同步队列是什么关系？

Condition 有自己的等待队列，线程先从同步队列获取锁，调用 `await` 后转去 Condition 队列，`signal` 再转回同步队列重新竞争。这个过程很适合拿来接深挖。""",
        "diagram": """适合画一张 AQS 骨架图：顶部是 state，左边是 tryAcquire/tryRelease 模板方法，下面是等待队列，线程获取失败后入队并 park，释放后唤醒后继节点。再标注独占模式和共享模式两个分支。""",
    },
    {
        "file": "content/questions/java/synchronized-reentrantlock.md",
        "one_liner": """`synchronized` 和 ReentrantLock 的差别不只是“一个关键字一个类”，而是一个更偏 JVM 内置、简单稳妥，一个更偏 AQS 显式控制、能力换复杂度。""",
        "interview": """回答时建议从“能力、成本、场景”三层对比：

1. `synchronized` 语法简单，异常时 JVM 会自动释放锁，适合大多数普通同步代码块。
2. `ReentrantLock` 需要手动 `lock/unlock`，但支持可中断获取、定时尝试、公平锁和多个 Condition。
3. `synchronized` 走 JVM monitor 机制，ReentrantLock 基于 AQS，自定义能力更强。
4. 如果只是保护一个临界区，优先用 `synchronized`；只有在需要高级特性或更细致的排队控制时，再上 ReentrantLock。""",
        "followups": """### 为什么现在很多场景下不再简单说 ReentrantLock 一定更快？

因为 JVM 这些年对 `synchronized` 做了很多优化，轻量级锁、锁消除、锁粗化等都让它在普通场景下足够好。面试里主动说“别再背老版本性能神话”，会显得很清醒。

### `lockInterruptibly()` 有什么实际价值？

它允许线程在等待锁时响应中断，适合做可取消任务、超时控制或避免长时间死等。这个点是 ReentrantLock 和 synchronized 很常见的分水岭。

### 多个 `Condition` 为什么有用？

你可以把不同等待条件分成不同队列，例如“队列非空”和“队列未满”各用一个 Condition，避免所有线程都挤在同一个 `wait/notifyAll` 语义里。

### 最容易踩的工程坑是什么？

忘记在 `finally` 里 `unlock()`。这也是为什么很多简单业务宁愿选 synchronized：能力少一点，但不容易把自己锁死。""",
        "diagram": """适合画一张对比表 + 流程图：左边列 synchronized 的自动释放、JVM monitor、简单语法；右边列 ReentrantLock 的显式加解锁、可中断、公平锁、多个 Condition。底部补一句选型原则：简单场景优先前者，高级控制再选后者。""",
    },
    {
        "file": "content/questions/java/reentrantreadwritelock.md",
        "one_liner": """读写锁的价值在于把“很多读可以并行、写必须独占”这件事显式表达出来，但只有在读多写少且临界区足够重时，它的收益才会真正跑出来。""",
        "interview": """这题别只背“读多写少”四个字，最好把适用条件说完整：

1. 读锁之间可以并发，写锁和所有其他锁互斥。
2. 它适合配置中心缓存、路由表、字典表这类读远大于写，且单次读取不是极轻量的共享数据。
3. 如果写很多、临界区特别短，读写锁本身的管理成本和竞争切换可能盖过收益。
4. 还要知道锁降级可以做，锁升级通常不建议直接做，否则很容易把自己卡住。""",
        "followups": """### 什么叫锁降级，为什么常被提？

典型做法是先拿写锁更新数据，再获取读锁，最后释放写锁，让当前线程以读者身份继续持有最新视图。它常用于缓存刷新后继续读取的场景，是面试里很爱追问的细节。

### 为什么锁升级容易出问题？

如果线程先持有读锁，再想升级成写锁，可能和其他读线程互相僵持，造成等待链。很多实现并不鼓励你这么干，通常更推荐释放读锁后重新按写路径竞争。

### 和 `StampedLock` 怎么比？

StampedLock 提供乐观读，适合读非常多且愿意做校验回退的场景；ReentrantReadWriteLock 语义更传统、可重入、使用门槛低。主动说出这个对比，会显得你知道不止一种读写控制方案。

### 线上怎么判断它值不值得上？

看读写比例、锁等待时间、吞吐变化和 CPU 消耗。不要因为“听起来高级”就直接替换 synchronized；读写锁是有管理成本的。""",
        "diagram": """适合画一张资料室模型图：多个读线程可同时进入，写线程到来时必须等待所有读者离开并独占进入。旁边再补两条箭头：写锁降级到读锁、读锁升级到写锁的风险提醒。""",
    },
    {
        "file": "content/questions/java/atomic-classes-cas.md",
        "one_liner": """Atomic 类的原子性来自 CAS 重试和内存语义，不是来自“没有锁就天然安全”；它擅长单变量无锁更新，但在高冲突和复合状态场景下并不是银弹。""",
        "interview": """这题建议按“怎么成功、为什么会失败、什么时候不用”来讲：

1. CAS 会拿内存里的当前值和期望值比较，相等才更新成新值。
2. 不相等说明中途被别的线程改过，当前线程就重试或失败返回。
3. AtomicInteger、AtomicLong 等就是把这套无锁更新封装成常用 API。
4. 它对单变量计数、状态位切换非常好用，但高竞争下会自旋烧 CPU，跨多个字段的一致性也不是它能直接解决的。""",
        "followups": """### 为什么它“无锁”却不等于“没有成本”？

因为失败线程会一直重试，竞争激烈时 CPU 会被自旋吞掉。也就是说，Atomic 省掉的是阻塞/唤醒成本，不是所有成本；这个取舍一定要讲出来。

### 和 `synchronized` 怎么选？

单变量轻量更新优先考虑 Atomic；涉及多个字段一致性、复杂条件检查或需要阻塞等待的场景，锁往往更清晰。能主动说“不是所有并发问题都该用 CAS”，答案会更稳。

### 为什么 LongAdder 也会被拿来和 AtomicLong 比？

因为在高并发计数场景下，LongAdder 通过分散热点降低 CAS 冲突，吞吐往往更高；但它换来的是求和成本和弱一点的实时一致视角。这个对比很有工程味。

### ABA 问题是不是 Atomic 类都躲不过？

普通 CAS 都可能遇到，只是很多计数场景里 ABA 没那么敏感；如果业务真关心“中间是否被改过”，就要上带版本戳的方案，而不是只看最终值相等。""",
        "diagram": """适合画一张 CAS 循环图：读取当前值 -> 与 expected 比较 -> 相等则写新值，不等则重新读取并重试。右侧补两个旁注：高竞争自旋成本、ABA 需要额外版本号。""",
    },
    {
        "file": "content/questions/java/volatile-visibility.md",
        "one_liner": """`volatile` 解决的是“看不见”和“乱排序”问题，不解决“多个线程同时改同一份状态”的原子性问题，所以它更像可见性公告牌，不像互斥锁。""",
        "interview": """面试里把三个关键词讲顺就够了：可见性、有序性、非原子性。

1. 一个线程写了 volatile 变量，其他线程随后读取时能看到最新值，这是可见性。
2. 对 volatile 读写前后会插入内存屏障，限制特定重排序，这是有序性。
3. 但像 `count++` 这种读改写复合操作，volatile 仍然保不住原子性，所以不能拿它代替锁或原子类。
4. 它更适合状态开关、停止标记、双重检查锁里的实例引用发布等“单点状态信号”场景。""",
        "followups": """### 为什么 `count++` 用了 volatile 还是会丢数据？

因为这不是一次原子写入，而是读、加、写三个步骤。volatile 只能保证每一步读到的是最新值，不能把三步打包成不可分割的整体。

### 它和 synchronized 最大区别是什么？

volatile 只保证看见和顺序，不保证互斥；synchronized 同时能提供可见性和原子临界区保护。把这个边界讲清，是这题最核心的加分点。

### 双重检查锁为什么一定要配 volatile？

因为对象创建可能发生指令重排，导致“引用已赋值但对象还没完全初始化”。volatile 能禁止这种关键重排，保证其他线程拿到的是完整对象。

### 线上怎么发现自己把 volatile 用错了？

最常见现象是开关变量没问题，但计数、库存、状态流转偶发不一致。也就是说，它能解决“停不下来”，解决不了“同时改乱了”。这个区分很重要。""",
        "diagram": """适合画一张对比图：左边是线程 A 写 volatile 后刷新主内存，线程 B 读取时看到最新值；右边是两个线程同时做 count++ 仍然发生丢失更新。重点突出“看得见不等于改得对”。""",
    },
    {
        "file": "content/questions/java/cas-aba.md",
        "one_liner": """CAS 的核心是“比较当前值是否还是我刚看到的那个”，而 ABA 问题说明“值看起来没变”并不代表“这段期间真的没人动过它”。""",
        "interview": """这题建议按“CAS 本身 -> ABA 风险 -> 解决思路”来答：

1. CAS 先读取旧值，再比较当前值是否仍等于旧值，相等就更新，不等就重试。
2. ABA 的问题在于：值从 A 改成 B，又改回 A，CAS 只看结果，会误以为没有变化。
3. 这在只关心“当前值对不对”的场景里不一定是问题，但在关心“中间有没有被别人拿走/改过”的场景里就很危险。
4. 常见解决办法是把版本号一起比较，例如 `AtomicStampedReference`，让值和时间戳共同参与校验。""",
        "followups": """### 为什么很多计数场景很少提 ABA？

因为加减计数更关心最终结果，不太关心中间是不是绕了一圈；但链表节点出栈、资源状态切换、对象复用这类场景就不一样，中间过程往往有业务含义。

### 带版本戳的方案成本是什么？

它多维护了一份标记或版本号，更新逻辑也更复杂。也就是说，ABA 不是“看到就一定上版本戳”，而是看中间变化对业务有没有意义。

### 只用锁能不能避开 ABA？

可以，因为锁把整个读改写过程包成了一个临界区；但代价是阻塞和唤醒成本更高。能把“CAS + 版本戳”和“直接上锁”的取舍一起说出来，答案会更完整。

### 面试里最容易说空的地方在哪？

就是只会复述“A 变 B 再变 A”这个故事，却说不出真实业务影响。你最好顺手举一个栈顶节点被弹出又复用、但 CAS 误判成功的场景。""",
        "diagram": """适合画一张时间线：线程 T1 读取值 A 并准备 CAS；线程 T2 把 A 改成 B 又改回 A；T1 再次比较时看到还是 A，于是错误地认为期间没人动过。右侧补一条带版本号的改进路径。""",
    },
]


for update in updates:
    file_path = ROOT / update["file"]
    content = file_path.read_text(encoding="utf-8")
    content = replace_section(content, "一句话结论", update["one_liner"])
    content = replace_section(content, "面试回答", update["interview"])
    content = replace_section(content, "常见追问", update["followups"])
    content = replace_section(content, "图解提示", update["diagram"])
    file_path.write_text(content, encoding="utf-8")


print(f"Updated {len(updates)} files for round6 batch 1.")
