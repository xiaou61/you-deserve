import fs from "node:fs";
import path from "node:path";

import matter from "gray-matter";

const questionRoot = path.join(process.cwd(), "content", "questions");
const visualPath = path.join(process.cwd(), "content", "visuals", "question-visuals.json");
const progressPath = path.join(process.cwd(), "content", "question-detail-progress.json");
const batchSize = 10;

const details = {
  "java-hashmap-thread-unsafe": {
    type: "structure",
    nodes: [
      ["定位问题", "多个线程同时读写同一张表"],
      ["put 竞争", "桶位判断和节点写入不是一个原子动作"],
      ["覆盖丢失", "后写线程可能覆盖先写结果"],
      ["resize 迁移", "扩容期间链表和数组同时变化"],
      ["读到中间态", "无锁和可见性不足导致读线程看到旧状态"],
      ["正确选择", "并发写用 ConcurrentHashMap 或外部同步"]
    ],
    detail: `HashMap 的线程不安全，最核心不是“源码里没有 synchronized”这么一句话，而是它的内部结构默认只为单线程修改服务。HashMap 底层是一张数组，数组里的每个位置叫桶，发生哈希冲突时，桶里会挂链表或红黑树。单线程 put 时，流程大致是先计算 hash，再定位桶位，再判断桶里有没有相同 key，最后插入、覆盖或触发扩容。这个流程拆开看有很多步骤，但 HashMap 没有把这些步骤包装成并发场景下的原子操作。

最常见的问题是覆盖。假设两个线程同时 put 到同一个桶，线程 A 看到桶为空，准备把自己的节点放进去；线程 B 也在几乎同一时间看到桶为空，也准备放。因为它们之间没有锁，也没有 CAS 协调，最后数组桶位只能保存最后一次写入的节点，另一个线程的写入就像从来没发生过。更麻烦的是，这种错误不一定抛异常，它可能只是业务上少了一条数据，所以线上表现会很隐蔽。

第二个风险来自扩容。HashMap 在元素数量超过阈值后会 resize，把旧数组里的节点搬到更大的新数组。扩容不是简单复制，它要重新计算节点在新数组中的位置，并调整链表或树结构。如果多个线程同时触发扩容，或者一个线程正在扩容另一个线程还在 put，旧表、新表、链表指针和元素数量都会同时变化。JDK 7 里曾经因为头插法迁移链表，在并发扩容时可能形成环，导致 get 时死循环。JDK 8 改进了迁移方式，但这不代表 HashMap 变成线程安全了，并发修改仍然可能导致元素丢失、覆盖、结构状态不一致。

第三个点是内存可见性。HashMap 的 table、size、节点 next 指针等字段不是为了并发读写设计的。一个线程修改完结构，另一个线程未必立刻看到完整结果；读线程可能看到扩容过程中的中间态，也可能看到旧数组或尚未连接完整的节点链。面试里如果只说“读操作没问题”，要补一句前提：没有并发写时多线程只读通常可以；一旦有写，读也不安全。

图解时可以画成三段：左边是两个线程同时进入 put，中间是同一个桶位被竞争，右边分成覆盖丢失、扩容迁移混乱、读到中间态三个后果。最后再接一个解决方案节点：多线程写优先使用 ConcurrentHashMap，或者在外层明确加锁。ConcurrentHashMap 的价值在于它把并发控制做在容器内部，JDK 8 之后主要用 CAS 加桶级 synchronized 控制冲突范围，比给整个 HashMap 粗暴加一把大锁更可控。

项目回答里还可以补一个判断标准：如果集合在初始化后只读，HashMap 可以接受；如果存在并发写，就必须换容器或加同步边界。这个补充能让答案从“背源码问题”变成“会做工程选择”。`
  },
  "jvm-gc-roots": {
    type: "flow",
    nodes: [
      ["对象分配", "对象进入堆内存"],
      ["可达性分析", "从 GC Roots 开始向外搜索"],
      ["根节点集合", "栈帧变量、静态字段、JNI 引用等"],
      ["引用链存在", "对象仍然可达就不能回收"],
      ["引用链断开", "对象进入可回收候选"],
      ["回收或保留", "结合 finalize、引用类型和收集器策略处理"]
    ],
    detail: `JVM 判断对象能不能回收，主流答案是可达性分析。它不是简单看对象有没有被某个变量“提到”，而是从一组被称为 GC Roots 的根对象出发，沿着引用关系向外搜索。凡是能从这些根对象一路走到的对象，都被认为还活着；如果某个对象从所有 GC Roots 都走不到，它就成为可回收候选。这个思路比“引用计数法”更能处理循环引用，因为两个对象互相引用但都不再连接到根节点时，仍然会被判定不可达。

GC Roots 的关键是“根”从哪里来。常见根包括虚拟机栈中的局部变量，比如当前正在执行方法里的对象引用；方法区里类的静态变量引用，比如某个单例或静态缓存；常量池中的引用；本地方法栈里 JNI 持有的引用；以及 JVM 内部正在使用的一些对象。你可以把 GC Roots 想成系统当前仍然认可的入口，只要对象还挂在这些入口能触达的引用链上，GC 就不能随便回收它。

面试时要注意，可达性分析说的是“能不能被回收的判断基础”，不等于对象一不可达就立刻释放。对象从不可达到真正被清理，中间还可能经过标记、引用处理、finalize 处理、分代收集策略等步骤。比如软引用、弱引用、虚引用的处理时机不同；finalize 虽然不推荐使用，但理论上对象在第一次被判定不可达后还可能通过 finalize 重新把自己挂回引用链。实际回答里不需要展开太偏，但要知道“不可达”是进入回收流程，不是立刻消失。

这道题还经常追问内存泄漏。Java 的内存泄漏并不是对象没人引用却没回收，而是对象已经没有业务价值，却仍然被 GC Roots 间接引用。比如 ThreadLocal 没清理、静态 Map 做缓存不淘汰、监听器注册后不注销、连接池或集合持有对象引用，这些对象对业务来说该死了，但从 GC Roots 看仍然活着，所以 GC 不会回收。排查时可以用 jmap、MAT、JFR 或堆转储分析引用链，看对象被哪个 GC Root 路径留住。

图解建议画成“根节点森林”：顶部是栈变量、静态字段、JNI 引用、常量池，下面用箭头连到一批对象。能连上的对象标成存活，断开的对象放在另一侧标成可回收候选。这样一眼就能解释为什么循环引用不是问题，也能解释为什么静态集合、ThreadLocal 这类对象容易造成泄漏。

如果面试官继续问“为什么不用引用计数”，可以顺手说出循环引用这个缺陷：A 引用 B、B 引用 A，但它们都不再被任何根节点触达时，引用计数不为零，可达性分析却能准确发现它们已经脱离业务入口。再往项目里落一句：看 GC 问题时，不要只盯“对象很多”，要追引用链，找到是谁把对象留住。`
  },
  "spring-ioc": {
    type: "flow",
    nodes: [
      ["业务类声明依赖", "对象不再自己 new 依赖"],
      ["容器扫描配置", "读取注解、XML 或配置类"],
      ["创建 BeanDefinition", "记录类型、作用域、依赖关系"],
      ["实例化和注入", "构造器、字段或 setter 注入"],
      ["生命周期回调", "初始化、代理增强、销毁"],
      ["业务获取 Bean", "拿到装配完成的对象图"]
    ],
    detail: `Spring IoC 解决的不是“帮你 new 对象”这么简单，而是把对象创建、依赖装配和生命周期管理从业务代码里抽出来，交给容器统一负责。没有 IoC 时，Service 里可能直接 new Repository，Controller 里直接 new Service。这样写小 demo 很快，但一旦对象之间依赖复杂，测试、替换实现、统一代理、事务增强、配置切换都会变得很痛苦。IoC 的核心价值就是控制反转：过去对象自己控制依赖怎么创建，现在对象只声明自己需要什么，容器负责把依赖准备好。

从运行流程看，Spring 启动时会先读取配置来源，比如注解扫描、配置类、XML 或自动配置。然后它把每个 Bean 的元信息解析成 BeanDefinition，里面记录类型、作用域、是否懒加载、依赖关系、初始化方法等。接着容器根据这些定义实例化对象，再通过构造器、setter 或字段注入完成依赖。最后还会执行 BeanPostProcessor、初始化回调、AOP 代理等步骤。你平时拿到的很多 Bean，可能已经不是原始对象，而是被代理增强后的对象。

IoC 和 DI 经常一起出现。IoC 是思想，强调控制权反转；DI 是实现方式，强调依赖注入。比如一个 OrderService 需要 PaymentClient，它不自己创建 PaymentClient，而是通过构造器声明依赖，由容器注入进来。这带来三个直接好处：第一，业务类更关注业务逻辑；第二，依赖可以替换，测试时可以注入 mock；第三，框架可以在对象创建过程中统一做事务、缓存、权限、监控等横切增强。

面试里容易被追问循环依赖。Spring 可以解决部分单例 Bean 的 setter 或字段注入循环依赖，依靠三级缓存提前暴露对象引用，但构造器循环依赖通常解决不了。更重要的是，能解决不代表设计合理。如果两个 Service 互相依赖，往往说明职责边界不清，应该抽出第三个协作服务或重新拆分业务流程。

图解可以画成“业务对象 -> 声明依赖 -> 容器装配 -> 代理增强 -> 可用 Bean”的流程。旁边再画一个对比：传统写法是对象 A 直接 new B，IoC 写法是 A 只声明需要 B，由容器把 B 注入。最后落点要强调：IoC 让对象关系从硬编码变成可配置、可替换、可增强，这是 Spring 能做事务、AOP、自动配置的基础。

落地时建议优先讲构造器注入，因为它能让依赖在对象创建时就明确下来，也更利于测试和不可变设计。字段注入写起来快，但隐藏依赖，不利于单测和重构；这类取舍说出来，会比只背 IoC 定义更像真实开发经验。`
  },
  "mysql-index-left-prefix": {
    type: "structure",
    nodes: [
      ["联合索引", "按定义顺序组织复合 key"],
      ["B+Tree 排序", "先按第一列排，再按第二列排"],
      ["命中左前缀", "从最左列开始连续匹配"],
      ["范围中断", "范围条件后的列难以继续用于定位"],
      ["排序复用", "where 与 order by 顺序匹配更容易省排序"],
      ["设计索引", "高区分度和常用过滤列优先"]
    ],
    detail: `联合索引遵守最左前缀，本质来自 B+Tree 的排序方式。假设有联合索引 (a, b, c)，索引项并不是分别建三棵树，而是把 a、b、c 拼成一个复合 key 放进一棵 B+Tree 里排序。排序规则是先按 a 排；a 相同的情况下再按 b 排；a 和 b 都相同的情况下再按 c 排。也就是说，b 和 c 的有序性是建立在前面列已经确定的前提上。

所以查询条件里如果有 a，可以在树上快速定位 a 的范围；如果同时有 a 和 b，可以在 a 的范围里继续缩小 b；如果有 a、b、c，就能继续精确定位。但如果跳过 a 直接查 b，整棵树并不是全局按 b 排的，MySQL 很难利用这棵索引快速定位，只能扫描更多索引项甚至放弃索引。这就是“最左”的含义：从索引定义的第一列开始，连续命中才最有效。

范围条件也是常见坑。比如 where a = 1 and b > 10 and c = 3，索引可以先用 a 定位，再用 b 做范围扫描，但到了 b 是范围后，c 通常不能继续用于缩小定位范围。原因是 b 大于 10 的区间里，c 的值分布在不同 b 值下面，并不是一个可以直接跳转的连续区间。面试里可以说“范围条件后的列可能无法继续用于索引定位”，但要注意 MySQL 还有 ICP 等优化，可能会在存储引擎层用 c 做过滤；这和“继续定位”不是一回事。

联合索引还能服务排序和分组。order by 如果和联合索引顺序一致，并且 where 条件把前导列约束住，就可能避免 filesort。比如索引 (user_id, create_time)，查询某个用户的订单并按 create_time 排序，就很适合。但如果 order by 的列顺序和索引不匹配，或者中间跳列，排序优势就会下降。

设计索引时不要机械地把所有字段塞进联合索引。要看查询模式：高频过滤条件、等值条件、区分度、范围条件、排序字段、覆盖索引需求都要一起考虑。一般等值过滤且区分度高的列更适合靠前；范围列放得太前，可能影响后续列的使用；只为低频查询建很长索引，会增加写入成本和存储成本。

图解可以画一棵联合索引树：第一层按 a 分区，第二层在同一个 a 里按 b 排，第三层在同一个 a、b 里按 c 排。然后画三条查询路线：a+b+c 是顺着树走；a+c 是中间断了一段；b+c 是直接跳过根排序。这样最左前缀就不再是口诀，而是 B+Tree 排序规则的自然结果。

真正设计索引时，还要结合业务查询频率。一个偶尔使用的复杂查询，不值得牺牲大量写入成本；一个核心列表页或账单查询，则值得为了过滤、排序和覆盖索引专门设计联合索引。这个工程取舍是面试加分点。`
  },
  "mysql-transaction-isolation": {
    type: "compare",
    nodes: [
      ["读未提交", "可能脏读"],
      ["读已提交", "避免脏读但可能不可重复读"],
      ["可重复读", "同一事务快照读结果稳定"],
      ["串行化", "最强隔离但并发最低"],
      ["MVCC", "快照读降低读写冲突"],
      ["锁机制", "当前读和范围修改仍需锁兜底"]
    ],
    detail: `MySQL 事务隔离级别解决的是并发事务互相影响的问题。数据库不是一个请求一个请求排队执行，而是大量事务同时读写同一批数据。隔离级别越低，并发性能通常越好，但可能看到更多不一致现象；隔离级别越高，一致性越强，但锁等待和并发成本也可能更高。面试里要把每个级别和它解决的问题对应起来，而不是只背四个名字。

读未提交是最低级别，一个事务还没提交的修改，另一个事务就可能读到，这叫脏读。脏读的问题是数据可能回滚，读到的结果从业务上看根本不成立。读已提交解决了脏读：只能读到已经提交的数据。但它仍然可能出现不可重复读，也就是同一个事务里两次读取同一行，中间另一个事务提交了更新，第二次读到的值变了。Oracle 默认就是读已提交，MySQL InnoDB 也支持。

可重复读强调同一个事务内多次快照读结果稳定。InnoDB 默认隔离级别是 Repeatable Read，它通过 MVCC 和 Read View 实现快照读。事务第一次进行一致性读时生成一个视图，后续快照读复用这个视图，于是即使别的事务提交了更新，本事务仍然看到自己视图里可见的版本。这里要补充：可重复读主要说普通 select 的快照读；update、delete、select for update 这类当前读要读取最新版本，并可能加锁。

幻读是更容易讲混的点。标准概念里，幻读是同一事务按同一条件读范围数据时，另一个事务插入了符合条件的新行，导致第二次读多出“幻影行”。InnoDB 在可重复读下，对快照读通过 MVCC 让结果稳定；对当前读和范围修改，则通过 next-key lock，也就是记录锁加间隙锁，限制其他事务往范围里插入，从而降低幻读风险。所以不要简单说“RR 完全靠 MVCC 解决幻读”，更准确是 MVCC 负责快照读，锁负责当前读和范围写的并发控制。

串行化是最高隔离级别，效果接近事务排队执行，能避免脏读、不可重复读和幻读，但并发性能最差，实际互联网业务很少作为默认选择。更多时候要根据业务选择：普通读多写少系统可以用默认 RR；需要每条 select 都看到最新已提交结果的场景可能选 RC；涉及余额、库存、订单状态这类强一致写入，通常还要配合唯一索引、行锁、乐观锁或状态机。

图解可以画成一个阶梯：从读未提交到串行化，一致性逐级增强，并发性能逐级下降。旁边画三类异常：脏读、不可重复读、幻读，再用勾叉标出各级别解决到哪里。最后加一条 InnoDB 实现线：快照读靠 MVCC/Read View，当前读靠锁。这样回答既有标准概念，也能落到 MySQL 实现。`
  },
  "redis-cache-penetration-breakdown-avalanche": {
    type: "compare",
    nodes: [
      ["缓存穿透", "查不存在的数据绕过缓存打到 DB"],
      ["缓存击穿", "热点 key 失效瞬间大量请求落库"],
      ["缓存雪崩", "大量 key 同时失效或 Redis 故障"],
      ["空值和布隆", "拦截不存在请求"],
      ["互斥和预热", "保护热点 key 重建过程"],
      ["分散过期和降级", "避免集中失效拖垮系统"]
    ],
    detail: `缓存穿透、击穿、雪崩都是缓存系统里的高频故障，但三者的触发点不一样。缓存穿透是请求的数据根本不存在，缓存里查不到，数据库里也查不到。如果攻击者不断请求随机 ID，每次都绕过缓存打到数据库，缓存就失去了保护作用。解决思路是把“不存在”也缓存起来，比如缓存空值并设置较短 TTL；或者用布隆过滤器在入口处判断这个 key 是否可能存在，不可能存在的请求直接拦截。

缓存击穿强调一个热点 key。这个 key 平时访问量很大，缓存命中时没问题，但它突然过期了，大量请求同时发现缓存没有，于是一起去查数据库并重建缓存，数据库瞬间被打爆。解决思路是保护重建过程：可以用互斥锁，只允许一个线程查库并回填缓存，其他线程等待或返回旧值；也可以热点 key 不设置普通过期，而是逻辑过期，后台异步刷新；还可以提前预热和续期，避免热点 key 在流量高峰自然过期。

缓存雪崩是更大范围的问题。大量 key 在同一时间过期，或者 Redis 集群故障，导致大批请求同时落到数据库。它和击穿的区别是规模：击穿是一个或少数热点 key，雪崩是一大片 key 或整个缓存层。解决方式包括过期时间加随机抖动，避免同一批 key 集中过期；热点数据提前预热；Redis 做高可用；服务端限流降级；数据库侧保护连接池和慢查询；必要时返回兜底数据。

面试里不要只背三句话，要能按“请求路径”讲。正常路径是请求先查缓存，命中直接返回；未命中再查数据库，查到后回填缓存。穿透的问题发生在“数据库也查不到”；击穿的问题发生在“热点 key 未命中后的回源重建”；雪崩的问题发生在“大量 key 或整个缓存层同时不可用”。沿着这条路径讲，三者区别会非常清楚。

项目里要补充监控指标。穿透会表现为大量不存在 key、缓存命中率下降、数据库 QPS 异常升高；击穿会表现为某个热点 key 过期点附近数据库压力尖刺；雪崩会表现为整体命中率骤降、数据库连接池耗尽、接口 P99 飙升。治理时不能只靠一个方案，比如布隆过滤器有误判，空值缓存要控制 TTL 和内存，互斥锁要防死锁和超时，逻辑过期要接受短暂旧数据。

图解可以画成三列对比：左列穿透，画“非法 key -> 缓存 miss -> DB miss -> 空值/布隆拦截”；中列击穿，画“热点 key 过期 -> 大量请求 -> 互斥重建”；右列雪崩，画“大量 key 同时过期 -> 请求洪峰 -> 随机 TTL/限流/降级”。最后统一落到一句话：缓存不是只提升速度，更重要是保护数据库。

如果要联系项目，可以补一句：缓存治理一定要配合监控和开关。没有命中率、热点 key、数据库 QPS 和降级开关，方案再漂亮也很难在线上稳定运行。`
  },
  "tcp-three-way-handshake": {
    type: "sequence",
    nodes: [
      ["客户端 SYN", "请求建立连接并发送初始序列号"],
      ["服务端 SYN+ACK", "确认客户端序列号并给出自己的序列号"],
      ["客户端 ACK", "确认服务端序列号"],
      ["双方同步序列号", "后续数据传输有可靠编号基础"],
      ["确认收发能力", "验证两端发送和接收链路可用"],
      ["进入 ESTABLISHED", "连接正式可传输数据"]
    ],
    detail: `TCP 三次握手的目标不是形式上“打三次招呼”，而是在通信双方之间确认连接参数、同步初始序列号，并验证双方的发送和接收能力。TCP 是可靠字节流协议，后续每个字节都要靠序列号和确认号来保证顺序、重传和去重。因此建立连接前，双方必须知道彼此的初始序列号，不能直接开始发业务数据。

第一次握手是客户端发送 SYN，告诉服务端：我想建立连接，这是我的初始序列号 client_isn。此时客户端进入 SYN_SENT，服务端收到后知道客户端的发送能力和服务端自己的接收能力是通的。第二次握手是服务端回复 SYN+ACK，一方面确认客户端的序列号，另一方面发送自己的初始序列号 server_isn。客户端收到后，就知道服务端能接收自己的 SYN，也能发送响应回来。第三次握手是客户端再发 ACK，确认服务端的初始序列号。服务端收到这个 ACK 后，才知道自己的发送能力和客户端的接收能力也是通的。

为什么不是两次？如果只有两次，服务端发出 SYN+ACK 后就认为连接建立，但它无法确认客户端是否真的收到了自己的序列号。更经典的问题是历史重复连接请求：网络里可能有一个旧 SYN 因延迟很久才到达服务端。如果两次握手就建立连接，服务端可能为这个已经失效的请求分配资源，造成半开连接或资源浪费。第三次 ACK 可以让客户端有机会拒绝或确认这次连接，让服务端更可靠地进入 ESTABLISHED。

为什么不是四次？理论上双方都要发送 SYN 和 ACK，但服务端的 SYN 和对客户端 SYN 的 ACK 可以合并在一个报文里，所以三次就够了。挥手通常是四次，是因为关闭连接时双方的数据发送方向可以独立关闭，服务端收到 FIN 后可能还有数据没发完，ACK 和 FIN 往往不能立刻合并。

项目排查时，三次握手问题常表现为连接超时、SYN_SENT 堆积、SYN_RECV 堆积或服务端 accept 队列满。可能原因包括网络不通、防火墙拦截、服务端端口没监听、SYN backlog 不够、半连接攻击、连接数耗尽等。可以用 netstat、ss、tcpdump、服务端日志和系统指标一起看。面试里能把协议流程和排查现象连起来，会比只背 SYN/ACK 更有说服力。

图解建议画成时序图：左边客户端，右边服务端，中间三条箭头分别是 SYN、SYN+ACK、ACK。每条箭头旁边标出“我发给你的序列号”“我确认收到了你的序列号”。底部补两个结果：同步序列号、确认双向收发能力。

回答时再补一个工程视角：三次握手发生在应用层请求之前，所以接口慢不一定都是业务代码慢。DNS、连接建立、TLS 握手、连接池复用情况都会影响用户看到的耗时。`
  },
  "thread-pool-core-parameters": {
    type: "flow",
    nodes: [
      ["任务提交", "execute 或 submit 进入线程池"],
      ["核心线程", "未满 corePoolSize 优先创建核心线程"],
      ["阻塞队列", "核心线程忙时任务进入队列"],
      ["非核心线程", "队列满后扩容到 maximumPoolSize"],
      ["拒绝策略", "线程和队列都满时触发"],
      ["回收线程", "keepAliveTime 控制空闲线程释放"]
    ],
    detail: `线程池核心参数要按任务进入线程池后的流转顺序理解，而不是孤立背七个字段。一个任务提交到 ThreadPoolExecutor 后，首先看当前工作线程数是否小于 corePoolSize。如果还没达到核心线程数，即使已有空闲线程，也可能创建核心线程来执行任务。核心线程数可以理解为线程池长期保留的基本处理能力。

当核心线程都忙了，新任务会尝试进入 workQueue，也就是阻塞队列。队列的选择非常关键：无界队列会让任务堆积，maximumPoolSize 基本失去意义，风险是内存不断上涨和延迟失控；有界队列可以限制堆积，但队列满后会触发扩容或拒绝；SynchronousQueue 不存任务，适合直接移交，常配合较大的 maximumPoolSize。很多线上问题不是线程池不会用，而是队列选错导致流量高峰时要么无限堆积，要么疯狂创建线程。

当队列满了，线程池才会继续创建非核心线程，直到达到 maximumPoolSize。这个参数不是越大越好。线程过多会增加上下文切换、内存占用和锁竞争，反而降低吞吐。设置时要看任务类型：CPU 密集型通常接近 CPU 核数；IO 密集型可以适当大一些，因为线程经常等待网络或磁盘；混合型要通过压测和监控确定。keepAliveTime 控制非核心线程空闲多久后被回收，allowCoreThreadTimeOut 开启后核心线程也可超时回收。

拒绝策略是线程池的最后防线。当线程数达到 maximumPoolSize 且队列也满时，新任务会触发 RejectedExecutionHandler。AbortPolicy 直接抛异常；CallerRunsPolicy 让提交任务的线程自己执行，能起到一定反压效果；DiscardPolicy 静默丢弃；DiscardOldestPolicy 丢弃队列头部任务再尝试提交。生产中最好不要静默丢任务，至少要打日志、计数、告警，并根据业务决定是快速失败、降级还是反压。

面试里要主动说监控。线程池上线后要看活跃线程数、队列长度、任务执行耗时、拒绝次数、完成任务数、异常数和调用方超时。线程池不是配置一次就结束，流量模型变了、下游变慢了、任务耗时增加了，都可能让原来的参数失效。项目里如果能讲出“参数怎么定、怎么压测、怎么监控、怎么降级”，就比只背 core、max、queue 更强。

图解可以画成任务漏斗：任务进入后先尝试核心线程，核心满了进队列，队列满了扩非核心线程，再满就拒绝；旁边标出 keepAliveTime 回收空闲非核心线程。这个流程图也能解释为什么无界队列会让最大线程数不生效。`
  },
  "volatile-visibility": {
    type: "structure",
    nodes: [
      ["普通变量", "线程可能读到本地缓存旧值"],
      ["volatile 写", "写入后对其他线程可见"],
      ["volatile 读", "读取时获取最新值"],
      ["禁止重排序", "建立一定的 happens-before 关系"],
      ["不保证原子性", "i++ 仍然是读改写三步"],
      ["适用场景", "状态标记、开关、单例双检配合锁"]
    ],
    detail: `volatile 主要解决两个问题：可见性和一定程度的有序性。Java 线程在执行时，不一定每次都直接从主内存读取变量，也可能使用工作内存里的副本。没有同步手段时，一个线程改了共享变量，另一个线程可能迟迟看不到最新值。把变量声明为 volatile 后，对它的写入会尽快刷新到主内存，对它的读取会从主内存获取最新值，从而保证线程之间对这个变量的可见性。

典型场景是停止标记。一个线程循环执行 while (running)，另一个线程把 running 改成 false。如果 running 是普通变量，循环线程可能一直读到旧值，导致停不下来。声明为 volatile 后，写线程修改 running 后，读线程能看到变化。这类“一写多读的状态开关”是 volatile 很适合的场景。

volatile 还会禁止特定类型的指令重排序。编译器和 CPU 为了优化性能，可能在不改变单线程结果的前提下调整指令顺序。但多线程下，重排序可能让其他线程看到不符合预期的中间状态。volatile 写和 volatile 读之间会建立 happens-before 关系：对一个 volatile 变量的写，先行发生于后续对这个变量的读。经典例子是双重检查锁单例中 instance 需要 volatile，防止对象分配、构造初始化、引用赋值的顺序被重排后，其他线程拿到一个尚未初始化完成的对象。

但是 volatile 不保证复合操作的原子性。比如 count++ 看起来是一行代码，实际包括读取 count、加一、写回 count 三步。多个线程同时执行时，可能都读到同一个旧值，再分别写回相同的新值，导致更新丢失。这个时候应该用 synchronized、Lock、AtomicInteger 或 LongAdder，而不是指望 volatile。面试里一定要把“可见性”和“原子性”分开说。

volatile 也不能替代锁。锁除了可见性，还提供互斥，能保护临界区内多个变量之间的一致性。如果一个业务状态由多个字段共同决定，只把其中一个字段 volatile 并不能保证整体状态一致。volatile 更适合简单状态标志、配置开关、发布不可变对象引用等场景；涉及读改写、条件判断后更新、多个字段组合更新，就要考虑锁或原子类。

图解可以画成两层内存模型：上面是主内存，下面是线程 A 和线程 B 的工作内存。普通变量路径里，线程 B 读到旧副本；volatile 路径里，线程 A 写后刷新，线程 B 读时重新获取。旁边再画一个 count++ 拆解图，标注“三步操作，所以 volatile 不等于原子”。`
  },
  "jvm-memory-areas": {
    type: "structure",
    nodes: [
      ["程序计数器", "记录当前线程执行位置"],
      ["虚拟机栈", "方法调用栈帧和局部变量"],
      ["本地方法栈", "JNI/native 方法调用"],
      ["堆", "对象实例和数组主要分配区域"],
      ["方法区", "类元数据、常量、静态信息"],
      ["直接内存", "NIO 等场景使用堆外内存"]
    ],
    detail: `JVM 运行时内存区域可以按“线程私有”和“线程共享”来理解。线程私有区域包括程序计数器、虚拟机栈、本地方法栈；线程共享区域主要包括堆和方法区，另外还有不属于 JVM 运行时数据区但经常一起讨论的直接内存。这样分类比死记名字更好，因为它能解释为什么某些区域会随线程创建销毁，某些区域会被所有线程共同竞争。

程序计数器是一块很小的线程私有内存，用来记录当前线程执行到哪条字节码指令。因为线程切换后要恢复执行位置，所以每个线程都需要自己的计数器。它通常不会出现 OutOfMemoryError，也是最容易被忽略但概念上很重要的区域。如果执行的是 native 方法，程序计数器值可能为空或未定义。

虚拟机栈也是线程私有的，每次方法调用都会创建一个栈帧，里面放局部变量表、操作数栈、动态链接、方法返回地址等信息。递归太深或栈帧太大，可能触发 StackOverflowError；如果线程栈需要扩展但无法申请到足够内存，可能触发 OutOfMemoryError。面试里讲栈时要强调它存的是方法调用相关数据，不是普通对象主要存放地。对象本体通常在堆里，栈里的局部变量可能只是对象引用。

堆是最重要的共享区域，绝大多数对象实例和数组都在堆上分配，也是垃圾收集器主要管理的区域。堆可以按代划分，比如新生代、老年代，也可能在不同收集器下有不同布局。对象创建频繁、缓存不释放、集合无限增长，都可能导致堆 OOM。排查堆问题通常看 GC 日志、堆转储、对象占用和引用链。

方法区用于存放类元信息、运行时常量池、静态变量、JIT 编译后的代码等。JDK 8 以后 HotSpot 用元空间替代永久代，元空间默认使用本地内存。大量动态生成类、类加载器泄漏、反射代理泛滥，都可能导致元空间 OOM。这里要注意“方法区是规范概念，永久代/元空间是 HotSpot 的实现”。

本地方法栈服务 native 方法；直接内存常见于 NIO、Netty 等场景，通过堆外内存减少拷贝或提升 IO 性能。直接内存不在 Java 堆里，但仍然占用机器内存，配置不当或释放不及时也会 OOM。项目里如果看到堆不大但进程 RSS 很高，就要考虑直接内存、线程栈、元空间等堆外部分。

图解建议画成一张分区图：左侧线程私有，每个线程一套程序计数器、虚拟机栈、本地方法栈；右侧线程共享，堆和方法区被所有线程访问；底部画直接内存连接到操作系统内存。回答时按“用途、是否线程私有、常见异常、排查入口”四列讲，结构会很清晰。`
  },
  "spring-transaction-failure": {
    type: "flow",
    nodes: [
      ["调用入口", "事务必须从代理对象进入"],
      ["代理增强", "AOP 在方法外层开启和提交事务"],
      ["异常判断", "默认只回滚 RuntimeException 和 Error"],
      ["传播行为", "REQUIRES_NEW、NESTED 等会改变边界"],
      ["数据库支持", "引擎和连接配置也会影响事务"],
      ["排查闭环", "看代理、日志、异常、SQL 和表引擎"]
    ],
    detail: `Spring 事务失效最常见的根因，是没有理解它依赖代理机制。声明式事务不是数据库自动识别注解，而是 Spring 在 Bean 外面包了一层代理对象。当外部调用代理对象的方法时，代理会在目标方法执行前开启事务，方法正常返回后提交，抛出符合规则的异常时回滚。如果调用没有经过代理，这层事务增强就没有机会执行，注解写得再漂亮也不会生效。

第一个高频坑是同类内部调用。比如一个 Service 里的 methodA 直接调用 this.methodB，而 methodB 上有 Transactional。这个调用发生在目标对象内部，没有经过 Spring 代理，因此 methodB 的事务不会被增强。解决思路不是“多加一个注解”，而是调整调用边界：把事务方法挪到另一个 Bean，通过代理调用；或者从 Spring 容器里拿到当前代理再调用，但这通常不如拆职责清晰。

第二个坑是方法可见性和代理方式。Spring 默认基于 AOP 代理，事务通常要求 public 方法从外部被调用。private、final 方法，或者 final 类在某些代理模式下不能被正常增强。JDK 动态代理基于接口，CGLIB 基于子类，不同代理方式对方法形态有要求。面试时不用展开太底层，但要能说明“事务注解不是贴在哪都能生效，它要能被代理拦截”。

第三个坑是异常类型。Spring 默认对 RuntimeException 和 Error 回滚，对受检异常不回滚。很多业务代码 catch 了异常后只打印日志不往外抛，事务代理看到方法正常返回，就会提交。还有些代码抛的是自定义受检异常，如果没有配置 rollbackFor，也不会按预期回滚。回答时可以说：要么让异常继续抛出，要么显式设置 rollbackFor，要么在 catch 后手动标记 rollback-only，但不要吞异常。

第四个坑是传播行为。外层方法和内层方法都加事务时，REQUIRED 会加入同一个事务；REQUIRES_NEW 会挂起外层事务，新开一个事务；NESTED 则依赖保存点。传播行为用错，就会出现“我以为一起回滚，结果内层已经提交”或“内层回滚影响外层”的情况。项目里尤其要注意日志、审计、消息表、库存扣减这些动作是否应该和主业务同事务。

最后还要看数据库层。MySQL 如果表引擎是 MyISAM，本身不支持事务；如果没有被同一个事务管理器管理，或者手动拿连接提交，也会破坏事务边界。图解可以画成“外部调用 -> 代理对象 -> 开启事务 -> 目标方法 -> 异常判断 -> 提交或回滚”，旁边列出失效分支：内部调用、非 public、异常被吞、rollbackFor 缺失、传播行为不对、数据库不支持。`
  },
  "mysql-index-failure": {
    type: "flow",
    nodes: [
      ["SQL 条件", "先看 where、join、order by"],
      ["函数计算", "索引列被加工后难以定位"],
      ["隐式转换", "类型不一致可能导致全表扫描"],
      ["最左前缀", "联合索引跳列或中断"],
      ["选择性过低", "优化器可能认为扫表更便宜"],
      ["用 explain 验证", "看 type、key、rows、Extra"]
    ],
    detail: `MySQL 索引失效不是“索引坏了”，而是优化器在当前 SQL、数据分布和成本估算下，没有办法或没有必要使用这个索引。面试里要先把思路摆正：索引是一种有序数据结构，能帮助快速定位范围；一旦 SQL 写法破坏了这种有序定位能力，或者用索引的成本比扫表更高，优化器就可能放弃索引。

最常见的是对索引列做函数或表达式计算。比如 where date(create_time) = 某天，create_time 本来在索引里按原值有序，但你把它包成 date 函数后，MySQL 很难直接用原始索引定位。更好的写法是把条件改成范围：create_time >= 当天零点 and create_time < 次日零点。类似地，对字段做运算、字符串截取、隐式函数转换，都可能让索引列无法直接参与定位。

隐式类型转换也是线上常见坑。字段是 varchar，SQL 里却用数字去比较，MySQL 可能把字段转换成数字再比较，相当于对列做函数，索引就难以使用。字符集或排序规则不一致也可能导致转换。这个问题在日志里不一定明显，所以排查时要看表结构、参数类型和最终 SQL。很多 Java 项目里，Mapper 参数类型写错，也会造成这种问题。

联合索引失效要结合最左前缀理解。索引 (a, b, c) 先按 a 排，再按 b 排，再按 c 排。查询跳过 a 直接查 b，通常无法有效使用这个联合索引。范围条件也可能中断后续列的定位能力，比如 a = 1 and b > 10 and c = 3，b 之后的 c 不一定能继续用于缩小扫描范围。注意这里说的是定位能力，不等于后续列完全没用，ICP 可能仍能用来过滤。

还有一种情况是优化器主动不用索引。比如性别、状态这类低选择性字段，命中大量行，走二级索引还要回表，成本可能比全表扫描更高。小表数据量很少时，扫表也可能更便宜。order by、like 前缀、or 条件、not equal、is not null 等也要结合数据量和索引结构判断，不要机械地说“一定失效”。

正确排查方式是 explain 加真实数据验证。重点看 type 是否从 ALL 改善到 range、ref、const；key 是否命中预期索引；rows 估算是否过大；Extra 里是否有 Using filesort、Using temporary、Using index condition、Using where。图解可以画成一条诊断链：SQL 条件进入后，依次检查函数计算、类型转换、联合索引顺序、范围中断、选择性和成本，最后用 explain 验证，而不是凭感觉改索引。`
  },
  "mysql-explain": {
    type: "structure",
    nodes: [
      ["type", "判断访问方式和扫描范围"],
      ["key", "实际使用了哪个索引"],
      ["rows", "估算要扫描多少行"],
      ["Extra", "观察排序、临时表、回表和过滤"],
      ["possible_keys", "候选索引不等于实际使用"],
      ["联合判断", "不能只盯一个字段下结论"]
    ],
    detail: `EXPLAIN 的作用是让你在 SQL 真正执行前，看到优化器计划怎么访问表。它不是性能真相的全部，但能快速暴露“有没有走索引、扫描范围大不大、是否需要额外排序或临时表”。面试里不要把字段挨个背完，抓住 type、key、rows、Extra 四个核心字段，再补 possible_keys、filtered、id、select_type，就能组织出比较完整的排查思路。

type 表示访问类型，能粗略反映访问效率。常见顺序可以从好到差理解：system、const、eq_ref、ref、range、index、ALL。const 表示通过主键或唯一索引等值命中一行；ref 表示普通索引等值匹配；range 表示范围扫描；index 表示扫描整棵索引；ALL 表示全表扫描。看到 ALL 不一定立刻判死刑，小表可以接受，但大表核心查询出现 ALL 就要警惕。

key 表示实际使用的索引，possible_keys 只是候选索引。很多人看到 possible_keys 里有索引就以为没问题，但真正要看 key。如果 key 是 null，就说明没有使用索引。key_len 可以辅助判断联合索引用到了哪些列，但它受字段类型、字符集、是否允许 null 等因素影响，实际排查时要结合索引定义和查询条件看。

rows 是优化器估算要扫描的行数，不是精确执行结果。rows 很大，说明查询可能要扫描大量数据；但它也可能因为统计信息不准而偏差。MySQL 8 可以用 EXPLAIN ANALYZE 看实际执行耗时和行数，生产排查时也可以配合慢日志和监控。filtered 表示过滤比例估算，rows 乘 filtered 大致能看出后续剩多少数据。

Extra 是最容易发现具体问题的位置。Using index 通常表示覆盖索引，不需要回表；Using where 表示还要在 server 层过滤；Using index condition 表示用了索引下推；Using filesort 表示需要额外排序，不一定真在磁盘排序，但说明索引顺序没完全满足排序；Using temporary 表示用了临时表，group by、distinct、复杂排序时常见。核心接口里出现 filesort 和 temporary，要结合数据量评估风险。

图解可以画成一张体检表：第一行看 type 判断病情轻重，第二行看 key 确认有没有用对索引，第三行看 rows 判断扫描规模，第四行看 Extra 找排序、临时表、回表等额外成本。最后强调：EXPLAIN 是诊断入口，不是最终答案。优化 SQL 时要结合表结构、索引定义、数据分布、慢日志和真实执行耗时一起判断。`
  },
  "redis-rdb-aof": {
    type: "compare",
    nodes: [
      ["RDB 快照", "按时间点生成二进制数据文件"],
      ["AOF 日志", "记录写命令用于重放恢复"],
      ["恢复速度", "RDB 通常更快"],
      ["数据安全", "AOF 通常丢数据更少"],
      ["重写机制", "AOF rewrite 压缩日志体积"],
      ["组合使用", "生产常用混合持久化和备份"]
    ],
    detail: `Redis 的 RDB 和 AOF 都是持久化机制，但解决问题的角度不同。RDB 是快照，把某一时刻内存里的数据保存成紧凑的二进制文件；AOF 是追加日志，把写命令记录下来，恢复时再按顺序重放。你可以把 RDB 想成拍照片，把 AOF 想成记流水账。照片恢复快、文件紧凑，但两张照片之间的变化可能丢；流水账更细，丢数据少，但日志会变大，恢复时要重放命令。

RDB 的优点是文件小、恢复速度快、适合做备份和全量复制。Redis 生成 RDB 时通常会 fork 子进程，由子进程把内存快照写到磁盘，主进程继续处理请求。这里要注意 fork 不是零成本，数据量大时会有内存页表复制和写时复制成本，可能造成短暂抖动。RDB 的缺点是如果 Redis 在两次快照之间宕机，这段时间的数据会丢失。比如配置 save 5 分钟一次，极端情况下可能丢几分钟数据。

AOF 的优点是数据安全性更好。它可以配置 appendfsync always、everysec、no。always 每次写都刷盘，最安全但性能最差；everysec 每秒刷一次，通常是生产折中，最多丢约 1 秒数据；no 交给操作系统决定，性能好但风险更高。AOF 的缺点是文件会越来越大，恢复需要重放写命令，速度可能慢于 RDB。

为了解决 AOF 文件膨胀，Redis 有 AOF rewrite。重写不是把旧日志逐行压缩，而是根据当前内存数据生成一份能恢复当前状态的最小命令集合。比如一个 key 被 set 了 100 次，重写后只需要记录最后状态。重写期间新写入还要被增量记录，最终切换到新 AOF 文件。这个过程也要关注 fork 和磁盘 IO 成本。

生产中常见做法不是二选一，而是组合使用。RDB 适合周期性备份和快速恢复，AOF 适合降低数据丢失窗口。Redis 4 以后支持混合持久化，AOF 文件前半部分可以是 RDB 格式，后半部分是增量 AOF，兼顾恢复速度和数据安全。真正做方案时，还要考虑主从复制、哨兵或集群、高可用切换、磁盘容量、备份恢复演练。

图解可以画成左右对比：左边 RDB，内存数据在某个时间点生成 dump 文件；右边 AOF，每次写命令追加到日志，恢复时重放。中间列出取舍：RDB 恢复快但可能丢快照后的数据，AOF 更安全但文件更大、恢复可能更慢。最后落到一句：持久化不是保证绝不丢数据，而是在性能、恢复速度和数据安全之间做取舍。

如果面试官追问线上怎么选，可以说核心缓存一般开启 AOF everysec 加 RDB 备份，并定期做恢复演练；如果只是可重建缓存，可以降低持久化要求，把重点放在高可用、预热和限流上。`
  },
  "redis-distributed-lock": {
    type: "flow",
    nodes: [
      ["加锁", "SET key value NX EX 原子写入"],
      ["唯一标识", "value 保存请求 ID 防误删"],
      ["执行业务", "在锁有效期内完成临界区"],
      ["Lua 解锁", "先比对 value 再删除 key"],
      ["续期风险", "业务超时需要看门狗或缩短临界区"],
      ["可靠性边界", "单 Redis、主从切换和 RedLock 取舍"]
    ],
    detail: `Redis 分布式锁的基本目标，是让多个进程或多台机器在访问同一份共享资源时，同一时刻只有一个持有者进入临界区。最基础的加锁命令应该用 SET key value NX EX seconds，一条命令同时完成“仅不存在时写入”和“设置过期时间”。不要用 setnx 后再 expire 两条命令，因为中间如果进程宕机，锁可能没有过期时间，形成死锁。

value 一定要是唯一标识，比如 UUID、请求 ID 或线程标识。原因是锁可能过期后被别人重新获取，如果旧持有者业务执行完直接 del key，就会误删新持有者的锁。因此释放锁必须先判断 value 是否还是自己的，再删除。这个判断和删除也要保持原子，通常用 Lua 脚本完成：if get(key) == value then del(key) end。面试里如果能说出唯一标识和 Lua 解锁，基本就避开了最危险的坑。

过期时间是分布式锁最难的取舍。设置太短，业务没执行完锁就过期，其他线程会进入临界区，导致并发问题；设置太长，持有者宕机后其他请求要等很久。可以通过压测和业务耗时设置合理 TTL，也可以使用看门狗机制自动续期，比如 Redisson 在业务线程仍然存活时定期延长锁时间。但续期不是万能的，如果业务卡死、GC 停顿、网络抖动，都可能影响锁的可靠性。

还要注意锁保护的资源是否真的具备幂等和兜底。分布式锁只能减少并发进入，不应该作为唯一一致性保障。比如扣库存、发优惠券、创建订单，还要有数据库唯一索引、状态机、乐观锁或幂等表兜底。否则锁一旦失效，就可能造成超卖或重复处理。面试里说“锁只是第一层保护，数据库约束是最后防线”，会更像生产答案。

Redis 主从切换也会带来边界问题。单实例 Redis 持锁后，如果还没同步到从库主节点就宕机，从库提升为主，另一个客户端可能重新拿到锁，出现两个持有者。RedLock 试图通过多个独立 Redis 节点投票提高可靠性，但实现复杂，也有争议。大多数业务要根据一致性要求选择：普通防重复可以用 Redis 锁加数据库兜底；强一致资源竞争可能更适合数据库锁、ZooKeeper、etcd 或业务状态机。

图解可以画成加锁流程：请求进入后执行 SET NX EX，成功则带唯一 value 执行业务，结束时 Lua 比对并删除；失败则等待、重试或快速失败。旁边画风险分支：锁过期、误删他人锁、业务超时、Redis 故障，并对应写上 TTL、唯一标识、Lua、续期和数据库兜底。

回答时还可以补充“锁粒度”。锁粒度太粗会降低并发，太细又可能保护不住共享资源。比如按商品维度锁库存，比全局锁更合理；但如果业务操作跨多个资源，还要考虑死锁顺序和超时释放。`
  },
  "tcp-four-way-wave": {
    type: "sequence",
    nodes: [
      ["主动方 FIN", "我这边没有数据要发了"],
      ["被动方 ACK", "确认收到关闭请求"],
      ["被动方继续发送", "剩余数据可以继续传输"],
      ["被动方 FIN", "我也发送完了"],
      ["主动方 ACK", "确认对方关闭"],
      ["TIME_WAIT", "等待旧报文消失并保证 ACK 可重传"]
    ],
    detail: `TCP 四次挥手要从全双工通信理解。TCP 连接里的两个方向是相对独立的：客户端到服务端是一条数据流，服务端到客户端也是一条数据流。某一方说“我不发了”，并不代表它不能继续接收对方数据，也不代表对方已经发完。所以关闭连接时，双方都要分别表达“我发完了”，并分别确认对方的关闭请求。

第一次挥手是主动关闭方发送 FIN，表示自己这个方向没有数据要发送了。第二次挥手是被动方回复 ACK，表示我知道你不发了。但此时被动方可能还有数据没发完，比如服务端还要把响应剩余部分传给客户端，所以它不能立刻也发 FIN。等被动方数据发送完成后，第三次挥手发送 FIN，表示我这边也发完了。第四次挥手是主动方回复 ACK，确认对方的 FIN。

为什么握手可以三次，挥手通常四次？建立连接时，服务端的 SYN 和对客户端 SYN 的 ACK 可以合并成 SYN+ACK，因为服务端收到连接请求后通常可以立即表达自己也愿意建立连接。但关闭连接时，ACK 和 FIN 不能总是合并，因为收到 FIN 只代表对方不发了，不代表自己也立刻没有数据要发。只有在某些情况下，第二次 ACK 和第三次 FIN 才可能被合并，看起来像三次报文，但逻辑上仍然是两个方向分别关闭。

TIME_WAIT 是另一个高频追问。主动关闭方发出最后一个 ACK 后，会进入 TIME_WAIT 等待一段时间，通常是 2MSL。这样做有两个目的：第一，确保最后的 ACK 如果丢失，对方重发 FIN 时主动方还能再次 ACK；第二，让网络里属于这条连接的旧报文自然过期，避免影响后续相同四元组的新连接。TIME_WAIT 多不一定都是问题，短连接高并发场景才需要结合端口耗尽、连接复用、负载均衡等一起看。

工程排查时，CLOSE_WAIT 多通常表示被动关闭方收到 FIN 后没有正确 close，可能是应用代码忘记关闭连接或线程卡住；TIME_WAIT 多通常出现在主动关闭方，可能是大量短连接导致。服务端如果 CLOSE_WAIT 堆积，要优先查代码和连接释放；客户端或网关 TIME_WAIT 多，则要考虑连接池、HTTP keep-alive、端口范围和系统参数。

图解建议画成时序图：左边主动关闭方，右边被动关闭方，四条箭头分别是 FIN、ACK、FIN、ACK。中间特别标出“被动方 ACK 后仍可继续发送剩余数据”，底部画 TIME_WAIT 的两个作用。这样既能解释为什么四次，也能自然带出线上连接状态排查。

如果要联系 HTTP 服务，可以补一句：大量短连接会让挥手和 TIME_WAIT 成本变高，所以生产里通常通过连接池、HTTP keep-alive、网关复用连接来减少频繁建连和断连。`
  },
  "process-thread-difference": {
    type: "compare",
    nodes: [
      ["进程", "资源分配的基本单位"],
      ["线程", "CPU 调度的基本单位"],
      ["内存隔离", "进程之间地址空间独立"],
      ["共享资源", "同进程线程共享堆和文件句柄"],
      ["切换成本", "线程通常更轻但更容易竞争"],
      ["工程选择", "隔离性、并发量、稳定性一起权衡"]
    ],
    detail: `进程和线程的区别，可以用一句话打底：进程是资源分配的基本单位，线程是 CPU 调度的基本单位。操作系统创建进程时，会给它独立的地址空间、文件句柄、内存映射等资源；一个进程里可以有多个线程，这些线程共享进程的堆、方法区或全局数据等资源，但每个线程有自己的栈、程序计数器和执行上下文。

进程之间天然隔离。一个进程崩溃，通常不会直接破坏另一个进程的内存；进程间通信需要管道、消息队列、共享内存、Socket 等机制。这种隔离带来更好的稳定性和安全边界，但创建、销毁和切换成本也更高。浏览器多进程架构、微服务拆分、数据库独立进程，都是利用进程隔离降低相互影响。

线程更轻量。同一个进程里的多个线程共享地址空间，所以通信方便，直接读写共享对象即可。但共享也带来并发问题：多个线程同时修改同一份数据，会有竞态条件、死锁、可见性和原子性问题。Java 里的 synchronized、Lock、volatile、线程池、并发容器，本质上都是在管理线程共享资源时的安全问题。

调度角度也不同。CPU 实际执行的是线程，操作系统调度器把时间片分给可运行线程。线程上下文切换通常比进程轻，但也不是免费，线程太多会带来栈内存占用、调度开销和锁竞争。很多人以为“线程越多并发越高”，实际上 CPU 密集型任务线程过多反而会慢；IO 密集型任务可以多一些线程，但也要受连接数、队列长度和下游能力限制。

面试里还可以结合 JVM。一个 Java 程序通常对应一个操作系统进程，里面有 main 线程、GC 线程、JIT 编译线程、业务线程池线程等。线程共享堆里的对象，所以 Java 并发问题主要围绕共享对象展开。栈是线程私有的，所以方法局部变量如果没有逃逸到共享对象里，通常不需要考虑线程安全。

图解可以画成两层：上层是多个进程，每个进程有独立地址空间；下层展开一个进程，里面多个线程共享堆和文件句柄，但各自拥有栈。右侧列一张对比表：资源拥有、调度单位、通信方式、隔离性、切换成本、常见风险。最后用一句工程判断收尾：要隔离和稳定性选进程，要轻量并发和共享数据选线程，但线程共享一定要配合同步机制。

项目里还要知道，进程模型和线程模型常常会混合使用。比如一个服务部署多个进程实例来利用多核和隔离故障，每个实例内部再用线程池处理请求。真正的并发设计不是二选一，而是根据隔离性、吞吐、延迟和故障半径组合选择。回答到这里再补一句：进程解决隔离边界，线程解决进程内部并发，协程或异步 IO 又是在更细粒度上减少阻塞等待。`
  },
  "mq-reliable-message": {
    type: "flow",
    nodes: [
      ["生产者发送", "确认消息是否成功到达 Broker"],
      ["Broker 持久化", "副本、刷盘和确认策略"],
      ["消费者消费", "处理成功后再提交 ack"],
      ["失败重试", "超时、异常进入重试或死信"],
      ["幂等处理", "重复消息不能造成重复业务结果"],
      ["监控补偿", "积压、死信、对账和人工重放"]
    ],
    detail: `消息队列保证消息不丢，要按链路拆成三段：生产者到 Broker、Broker 自身存储、Broker 到消费者。任何一段都可能丢，所以不能只说“MQ 很可靠”。完整回答应该说明每一段怎么确认、失败怎么重试、重复怎么处理、最终怎么补偿。可靠消息的本质不是绝对不出问题，而是出问题时可发现、可重试、可恢复。

第一段是生产者发送。生产者把消息发出去后，必须知道 Broker 是否真的收到。不同 MQ 有不同确认机制，比如 RabbitMQ publisher confirm，Kafka producer acks，RocketMQ send result。发送失败要重试，但重试会带来重复消息，所以消息体里最好带业务唯一键，比如订单号、事件 ID。对于本地事务和发消息的一致性，可以用事务消息、本地消息表或 outbox pattern，避免“数据库提交了但消息没发出去”。

第二段是 Broker 存储。Broker 收到消息后，如果只在内存里，还没刷盘或复制就宕机，消息仍可能丢。Kafka 里要看 acks、min.insync.replicas、副本 ISR；RabbitMQ 要看队列和消息是否持久化；RocketMQ 要看刷盘策略和主从复制。这里有性能和安全取舍：同步刷盘更安全但延迟更高，异步刷盘性能好但可能丢最近一小段数据。

第三段是消费者。消费者拉到消息后，不能一拿到就提交 ack，而应该在业务处理成功后再确认。否则处理过程中服务宕机，Broker 以为消息已经消费，消息就丢了。如果处理失败，要进入重试；多次重试仍失败，可以进入死信队列，后续人工或补偿任务处理。消费超时、线程池满、下游数据库慢，都会导致消费积压，所以消费者也要有监控。

可靠消息一定伴随重复消息。只要有确认和重试，就可能出现 Broker 已收到但生产者没收到 ack，于是生产者重发；或者消费者处理成功但提交 ack 前宕机，重启后又消费一次。因此消费者必须幂等。常见做法是业务唯一键、去重表、状态机校验、数据库唯一索引。比如订单支付成功消息重复来，只能把订单从待支付改成已支付一次，不能重复加余额或重复发券。

图解可以画成三段流水线：生产者确认发送，Broker 持久化和副本保护，消费者处理成功后 ack。每段旁边标出失败分支：发送失败重试、Broker 宕机恢复、消费失败重试、死信队列、补偿对账。最后强调监控指标：发送失败率、Broker 积压、消费延迟、重试次数、死信数量、业务对账差异。没有监控和补偿，就谈不上真正可靠。`
  },
  "spring-bean-lifecycle": {
    type: "flow",
    nodes: [
      ["实例化", "根据 BeanDefinition 创建对象"],
      ["属性填充", "注入依赖和配置值"],
      ["Aware 回调", "注入容器相关上下文"],
      ["初始化前后处理", "BeanPostProcessor 参与增强"],
      ["初始化方法", "InitializingBean 或 init-method"],
      ["销毁", "容器关闭时执行 destroy 回调"]
    ],
    detail: `Spring Bean 生命周期可以按一条主线记：实例化、属性填充、初始化、使用、销毁。实例化是把 Bean 对象创建出来，属性填充是把依赖注入进去，初始化是让 Bean 做准备工作并可能被后处理器增强，使用阶段就是业务代码拿到 Bean 执行方法，销毁阶段在容器关闭时释放资源。面试里不要只背步骤名，要说出每一步能做什么、框架在什么位置插手。

第一步是根据 BeanDefinition 实例化对象。BeanDefinition 来自注解扫描、配置类、XML 或自动配置，里面记录了 Bean 的类型、作用域、构造参数、依赖关系等。实例化可以通过构造器、工厂方法等方式完成。此时对象只是创建出来，依赖还未必注入完成，所以不要以为构造方法里就能使用所有依赖。

第二步是属性填充，也就是依赖注入。Spring 会把需要的字段、setter、构造器参数或配置属性填进去。循环依赖也主要发生在这个阶段附近，单例 setter 循环依赖可能通过三级缓存提前暴露引用解决，但构造器循环依赖通常无法解决。理解生命周期有助于解释为什么某些 Bean 在初始化时拿到的是代理对象，某些时候又拿到早期引用。

第三步是一系列回调。Aware 接口会让 Bean 拿到 BeanName、BeanFactory、ApplicationContext 等容器信息。BeanPostProcessor 会在初始化前后插手，这是 Spring 扩展能力的核心入口之一。AOP 代理、一些自动注入增强、校验处理，都可能通过后处理器完成。也就是说，你最终从容器里拿到的 Bean，可能已经不是原始实例，而是经过代理包装的对象。

初始化阶段可以通过 PostConstruct、InitializingBean.afterPropertiesSet 或自定义 init-method 做准备工作，比如加载本地缓存、校验配置、建立连接。但不建议在这里做太重的远程调用，否则会拖慢启动，甚至让容器启动失败。销毁阶段可以通过 PreDestroy、DisposableBean 或 destroy-method 释放资源，比如关闭线程池、连接、文件句柄。

图解建议画成时间轴：BeanDefinition -> 实例化 -> 属性填充 -> Aware -> 初始化前 BeanPostProcessor -> 初始化方法 -> 初始化后 BeanPostProcessor -> 可用 Bean -> 销毁回调。旁边标注几个扩展点：PostConstruct 发生在初始化阶段，AOP 可能发生在初始化后处理器，销毁回调只对容器管理的单例 Bean 更常见。这样能把生命周期和实际框架扩展联系起来。`
  },
  "arraylist-linkedlist": {
    type: "compare",
    nodes: [
      ["ArrayList", "基于动态数组"],
      ["LinkedList", "基于双向链表"],
      ["随机访问", "ArrayList O(1) 更强"],
      ["中间插入删除", "LinkedList 找到位置后改指针"],
      ["真实性能", "缓存友好和遍历成本很关键"],
      ["业务选择", "大多数列表优先 ArrayList"]
    ],
    detail: `ArrayList 和 LinkedList 的区别，不能只停在“数组查询快、链表增删快”。这个说法有一定道理，但在真实 Java 集合里容易误导。ArrayList 底层是动态数组，元素在内存逻辑上连续，通过下标可以 O(1) 访问。LinkedList 底层是双向链表，每个节点保存元素以及前后指针，按下标访问需要从头或尾遍历，时间复杂度是 O(n)。

ArrayList 的优势是随机访问和遍历性能。因为数组结构连续，CPU 缓存友好，for 循环遍历很快。尾部追加元素通常也很快，只有容量不够时才需要扩容，扩容会创建更大的数组并复制旧元素。这个复制成本不是每次都发生，均摊下来尾插仍然是 O(1)。如果提前知道数据量，可以指定初始容量，减少扩容次数。

LinkedList 的理论优势是插入删除节点时，只要已经定位到节点，改前后指针即可，不需要像数组一样搬移大量元素。但关键在“已经定位到节点”。如果你是按下标在中间插入，比如 add(index, value)，LinkedList 仍然要先遍历到 index，这一步是 O(n)。所以很多场景下，它并不比 ArrayList 快。并且 LinkedList 每个节点都是对象，额外存前后指针，占用更多内存，也不如数组缓存友好。

删除也类似。如果你已经持有 Iterator，并在遍历过程中删除当前节点，LinkedList 改指针确实比较自然；但如果你频繁按下标删除，定位成本仍然存在。ArrayList 中间删除需要搬移后续元素，但在现代 CPU 缓存和批量内存复制优化下，小中等规模数据不一定慢。实际性能要看数据量、访问模式和 JVM 优化，不能只按复杂度口诀判断。

业务选择上，大多数 List 场景优先 ArrayList。比如查询结果、配置列表、DTO 集合、需要按下标访问或频繁遍历的集合，都适合 ArrayList。LinkedList 更适合队列、双端队列这类频繁头尾操作的场景，但 Java 里如果要做队列，通常更推荐 ArrayDeque，而不是 LinkedList，因为 ArrayDeque 更轻量、缓存友好。LinkedList 还实现了 Deque，但不是默认首选。

图解可以画成左右对比：左边 ArrayList 是一排连续格子，标出下标访问、扩容复制、中间删除搬移；右边 LinkedList 是节点用箭头串起来，标出前后指针、定位遍历、节点额外内存。最后给一个决策节点：按下标访问和遍历多选 ArrayList，频繁头尾队列操作考虑 ArrayDeque，只有明确需要链表特性时再考虑 LinkedList。`
  },
  "cas-aba": {
    type: "flow",
    nodes: [
      ["读取旧值", "线程先看到内存值 A"],
      ["比较交换", "只有当前值仍是 A 才更新"],
      ["ABA 问题", "值从 A 到 B 又回到 A"],
      ["CAS 被骗过", "表面没变但过程已经变了"],
      ["版本号", "用 AtomicStampedReference 识别变化"],
      ["适用边界", "低冲突乐观更新更合适"]
    ],
    detail: `CAS 是 compare and swap，也就是比较并交换。它是一种乐观锁思想：线程不先加互斥锁，而是读取当前值和期望值，更新时再判断内存里的值是否仍然等于期望值。如果相等，就把它改成新值；如果不相等，说明期间被别人改过，本次更新失败，通常会重试。Java 里的 AtomicInteger、AtomicReference 等原子类底层就大量使用 CAS。

CAS 的优势是避免阻塞。synchronized 或 Lock 在竞争时可能让线程挂起和唤醒，而 CAS 在用户态或 CPU 原子指令层面尝试更新，失败就自旋重试，适合计数器、状态标记、无锁队列等短小操作。它的典型模式是：读旧值、基于旧值计算新值、CAS 尝试写回。如果失败，说明旧值过期，再读一次重新计算。

但 CAS 不是万能的。第一个问题是自旋成本。竞争很激烈时，很多线程不断失败和重试，会消耗 CPU。第二个问题是只能保证一个变量或一个引用更新的原子性，涉及多个字段的一致性时就很麻烦。第三个问题就是 ABA：线程 1 读到值 A，准备 CAS；线程 2 把 A 改成 B，又改回 A。线程 1 再执行 CAS 时发现值还是 A，于是认为没有变化，更新成功。但实际上这个值中间经历过变化，相关对象状态可能已经完全不同。

ABA 在普通数字计数里未必有影响，但在栈、队列、对象引用、内存复用这类场景里风险很大。比如一个无锁栈的头节点从 A 变到 B，再变回 A，表面引用相同，但 A 节点的 next 关系或业务含义可能已经变化。CAS 只比较“当前值等不等于期望值”，无法知道中间发生过什么。

解决 ABA 的常见方法是加版本号或时间戳。AtomicStampedReference 会把引用和 stamp 一起比较，只有引用相同且版本号也相同才更新。这样 A -> B -> A 后，值虽然回到 A，但版本号已经变化，CAS 就能识别出它不是原来的 A。也可以用 AtomicMarkableReference 用布尔标记，或在业务上避免对象复用带来的歧义。

图解可以画成一条时间线：线程 1 读取 A 后暂停；线程 2 把 A 改成 B，再改回 A；线程 1 CAS 成功但误判。旁边再画版本号方案：A1 -> B2 -> A3，线程 1 期望 A1，发现当前是 A3，因此失败。最后落点：CAS 适合低冲突、短逻辑、单变量更新；高冲突或多变量一致性仍然要考虑锁、队列化或事务。回答时还可以补一句：乐观锁不是不加锁，而是把冲突检测推迟到提交那一刻，失败后要有重试或失败处理策略。线上如果 CAS 失败率很高，就要看是否需要降低竞争、分段计数或改用锁。`
  },
  "jvm-class-loading": {
    type: "flow",
    nodes: [
      ["加载", "读取 class 字节码并创建 Class 对象"],
      ["验证", "确保字节码结构和语义安全"],
      ["准备", "为静态变量分配内存并设默认值"],
      ["解析", "把符号引用转成直接引用"],
      ["初始化", "执行静态变量赋值和静态代码块"],
      ["使用和卸载", "类被引用使用，满足条件后可卸载"]
    ],
    detail: `类加载过程描述的是一个 class 文件如何从字节码变成 JVM 里可使用的 Class 对象。标准阶段可以记为加载、验证、准备、解析、初始化，后面还有使用和卸载。面试里最容易混的是“加载”和“初始化”，以及“准备阶段静态变量到底是什么值”。按阶段讲清楚，就能应对大多数追问。

加载阶段做三件事：通过类的全限定名找到字节码来源，把字节码读入 JVM，在方法区或元空间里生成类的元数据，并在堆里创建一个 java.lang.Class 对象，作为访问这个类元数据的入口。字节码来源不一定是本地 class 文件，也可能来自 jar、网络、动态生成或自定义 ClassLoader。

验证阶段是安全检查，确保字节码符合 JVM 规范，不会破坏虚拟机安全。包括文件格式验证、元数据验证、字节码验证、符号引用验证等。准备阶段给类变量，也就是 static 变量分配内存并设置默认零值。注意这里通常不是代码里写的初始值，比如 static int x = 10，在准备阶段 x 先是 0，初始化阶段才变成 10。final static 常量如果编译期可确定，可能在准备阶段就直接赋常量值。

解析阶段把常量池里的符号引用转换为直接引用。符号引用像“我要找某个类的某个方法”，直接引用则是 JVM 能直接定位到的地址、偏移或句柄。解析可以在初始化前做，也可能延迟到真正使用时做，JVM 规范允许一定灵活性。

初始化阶段才会真正执行类构造器 clinit，它由静态变量显式赋值和静态代码块按源码顺序合并而成。初始化有触发条件，比如 new 一个对象、访问类的静态变量、调用静态方法、反射使用类、初始化子类前先初始化父类等。只声明数组、访问编译期常量，通常不会触发目标类初始化。这个点常被用来考察代码执行顺序。

图解建议画成流水线：class 字节码进入 ClassLoader，依次经过加载、验证、准备、解析、初始化。准备阶段节点旁标出“默认值”，初始化阶段节点旁标出“执行 static 赋值和 static block”。再加一条父子类初始化顺序：先父类静态，再子类静态，然后对象创建时父类构造、子类构造。这样类加载和初始化就能分清楚。项目里如果遇到 ClassNotFound、NoClassDefFoundError、静态代码块执行时机异常，也可以沿着这条加载链路排查。还可以补充类加载器隔离：同名类如果由不同 ClassLoader 加载，在 JVM 看来也是不同类型。这个点在插件化、热部署、应用服务器里尤其常见。`
  },
  "jvm-gc-collectors": {
    type: "compare",
    nodes: [
      ["Serial", "简单单线程，适合小堆或客户端"],
      ["Parallel", "吞吐优先，多线程回收"],
      ["CMS", "老年代低停顿但碎片和并发失败风险"],
      ["G1", "区域化管理，平衡吞吐和停顿"],
      ["ZGC/Shenandoah", "超低停顿，适合大堆低延迟"],
      ["选择依据", "堆大小、停顿目标、吞吐和 JDK 版本"]
    ],
    detail: `垃圾收集器怎么选，核心不是背名字，而是看业务目标。不同收集器在吞吐量、停顿时间、堆大小、CPU 占用、实现复杂度之间做不同取舍。吞吐优先的系统希望单位时间内业务线程跑得更多；低延迟系统希望每次 Stop The World 尽可能短；大堆系统还要考虑标记、整理和并发阶段的成本。

Serial 是最简单的单线程收集器，回收时会暂停所有用户线程。它实现简单，额外开销小，适合小堆、客户端工具、单核或资源受限环境。Parallel Scavenge 和 Parallel Old 更偏吞吐优先，使用多线程回收，适合后台计算、批处理、对停顿不极端敏感但要求整体吞吐的场景。很多早期服务用 Parallel，是因为它稳定、吞吐好。

CMS 曾经是老年代低停顿代表。它采用并发标记和并发清理，减少长时间停顿。但 CMS 有明显缺点：基于标记清除，会产生内存碎片；并发阶段业务线程还在运行，会产生浮动垃圾；如果预留空间不足，可能 concurrent mode failure，退化成更慢的回收。JDK 9 后 CMS 被废弃，后续版本逐渐退出历史舞台。

G1 是现在常见的默认选择之一。它把堆划分成多个 Region，不再严格按连续的新生代老年代大块管理，可以优先回收收益高的 Region。G1 的目标是可预测停顿，通过设置 MaxGCPauseMillis 尝试在吞吐和停顿之间平衡。它适合中大堆、服务端应用、对停顿有要求但不一定极限低延迟的场景。需要注意，停顿目标不是硬保证，业务分配速率、对象存活率、堆大小都会影响实际效果。

ZGC 和 Shenandoah 主打超低停顿，很多阶段和业务线程并发执行，适合大堆、低延迟服务。它们通过染色指针、读屏障、并发整理等技术把停顿压得很短，但会消耗一定 CPU 和实现复杂度，也依赖较新的 JDK。选择它们前要确认 JDK 版本、部署环境、监控和压测结果。

图解可以画成决策树：小堆简单应用选 Serial；吞吐优先选 Parallel；通用服务端默认考虑 G1；大堆低延迟考虑 ZGC 或 Shenandoah。旁边列指标：平均停顿、P99 停顿、Full GC 次数、分配速率、晋升速率、老年代占用、CPU。面试里最后要强调：GC 调优不是先调参数，而是先看日志和指标，确认问题是停顿、吞吐、内存泄漏还是分配过快。真正上线前还要用接近真实流量的压测验证，不能只凭默认参数或经验值拍脑袋。收集器选择也要跟 JDK 版本绑定，不能在老版本上硬套新收集器经验。服务端还要关注容器内存限制，避免 JVM 看到的内存和容器限制不一致。`
  },
  "spring-aop": {
    type: "structure",
    nodes: [
      ["横切关注点", "日志、事务、权限、监控等通用逻辑"],
      ["切点", "定义哪些方法需要增强"],
      ["通知", "定义增强逻辑何时执行"],
      ["代理对象", "调用先进入代理再到目标方法"],
      ["适用场景", "无侵入地复用通用能力"],
      ["失效边界", "内部调用、final、非代理对象等"]
    ],
    detail: `Spring AOP 解决的是横切逻辑复用问题。很多系统里，日志、事务、权限、监控、审计、缓存这些逻辑会散落在大量业务方法前后。如果每个方法都手写一遍，业务代码会被污染，维护成本也高。AOP 的思想是把这类横切关注点抽成切面，在不修改目标业务方法的情况下，把增强逻辑织入到调用链里。

Spring AOP 的核心概念有几个。切点定义哪些连接点需要增强，通俗说就是匹配哪些类、哪些方法；通知定义增强逻辑什么时候执行，比如方法前、方法后、异常时、环绕执行；切面是切点和通知的组合；目标对象是原始业务对象；代理对象是 Spring 创建出来、对外暴露的增强对象。真正调用时，请求先进入代理对象，代理再决定是否执行增强，再调用目标方法。

Spring AOP 主要基于运行时代理，而不是像 AspectJ 那样做更深的编译期或类加载期织入。JDK 动态代理要求目标实现接口，CGLIB 通过生成子类代理目标类。正因为基于代理，Spring AOP 有边界：同类内部 this 调用不会经过代理；final 类或 final 方法难以被 CGLIB 覆盖；private 方法无法被正常代理增强；手动 new 出来的对象也不受容器代理管理。这些边界和 Spring 事务失效是同一类根因。

AOP 最典型的应用就是声明式事务。Transactional 本质上就是通过 AOP 在方法执行前开启事务，执行成功后提交，异常时回滚。除此之外，还可以做接口耗时统计、统一日志、权限校验、幂等校验、数据源切换、审计记录等。判断是否适合 AOP，要看这段逻辑是否跨多个业务模块重复出现，是否可以和业务主流程解耦。

不要滥用 AOP。过度切面会让代码执行路径变得隐蔽，调试时不知道逻辑从哪里插进来；切点写得太宽，可能误伤无关方法；切面顺序不清晰，事务、缓存、权限之间可能互相影响。生产里要注意切点范围、切面顺序、异常处理和日志可观测性。

图解可以画成两层：上层是业务方法 A、B、C 都需要日志、权限、事务；下层把这些横切逻辑抽成切面，由代理统一织入。再画调用链：调用方 -> 代理对象 -> 前置通知 -> 目标方法 -> 后置或异常通知。最后标出失效边界：内部调用不经过代理，非容器对象没有代理，final/private 方法不适合代理增强。排查 AOP 不生效时，就按“对象是否归容器管理、调用是否经过代理、切点是否匹配、代理方式是否支持”四步看。多个切面同时存在时，还要关注 order 顺序，否则日志、事务、异常处理的包裹关系会影响结果。`
  },
  "spring-mvc-flow": {
    type: "flow",
    nodes: [
      ["请求进入", "Tomcat 接收 HTTP 请求"],
      ["DispatcherServlet", "Spring MVC 的前端控制器"],
      ["HandlerMapping", "根据路径找到处理器"],
      ["HandlerAdapter", "适配并调用 Controller"],
      ["参数和返回值处理", "转换、校验、序列化"],
      ["响应返回", "视图渲染或 JSON 写回客户端"]
    ],
    detail: `Spring MVC 一次请求的执行流程，可以围绕 DispatcherServlet 这个前端控制器展开。HTTP 请求先到 Servlet 容器，比如 Tomcat。容器根据映射把请求交给 DispatcherServlet。DispatcherServlet 不直接写业务，而是负责调度整条 MVC 链路：找处理器、适配调用、处理参数、执行 Controller、处理返回值、渲染视图或写 JSON。

第一步是 HandlerMapping。它根据请求路径、HTTP 方法、请求头等信息，找到对应的 Handler，也就是你写的 Controller 方法以及拦截器链。RequestMappingHandlerMapping 是最常见的实现，它会在启动时扫描 RequestMapping、GetMapping、PostMapping 等注解，建立路径到方法的映射关系。如果路径匹配不到，就可能返回 404。

第二步是 HandlerAdapter。为什么找到 Handler 后还需要 Adapter？因为不同类型的处理器调用方式可能不同，适配器负责用统一方式调用它们。现在常见的是 RequestMappingHandlerAdapter，它会处理 Controller 方法参数，比如 PathVariable、RequestParam、RequestBody、ModelAttribute，还会触发类型转换、消息转换、数据绑定和校验。

调用 Controller 前后，拦截器会参与。preHandle 在 Controller 前执行，可以做登录校验、权限判断、TraceId 注入；postHandle 在 Controller 返回后、视图渲染前执行；afterCompletion 在整个请求完成后执行，适合清理资源和记录日志。要注意 Filter、Interceptor、AOP 的层次不同：Filter 在 Servlet 层更靠前，Interceptor 在 Spring MVC 层，AOP 则围绕 Bean 方法。

Controller 执行后，返回值会交给返回值处理器。返回页面时可能走 ViewResolver 找视图；返回 JSON 时，ResponseBody 或 RestController 会让 HttpMessageConverter 把对象序列化成 JSON 写入响应。请求体 JSON 反序列化也是消息转换器完成的，比如 MappingJackson2HttpMessageConverter。

图解建议画成请求流水线：客户端 -> Tomcat -> Filter -> DispatcherServlet -> HandlerMapping -> Interceptor preHandle -> HandlerAdapter -> 参数解析和消息转换 -> Controller -> 返回值处理 -> Interceptor afterCompletion -> 响应。回答时补一句排查思路：404 看映射，400 看参数绑定和校验，415/406 看消息转换器和 Content-Type，500 看 Controller、Service 或异常处理器。`
  },
  "spring-boot-autoconfiguration": {
    type: "flow",
    nodes: [
      ["引入 starter", "带来依赖和自动配置入口"],
      ["启动应用", "SpringApplication 创建上下文"],
      ["加载自动配置类", "从 imports 候选配置中筛选"],
      ["条件注解判断", "按 class、bean、property 决定是否生效"],
      ["注册默认 Bean", "没有用户自定义时提供默认实现"],
      ["覆盖和调试", "通过配置、排除和条件报告定位"]
    ],
    detail: `Spring Boot 自动配置解决的是“约定大于配置”。传统 Spring 项目要手动配 DispatcherServlet、数据源、事务管理器、消息转换器、模板引擎等。Spring Boot 通过 starter 引入依赖，再根据类路径、配置项和已有 Bean 自动创建一批默认 Bean，让项目开箱即用。自动配置不是魔法，而是一组带条件判断的配置类。

starter 的作用是把常用依赖组合好。比如 web starter 会带来 Spring MVC、嵌入式 Tomcat、Jackson 等依赖。引入依赖后，自动配置类才有生效的基础。启动时，Spring Boot 会通过自动配置导入机制加载候选配置类。新版本主要通过 AutoConfiguration.imports 文件列出候选，旧版本常见 spring.factories。候选类很多，但不是全部生效。

是否生效取决于条件注解。ConditionalOnClass 表示类路径里有某个类才配置；ConditionalOnMissingBean 表示容器里没有用户自定义 Bean 才创建默认 Bean；ConditionalOnProperty 表示某个配置项满足条件才生效；ConditionalOnWebApplication 表示 Web 环境才生效。通过这些条件，Boot 可以做到“你引了什么依赖、配了什么属性、自己有没有定义 Bean，我就按情况补默认配置”。

自动配置遵循一个重要原则：用户自定义优先。比如你自己定义了 DataSource 或 ObjectMapper，很多默认配置会因为 ConditionalOnMissingBean 而退让。这就是为什么 Boot 既能开箱即用，又不会完全剥夺定制能力。你也可以通过配置属性调整默认 Bean，或者使用 exclude 排除某个自动配置类。

排查自动配置问题时，可以打开 debug 或查看条件评估报告，看看某个自动配置为什么 matched 或 not matched。常见问题包括依赖没引入、配置项写错、自己定义的 Bean 导致默认 Bean 不再创建、多个 starter 带来冲突。面试里能说出“看 condition evaluation report”，会比只背 EnableAutoConfiguration 更实用。

图解可以画成漏斗：starter 带来依赖，自动配置候选类进入漏斗，ConditionalOnClass、ConditionalOnProperty、ConditionalOnMissingBean 层层筛选，最后注册默认 Bean。旁边画用户自定义 Bean 可以覆盖默认配置。最后落点：自动配置的本质是条件化配置类，不是隐藏逻辑；能用默认，也能通过配置、Bean 或 exclude 接管。`
  },
  "mysql-mvcc": {
    type: "sequence",
    nodes: [
      ["行记录", "包含隐藏事务 ID 和回滚指针"],
      ["undo log", "保存旧版本并串成版本链"],
      ["Read View", "记录当前活跃事务快照"],
      ["可见性判断", "根据事务 ID 判断版本能否看到"],
      ["快照读", "普通 select 读历史可见版本"],
      ["当前读", "update 或 for update 读最新并加锁"]
    ],
    detail: `MVCC 是多版本并发控制，InnoDB 用它来提升读写并发。没有 MVCC 时，读和写很容易互相阻塞：写事务改一行，读事务要么等锁，要么读到不一致数据。MVCC 的思路是为同一行保留多个历史版本，读事务不一定读最新版本，而是根据自己的 Read View 找到“对我可见”的版本。这样普通 select 可以快照读，不必阻塞正在写的事务。

InnoDB 每行记录里有隐藏字段，常讲的是 trx_id 和 roll_pointer。trx_id 记录最近一次修改这行的事务 ID，roll_pointer 指向 undo log 里的旧版本。每次更新行时，InnoDB 会把旧值写入 undo log，并让当前记录指向旧版本，于是形成一条版本链。最新记录在数据页里，历史版本在 undo log 中。

Read View 是快照读的关键。它记录生成快照时系统中活跃事务列表、最小活跃事务 ID、下一个将分配的事务 ID 等信息。读某个版本时，InnoDB 会根据版本上的 trx_id 和 Read View 判断这个版本是否可见：如果版本由当前事务自己修改，可见；如果事务 ID 早于活跃事务范围，通常可见；如果创建版本的事务在 Read View 活跃列表里，说明当时还没提交，不可见；如果太新，也不可见。不可见就沿着 undo 版本链往前找旧版本。

隔离级别会影响 Read View 生成时机。Read Committed 下，每次快照读都会生成新的 Read View，所以同一事务两次 select 可能看到其他事务新提交的数据。Repeatable Read 下，事务内第一次快照读生成 Read View，后续通常复用，因此多次读取结果稳定。这也是 MySQL 默认 RR 能做到可重复读的重要原因。

MVCC 不等于完全不加锁。普通 select 是快照读，读历史可见版本，不加行锁；但 update、delete、insert、select for update、select in share mode 属于当前读，要读最新版本并配合锁。范围更新还可能涉及 next-key lock 来防幻读。面试里要把快照读和当前读分开，否则容易说成“MVCC 解决所有并发问题”。

图解可以画一行数据的版本链：当前版本 trx_id=30，roll_pointer 指向 trx_id=20，再指向 trx_id=10。旁边画一个 Read View，标出活跃事务列表。读取时从新到旧逐个判断，找到第一个可见版本返回。底部再分叉：普通 select 走快照读，update/for update 走当前读和锁。`
  },
  "mysql-b-plus-tree": {
    type: "structure",
    nodes: [
      ["多叉树", "一个节点存多个 key 降低树高"],
      ["磁盘页", "节点大小贴合页读取"],
      ["非叶子索引", "只存 key 和指针提高扇出"],
      ["叶子存数据", "聚簇索引叶子保存整行"],
      ["链表相连", "范围查询顺序扫描更高效"],
      ["稳定 IO", "少量随机 IO 定位，大量顺序读范围"]
    ],
    detail: `MySQL 用 B+ 树做索引，核心原因是它适合磁盘和范围查询。数据库索引不是只在内存里跑，更多时候要考虑磁盘页读取成本。一次磁盘随机 IO 很贵，所以索引结构要尽量降低树高，让定位一条记录需要读取的页数尽可能少。B+ 树是多叉平衡树，一个节点能存很多 key 和指针，扇出大，树高低，几层就能管理大量数据。

和二叉搜索树相比，B+ 树每个节点不是只有两个孩子，而是可以有很多孩子。假设一个页 16KB，一个索引项较小，一个非叶子节点可以容纳大量 key 和页指针。这样千万级数据可能也只需要三四层树高。查询时从根节点到叶子节点，每层读一个页，随机 IO 次数可控。红黑树虽然在内存里查找效率好，但树高相对高，节点分散，不适合磁盘页批量读取。

B+ 树和 B 树的区别也要会说。B 树的非叶子节点也可能存数据，B+ 树通常把数据都放在叶子节点，非叶子节点只做索引导航。这样非叶子节点能放更多 key，扇出更大，树更矮。并且所有数据都在叶子层，查询路径更稳定。InnoDB 聚簇索引的叶子节点存整行数据，二级索引的叶子节点存主键值，再通过主键回表查整行。

B+ 树的另一个优势是范围查询。叶子节点之间有链表连接，查到范围起点后，可以沿着叶子链表顺序向后扫描。这对 between、order by、分页、前缀范围非常重要。哈希索引等值查询可能很快，但不支持范围有序扫描；跳表也能做范围，但数据库页管理和稳定性上 B+ 树更成熟。

当然 B+ 树也不是所有场景最优。低选择性字段建索引收益有限，频繁更新索引列会带来维护成本，随机主键插入可能导致页分裂和碎片。为什么推荐自增主键或趋势递增 ID？因为聚簇索引按主键组织数据，递增写更容易追加到后面，减少页分裂。UUID 做主键可能导致随机插入，影响写性能和空间利用率。

图解可以画成三层 B+ 树：根节点、非叶子节点、叶子节点。非叶子节点只画 key 和箭头，叶子节点画数据行并用链表连起来。查询单行时从根向下定位，范围查询时先定位起点再沿叶子链表扫描。最后一句收束：B+ 树不是因为查找复杂度看起来高级，而是因为它把磁盘 IO、树高、范围扫描和页结构都平衡得很好。再补一个工程点：索引越多写入越慢，因为每次插入、更新索引列都要维护对应 B+ 树。设计索引时要同时考虑查询收益和写入维护成本，不是越多越好。还要考虑主键选择，聚簇索引会影响整行数据的物理组织，随机主键会增加页分裂概率。二级索引查询如果不能覆盖字段，还要回到聚簇索引取整行，这就是回表成本。`
  },
  "mysql-redo-undo-binlog": {
    type: "compare",
    nodes: [
      ["redo log", "InnoDB 崩溃恢复，保证已提交修改不丢"],
      ["undo log", "事务回滚和 MVCC 历史版本"],
      ["binlog", "Server 层归档日志，用于复制和恢复"],
      ["两阶段提交", "协调 redo 和 binlog 一致"],
      ["主从复制", "从库重放 binlog"],
      ["排查取舍", "看恢复、回滚、复制分别找谁"]
    ],
    detail: `redo log、undo log、binlog 的区别，可以按用途拆：redo log 保证崩溃恢复，undo log 支持事务回滚和 MVCC，binlog 用于主从复制和归档恢复。三者都和“日志”有关，但层次不同、记录内容不同、服务目标也不同。面试里不要混成“都是恢复用的日志”。

redo log 是 InnoDB 存储引擎层的重做日志。数据库修改数据页时，不一定立刻把脏页刷到磁盘，因为随机写数据页成本高。InnoDB 会先把修改写入 redo log，再在合适时机刷脏页。只要事务提交时 redo log 按策略落盘，即使数据库宕机，重启后也能根据 redo log 把已提交但尚未刷入数据页的修改重做回来。这就是 WAL，先写日志再写数据。

undo log 记录的是修改前的旧版本。事务回滚时，需要根据 undo log 把数据恢复到修改前状态；MVCC 快照读也依赖 undo log 形成版本链，让读事务能看到历史可见版本。比如一行从 10 改成 20，undo 里会保留 10。和 redo 的方向相反：redo 是把已提交修改重做，undo 是把未提交或需要回滚的修改撤销。

binlog 是 MySQL Server 层的二进制日志，不只 InnoDB 有，其他引擎也可以写。它记录数据库层面的逻辑变更，常见格式有 statement、row、mixed。主从复制依赖 binlog，从库拉取主库 binlog 并重放；按时间点恢复也依赖全量备份加 binlog 增量回放。binlog 更偏归档和复制，redo 更偏崩溃恢复。

redo 和 binlog 都要保证事务提交一致，所以有两阶段提交。大致流程是 InnoDB 先写 redo prepare，然后 Server 写 binlog，再写 redo commit。这样崩溃恢复时可以根据 redo 和 binlog 状态判断事务是否完整提交，避免出现主库数据和 binlog 不一致，进而导致主从不一致。这个点是高级追问，能说清楚会很加分。

图解可以画成一次 update 的流程：修改前写 undo，修改产生 redo，提交时协调 redo 和 binlog。旁边三列对比：redo 负责崩溃恢复，undo 负责回滚和版本链，binlog 负责复制和归档。最后给排查提示：宕机后数据恢复看 redo，事务撤销和 MVCC 看 undo，主从同步和误删恢复看 binlog。回答时还可以强调层次：redo 和 undo 更偏 InnoDB，binlog 位于 Server 层，所以它们的职责边界不同。误删恢复一般依赖备份加 binlog，而不是靠 redo 直接回到任意历史点。`
  },
  "mysql-lock-deadlock": {
    type: "flow",
    nodes: [
      ["事务 A 持有资源 1", "等待资源 2"],
      ["事务 B 持有资源 2", "等待资源 1"],
      ["循环等待", "形成死锁条件"],
      ["InnoDB 检测", "发现等待图中的环"],
      ["回滚一方", "选择代价较小事务作为牺牲者"],
      ["排查治理", "固定加锁顺序、缩短事务、建好索引"]
    ],
    detail: `MySQL 死锁的本质是多个事务互相等待对方持有的锁，形成循环等待。最简单例子是事务 A 锁住 id=1 后想锁 id=2，事务 B 锁住 id=2 后想锁 id=1，双方都不释放已有锁，就卡住了。InnoDB 会做死锁检测，发现等待图中有环后，主动回滚其中一个事务，让另一个事务继续执行，所以应用层通常看到 Deadlock found when trying to get lock 这类错误。

死锁不只发生在显式 select for update。普通 update、delete、唯一索引插入、外键检查、范围更新都可能加锁。尤其是没有合适索引时，MySQL 可能扫描更多行并加更多锁，锁范围变大，死锁概率上升。InnoDB 在可重复读下还有 next-key lock，范围条件可能锁住记录和间隙，防止幻读，但也会扩大锁冲突范围。

死锁的四个必要条件是互斥、持有并等待、不可抢占、循环等待。数据库里前三个很常见，很难完全消除，所以治理重点通常是打破循环等待。最有效的方法是固定加锁顺序：所有业务都按同样顺序访问资源，比如按账户 ID 从小到大扣款，按商品 ID 排序后锁库存。这样就不容易出现 A 等 B、B 等 A 的环。

还要缩短事务。事务里不要夹杂远程调用、用户交互、大量计算或慢 SQL。事务越长，持锁时间越长，别人等待的窗口越大。把无关逻辑挪到事务外，先查后算，最后用短事务更新关键数据。批量更新要控制批次，避免一次锁太多行。索引也很关键，where 条件命中合适索引，可以减少扫描和加锁范围。

排查死锁时，第一步看 SHOW ENGINE INNODB STATUS 里的 latest detected deadlock，里面会显示两个事务各自持有什么锁、等待什么锁、执行的 SQL。MySQL 也可以开启 innodb_print_all_deadlocks 把死锁日志打到错误日志。拿到 SQL 后，要结合表结构、索引、事务代码路径，还原两个事务的加锁顺序。不要只看报错 SQL，因为真正持锁的 SQL 可能在事务前面。

图解可以画成等待环：事务 A 持有行 1 等行 2，事务 B 持有行 2 等行 1，中间形成闭环。旁边画治理链路：固定顺序、缩短事务、命中索引、降低隔离范围、失败重试。最后补一句工程实践：死锁在高并发数据库里不能保证绝对不出现，应用层要能捕获死锁异常并做有限次数重试，同时通过日志和监控持续降低发生概率。还要避免重试风暴，重试应有次数限制、退避和幂等保护。排查结论最好沉淀到代码规范里，比如统一资源排序加锁。`
  },
  "redis-data-structures": {
    type: "compare",
    nodes: [
      ["String", "计数、缓存、分布式锁基础值"],
      ["Hash", "对象字段缓存，适合局部更新"],
      ["List", "有序列表和简单队列"],
      ["Set", "去重集合、共同关注、标签"],
      ["ZSet", "排行榜、延迟队列、按分数排序"],
      ["选择原则", "按访问模式和数据规模选结构"]
    ],
    detail: `Redis 数据结构的回答重点，是把结构和业务场景对应起来。Redis 不是只有 key-value，它的 value 可以是 String、Hash、List、Set、ZSet、Stream、Bitmap、HyperLogLog 等。面试常问的基础版本，至少要把 String、Hash、List、Set、ZSet 讲清楚，并能说明为什么某个业务场景适合它，而不是只背命令名。

String 是最基础的结构，可以存字符串、数字、JSON、二进制。常见场景是普通缓存、计数器、分布式锁的锁值、限流计数。incr/decr 可以做原子计数，set nx ex 可以做简单锁。缺点是如果把大对象整体序列化成一个 String，局部字段更新时要整块读写，容易形成大 key。

Hash 适合对象型数据，比如用户资料、商品信息、配置项。它可以按字段读写，比整个 JSON String 更适合局部更新。但 Hash 字段太多、value 太大，也会变成大 key。是否用 Hash，要看业务是否经常只读写对象的一部分字段，以及字段数量是否可控。

List 是按插入顺序排列的列表，可以从两端 push/pop，适合简单队列、时间线、最新列表。但如果要做可靠消息、消费者组、确认机制，List 就不如 Stream 或专业 MQ。List 按下标访问中间元素不是强项，列表很长时也要注意阻塞和内存。

Set 是无序去重集合，适合标签、关注关系、抽奖去重、共同好友、黑白名单。它的交集、并集、差集很好用，但集合过大时这些集合运算可能很重，生产里要注意拆分、异步化或限制规模。ZSet 是带分数的有序集合，适合排行榜、按时间排序任务、延迟队列、热度排序。它能按 score 范围查询，也能取排名，但更新分数和维护跳表/字典都有成本。

图解可以画成一张选择表：左列结构，中列核心特性，右列业务场景。最后加决策节点：是否需要排序，选 ZSet；是否需要去重，选 Set；是否是对象字段，选 Hash；是否是简单值或计数，选 String；是否是顺序队列，考虑 List 或 Stream。回答里补一句：选结构要看读写模式、数据量、过期策略和是否会形成热 key 或大 key。真正落地时还要考虑序列化方式、key 命名规范、TTL、内存淘汰策略和监控，否则结构选对了也可能因为容量和运维问题出事故。

如果面试官给具体业务，可以先问访问模式：是否按 ID 点查、是否按时间排序、是否需要去重、是否需要集合运算、单 key 数据量会不会增长。根据这些问题反推结构，会比直接报“用 ZSet”更稳。`
  },
  "redis-hot-key-big-key": {
    type: "compare",
    nodes: [
      ["热 key", "访问过于集中导致单点压力"],
      ["大 key", "单个 key 占用内存或操作耗时过大"],
      ["发现方式", "监控、slowlog、bigkeys、hotkeys"],
      ["拆分分散", "分片、复制、本地缓存"],
      ["异步处理", "删除和重建避免阻塞"],
      ["治理闭环", "限制大小、预警、压测和降级"]
    ],
    detail: `热 key 和大 key 是 Redis 两类不同风险。热 key 的问题是访问集中：某个 key 被大量请求同时读写，导致单个 Redis 节点、单个分片、甚至单线程事件循环压力过高。大 key 的问题是体积过大：一个 key 里存了太多元素或太大的 value，操作它会占用大量内存、网络和 CPU，甚至阻塞 Redis。一个 key 既可能热，也可能大，但治理思路不同。

热 key 常见于热点商品、热门活动配置、明星用户信息、秒杀库存、全站配置。读热 key 可以用本地缓存、客户端缓存、多级缓存、读写分离、热点 key 副本分散等方式缓解。写热 key 更麻烦，因为写要保持一致性，可能要通过分片计数、队列削峰、批量合并或业务维度拆分来降低单点冲突。比如计数器可以拆成多个分片 key，读取时汇总。

大 key 常见于一个 Hash 存了几十万字段，一个 List 或 ZSet 存了大量元素，一个 String 存了超大 JSON。大 key 的危害包括内存倾斜、迁移慢、删除阻塞、网络传输大、AOF/RDB 成本高、集群 rehash 或主从同步抖动。删除大 key 不建议直接 del，可以用 unlink 异步释放，或者分批 scan/hscan/zscan 删除。

发现热 key 和大 key 要靠工具和监控。Redis 自带 --bigkeys 可以粗略扫描大 key，--hotkeys 在 LFU 策略下可辅助发现热 key；slowlog 能看到慢命令；monitor 不建议生产长时间使用；云厂商或代理层通常也有热点分析。业务侧也应该打点 key 维度访问量、命中率、value 大小和操作耗时。

治理时要把预防做在设计阶段。key 的 value 大小要有限制，集合类结构要有最大元素数或分片规则；热门活动要提前预热和压测；缓存穿透、击穿、雪崩要有保护；删除和迁移要异步或分批。不要等线上 Redis CPU 打满后才发现某个 key 被全站请求打爆。

图解可以画左右对比：热 key 是很多箭头打向一个小 key，解决方案是副本、本地缓存、分片和限流；大 key 是一个 key 体积膨胀，解决方案是拆分、限制元素数、分批处理和异步删除。底部加监控闭环：发现、定位、拆分、验证、告警。面试里还可以补一个灰度治理思路：先定位具体 key 和调用方，再小流量切换拆分方案，确认命中率、延迟和 Redis CPU 都下降后再全量。

常见追问是“怎么预防”。可以回答：上线前限制集合元素数量，定期扫描 big key，活动前预热热点数据，客户端统计 key 访问频率，删除大 key 使用 unlink 或分批删除。`
  },
  "mq-idempotent-consume": {
    type: "flow",
    nodes: [
      ["至少一次投递", "重复消息天然可能出现"],
      ["业务唯一键", "订单号、消息 ID、事件 ID"],
      ["幂等判断", "先查状态或去重表"],
      ["原子落库", "唯一索引或事务保护"],
      ["重复直接返回", "不要重复执行业务副作用"],
      ["监控补偿", "记录重复、失败和死信"]
    ],
    detail: `MQ 重复消费的前提是：很多消息系统为了保证不丢，采用至少一次投递。只要有 ack、重试、网络超时、消费者宕机，就可能出现消息已经处理成功但 ack 没提交，Broker 之后又投递一次。也可能生产者发送后没收到确认，于是重发，导致 Broker 里本来就有重复消息。所以重复不是异常边角，而是可靠消息模型下必须面对的常态。

解决重复消费，核心不是让 MQ 永远不重复，而是业务幂等。幂等的意思是同一条业务消息执行一次和执行多次，最终结果一样。要做到幂等，消息里必须有业务唯一标识，比如订单号、支付流水号、事件 ID、用户 ID 加活动 ID。没有唯一键，就很难判断两条消息是不是同一件事。

常见做法有几类。第一类是状态机幂等，比如订单只能从待支付变成已支付，已经已支付再收到支付成功消息就直接返回。第二类是数据库唯一索引，比如发券记录用 user_id + coupon_id 做唯一约束，重复插入会失败。第三类是去重表，消费前先插入 message_id，插入成功才处理，插入失败说明处理过。第四类是 Redis setnx 去重，但要注意过期时间和最终一致性，强一致场景最好数据库兜底。

幂等判断和业务处理要尽量在同一个事务里完成。否则先判断没处理，业务执行成功，但去重记录没写入，后续还会重复；或者先写去重，业务失败，消息再来也被误判已处理。设计时要把“幂等记录、业务状态变更、ack 时机”串起来。一般是业务处理成功并提交事务后再 ack；失败则不 ack 或抛异常让 MQ 重试。

还要处理副作用。比如发送短信、调用三方支付、发货这些操作一旦重复，后果比较大。可以把外部调用也设计成幂等接口，传入业务唯一号，让对方重复请求返回同一结果。或者把外部副作用放到状态机后面，确保只有状态从未处理推进到已处理的那一次才触发。

图解可以画成消费流程：收到消息 -> 提取业务唯一键 -> 查状态或插入去重表 -> 首次处理则执行业务并提交 -> ack；如果发现已处理则直接 ack；如果失败则重试或死信。最后强调：重复消费不是靠“消费者小心一点”解决，而是靠业务唯一键、状态机、唯一索引和事务边界共同保证。项目里还要记录重复消息次数和幂等命中率，它们能反映上游重试、网络抖动或消费者超时问题。

如果是金额、库存、发券这类强副作用业务，幂等一定要落到数据库唯一约束或状态机，不能只依赖 Redis 去重。Redis 过期或丢失后，数据库仍然要能兜底。消费者代码也要避免先 ack 后处理，正确顺序通常是业务事务成功后再确认消息。`
  },
  "mq-ordering": {
    type: "flow",
    nodes: [
      ["明确顺序范围", "全局有序还是局部有序"],
      ["同 key 路由", "同订单或用户进入同一队列分区"],
      ["单线程消费", "同一分区内按顺序处理"],
      ["失败阻塞", "前一条失败会影响后续消息"],
      ["业务补偿", "乱序时靠状态机和重试兜底"],
      ["权衡吞吐", "顺序越强并发越低"]
    ],
    detail: `消息队列保证顺序，第一步要问清楚“什么范围内有序”。全局顺序是所有消息都按发送顺序消费，代价非常高，通常需要单队列、单分区、单消费者，吞吐会很差。大多数业务并不需要全局有序，只需要某个业务维度内有序，比如同一订单的创建、支付、发货消息有序；同一用户的账户变更有序；同一商品的库存流水有序。

局部有序的常见做法是按业务 key 路由。生产者发送消息时，把同一个订单号或用户 ID 的消息发送到同一个队列、同一个分区或同一个 MessageQueue。Broker 在单个队列或分区内通常能保持消息顺序；消费者对同一队列按顺序拉取和处理，就能保证该 key 的局部顺序。Kafka 用 partition key，RocketMQ 可以用 MessageQueueSelector，道理类似。

消费者侧也要配合。如果一个分区的消息被多个线程并发处理，即使拉取顺序正确，业务完成顺序也可能乱。所以要么同一队列单线程消费，要么在消费者内部按 key 做串行化。很多系统会把不同 key 分散到不同队列以提升并发，但同一个 key 内保持顺序。顺序和吞吐永远有取舍：顺序范围越大，并发空间越小。

失败处理是顺序消息最容易踩坑的地方。如果某条消息处理失败，后面的消息是否能继续？如果继续，业务顺序可能乱；如果不继续，整个队列可能被卡住。RocketMQ 顺序消费失败会暂停当前队列一段时间再重试。业务上要让单条消息处理尽量幂等、快速、可重试，必要时进入人工补偿或跳过机制，但跳过要能保证状态机不会被破坏。

还要承认网络和重试可能造成乱序。生产者重试、不同分区、消费者重平衡、手动补偿，都可能让消息顺序变复杂。因此核心业务不要只依赖 MQ 顺序，还要靠业务状态机兜底。比如订单已经发货，再收到支付成功重复消息，状态机应该识别当前状态并忽略或补偿，而不是盲目回退。

图解可以画成两层：生产者按 orderId 哈希，把同一订单消息路由到同一队列；消费者按队列顺序处理。旁边画一个失败分支：消息 2 失败，消息 3 暂停或进入补偿。最后落点：先定义顺序范围，再设计路由和消费模型，最后用幂等和状态机兜底。如果面试官追问吞吐，就说可以扩大队列数量，让不同业务 key 并行，但同一 key 内仍串行。

还要补充乱序容忍设计。很多业务可以通过版本号、状态流转校验、更新时间比较来拒绝旧消息，这样即使偶发乱序，也不会把业务状态倒退。如果某条消息长期失败，要有告警和人工补偿入口，否则一个分区可能被卡住，后续同 key 消息都无法推进。`
  },
  "http-https": {
    type: "compare",
    nodes: [
      ["HTTP", "明文传输，默认 80 端口"],
      ["HTTPS", "HTTP over TLS，默认 443 端口"],
      ["加密", "防止内容被窃听"],
      ["身份认证", "证书证明服务端身份"],
      ["完整性", "防止内容被篡改"],
      ["成本和运维", "证书、握手、性能和续期"]
    ],
    detail: `HTTP 和 HTTPS 的核心区别是安全层。HTTP 是明文协议，请求和响应内容在网络中可以被中间人看到和篡改。HTTPS 不是一个全新的应用层协议，而是在 HTTP 和 TCP 之间加了 TLS/SSL 安全层，可以提供加密、身份认证和完整性保护。默认端口上，HTTP 常用 80，HTTPS 常用 443。

加密解决窃听问题。用户提交账号、密码、token、订单信息时，如果用 HTTP 明文传输，经过同一网络或中间链路的人可能直接看到内容。HTTPS 通过 TLS 协商出会话密钥，后续应用数据用对称加密传输。对称加密效率高，但密钥协商需要非对称加密或密钥交换算法参与。

身份认证解决“我连的是不是真的服务器”。服务端会提供证书，证书由 CA 签发，里面包含域名、公钥、有效期等信息。客户端浏览器会验证证书链、域名是否匹配、是否过期、是否被吊销。没有证书认证，就可能被中间人伪装成目标站点。HTTPS 不是只加密，还要确认对方身份。

完整性保护解决篡改问题。TLS 会对传输内容做消息认证，防止中间人悄悄修改响应内容，比如插入脚本、替换下载文件、篡改接口返回。加密、认证、完整性三者合起来，才是 HTTPS 的主要价值。

HTTPS 的成本包括 TLS 握手、证书申请和续期、加解密开销、配置复杂度。但现代硬件和协议优化已经让性能成本大幅下降，HTTP/2、TLS 1.3、连接复用、会话恢复都能降低开销。生产里更常见的问题反而是证书过期、证书链不完整、域名不匹配、代理层终止 TLS 后内网明文传输是否可接受。

图解可以画两条链路：HTTP 是客户端直接明文到服务端，中间人可以看见和修改；HTTPS 是客户端先和服务端通过证书和握手建立 TLS 通道，再在加密通道里传 HTTP。底部列对比：安全性、端口、证书、性能、运维复杂度。最后一句收束：现在涉及登录、支付、用户数据、后台管理的系统，HTTPS 基本是默认要求。生产里还要关注 HSTS、证书自动续期、TLS 版本禁用和反向代理透传真实协议头，否则也会出现安全或跳转问题。

如果继续追问握手，可以简化回答：TLS 先验证证书和协商密钥，之后用对称加密传输数据。TLS 1.3 减少了握手往返，连接复用和会话恢复也能降低 HTTPS 开销。排查 HTTPS 问题时，看证书链、域名匹配、过期时间、TLS 版本、反向代理配置和浏览器安全报错。还要注意混合内容，HTTPS 页面加载 HTTP 资源会被浏览器拦截或降级，接口域名也要统一协议和跳转策略。`
  },
  "dns-resolution": {
    type: "sequence",
    nodes: [
      ["浏览器缓存", "先查本地已有解析结果"],
      ["系统和 hosts", "查操作系统缓存和 hosts 文件"],
      ["本地 DNS", "运营商或配置的递归解析器"],
      ["根域 DNS", "告诉顶级域服务器位置"],
      ["顶级域 DNS", "找到权威 DNS"],
      ["权威 DNS", "返回最终 IP 并按 TTL 缓存"]
    ],
    detail: `DNS 解析的目标，是把用户输入的域名转换成 IP 地址。一次完整解析通常不是每次都从根域开始，因为浏览器、操作系统、本地 DNS 都会缓存结果。面试时可以按“缓存优先，递归查询，分级授权”的思路讲：先查本地能不能直接得到答案，查不到再让本地 DNS 递归地去问根域、顶级域和权威 DNS。

第一步通常是浏览器缓存。浏览器可能缓存最近解析过的域名。没有命中时，会查操作系统 DNS 缓存和 hosts 文件。hosts 是本地静态映射，优先级通常比较高，开发和测试环境经常用它临时指向某个 IP。再没有命中，就把请求交给本地 DNS 服务器，也叫递归解析器，可能是运营商 DNS、公司内网 DNS、公共 DNS。

本地 DNS 如果自己缓存里有结果，就直接返回；没有就开始迭代查询。它先问根域 DNS：某个顶级域，比如 com、cn，该去问谁？根域返回对应顶级域 DNS 地址。本地 DNS 再问顶级域 DNS：这个域名的权威 DNS 是谁？顶级域返回权威 DNS 地址。最后本地 DNS 问权威 DNS，拿到 A 记录或 AAAA 记录，也就是 IPv4 或 IPv6 地址。

DNS 还有 CNAME、TTL、负载均衡等重要点。CNAME 表示别名，常用于 CDN 或云服务，把业务域名指向另一个域名；TTL 决定解析结果缓存多久，TTL 太长，切流或故障迁移生效慢；TTL 太短，解析压力更高。很多大型网站会通过 DNS 返回不同地区、不同运营商、不同健康状态的 IP，实现就近访问或容灾切换。

排查 DNS 问题时，要分层看。浏览器访问慢不一定是后端慢，可能是 DNS 慢、解析到了错误 IP、缓存没刷新、权威 DNS 配置错、CNAME 链太长、运营商 DNS 污染。常用工具包括 nslookup、dig、ping、curl 观察连接目标、浏览器 network 面板。修改 DNS 后不要立刻以为全球生效，要考虑 TTL 和各级缓存。

图解可以画成时序图：浏览器 -> 操作系统/hosts -> 本地 DNS -> 根域 -> 顶级域 -> 权威 DNS -> 返回 IP。每一层旁边标出缓存和 TTL。最后落点：DNS 是分布式分层命名系统，既影响访问速度，也影响故障切换和流量调度。项目里做域名切换时，提前降低 TTL、灰度验证解析结果、准备回滚记录很重要，不然故障迁移会被缓存拖慢。

排查时不要只 ping 域名一次就下结论。要分别查本地 DNS、公共 DNS、权威 DNS 的结果，看是否存在不同地区解析不一致、CNAME 链异常或缓存未过期。CDN 场景还要确认解析到的边缘节点是否符合地域和运营商预期，否则用户会绕远访问。`
  },
  "os-deadlock": {
    type: "structure",
    nodes: [
      ["互斥", "资源一次只能被一个执行单元占用"],
      ["占有且等待", "持有资源同时等待新资源"],
      ["不可抢占", "资源不能被强行夺走"],
      ["循环等待", "多个执行单元形成等待环"],
      ["预防思路", "破坏任一必要条件"],
      ["工程治理", "顺序加锁、超时、检测和回滚"]
    ],
    detail: `死锁是多个执行单元互相等待对方释放资源，导致都无法继续执行。操作系统、数据库、并发程序里都会出现死锁。经典四个必要条件是互斥、占有且等待、不可抢占、循环等待。它们是“必要条件”，也就是说死锁发生时四个条件都成立；只要破坏其中任意一个，就能预防死锁。

互斥是指某些资源一次只能被一个线程或进程占用，比如锁、打印机、数据库行锁。占有且等待是指一个执行单元已经持有一部分资源，同时还在等待其他资源。不可抢占是指资源不能被外部强行拿走，只能由持有者主动释放。循环等待是指 A 等 B，B 等 C，C 又等 A，形成等待环。四个条件里，工程上最常破坏的是循环等待。

预防死锁的方法要和条件对应。破坏占有且等待，可以要求线程一次性申请所有资源，拿不到就释放已拿资源，但这会降低并发。破坏不可抢占，可以设置锁超时，拿不到就放弃并回滚。破坏循环等待，可以给资源排序，所有线程都按同一顺序申请资源。数据库转账、库存扣减、多账户操作里，按 ID 排序加锁就是典型做法。

死锁检测则是允许死锁可能发生，但系统负责发现和解除。数据库 InnoDB 会检测等待图中的环，然后回滚一个事务。操作系统或应用层也可以通过等待图、超时、线程栈来定位。Java 程序里可以用 jstack 看到线程互相等待的锁，很多 JVM 工具也会直接标出 deadlock。

工程里还要区分死锁、活锁和饥饿。死锁是互相等不动；活锁是大家都在动但不断让步，始终没有进展；饥饿是某个线程长期拿不到资源。解决方案不同，不能混为一谈。比如公平锁可以缓解饥饿，但不一定解决死锁；超时重试可能解决死锁，但如果所有线程同时重试，可能造成活锁或重试风暴。

图解可以画四个线程围成环：T1 持有 R1 等 R2，T2 持有 R2 等 R3，T3 持有 R3 等 R1。旁边列四个必要条件，并用红色标出“循环等待”这个最常破坏点。最后给工程建议：固定加锁顺序、缩短临界区、避免锁内远程调用、设置超时、失败可回滚、用监控和线程栈定位。回答时还可以联系数据库死锁和 Java 锁死锁，说明原理一致，只是资源类型和检测工具不同。

如果问怎么定位 Java 死锁，可以说用 jstack 或线程转储查看 BLOCKED 线程和 locked ownable synchronizers，找到互相等待的锁对象和代码位置。修复后要补并发测试和锁顺序规范，避免同一类问题换个入口再次出现。锁里不要调用不稳定的外部接口，这是减少死锁和长时间阻塞的基本习惯，也能降低故障扩散风险。`
  },
  "io-multiplexing": {
    type: "compare",
    nodes: [
      ["传统阻塞 IO", "一个线程容易被一个连接阻塞"],
      ["多路复用", "一个线程监听多个 fd"],
      ["select", "数组扫描，有数量限制"],
      ["poll", "链表结构，无固定数量限制但仍扫描"],
      ["epoll", "事件驱动，适合大量连接"],
      ["适用场景", "高并发网络服务和事件循环"]
    ],
    detail: `IO 多路复用解决的是一个线程如何高效管理多个连接的问题。传统阻塞 IO 中，如果一个线程调用 read 读取某个 socket，而这个 socket 没有数据，线程就会阻塞。要处理很多连接，就可能为每个连接分配一个线程，线程数量一多，内存、调度和上下文切换成本都会很高。多路复用让一个线程可以同时监听多个文件描述符，哪个就绪就处理哪个。

select 的思路是把一批 fd 交给内核，内核等待其中任意一个就绪后返回。问题是 fd 集合大小通常有限制，而且每次调用都要把集合从用户态拷到内核态，返回后应用还要遍历整个集合找哪些就绪。连接数很多但活跃连接不多时，扫描成本比较浪费。

poll 改进了 select 的固定数量限制，用 pollfd 数组或链表描述 fd 集合，但本质上仍然要把集合传给内核，返回后也要遍历所有 fd 查找就绪事件。因此 poll 解决了一部分容量问题，但没有根本解决大规模扫描成本。

epoll 是 Linux 下更适合高并发连接的机制。它通过 epoll_create 创建一个内核事件表，用 epoll_ctl 注册、修改、删除关注的 fd，用 epoll_wait 等待就绪事件。已经注册的 fd 不需要每次重复传入，内核把就绪事件放到就绪队列，应用拿到的更接近“发生了事件的 fd 列表”。因此在大量连接、少量活跃的场景下，epoll 更高效。

epoll 还有水平触发和边缘触发。水平触发下，只要 fd 还有数据没读完，epoll_wait 会持续通知；边缘触发只在状态变化时通知一次，性能更好但要求一次把数据读到 EAGAIN，否则可能遗漏。Nginx、Redis、Netty 这类高并发网络程序都大量依赖事件循环和 IO 多路复用。

图解可以画成对比：阻塞 IO 是多个线程分别等多个连接；多路复用是一个事件循环管理多个 fd，就绪后分发处理。再画 select/poll 和 epoll 的差别：前者每次提交和扫描一批 fd，后者维护内核事件表和就绪队列。最后一句收束：多路复用不是让 IO 变快，而是减少线程阻塞和调度成本，让少量线程管理大量连接。落到 Java，可以顺手提 NIO Selector 和 Netty 事件循环，把操作系统机制和框架实践连起来。

还要注意，多路复用适合 IO 等待多、连接多的场景；如果业务处理本身 CPU 很重，仍然要把计算任务丢给工作线程池，避免阻塞事件循环。Netty 里 boss 线程负责接收连接，worker 线程处理 IO 事件，就是这个思想的工程化落地。回答时把“少线程管多连接”和“事件就绪再处理”说出来就很清楚。`
  },
  "docker-basic": {
    type: "structure",
    nodes: [
      ["镜像", "应用和依赖的只读模板"],
      ["容器", "镜像运行后的隔离进程"],
      ["环境一致", "开发测试生产使用同一交付物"],
      ["隔离机制", "namespace 隔离资源视图"],
      ["资源限制", "cgroups 控制 CPU 和内存"],
      ["部署迁移", "配合仓库和编排实现标准化发布"]
    ],
    detail: `Docker 解决的核心问题是环境一致性和交付标准化。传统部署里，开发环境、测试环境、生产环境的 JDK 版本、系统库、配置文件、启动脚本可能都不一样，导致“我本地能跑，线上不行”。Docker 把应用、依赖、运行环境和启动方式打包成镜像，再在不同机器上以容器方式运行，显著降低环境差异。

镜像可以理解为只读模板，里面包含文件系统层、应用包、依赖和默认启动命令。镜像是分层的，Dockerfile 里的每一步通常会形成一层，层可以复用，构建和分发更高效。容器是镜像运行后的实例，本质上仍然是宿主机上的进程，只是通过 namespace 看到隔离的进程、网络、挂载点等视图，通过 cgroups 限制 CPU、内存等资源。

Docker 不是轻量虚拟机。虚拟机通常运行完整操作系统内核，隔离更重；容器共享宿主机内核，因此启动更快、资源开销更小。但共享内核也意味着容器隔离边界和虚拟机不同，安全和内核兼容要特别关注。面试里说“容器就是一个被隔离和限制的进程”，比说“迷你虚拟机”更准确。

Docker 的工程价值还包括快速回滚和可复制部署。镜像有版本号，发布时拉取指定镜像运行；出问题可以回滚到上一版镜像。CI/CD 可以把代码构建成镜像，推送到镜像仓库，再由服务器或 Kubernetes 拉取部署。这样交付物从 jar 包加一堆脚本，变成标准镜像。

使用 Docker 也有坑。镜像过大影响拉取和启动；容器里写本地文件不做 volume 挂载会丢数据；容器时间、网络、DNS、资源限制配置不当会造成线上问题；Java 应用还要注意容器内存限制和 JVM 参数匹配。日志最好输出到 stdout/stderr，由平台采集，而不是只写容器内部文件。

图解可以画成“代码 -> Dockerfile -> 镜像 -> 镜像仓库 -> 容器运行”的链路。旁边展开容器隔离：namespace 提供独立视图，cgroups 控制资源，分层文件系统复用镜像层。最后落点：Docker 的价值不是单纯“能跑起来”，而是让应用交付、部署、回滚和迁移变得可复制。项目里还要配合健康检查、镜像扫描、最小基础镜像和多阶段构建，减少安全风险和镜像体积。

如果面试官问容器数据怎么办，要说容器本身应该尽量无状态，数据库、上传文件、日志等持久数据要用 volume、对象存储或外部服务承载。容器退出后本地可写层不应该承载关键数据，日志也要交给平台统一采集。这样容器才能随时重建、迁移和水平扩容，发布回滚也更可控稳定。`
  },
  "linux-common-debug": {
    type: "flow",
    nodes: [
      ["先看现象", "接口慢、错误率、进程退出还是资源满"],
      ["CPU", "top、pidstat、线程栈"],
      ["内存", "free、ps、pmap、OOM 日志"],
      ["磁盘", "df、du、iostat、日志暴涨"],
      ["网络", "ss、netstat、curl、tcpdump"],
      ["日志闭环", "journalctl、应用日志、时间线复盘"]
    ],
    detail: `线上 Linux 服务排查，最重要的是先定方向，不要一上来就乱敲命令。先问清楚现象：是接口变慢、错误率升高、进程退出、CPU 高、内存高、磁盘满、连接超时，还是某个下游异常。然后按 CPU、内存、磁盘、网络、日志五条线展开。命令只是工具，排查顺序和证据链更重要。

CPU 高时，先用 top 看整体负载和哪个进程占用高，再用 top -H -p pid 或 pidstat 看线程级别。Java 服务可以把线程 ID 转成十六进制，在 jstack 里找对应线程栈，判断是业务循环、锁竞争、GC 线程、正则、序列化还是加密计算。load average 高不一定等于 CPU 忙，也可能是不可中断 IO 等待，要结合 us、sy、wa、队列长度一起看。

内存问题先用 free 看系统可用内存，用 ps 或 top 看进程 RSS，用 pmap 或 smaps 看进程内存分布。Java 服务要区分堆、元空间、直接内存、线程栈、mmap 和 native 内存。系统发生 OOM 时，要看 dmesg 或 journalctl 里的 OOM killer 日志，确认是谁被杀、当时内存状态如何。不要只看 JVM 堆没满就排除内存问题。

磁盘问题常见是 df 显示分区满，du 定位目录，lsof | grep deleted 找已删除但仍被进程占用的文件。iostat 可以看磁盘 IO 是否打满，await、util 等指标能反映等待。日志暴涨、临时文件、上传目录、数据库文件都可能撑爆磁盘。磁盘满会引发写日志失败、数据库不可写、服务启动失败等连锁问题。

网络问题可以用 ss 或 netstat 看监听端口、连接状态、TIME_WAIT、CLOSE_WAIT、ESTABLISHED 数量。curl 可以验证本机到目标服务是否通，telnet 或 nc 可以测端口，tcpdump 可以抓包看握手、重传、RST。DNS 问题用 nslookup 或 dig，路由问题用 ip route、traceroute。连接池耗尽和网络不通表现都可能是超时，需要结合服务端日志和连接状态判断。

图解可以画成排查漏斗：现象 -> 资源方向 -> 命令定位 -> 日志确认 -> 临时止血 -> 根因复盘。每个方向列 2 到 3 个核心命令。最后强调：线上排查要先止血，比如扩容、重启、清理磁盘、限流、回滚；再保留现场和日志做根因分析，避免只靠重启解决一切。复盘时要补监控、告警阈值和自动化脚本，让同类问题下次更早暴露、更快恢复。

面试时可以按一次真实故障来讲：先看告警和时间线，再定位资源瓶颈，然后验证假设，最后止血和复盘。这比单纯背命令列表更像做过线上运维。`
  },
  "equals-hashcode": {
    type: "structure",
    nodes: [
      ["equals", "判断两个对象业务上是否相等"],
      ["hashCode", "决定对象进入哈希桶的位置"],
      ["约定", "equals 相等则 hashCode 必须相等"],
      ["HashMap 场景", "先定位桶再比较 key"],
      ["错误后果", "查不到、去重失败、集合异常"],
      ["实现原则", "用稳定字段并保持一致"]
    ],
    detail: `equals 和 hashCode 要一起重写，是因为 Java 集合里很多结构依赖二者共同判断对象身份。equals 解决“两个对象业务上是否相等”，hashCode 解决“对象在哈希结构里大致放到哪个桶”。在 HashMap、HashSet、ConcurrentHashMap 这类哈希集合里，查找流程通常是先算 hashCode 定位桶，再在桶里用 equals 判断具体是不是同一个 key。

Java 的基本约定是：如果 a.equals(b) 为 true，那么 a.hashCode() 必须等于 b.hashCode()。反过来不要求成立，hashCode 相等只能说明可能在同一个桶里，还要用 equals 做最终判断。如果只重写 equals 不重写 hashCode，就可能出现两个业务相等的对象 hashCode 不同，被放到不同桶里，HashSet 去重失败，HashMap 用等价 key 取不到值。

举例说，一个 User 用 id 判断相等。你 new 两个 User，id 都是 1。如果 equals 按 id 返回 true，但 hashCode 仍然继承 Object 默认实现，那么两个对象的 hashCode 可能不同。把第一个 User 放进 HashMap，再用第二个 User 去 get，HashMap 会先按第二个对象的 hashCode 找桶，桶都不同，自然找不到。这就是“明明 equals 相等却取不到”的根因。

实现时要选择稳定字段。用于 equals 和 hashCode 的字段最好在对象放入集合后不要变化。如果把可变字段作为 hashCode 依据，对象放进 HashSet 后字段变了，hashCode 变了，但对象还停留在旧桶里，后续 contains 或 remove 可能失败。实体类如果用数据库 id，要注意新对象 id 为空时的相等逻辑；值对象则可以用所有业务字段。

IDE 或 Lombok 能自动生成 equals/hashCode，但也要理解生成策略。继承场景尤其要小心，父类字段是否参与、getClass 还是 instanceof、canEqual 等都可能影响对称性和传递性。面试里不必展开所有细节，但要说出五个约定：自反、对称、传递、一致、非空比较为 false，以及 equals 相等必须 hashCode 相等。

图解可以画 HashMap 查找过程：对象 key 先计算 hashCode，进入对应桶；桶里可能有多个节点，再用 equals 逐个比较。旁边画错误分支：两个 equals 相等对象 hashCode 不同，被分到不同桶，导致 get 失败。最后落点：只要对象会作为 HashMap key 或放进 HashSet，就必须认真设计 equals 和 hashCode。`
  },
  "synchronized-reentrantlock": {
    type: "compare",
    nodes: [
      ["synchronized", "JVM 内置锁，语法简单"],
      ["ReentrantLock", "显式加锁和释放"],
      ["可重入", "同一线程可重复获得同一把锁"],
      ["能力差异", "可中断、公平锁、tryLock、条件队列"],
      ["释放方式", "synchronized 自动释放，Lock 要 finally"],
      ["选择建议", "简单同步优先 synchronized，复杂控制用 Lock"]
    ],
    detail: `synchronized 和 ReentrantLock 都能实现互斥，并且都是可重入锁。可重入的意思是同一个线程已经拿到某把锁后，再次进入需要同一把锁的代码不会把自己锁死，而是计数加一，退出时逐层释放。它们都能保证临界区内的原子性、可见性和有序性，区别主要在使用方式、能力边界和可控制程度。

synchronized 是 JVM 关键字，可以修饰实例方法、静态方法或代码块。锁对象可以是当前实例、Class 对象或任意对象。它的优点是语法简单，进入代码块自动加锁，退出时无论正常返回还是抛异常都会自动释放锁，不容易忘记 unlock。现代 JVM 对 synchronized 做了大量优化，比如偏向锁曾经存在、轻量级锁、锁粗化、锁消除、自适应自旋等，所以不能再简单说 synchronized 性能一定差。

ReentrantLock 是 JUC 包里的显式锁，需要手动 lock 和 unlock。它的优点是功能更丰富：tryLock 可以尝试获取锁，拿不到就返回，避免永久等待；lockInterruptibly 可以响应中断；构造时可以选择公平锁，让等待时间更久的线程优先获得锁；Condition 可以创建多个条件队列，比 synchronized 的 wait/notify 更灵活。缺点是必须在 finally 里 unlock，否则异常路径可能导致锁永远不释放。

选择时可以按复杂度判断。普通临界区、简单同步、方法级互斥，优先 synchronized，代码更短，释放更安全。如果需要超时获取锁、可中断等待、公平锁、多条件队列，选择 ReentrantLock。比如生产者消费者有多个等待条件，用 Condition 会更清晰；需要避免死等，用 tryLock 加超时更安全。

面试里还可以补充锁和等待通知的关系。synchronized 搭配 wait、notify、notifyAll，要求在持有对象监视器时调用；ReentrantLock 搭配 Condition 的 await、signal、signalAll，一个锁可以创建多个 Condition，把不同等待队列分开。这样能避免 notifyAll 唤醒一堆无关线程。

图解可以画左右对比：左边 synchronized 进入代码块自动加锁，退出自动释放；右边 ReentrantLock 显式 lock，try/finally unlock。中间列能力差异：自动释放、可中断、公平性、tryLock、多个条件队列。最后强调：二者不是谁完全替代谁，简单场景用内置锁，复杂并发控制用显式锁。`
  },
  "spring-security-authentication-authorization": {
    type: "flow",
    nodes: [
      ["认证", "确认你是谁"],
      ["凭证校验", "用户名密码、token、短信、OAuth2"],
      ["SecurityContext", "保存当前登录主体"],
      ["授权", "判断你能访问什么"],
      ["权限规则", "角色、权限、资源、表达式"],
      ["拒绝或放行", "未登录 401，无权限 403"]
    ],
    detail: `Spring Security 里认证和授权是两个阶段。认证 authentication 解决“你是谁”，授权 authorization 解决“你能做什么”。用户输入用户名密码、携带 JWT、OAuth2 登录、短信验证码登录，这些都属于认证方式。认证成功后，系统会得到一个表示当前用户身份的 Authentication 对象，里面包含 principal、credentials、authorities 等信息，并放入 SecurityContext。

认证流程通常经过一组过滤器。请求进入 Spring Security FilterChain 后，某个认证过滤器会尝试从请求里提取凭证，比如表单登录提取用户名密码，JWT 过滤器提取 Authorization 头。然后交给 AuthenticationManager，后者再委托 AuthenticationProvider 校验凭证。校验成功后，把认证结果放入 SecurityContextHolder，后续代码就能知道当前用户是谁。

授权发生在认证之后。系统拿到当前用户的角色或权限，再根据 URL 规则、方法注解或自定义权限判断，决定是否允许访问。比如 hasRole、hasAuthority、PreAuthorize 都是授权表达。认证失败通常返回 401，表示你还没登录或凭证无效；授权失败通常返回 403，表示你已经登录但没有权限。这个区分非常适合面试回答。

角色和权限也要分清。角色通常是权限集合，比如 ADMIN、USER；权限更细，比如 order:read、order:write。简单系统可以只用角色，复杂系统通常要做 RBAC，把用户、角色、权限、资源关联起来。多租户系统还要把租户 ID、部门、数据范围纳入授权判断，否则登录成功的人可能越权访问别人的数据。

JWT 场景下，服务端可能不存 session，而是每次请求解析 token 得到用户身份。但这不代表不需要授权，token 只是认证载体，真正能不能访问接口还要看 authorities、资源归属和业务数据权限。token 过期、刷新、注销、黑名单、密钥轮换也要考虑。

图解可以画请求链：请求进入过滤器链，认证过滤器提取凭证，Provider 校验，成功后写 SecurityContext；然后进入授权判断，匹配 URL 或方法权限，成功放行，失败返回 401 或 403。最后一句：认证是门禁刷卡确认身份，授权是进门后判断你能进哪些房间。`
  },
  "seckill-system-design": {
    type: "scenario",
    nodes: [
      ["入口削峰", "按钮防抖、验证码、排队、限流"],
      ["资格校验", "活动时间、用户资格、防刷"],
      ["Redis 库存", "原子预扣库存，快速挡流量"],
      ["防重复", "用户和商品维度唯一约束"],
      ["MQ 异步下单", "削峰后落库创建订单"],
      ["一致性兜底", "补偿、对账、状态机和回滚预案"]
    ],
    detail: `秒杀系统设计的核心是把瞬时高并发变成系统能承受的有序处理。不要让所有请求直接打到数据库扣库存，否则数据库连接、锁竞争、行更新都会被打爆。完整链路应该从入口削峰开始，再做资格校验、库存预扣、防重复、异步下单、最终一致性和监控补偿。面试里要展示的是系统思维，而不是只说“Redis 扣库存”。

入口层先挡无效流量。前端按钮置灰、防重复提交、验证码、排队页可以减少脚本和误点请求。网关层做限流、黑名单、设备指纹、IP 频控、用户频控。活动开始前可以静态化页面、CDN 分发活动资源，避免所有请求回源。这个阶段的目标是让进入核心链路的请求已经经过第一轮过滤。

库存一般预热到 Redis，使用 Lua 脚本或原子命令扣减，保证判断库存和扣库存是原子的。扣减成功不代表订单已经创建成功，只代表用户拿到一个下单资格。为了防重复，同一用户同一商品要用 Redis 标记、数据库唯一索引或订单状态判断兜底。Redis 层防重复是前置保护，数据库唯一约束是最后防线。

扣减成功后，把下单请求写入 MQ，由消费者异步创建订单。这样可以削峰，让数据库按可控速度写入。消费者落库要幂等，订单状态要清楚，比如待支付、已支付、已取消、已超时。订单创建失败、消息消费失败、Redis 扣了库存但 DB 没落单，都要有补偿任务。可以设计库存流水或冻结库存记录，保证每一次扣减都可追踪。

一致性是秒杀题的重点。Redis 库存和数据库库存不可能只靠一句“最终一致”就结束，要说明怎么对账、怎么补偿、怎么防超卖。数据库事务和唯一索引兜底，MQ 重试和死信处理异常，定时任务扫描长时间未完成订单，支付超时释放库存。高峰后还要核对 Redis 剩余库存、数据库订单数、库存流水是否一致。

图解可以画成场景链路：用户请求 -> CDN/网关限流 -> 资格校验 -> Redis Lua 扣库存和防重复 -> MQ -> 订单服务落库 -> 支付和超时取消 -> 补偿对账。旁边画异常分支：Redis 扣成功但 MQ 失败、订单落库失败、重复请求、支付超时。最后落点：秒杀系统不是追求所有请求成功，而是保护系统、控制库存、保证数据最终可修复。`
  },
  "cap-base": {
    type: "compare",
    nodes: [
      ["C 一致性", "所有节点读到同一份最新数据"],
      ["A 可用性", "每个请求都能收到非错误响应"],
      ["P 分区容错", "网络分区时系统仍能继续运行"],
      ["CAP 取舍", "分区发生时在 C 和 A 间取舍"],
      ["BASE", "基本可用、软状态、最终一致"],
      ["业务落地", "按场景选择强一致或最终一致"]
    ],
    detail: `CAP 理论说的是分布式系统在网络分区发生时，一致性、可用性、分区容错性三者不能同时完全满足。C 是一致性，强调所有节点看到同一份最新数据；A 是可用性，强调每个请求都能得到非错误响应；P 是分区容错性，强调节点之间网络出问题时系统仍能继续运行。在真实分布式系统里，网络分区无法完全避免，所以 P 基本必须考虑，关键是在分区发生时选择偏 C 还是偏 A。

如果选择 CP，系统在分区时优先保证一致性，可能拒绝部分请求或让请求等待。比如强一致配置中心、分布式锁、金融余额系统，宁可短暂不可用，也不能让两个节点同时认为自己是主或让余额错乱。如果选择 AP，系统在分区时优先响应请求，允许不同节点暂时不一致，后续再通过同步和冲突解决达到一致。比如一些内容点赞、浏览量、缓存、社交动态，短暂不一致通常可以接受。

CAP 容易被误解成“系统只能三选二”。更准确地说，正常没有分区时，系统可以同时有较好的一致性和可用性；只有发生分区时，才必须在 C 和 A 之间做取舍。而且一致性和可用性不是非黑即白，有很多强弱程度。业务设计要具体到场景，不能一句“我们选 AP”就结束。

BASE 是对强一致 ACID 的一种柔性思路，常用于大规模互联网系统。Basically Available 基本可用，表示系统在故障或高峰时可以降级，但核心功能可用；Soft State 软状态，表示中间状态允许存在，数据不必每一刻都强一致；Eventually Consistent 最终一致，表示经过一段时间同步和补偿后，系统最终达到一致。

最终一致不是不一致，也不是不管。它依赖消息重试、幂等、补偿任务、对账、状态机、人工修复工具。比如订单创建后异步发券，短时间内用户可能还没看到券，但系统会通过 MQ 和补偿确保最终发放或明确失败。能不能接受最终一致，要看业务是否允许延迟、是否可补偿、用户是否感知。

图解可以画 CAP 三角形，P 放在底部标成分布式环境必须面对；网络分区出现时，一条路走 CP：拒绝或等待以保一致；另一条路走 AP：先响应再同步。旁边画 BASE 流程：请求成功进入软状态，异步同步，重试补偿，最终一致。回答收尾要说：技术选型服从业务，余额库存偏 CP，点赞浏览偏 BASE 最终一致。`
  },
  "distributed-transaction": {
    type: "compare",
    nodes: [
      ["2PC/XA", "强一致但阻塞和性能成本高"],
      ["TCC", "Try、Confirm、Cancel 三阶段业务补偿"],
      ["Saga", "长事务拆成本地事务和补偿步骤"],
      ["可靠消息", "本地事务加消息最终一致"],
      ["最大努力通知", "适合通知型结果同步"],
      ["选择依据", "一致性要求、补偿难度、链路复杂度"]
    ],
    detail: `分布式事务要解决的是一次业务操作跨多个服务、多个数据库或多个资源时，如何保证整体状态可控。单库事务可以依赖数据库 ACID，但订单、库存、支付、积分拆成多个服务后，一个本地事务无法覆盖全链路。分布式事务方案很多，面试里要按一致性强弱和业务补偿能力来讲，而不是只背 2PC、TCC、Saga 名字。

2PC 或 XA 是两阶段提交。第一阶段协调者让各参与者 prepare，参与者锁定资源并回答能否提交；第二阶段协调者通知 commit 或 rollback。它的一致性较强，但缺点是阻塞、锁持有时间长、协调者单点、性能差。适合强一致要求高、参与方较少、吞吐要求不极端的场景，但互联网高并发业务一般谨慎使用。

TCC 是 Try、Confirm、Cancel。Try 阶段预留资源，比如冻结库存、冻结余额；Confirm 阶段真正提交；Cancel 阶段释放预留资源。TCC 的优点是控制力强，适合资源预留明确的业务；缺点是业务侵入大，每个接口都要实现三套逻辑，还要保证 Confirm 和 Cancel 幂等、空回滚、防悬挂。比如 Try 超时后 Cancel 先到，后续 Try 又成功，就会出问题，需要事务状态表控制。

Saga 适合长流程，把大事务拆成一串本地事务，每个步骤都有对应补偿动作。比如下单、扣库存、扣优惠券、创建物流，如果后面失败，就按反向顺序补偿前面已完成步骤。Saga 不保证中间过程强一致，适合可补偿业务。难点是补偿不一定等价于回滚，比如优惠券已经被用户看见、通知已经发出，就要设计业务可接受的补偿。

可靠消息最终一致常用于异步解耦。发送方在本地事务里写业务数据和消息表，后台或事务消息机制确保消息投递到 MQ；消费者幂等处理。它牺牲实时一致，换取高可用和低耦合。最大努力通知则适合支付回调、外部系统通知这类场景，发送方尽力多次通知，接收方主动查询兜底。

图解可以画方案矩阵：横轴是一致性强弱，纵轴是业务侵入程度。2PC 强一致但性能和阻塞成本高；TCC 一致性较强但侵入大；Saga 和可靠消息偏最终一致，需要补偿和幂等。最后落点：没有万能分布式事务，优先避免跨服务强事务；必须跨服务时，按业务能否补偿、是否允许延迟、失败是否可人工修复来选。`
  },
  "rate-limiter": {
    type: "compare",
    nodes: [
      ["固定窗口", "实现简单但边界突刺明显"],
      ["滑动窗口", "统计更平滑，成本更高"],
      ["漏桶", "恒定速率流出，削峰明显"],
      ["令牌桶", "按速率发令牌，允许短突发"],
      ["分布式限流", "Redis Lua 或网关统一计数"],
      ["落地指标", "限流维度、阈值、降级响应"]
    ],
    detail: `限流的目标是保护系统，把超过承载能力的流量挡在外面或降级处理。常见算法有固定窗口、滑动窗口、漏桶、令牌桶。面试里要讲清楚每种算法解决什么问题、有什么缺点，以及业务上如何选择限流维度和阈值。限流不是为了拒绝用户，而是为了让核心服务在流量异常时仍然可用。

固定窗口最简单，比如每分钟最多 100 次。实现上用 key 加当前分钟计数即可。但它有边界突刺问题：用户在上一分钟最后一秒打 100 次，下一分钟第一秒再打 100 次，短时间内实际承受 200 次。滑动窗口把时间切成多个小格，或者记录最近一段时间请求时间戳，统计更平滑，但实现和存储成本更高。

漏桶算法把请求倒进桶里，桶以固定速率流出。桶满了就拒绝新请求。它适合把突发流量整形成稳定速率，下游处理会很平滑，但对突发不友好。令牌桶则按固定速率生成令牌，请求拿到令牌才能通过。桶里可以积累一定令牌，所以允许短时间突发，长期平均速率受限。很多网关和客户端限流更喜欢令牌桶，因为它兼顾平均限制和突发弹性。

限流要先确定维度。可以按 IP、用户 ID、接口、租户、设备、商户、API key、全局 QPS 等维度限流。维度太粗会误伤正常用户，太细可能挡不住整体洪峰。阈值也不能拍脑袋，要基于压测、下游容量、历史峰值和错误预算。核心接口和非核心接口的策略也不同，非核心可以更早降级，核心要保留容量。

单机限流适合服务实例本地保护，简单高效；分布式限流需要多个实例共享计数，常用 Redis Lua 保证原子性，或者在 API 网关统一限流。分布式限流要考虑 Redis 自身成为瓶颈，所以高流量场景可能用本地预分配令牌、分片计数、近似限流等方式降低中心依赖。

图解可以画四个算法对比：固定窗口边界突刺，滑动窗口平滑统计，漏桶匀速流出，令牌桶积累令牌允许突发。底部画落地链路：请求进入网关，按用户和接口生成限流 key，Redis Lua 判断，超限返回 429 或降级响应，监控记录通过量、拒绝量和下游错误率。`
  },
  "circuit-breaker-degrade-rate-limit": {
    type: "compare",
    nodes: [
      ["限流", "控制入口流量别超过系统容量"],
      ["熔断", "下游持续失败时暂时切断调用"],
      ["降级", "牺牲非核心能力保核心链路"],
      ["触发依据", "QPS、错误率、超时、资源水位"],
      ["恢复策略", "半开探测、灰度恢复"],
      ["目标", "防止故障扩散和雪崩"]
    ],
    detail: `限流、熔断、降级经常一起出现，但解决的问题不同。限流是入口控制，防止流量超过系统承载能力；熔断是调用保护，当下游持续超时或错误时，暂时切断调用，避免调用方线程被拖死；降级是功能取舍，在资源不足或依赖故障时，牺牲非核心能力，保住核心链路。三者目标都是防止系统雪崩，但作用位置不同。

限流通常发生在网关、服务入口或热点接口前。它根据 QPS、并发数、用户维度、接口维度等限制请求量。超限后可以返回 429、排队、降级数据或提示稍后再试。限流关注的是“不要让太多请求进来”。秒杀、登录、发送验证码、开放 API 都需要限流。

熔断关注下游依赖。如果服务 A 调用服务 B，B 开始大量超时，A 的线程会被阻塞，连接池耗尽，最后 A 也不可用。熔断器会统计一段时间内的错误率、慢调用比例或超时数量，达到阈值后打开熔断，后续请求直接失败或走 fallback，不再打到 B。过一段时间进入半开状态，放少量请求探测 B 是否恢复，成功再关闭熔断。

降级强调功能取舍。比如首页推荐服务挂了，可以返回默认推荐；评论服务慢了，可以暂时不展示评论；积分服务异常，可以先完成下单，后续补发积分。降级不是异常处理的同义词，它要求提前设计 fallback 数据、开关和用户体验。核心原则是保交易、保登录、保主链路，非核心功能可临时牺牲。

三者经常组合使用。高峰流量先由限流挡住一部分；下游故障由熔断防止拖垮上游；资源紧张时通过降级减少非核心消耗。它们都需要监控指标和动态配置，否则阈值不合理会误伤或保护不足。恢复也要谨慎，熔断半开探测、限流阈值逐步放开、降级功能灰度恢复，避免刚恢复又被流量打垮。

图解可以画成调用链：用户请求先经过限流，进入服务后调用下游，下游异常触发熔断，页面或接口返回降级结果。旁边列触发指标：QPS、并发、错误率、超时、慢调用、CPU、线程池队列。最后收束：限流管入口，熔断管依赖，降级管功能取舍。`
  },
  "consistent-hash": {
    type: "structure",
    nodes: [
      ["普通取模", "节点数变化导致大量 key 重映射"],
      ["哈希环", "把 key 和节点映射到同一环上"],
      ["顺时针查找", "key 归属遇到的第一个节点"],
      ["节点增删", "只影响相邻区间"],
      ["虚拟节点", "解决数据倾斜和负载不均"],
      ["应用场景", "缓存分片、路由、分布式存储"]
    ],
    detail: `一致性哈希解决的是分布式缓存或存储扩缩容时，大量 key 重新分布的问题。普通取模方式很直观：hash(key) % 节点数。问题是节点数一变，比如从 3 台变成 4 台，绝大多数 key 的取模结果都会变化，缓存会大面积失效，存储也要大量迁移数据。这在缓存集群扩容或节点故障时会造成雪崩式压力。

一致性哈希把哈希空间想象成一个环，比如 0 到 2 的 32 次方减 1。先把每个节点通过哈希映射到环上的某个位置，再把 key 也映射到环上。一个 key 顺时针找到的第一个节点，就是它的归属节点。这样当新增一个节点时，只会接管它和前一个节点之间的一段 key；当一个节点下线时，也主要由下一个节点接管它负责的区间。相比普通取模，迁移范围小很多。

但基础一致性哈希有数据倾斜问题。如果真实节点数量少，节点在环上的分布可能不均匀，有的节点负责很大区间，有的负责很小区间。解决方式是虚拟节点：每个真实节点映射成多个虚拟节点，分散到环上。key 先落到虚拟节点，再映射回真实节点。虚拟节点越多，分布越均匀，也更容易按机器性能设置权重。

一致性哈希常用于缓存分片、分布式存储、负载路由。比如客户端根据 key 算出应该访问哪个 Redis 节点，或者对象存储决定数据落在哪个节点。它的价值是降低节点增删时的重映射比例，提高扩缩容稳定性。但它不自动解决所有问题：热点 key 仍可能压垮单节点，节点故障后的数据补齐、复制、副本一致性还要单独设计。

面试时可以拿普通取模做对比。先说取模扩容会导致大量 key 失效，再引入哈希环，说明节点增删只影响局部区间，最后补虚拟节点解决倾斜。这样逻辑很顺，不会变成抽象概念堆砌。还可以说如果是 Redis Cluster，它使用的是固定 16384 个 slot，也是一种把 key 映射到槽再映射到节点的思想。

图解可以画一个圆环，环上放节点 A、B、C，再放几个 key，箭头顺时针指向归属节点。新增节点 D 后，只标出一小段 key 改归 D。旁边再画虚拟节点 A1、A2、A3 分散在环上，说明它们都归真实节点 A。最后落点：一致性哈希减少迁移，不等于不需要副本、限流和热点治理。`
  },
  "api-gateway": {
    type: "flow",
    nodes: [
      ["统一入口", "客户端先访问网关而不是直连微服务"],
      ["路由转发", "按路径、域名、Header 或版本规则分发"],
      ["横切治理", "鉴权、限流、日志、灰度在入口统一处理"],
      ["协议聚合", "对外统一协议，对内可调用多个服务"],
      ["安全隔离", "隐藏内网地址并收敛暴露面"],
      ["风险取舍", "网关不能过重，要防单点和性能瓶颈"]
    ],
    detail: `API 网关在微服务里的第一价值，是把“客户端面对一堆分散服务”这件事收敛成“客户端只面对一个统一入口”。没有网关时，前端或外部调用方要知道用户服务、订单服务、商品服务、支付服务分别在哪个地址、用什么协议、怎么鉴权、怎么处理版本差异。这样一来，客户端复杂度会迅速上升，而且每增加一个服务或变更一个地址，都可能牵动外部系统联调。网关把这些变化挡在内部，让调用方只关心统一域名和统一访问规则。  

第二个价值是统一路由和服务编排。网关可以根据 URL 路径、Host、Header、租户、版本号甚至灰度标记，把请求转发到不同服务实例。比如 /api/orders/** 转到订单服务，/api/users/** 转到用户服务；灰度用户带特定 Header 时转到新版本实例。这使得服务的发布、迁移、拆分更可控。更进一步，网关还能做 BFF 或聚合，把一次客户端请求拆成多个内部调用，再拼装成一个响应返回。这样可以减少客户端多次往返，但也要警惕把网关做成“大而全业务层”。  

第三个价值是把横切治理放到统一入口。鉴权、限流、黑白名单、签名校验、日志埋点、链路追踪、脱敏、CORS、WAF、防刷、防重放，这些能力如果分散在每个服务里重复实现，不仅成本高，而且标准难统一。放到网关之后，可以集中配置、统一观测、统一灰度。比如开放 API 可以在网关按 appKey 做签名校验和频控；内部前台流量可以在网关按用户或 IP 做限流；灰度发布时也可以按请求头做版本路由。  

第四个价值是安全和边界管理。微服务很多时候部署在内网，不应该被外部直接暴露。网关作为唯一外部入口，可以隐藏内部拓扑，减少攻击面，并把 TLS 终止、证书管理、来源校验、访问审计这些安全能力集中处理。这样即使后端服务地址变化，对外暴露面仍然稳定。  

但 API 网关不是越重越好。它如果承载太多业务逻辑，就会变成新的单体瓶颈：配置复杂、变更频繁、发布风险高、性能压力大。正确姿势是把网关聚焦在“流量入口治理”和“轻量聚合”上，把核心业务决策留在后端服务里。工程上还要考虑高可用：多实例部署、无状态、配置中心、熔断降级、限流保护、监控告警一样不能少。  

图解可以画成一条入口链路：客户端先到网关，网关先做鉴权和限流，再根据路由规则分发到不同微服务，必要时聚合多个服务结果后返回。旁边再补一列“适合放网关”和“不适合放网关”的对照：鉴权、路由、限流、协议转换适合放网关；复杂业务规则、强事务编排、不稳定重逻辑不适合放网关。这样回答就不只是“网关能做很多事”，而是清楚说明它在微服务架构里的职责边界。`
  },
  "lru-cache": {
    type: "structure",
    nodes: [
      ["访问目标", "既要 O(1) 查找也要 O(1) 淘汰"],
      ["HashMap", "按 key 快速找到节点"],
      ["双向链表", "维护最近访问顺序"],
      ["命中移动", "访问后把节点移到头部"],
      ["容量淘汰", "满了删除尾部最久未使用节点"],
      ["易错点", "更新、删除和边界指针一致性"]
    ],
    detail: `手写 LRU 缓存这道题，本质考的是“你能不能把需求翻译成数据结构组合”。LRU 的完整要求通常有两个：第一，get 和 put 都要尽量快，理想是 O(1)；第二，当容量满了时，要淘汰“最近最少使用”的元素。如果只用数组或链表，淘汰顺序好维护，但查找太慢；如果只用 HashMap，查找快，但不知道哪个元素最久没用。所以标准解法几乎总是 HashMap 加双向链表。  

HashMap 负责通过 key O(1) 找到节点；双向链表负责维护访问顺序。链表头表示最近刚访问过的节点，链表尾表示最久没访问的节点。每次 get(key) 命中，除了返回值，还要把这个节点移到链表头，因为它刚被使用过。每次 put(key, value) 分两种情况：如果 key 已存在，就更新值并移动到头部；如果 key 不存在，就新建节点插到头部，同时写入 HashMap。如果插入后容量超限，就删掉尾部节点，并把它从 HashMap 里也移除。  

为什么必须用双向链表，而不是单向链表？因为节点移动和删除要求 O(1)。单向链表删除一个中间节点时，你还得先找到它的前驱；双向链表拿到节点本身后，就能直接通过 prev 和 next 把它摘掉。很多实现还会额外放两个哨兵节点 head 和 tail，这样插入头部、删除尾部、删除中间节点都能统一处理，少写很多 if 判断。  

这题真正容易错的，不在思路，而在细节。比如：更新已有 key 时，不能新建节点后忘删旧节点；淘汰尾节点时，要先从链表摘除，再从 Map 删除；容量为 0 或 1 时边界怎么处理；get 未命中返回什么；节点复用时指针是否残留旧引用。很多人能说出“HashMap + 双向链表”，但现场写代码会因为指针更新顺序出 bug。  

面试里还可以顺带讲 JDK 的 LinkedHashMap。它内部就维护了访问顺序，通过重写 removeEldestEntry 可以快速做一个 LRU，这说明你知道标准题解，也知道工程里不一定手写轮子。但如果面试官要求手写，你还是得能把核心操作讲清楚：addFirst、removeNode、moveToHead、removeLast 四个动作就是整题骨架。  

图解建议画成两层：上层是 HashMap，把 key 指到链表节点；下层是双向链表，从头到尾表示“最近使用 -> 最久未使用”。然后演示三个动作：一次 get 命中把中间节点移到头部；一次 put 新增插到头部；一次容量溢出删除尾节点。最后收一句：LRU 的关键不是“背模板”，而是理解为什么需要同时满足快速定位和快速维护顺序。`
  },
  "top-k": {
    type: "compare",
    nodes: [
      ["先看场景", "要最大 K 个、最小 K 个还是第 K 大"],
      ["排序法", "实现简单但通常是 O(n log n)"],
      ["堆方法", "维护大小为 K 的小顶堆或大顶堆"],
      ["快选法", "基于 partition 期望 O(n)"],
      ["流式数据", "数据持续到来更适合堆"],
      ["取舍", "一次离线计算和在线维护方案不同"]
    ],
    detail: `Top K 问题表面上像一道算法题，实际上先要问清楚“Top K 的业务语义”。是求最大的 K 个数，还是最小的 K 个数？是一次性离线算完，还是数据流不断到来时持续维护？是只要结果集合，还是还要结果有序？这些前提不同，最优解法也不同。很多人一上来就背堆，其实面试官更想看你是否先做问题建模。  

最直观的方法是排序。把数组整体排好序后，取前 K 个或后 K 个即可。这个方法容易写，也容易讲清楚，但复杂度通常是 O(n log n)。如果 n 不大、代码可读性优先、或者后续本来就需要整体有序，这种方法并不丢人。只是当 n 很大、K 很小的时候，全量排序会做很多不必要工作。  

更经典的做法是堆。求最大的 K 个数时，维护一个大小为 K 的小顶堆。先把前 K 个元素放进去，之后每来一个新元素，就和堆顶比较：如果比堆顶大，说明它有资格进入 Top K，就弹出堆顶再插入它；如果不比堆顶大，直接跳过。这样所有元素扫描一遍，复杂度是 O(n log K)。当 K 远小于 n 时，这比排序划算得多。反过来，求最小的 K 个数，则维护一个大小为 K 的大顶堆。  

如果题目是“找第 K 大”或者“一次性找前 K 个，不要求最终完全排序”，还可以用快速选择，也就是快排 partition 思想。每次 partition 后，如果枢轴位置刚好落在第 K 相关的位置，就找到答案；如果偏左或偏右，就只递归一边。它平均复杂度 O(n)，但最坏可能退化到 O(n^2)，而且代码和边界处理比堆更容易出错。工程里如果输入分布不确定，堆往往更稳。  

Top K 还有一个高频变体是海量流式数据。比如实时热门搜索词、实时销量前 100、实时慢 SQL Top N。数据一条条进来，不可能每次全量排序。这时候小顶堆几乎是天然方案，因为它能持续维护一个固定容量候选集。再往上还有分布式场景：每台机器先算本地 Top K，再做汇总归并。  

如果面试官继续追问工程落地，你还可以主动补一句：Top K 的结果往往不是静态答案，而是榜单能力的一部分，所以还要考虑去重、时间窗口、热点刷新频率、并发更新和最终展示排序。把算法题讲到业务榜单，会比只会写堆代码更像真实项目经验。再往前一步，如果数据量特别大，还会配合分桶、分区、离线预聚合或流式计算框架先做局部 Top K，再做全局归并，这样才能兼顾吞吐和时效。  

图解可以画成三种路线：排序法是“全量排序 -> 截取”；堆法是“扫描元素 -> 与堆顶比较 -> 保持 K 个候选”；快选法是“partition -> 判断落点 -> 缩小区间”。最后给出选型结论：一次性、追求简单就排序；K 远小于 n 或流式输入优先堆；只求第 K 或期望线性时间可以用快选。这样回答会比单背某个模板更完整。`
  },
  "binary-search": {
    type: "flow",
    nodes: [
      ["确认单调性", "二分前先确认区间有序或答案单调"],
      ["边界定义", "决定用闭区间还是左闭右开"],
      ["中点计算", "防溢出且与边界更新规则配套"],
      ["缩区间", "命中条件时保留哪一边要一致"],
      ["终止条件", "循环结束后返回 left 还是 right"],
      ["常错点", "死循环、越界、找边界值写错"]
    ],
    detail: `二分查找最容易错，不是因为它难，而是因为它看起来太简单，很多人以为自己会，实际上边界没想清楚。二分的第一前提不是“数组里找数字”，而是“问题具有单调性”。经典数组查找当然有序，但很多题是“答案二分”，比如最小可行速度、最大可承受容量、最短满足时间，这时不是数组单调，而是判定函数单调。面试一开始先说出这一点，层次会高很多。  

真正写代码时，第一步要统一区间定义。最常见有两套：闭区间 [left, right]，循环条件写 left <= right；或者左闭右开 [left, right)，循环条件写 left < right。两套都能写对，但不要混用。你一旦决定了区间语义，中点命中后的处理、没命中时左右边界怎么收缩、最后返回什么，就都要围绕这个语义保持一致。很多 bug 都是把闭区间的终止条件和开区间的更新规则拼在一起。  

第二个高频错误是中点和收缩规则。中点通常写成 mid = left + (right - left) / 2，主要是避免极端大下标相加溢出。更关键的是收缩时不能丢掉答案。比如闭区间查找精确值时，nums[mid] < target 说明目标只可能在右边，所以 left = mid + 1；nums[mid] > target 则 right = mid - 1。如果你写成 left = mid 或 right = mid，就可能死循环。  

第三个难点是“找边界值”。很多题不是判断存不存在，而是找第一个大于等于 target 的位置、最后一个小于等于 target 的位置、插入位置、左边界、右边界。这些题最容易在命中条件时写错。例如找左边界时，即使 nums[mid] == target，也不能立刻返回，而要继续压缩右边界，看看左边是否还有相同值。右边界则相反。也就是说，二分不是只有一种模板，而是要根据“保留哪一侧可能的答案”调整。  

工程上还有一种常见坑叫“答案二分判定函数写错”。比如判断某个容量是否可行，如果可行集和不可行集的边界没有严格单调，二分就失效。面试时可以主动补一句：二分前先验证判定函数的单调性，否则再漂亮的模板也没用。  

再补一个工程感很强的点：很多线上问题不是二分不会写，而是边界没做回归。比如空数组、只有一个元素、目标值比所有元素都小或都大、重复元素成片存在，这些都是最该优先写测试的样例。你能主动讲测试边界，面试官会更相信你真的写过而不是背过。  

图解适合画一条有序区间，标出 left、mid、right 三个指针。然后分三种箭头：目标在左边、目标在右边、命中但仍继续找左边界或右边界。旁边再列出两个口诀：先统一区间语义，再让循环条件、边界更新、返回值三件事完全配套。这样面试官会觉得你不是只会背模板，而是真知道二分为什么容易错。`
  },
  "two-pointers": {
    type: "compare",
    nodes: [
      ["同向双指针", "窗口扩张收缩处理连续区间"],
      ["相向双指针", "从两端逼近处理有序数组或回文"],
      ["快慢指针", "不同速度处理链表和去重问题"],
      ["排序前置", "很多题先排序再用双指针"],
      ["核心收益", "把嵌套搜索降成线性扫描"],
      ["易错点", "移动时机和不变量维护"]
    ],
    detail: `双指针不是一道题，而是一类思维方式。它的共性是用两个位置指针在数据结构上协同移动，减少重复扫描，从而把原本 O(n^2) 的暴力搜索压到 O(n) 或 O(n log n)。面试里如果只说“两头往中间走”，答案会太窄，因为双指针至少有三种常见形态：同向、相向、快慢。  

同向双指针最典型的是滑动窗口。两个指针都从左往右走，一个负责扩张窗口，一个负责收缩窗口。适合解决“连续子数组/子串”类问题，比如最长不重复子串、最小覆盖子串、和大于等于某值的最短子数组。这里核心不是两个指针本身，而是你能否维护窗口内的不变量，比如字符频次、窗口和、合法性条件。一旦窗口满足或不满足条件，哪个指针该动、何时更新答案，决定了代码是否正确。  

相向双指针通常发生在有序数组或需要从两端逼近的时候。比如两数之和 II、有序数组去重、判断回文串、盛最多水的容器等。它依赖某种单调关系：和太小就左指针右移，和太大就右指针左移；两端字符相同就继续收缩，不同就直接失败。很多题在排序之后就能从哈希解法转成相向双指针，空间从 O(n) 降到 O(1)。  

快慢指针更像一种节奏差。链表找中点、判断链表有环、删除倒数第 N 个节点、数组原地去重都常用它。链表有环里，快指针一次走两步，慢指针一次走一步，如果存在环，两者最终会在环内相遇；数组去重里，慢指针维护“已处理结果末尾”，快指针负责扫描新元素。这类题的关键是想清楚每个指针分别代表什么状态，而不是机械记“一个快一个慢”。  

很多双指针题其实还伴随前置条件。比如三数之和、四数之和，往往先排序，再固定一个数，把剩余部分交给双指针。也就是说，双指针常常是“排序 + 扫描”组合拳。面试里说出这个组合，会比只背单个题更有方法论。  

如果你想把答案再拔高一点，可以补一句“不变量”视角：双指针真正难的是每一步移动后，窗口是否合法、左右区间是否仍保留答案、快慢关系是否仍成立。谁来移动、为什么现在移动、移动后丢没丢答案，这三个问题想明白，双指针题就不容易写乱。实际刷题时，很多人不是不会写，而是没有先把“当前窗口代表什么、左右指针各自负责什么”写在脑子里，所以一加去重、排序或边界条件就容易乱套。  

图解建议画三种模板并列：同向窗口、相向夹逼、快慢追赶。每种旁边写一个问题类型和状态含义，比如“窗口是否合法”“和偏大还是偏小”“是否会相遇”。最后收束成一句：双指针解决的不是某道固定题，而是通过维护不变量，让两个位置协同移动，减少重复搜索。这种总结比背题目名字更有泛化能力。`
  },
  "linked-list-cycle": {
    type: "flow",
    nodes: [
      ["定义问题", "next 指针最终是否会回到已访问节点"],
      ["快慢指针", "慢一步快两步同时出发"],
      ["是否相遇", "有环时两指针终会在环内相遇"],
      ["无环结束", "快指针先到 null 就说明无环"],
      ["进阶定位", "相遇后再找入环点"],
      ["边界检查", "空链表和单节点要先处理"]
    ],
    detail: `判断链表是否有环，最经典的方法是快慢指针，也叫 Floyd 判圈算法。题目本身不复杂，但它考的是你能不能把“指针运动”和“数学关系”讲清楚。链表有环的意思是某个节点的 next 最终会指回前面某个节点，导致沿着 next 一直走不会走到 null，而是会在环里无限循环。  

为什么快慢指针能判断有环？设慢指针每次走一步，快指针每次走两步。如果链表无环，快指针一定先走到 null，因为它速度更快，终点又是真实存在的。如果链表有环，慢指针进入环后会一直在环里转，快指针也会进入环，而且它每次都比慢指针多走一步，相当于在环上不断追赶慢指针。只要两者都进入同一个有限长度的环，快指针迟早会追上慢指针，于是两者相遇。  

这题容易被问到第二层：为什么相遇后还能找到入环点？设头节点到入环点距离为 a，入环点到第一次相遇点距离为 b，环剩余长度为 c。快指针走过的总距离是慢指针的两倍，于是可以推导出：从头节点再出发一个指针，从第一次相遇点再出发一个指针，两者都每次走一步，最终会在入环点相遇。这个推导不用背复杂公式，面试里能说清“慢指针走了 a+b，快指针多绕了若干圈，因此头到入环点的距离，等于相遇点再走若干步到入环点的距离”就够了。  

实现层面的细节也很重要。循环条件通常写成 while (fast != null && fast.next != null)，因为快指针每次要走两步，必须先保证两步都合法。很多人写成只判断 fast != null，第二步就会空指针异常。空链表、单节点、单节点自环这几种边界情况也要考虑。  

如果面试官追问“能不能用 HashSet”，答案当然可以：遍历链表，把访问过的节点放进集合，若再次遇到同一节点就说明有环。这种方法思路直白，但空间复杂度 O(n)；快慢指针的优势是 O(1) 额外空间。对比讲出来，会显得你有方案取舍。  

再往上一步，很多链表题其实都能借这个题建立“指针运动模型”。比如找中点、找倒数第 N 个节点、判断相交、删除节点，本质都和“不同指针代表不同状态”有关。你能把有环判断和其他链表模板联系起来，说明你掌握的是套路背后的共性。工程上虽然未必经常手写链表，但这种思维方式在理解消息链、调用链、状态推进和指针式结构时都很有用，这也是面试官爱问它的原因。  

图解非常适合画：一条链表前半段是直线，后半段连成一个环；慢指针每次走一格，快指针每次走两格，在环内相遇。再补一张进阶图，从头节点和相遇点分别发出两个一步一走的指针，最终在入环点汇合。这样这道题就从“背模板”变成了“看得见的运动过程”。`
  },
  "mybatis-hash-dollar": {
    type: "compare",
    nodes: [
      ["#{}", "预编译占位符，自动做参数绑定"],
      ["${}", "字符串直接拼接到 SQL 文本"],
      ["安全差异", "${} 容易造成 SQL 注入"],
      ["类型处理", "#{} 会走 TypeHandler 转换"],
      ["适用场景", "动态表名或排序字段才谨慎用 ${}"],
      ["工程原则", "默认优先 #{}，白名单兜底 ${}"]
    ],
    detail: `MyBatis 里 #{} 和 \${} 的区别，核心不在“一个有引号一个没引号”，而在于它们进入数据库的方式完全不同。#{} 会被 MyBatis 处理成 JDBC 预编译参数占位符 ?，真正的值不是直接拼进 SQL 文本，而是在执行阶段单独绑定进去。\${} 则是纯字符串替换，MyBatis 在生成最终 SQL 时直接把文本原样拼接进去。这个差异决定了安全性、可维护性和适用场景。  

先看 #{}。比如 where id = #{id}，最终发给数据库的 SQL 类似 where id = ?，参数 id 由 PreparedStatement 绑定。这样做有三个好处：第一，天然防 SQL 注入，因为参数值不会被当成 SQL 语法解释；第二，数据库可以利用预编译执行计划；第三，MyBatis 会配合 TypeHandler 做类型转换，比如 Java 时间类型、枚举、自定义对象都能被正确映射。绝大多数普通查询条件都应该用 #{}。  

\${} 则完全不同。比如 order by \${column}，如果传入 name desc，最终 SQL 就真的会拼成 order by name desc。这使得 \${} 适合处理不能作为预编译参数的位置，例如动态表名、动态列名、动态排序关键字。因为数据库不允许把表名或列名写成 ? 占位符，所以这种结构性变化只能通过文本拼接实现。  

但正因为 \${} 是拼文本，所以风险非常高。如果用户输入未经校验就进入 \${}，就可能构造恶意 SQL。最常见的是动态排序字段、动态表名这种需求，很多人偷懒直接把前端传来的字段名塞进 \${}，这几乎等于把 SQL 语法控制权交给外部。正确做法是白名单校验：前端传的是业务字段标识，后端把它映射成有限可选列名，而不是原样拼接。  

面试时不要只说“#{} 防注入，\${} 不防”，还要说出为什么。#{} 的本质是参数绑定，\${} 的本质是文本替换。再补一条实践原则：只要是值，优先 #{}；只有 SQL 结构本身必须动态变化时，才考虑 \${}，而且一定配白名单。  

项目里还可以补一个代码审查视角：只要 Mapper 里出现 \${}，就应该下意识问一句“这个值从哪里来，是否经过枚举映射或白名单”。很多安全问题不是框架不会防，而是开发者把本该用 #{} 的地方写成了 \${}。这句一说，答案就从语法差异升级成安全意识。  

图解可以画成两条链路：#{} 路线是“Mapper 参数 -> MyBatis -> SQL 中的 ? -> JDBC 绑定”；\${} 路线是“Mapper 参数 -> 直接拼接到 SQL 字符串 -> 数据库解析执行”。旁边用红色标出 \${} 的注入风险点。最后落一句工程经验：不是禁止 \${}，而是要把它关在强校验和有限场景里。这样就不是死背八股，而是能落到真实代码审查。`
  },
  "mybatis-cache": {
    type: "compare",
    nodes: [
      ["一级缓存", "SqlSession 级别，默认开启"],
      ["二级缓存", "Mapper namespace 级别，可跨 Session"],
      ["命中前提", "相同语句和参数且缓存未失效"],
      ["失效时机", "增删改或提交回滚会清理相关缓存"],
      ["一致性风险", "跨会话缓存更容易过期不准"],
      ["工程建议", "二级缓存慎用，优先显式缓存方案"]
    ],
    detail: `MyBatis 一级缓存和二级缓存的区别，核心是作用域不同，以及由此带来的命中效果和一致性风险不同。一级缓存是 SqlSession 级别，默认开启，同一个 SqlSession 中相同查询在没有清缓存的前提下会直接命中；二级缓存是 Mapper namespace 级别，需要显式开启，多个 SqlSession 可以共享。  

先说一级缓存。你可以把它理解成一次会话里的查询结果暂存。比如在同一个 SqlSession 内连续执行两次完全相同的查询，第一次会打数据库，第二次可能直接从本地缓存返回。它的优点是简单、透明，且作用域小，一般不会引出太大一致性问题。但它也不是无条件命中：查询语句、参数、分页条件、环境等要一致；如果中间执行了更新、提交、回滚，缓存通常会被清空，因为 MyBatis 要避免同一个会话里读到自己刚修改前的旧数据。  

二级缓存则更激进。它把缓存提升到 Mapper 维度，也就是同一个 namespace 下的查询结果可被不同 SqlSession 复用。这样能减少数据库压力，但风险立刻上来了：多个请求、多个事务、多个会话共享同一份缓存，一旦数据更新频繁，就容易出现脏数据、失效不及时、命中不可预期等问题。尤其在分布式系统里，单 JVM 内的二级缓存价值有限，而一致性复杂度却明显上升。  

所以面试里最好别把二级缓存讲成“性能优化必备”。真实项目里，MyBatis 二级缓存反而经常被关闭，因为现在更常见的做法是把缓存明确设计在业务层或 Redis 这类独立缓存层中，由业务自己决定缓存 key、TTL、失效策略和多实例一致性。相比之下，框架内建的二级缓存虽然方便，但控制力不足。  

还可以补一个易混点：一级缓存是 MyBatis 执行器层面的本地缓存，不等于数据库缓存；二级缓存也不是分布式共享缓存。它们主要解决的是 ORM 层重复查询的问题，而不是全局缓存架构问题。  

如果面试官追问“那二级缓存是不是没用”，别走极端。更准确的说法是：读多写少、数据稳定、单体或单 JVM 场景里它可以有价值；但只要更新频繁、部署实例多、缓存一致性要求高，就要非常谨慎。把场景说清楚，比简单站队更有说服力。  

图解可以画两个层次：同一个 SqlSession 内部有一级缓存，多个 SqlSession 上层共享一个 namespace 二级缓存。再画出一次更新操作，箭头指向“清一级缓存”和“刷新/失效二级缓存”。最后总结成一句：一级缓存作用域小、风险低；二级缓存共享范围大、收益和风险都更高，工程里要谨慎评估，而不是默认开启。`
  },
  "n-plus-one-query": {
    type: "flow",
    nodes: [
      ["先查主列表", "一次查出 N 条主记录"],
      ["循环补详情", "每条记录再单独查一次关联数据"],
      ["总查询数", "1 次主查询加 N 次子查询"],
      ["问题表现", "延迟高、QPS 高、数据库来回抖动"],
      ["优化手段", "join、批量 in 查询、预加载、聚合"],
      ["排查线索", "SQL 日志里重复语句大量出现"]
    ],
    detail: `N+1 查询问题说白了，就是“先查一批主数据，再对每条主数据单独补查关联信息”，最终造成 1 次主查询加 N 次子查询。比如先查 100 条订单，再在循环里对每个订单单独查用户信息或商品信息，于是数据库总共被打 101 次。这种写法在数据量小时不明显，但一上量就会把数据库往返次数、连接池占用和接口时延全抬上去。  

为什么它危险？因为问题不只是“多执行了几条 SQL”，而是把原本可以一次性批量处理的事情，拆成了大量小而重复的远程调用。数据库查询本身有网络往返、连接申请、SQL 解析、执行、返回结果这些固定开销。N 一旦变大，总成本会急剧上升，P99 时延尤其明显。很多接口在测试环境数据少时没问题，线上分页大小一扩大或关联关系一复杂，就突然慢下来，根因经常就是 N+1。  

在 MyBatis、JPA、Hibernate 这类 ORM 或映射框架里，N+1 常常来自“看起来很优雅”的懒加载或循环查询。比如列表接口先查主表，然后 Java 代码里 for 循环调用 mapper 根据外键查详情；或者对象访问某个懒加载属性时，框架悄悄帮你补发一条 SQL。面试里如果能顺手提到“开启 SQL 日志后会看到大量模板相同、参数不同的重复查询”，就说明你真排查过。  

优化思路的核心是“批量化”和“前置规划”。如果关联关系简单，可以直接 join 一次查出；如果 join 会带来重复行或太重，可以先查主表 ID 列表，再用 where id in (...) 一次把关联数据批量查出来，回到内存里按 key 组装；如果是典型树形或多层对象，还可以预加载、分段聚合或缓存热点字典数据。到底选哪种，要看结果集大小、字段量、分页方式和数据库索引情况。  

还要注意另一个极端：为了解决 N+1 把所有关联都无脑 join 起来，结果 SQL 巨大、重复数据爆炸、分页失真，这也不一定更好。所以工程上不是一听到 N+1 就全部 join，而是要评估主表规模、关联基数和返回字段。  

再补一个排查经验：只看单条 SQL 的慢日志，有时不容易发现 N+1，因为每一条子查询本身可能都不慢。真正的信号往往是一次接口请求打出几十上百条结构相似的 SQL，所以链路日志、ORM 调试日志和 APM 的数据库调用次数统计特别关键。  

图解非常适合画成两列对比：左边是错误做法，先查主列表，再在循环里对每条记录发一条子查询；右边是优化做法，一次主查询加一次批量关联查询，最后在服务内存中组装结果。旁边标上“SQL 次数从 1+N 变成 1+1 或 1+少量批次”。这样的图一出来，N+1 的性能问题和优化方向会非常直观。`
  },
  "threadlocal-memory-leak": {
    type: "flow",
    nodes: [
      ["线程私有变量", "ThreadLocal 让每个线程有独立副本"],
      ["底层存储", "值放在线程的 ThreadLocalMap 里"],
      ["弱引用键", "key 是弱引用但 value 不是"],
      ["线程池复用", "线程长期不销毁导致旧 value 留存"],
      ["泄漏成因", "key 被回收后 value 仍挂在线程上"],
      ["正确姿势", "用完 remove 并警惕大对象和线程池"]
    ],
    detail: `ThreadLocal 的本意是“给每个线程一份独立变量副本”，常用于保存用户上下文、事务上下文、格式化器、追踪 ID 等线程内共享但线程间隔离的数据。它并不是把数据存在 ThreadLocal 对象自己身上，而是存在当前线程内部的 ThreadLocalMap 里。也就是说，真正持有值的是线程，而 ThreadLocal 更像访问这块线程私有存储的钥匙。  

这就引出了为什么会内存泄漏。ThreadLocalMap 的 key，也就是 ThreadLocal 对象本身，通常是弱引用；但 value 不是弱引用，而是普通强引用。这样设计是为了当外部没有再持有某个 ThreadLocal 时，key 可以被 GC 回收，避免 key 永远活着。问题在于：key 被回收后，对应的 value 并不会自动马上消失，它还挂在那个线程的 ThreadLocalMap 槽位里，只是变成了“key 为 null、value 仍然存在”的脏条目。  

如果这个线程很快结束，整个线程对象和 ThreadLocalMap 一起被回收，问题不大；真正危险的是线程池。线程池里的工作线程会被长期复用，不会随着一次请求结束就销毁。于是如果你在请求处理中 set 了一个大对象或上下文，却没有在 finally 里 remove，这个 value 可能会一直跟着那个线程存活很久。久而久之，线程池每个线程都挂一堆脏 value，就形成了典型的内存泄漏。  

很多人会误解成“弱引用不是会自动清理吗”。要强调：弱引用只保证 key 可以被回收，不保证 value 立刻被清理。ThreadLocalMap 只有在后续 get、set、remove 等操作时，才会顺便清理部分脏条目；如果这个线程后面很少再碰对应位置，脏 value 可能挂很久。  

工程上的正确姿势非常明确：try { set } finally { remove }。尤其在线程池、Web 请求、异步任务里，只要用了 ThreadLocal，几乎都应该在 finally 里清理。还要避免往里面放大对象、连接、会话这类重资源。异步线程切换时也不能想当然依赖 ThreadLocal 透传，否则上下文可能丢失或串数据。  

图解建议画出一条线程，线程里有 ThreadLocalMap。外部 ThreadLocal 作为 key 指向 map 条目，value 存在线程内部；随后 key 被 GC 清掉，但 value 还挂在长期存活的线程上。再补一条线程池复用链路，突出“线程不死，value 不走”。最后用绿色标出规范动作：在 finally 调用 remove。这样面试回答就能把“为什么会泄漏”讲透，而不是只停在一句口诀。`
  },
  "completablefuture": {
    type: "flow",
    nodes: [
      ["异步目标", "把串行等待改成非阻塞编排"],
      ["任务提交", "supplyAsync 或 runAsync 发起异步任务"],
      ["结果组合", "thenApply、thenCompose、thenCombine 串接流程"],
      ["异常处理", "exceptionally、handle 统一兜底"],
      ["线程池选择", "避免全压默认线程池造成争抢"],
      ["适用边界", "IO 编排很好用，复杂业务别写成回调地狱"]
    ],
    detail: `CompletableFuture 适合解决的核心问题，不是“开线程”本身，而是把多个可能耗时的步骤做成异步编排，减少主线程阻塞等待。最常见场景是一个请求需要同时调多个下游服务：查用户、查订单、查推荐、查库存，如果全都串行执行，总耗时接近各步骤之和；如果它们彼此独立，就可以并行发起，最后再汇总结果。CompletableFuture 正是为这种“异步发起 + 结果组合 + 异常兜底”设计的。  

它比传统 Future 好用的地方，在于 Future 只代表“将来会有结果”，但拿结果时经常还是要手动阻塞 get，而且任务之间的串联、聚合、异常处理都不优雅。CompletableFuture 则提供了一套链式 API。比如 thenApply 适合对上一步结果继续加工，thenCompose 适合把“异步结果再触发另一个异步任务”连起来，thenCombine 适合把两个独立异步结果合并，allOf 和 anyOf 适合做多任务等待和竞速。面试里能把这些接口按用途分层说出来，会比只会背名字更清楚。  

真实项目里最常见的价值，是提升接口尾延迟表现。比如首页接口依赖四个服务，其中三个 IO 调用互不依赖，用 CompletableFuture 并发发起后，总耗时更接近最慢那个服务，而不是四个服务相加。再比如批量处理任务时，一个主流程可以并发拉取补充信息、并发校验、并发生成结果，最后统一 join 汇总。这里它解决的是“等待时间重叠”的问题，不是无脑多线程就更快。  

但它也很容易被用坏。第一，线程池选择非常关键，很多人默认用 ForkJoinPool.commonPool，结果线上所有异步任务和别的公共任务抢资源，出现线程饥饿。第二，异步链过长时可读性会下降，如果到处 thenApply、thenCompose 嵌套，再混合异常处理，代码会变得非常难维护。第三，如果任务本身是 CPU 密集型、又没有合适线程池隔离，盲目并发可能让上下文切换和竞争开销比收益更大。  

异常处理也是高频追问。异步任务里抛出的异常不会像同步代码那样自然冒泡到当前调用栈，所以要用 exceptionally、handle、whenComplete 这类接口做降级、日志和补偿。项目里还要考虑超时控制、取消、线程池隔离和上下文透传，比如 TraceId、用户上下文、MDC 日志变量等，否则异步一上来，排查难度会明显提高。  

图解可以画成一条请求编排链：主线程发起三个异步任务，任务 A、B、C 并行执行，中间分别有成功和异常分支，最后在汇总节点 join 或 combine 成最终响应。旁边再补一列“适合”和“不适合”的场景：独立 IO 调用、聚合查询、批量并发处理适合；强顺序业务、复杂事务、一堆共享可变状态的逻辑就不适合。这样回答就不是“会用 API”，而是知道 CompletableFuture 在系统里的最佳位置。`
  },
  "generic-erasure": {
    type: "structure",
    nodes: [
      ["编译期泛型", "类型检查发生在编译阶段"],
      ["擦除过程", "编译后类型参数被替换成边界或 Object"],
      ["桥接方法", "为保持多态和重写关系自动生成方法"],
      ["运行时限制", "无法直接拿到 T 的真实类型"],
      ["典型影响", "不能 new T、不能重载仅靠泛型区分"],
      ["工程应对", "显式传 Class、TypeReference 或边界信息"]
    ],
    detail: `Java 泛型擦除，说白了就是泛型主要服务于编译期类型安全，而不是把完整的类型参数一直保留到运行时。你在源码里写 List<String>、List<Integer>，编译器会据此做类型检查，防止把错误类型塞进集合；但编译成字节码后，大部分类型参数信息会被擦掉，运行时看到的底层类型通常还是原始结构。这个设计是为了兼容早期没有泛型的 Java 字节码和类库。  

所谓“擦除”，并不是把一切都变没了，而是把类型参数替换成它的上界。如果没有显式边界，就替换成 Object；如果写了 <T extends Number>，那么相关位置会用 Number 作为擦除后的类型。这就解释了很多现象：为什么运行时不能直接判断 List<String> 和 List<Integer> 的元素类型差异，为什么不能直接 new T()，为什么很多泛型工具类都得额外传一个 Class 或 TypeReference。  

面试里很容易被追问桥接方法。比如子类重写了一个带泛型返回值的方法，擦除后为了维持多态一致性，编译器可能自动生成桥接方法。这个点不一定要求你把字节码细节讲得很深，但最好知道：泛型擦除后，Java 还要借助编译器补一些桥接逻辑，才能保证源码层面的继承和重写语义仍然成立。  

泛型擦除带来的限制特别值得讲。第一，不能直接创建泛型数组，比如 new T[10]；第二，不能靠方法参数里的泛型类型不同来重载，因为擦除后签名可能一样；第三，运行时很多反射场景下你拿到的是原始类型，需要从声明处、父类签名或字段签名里额外解析泛型信息。面试如果只说“为了兼容老版本”，会太浅；把这些限制说出来，才说明你真碰到过。  

工程实践里，应对办法通常有三类。第一，显式传入 Class<T> 或工厂接口；第二，利用父类或匿名内部类保留参数化类型信息，比如某些 JSON 框架里的 TypeReference；第三，合理使用泛型边界和通配符，让编译期检查尽量多做事情。泛型的真正价值在于提前发现类型问题，而不是运行时再去赌。  

如果面试官继续深挖，你还可以补一句“泛型擦除为什么没有毁掉框架生态”。答案是很多框架会在字段声明、方法签名、父类签名这些地方通过反射补拿泛型元数据，所以虽然运行时对象本身看不到完整 T，但声明处的信息仍然常常可以被解析出来。这个补充能把语言机制和框架实践连起来。  

图解可以画成两层：源码层是 List<String>、Box<User> 这类带类型参数的代码；编译后进入“擦除层”，类型参数被替换成 Object 或上界，同时可能生成桥接方法；运行时再展示那些受限行为，比如无法直接知道 T 是谁。最后落一句：泛型让 Java 在不改变运行时模型的前提下获得了编译期类型安全，但代价就是运行时类型信息天然不完整。`
  },
  "jmm-happens-before": {
    type: "flow",
    nodes: [
      ["JMM 目标", "定义多线程读写共享变量的可见性和有序性规则"],
      ["主内存与工作内存", "线程可能先在本地副本里读写"],
      ["happens-before", "描述一个操作结果对另一个操作可见的先后关系"],
      ["建立规则", "锁、volatile、线程启动结束等都能建立关系"],
      ["禁止误解", "它不是时间先后，而是可见性与有序性约束"],
      ["工程意义", "判断并发代码是否安全的核心依据"]
    ],
    detail: `JMM，也就是 Java Memory Model，解决的是多线程环境下“一个线程写的数据，另一个线程什么时候能看到，以及编译器和 CPU 能不能重排这些操作”的问题。因为线程执行并不是所有读写都直接去主内存完成，很多值可能暂存在寄存器、CPU Cache 或线程可见的工作内存视角里，所以如果没有一套统一规则，程序员看到的执行顺序和机器实际执行顺序可能完全不是一回事。  

happens-before 是 JMM 里最关键的判断标准。它不是简单的“时间上谁先发生”，而是“如果操作 A happens-before 操作 B，那么 A 的结果对 B 可见，并且 A 在排序上要排在 B 前面”。这句话特别重要，因为很多人一看到 happens-before 就误以为是时间顺序，其实它描述的是内存可见性和有序性约束。  

JMM 提供了一组建立 happens-before 关系的规则。最常见的有：程序次序规则，同一线程里前面的操作先行发生于后面的操作；监视器锁规则，解锁 happens-before 后续对同一把锁的加锁；volatile 变量规则，对一个 volatile 变量的写 happens-before 后续对同一变量的读；线程启动规则，Thread.start 之前的操作对新线程可见；线程终止规则，线程中所有操作对另一个检测到它结束的线程可见。这些规则就是并发面试里的基础骨架。  

为什么这套规则重要？因为它决定了哪些写法是安全的，哪些只是“看起来能跑”。比如双重检查单例如果没有 volatile，可能因为指令重排让另一个线程看到一个“引用已经赋值、对象却还没完全初始化”的半成品。再比如没有同步保护的共享标志位，一个线程改了值，另一个线程可能一直看不到。你讲 happens-before 时如果能顺手举出这些经典坑，答案会非常扎实。  

JMM 不等于“禁止所有重排”。实际上只要不破坏 happens-before 语义，编译器和 CPU 仍然可以为性能做很多优化。所以并发编程的关键不是幻想机器会按源码逐行乖乖执行，而是通过锁、volatile、final 安全发布、线程同步工具建立足够的可见性和顺序性边界。  

图解可以画成两个线程和一块共享内存。线程 A 先写共享变量，再通过 volatile 写、解锁或线程结束建立 happens-before 边；线程 B 在相应的 volatile 读、加锁或 join 后读取共享变量，于是能看到 A 的结果。旁边再标一句：happens-before 是并发正确性的证明工具，不是时间线故事。这个角度一出来，JMM 就不再只是抽象名词。`
  },
  "java-oom-troubleshooting": {
    type: "flow",
    nodes: [
      ["先看现象", "确认报错类型和发生区域"],
      ["定位区域", "堆、Metaspace、直接内存还是线程栈"],
      ["保留证据", "dump、GC 日志、监控和线程信息"],
      ["分析对象", "看谁占内存、谁持有引用链"],
      ["判断根因", "泄漏、突增、配置不合理或流量异常"],
      ["修复验证", "改代码或参数后复现与回归"]
    ],
    detail: `Java OOM 排查的第一步，不是上来就调大堆，而是先搞清楚到底是哪一种 OOM。常见报错包括 Java heap space、Metaspace、GC overhead limit exceeded、Direct buffer memory、Unable to create new native thread 等。它们背后的问题区域完全不同：堆满了、类元数据爆了、GC 过于频繁、直接内存耗尽、线程数过多，排查路径都不一样。  

所以真正的第一动作是保留现场证据。看报错日志、应用监控、GC 日志、堆 dump、线程栈、容器资源限制，尽量确认问题发生时的内存曲线和系统状态。比如堆使用量持续爬升不回落，常见是对象泄漏；内存锯齿正常但峰值突然超过上限，可能是流量尖峰或一次性大对象；Metaspace 持续涨，可能是频繁动态生成类、类加载器泄漏；Direct buffer memory 相关则要看 NIO、Netty、堆外缓存。  

如果是堆 OOM，最核心的分析工具就是堆转储。用 MAT、VisualVM、JProfiler 或商业 APM 去看大对象、对象数量、支配树、引用链。真正要回答的是：哪些对象占了最多内存，它们为什么还活着，是业务上需要的正常缓存，还是不该继续被引用的泄漏对象。比如静态集合没清、ThreadLocal 没 remove、监听器没注销、缓存没有淘汰、请求上下文挂在线程上，这些都是高频泄漏源。  

如果是 GC 很忙却回收不掉，那就要结合 GC 日志看分代回收、停顿时间、晋升失败、老年代压力。很多时候不是简单的“堆太小”，而是对象生命周期和参数配置不匹配，或者短时间创建了过多中间对象。对于线程相关 OOM，则要看线程池配置、是否无限创建线程、容器 ulimit、阻塞导致线程堆积。  

排查 OOM 还要把代码和流量结合起来。比如一个导出接口一次把几十万行数据全读进内存，或者 JSON 反序列化把超大报文整包持有，这些并不一定是泄漏，但同样会导致 OOM。也就是说，要区分“内存回不来”和“单次用得太猛”。前者偏泄漏，后者偏模型和容量设计问题。  

修复之后也别忘了闭环验证。不是 dump 看明白就结束了，而是要通过压测、灰度、监控曲线和回归验证确认内存峰值、GC 次数、停顿时间和错误率都回到合理区间。很多 OOM 问题如果没有复现场景和回归标准，只是暂时“看起来好了”。  

图解可以画成一棵排查树：先按 OOM 类型分支，再按堆、Metaspace、Direct Memory、线程数分别进入不同证据链；堆分支继续到 dump 分析、引用链、业务代码定位；最后落到修复动作：调参数、修泄漏、拆批处理、限制线程、削峰限流。这样回答会很像真实线上排障，而不是只会说“看下 dump”。`
  },
  "spring-circular-dependency": {
    type: "flow",
    nodes: [
      ["问题出现", "Bean A 依赖 B，Bean B 又依赖 A"],
      ["实例化先行", "Spring 先创建原始对象再填充属性"],
      ["三级缓存", "提前暴露单例引用以打破 setter 循环"],
      ["代理处理", "必要时提前暴露代理而不是原始对象"],
      ["边界限制", "构造器循环和原型 Bean 解决不了"],
      ["设计建议", "能重构职责就别把框架能力当设计常态"]
    ],
    detail: `Spring 解决循环依赖，经典答案是“靠三级缓存”，但如果只停在这句，面试官通常会继续追问：为什么是三级，不是一级或二级？哪些情况能解决，哪些情况不能？真正要讲清楚的，是 Spring 在单例 Bean 创建流程里，如何通过“先实例化、后注入”和“提前暴露引用”把一部分循环依赖绕过去。  

先看场景。假设 Bean A 依赖 Bean B，Bean B 又依赖 Bean A。如果是字段注入或 setter 注入，Spring 创建 A 时，先把 A 实例化出来，但属性还没填完；接着发现需要 B，于是去创建 B；创建 B 时又发现需要 A，这时如果 Spring 能拿到一个“已经实例化但还没完全初始化的 A 引用”，B 就能先注入它，等 B 创建完再回头完成 A。这个“先拿半成品引用顶上”的能力，就是循环依赖得以解决的关键。  

三级缓存分别承担不同职责。一级缓存放完全初始化好的单例；二级缓存放提前暴露出来的早期 Bean 引用；三级缓存放生成早期引用的工厂。之所以需要三级，而不是直接两个缓存，是因为 Spring 还要考虑 AOP 代理。如果某个 Bean 最终应该暴露的是代理对象，那在循环依赖场景里，提前给别人的也最好是同一个代理，而不是原始对象。三级缓存里的 ObjectFactory 就给了 Spring 一个“按需生成早期引用或代理”的机会。  

但这个能力有明显边界。第一，构造器循环依赖通常解决不了，因为对象连实例化这一步都还没完成，根本没有早期引用可暴露；第二，原型作用域 Bean 默认也解决不了，因为 Spring 不会像单例那样缓存它们；第三，如果 Bean 创建过程非常复杂，涉及自定义后置处理器、不正确的代理顺序等，也可能触发异常或得到不符合预期的早期引用。  

所以工程上不要把“Spring 能兜底”误解成“循环依赖是合理设计”。很多时候 A 依赖 B、B 又依赖 A，说明职责边界本来就糊了，应该抽第三个协调者、事件机制、接口拆分或状态下沉，而不是长期依赖框架魔法。面试里能主动说出这一点，往往比把三级缓存背得很熟更加分。  

再补一句实践判断：如果一个循环依赖只能靠加 @Lazy、拆 setter、改注入方式才勉强通过，往往意味着模型本身已经值得重构。框架能帮你启动成功，不等于这段依赖关系长期可维护。把“能运行”和“设计健康”分开说，答案会更成熟。  

图解可以画成创建链：Spring 开始创建 A，A 需要 B，于是创建 B；B 回头需要 A 时，从三级缓存经由工厂拿到 A 的早期引用，再继续完成 B，最后回到 A 完成初始化。旁边再标红两个失败场景：构造器循环依赖、原型 Bean 循环依赖。这样就把“Spring 如何解决循环依赖”讲成了一个完整过程，而不是孤零零一句术语。`
  },
  "spring-transaction-propagation": {
    type: "compare",
    nodes: [
      ["REQUIRED", "有事务就加入，没有就新建"],
      ["REQUIRES_NEW", "挂起外层事务，自己独立开新事务"],
      ["NESTED", "在当前事务里建保存点做局部回滚"],
      ["SUPPORTS 等", "按是否存在事务决定是否跟随"],
      ["传播本质", "解决方法嵌套调用时事务边界怎么传递"],
      ["易错点", "自调用、异常捕获和代理边界影响生效"]
    ],
    detail: `Spring 事务传播行为，本质是在回答一个问题：一个带事务的方法内部再调用另一个带事务的方法时，内层方法到底是加入外层事务、开自己的事务，还是干脆不在事务里执行？如果只把它当成七个英文枚举名去背，很容易一追问就乱；更稳的方式是先抓住“谁和谁共享一个事务边界”这个主线。  

最常用的是 REQUIRED，它的语义是“有就加入，没有就新建”。这也是默认值。也就是说，如果 ServiceA 已经开启事务，再调用另一个 REQUIRED 方法，后者会加入同一个事务，提交和回滚命运绑在一起。这最符合大多数业务直觉。REQUIRES_NEW 则相反，它一定新开一个事务，外层事务会被暂时挂起。这样内层提交或回滚独立于外层，常见于审计日志、补偿记录、消息落库这类希望“即使主流程失败也尽量单独提交”的场景。  

NESTED 则容易和 REQUIRES_NEW 混。NESTED 不是完全独立事务，而是在当前事务里创建保存点。内层失败时可以回滚到保存点，不一定把外层整个事务都打掉；但前提通常依赖底层数据库和事务管理器支持保存点。它更像“大事务里的局部回滚能力”，不是彻底隔离的新事务。  

其他传播行为则偏边缘但也要知道语义。 SUPPORTS 表示有事务就跟着，没有就非事务执行；MANDATORY 表示必须在已有事务里，否则报错；NOT_SUPPORTED 表示以非事务方式执行，如果当前有事务则挂起；NEVER 表示绝不能在事务里执行；NESTED 前面已经讲过。面试不一定要求你把所有名字倒背如流，但至少 REQUIRED、REQUIRES_NEW、NESTED 三个要说清。  

真正容易踩坑的，是事务传播和 Spring AOP 代理边界绑在一起。自调用不会走代理，事务可能根本没生效；异常被你自己 catch 掉不继续抛，默认回滚也可能失效；REQUIRES_NEW 虽然独立，但并不意味着调用链上所有资源和锁都自动安全。也就是说，传播行为不是纸面概念，它和代理、异常规则、底层连接管理一起才构成真实事务效果。  

如果要把这题讲得更像项目经验，可以顺手补一个选择原则：主流程通常先用 REQUIRED 保持整体一致；日志、审计、补偿记录这类想独立提交的动作才考虑 REQUIRES_NEW；NESTED 则更多看底层是否支持保存点。这样不是在背枚举，而是在做事务边界设计。  

图解可以画成外层方法 A 调内层方法 B 的调用链，分别展示 REQUIRED 共用一个事务、REQUIRES_NEW 挂起外层后开新事务、NESTED 在同一事务里打保存点。最后再补一个注意事项区：自调用、异常捕获、只对运行时异常回滚这些默认规则会影响最终表现。这样回答就很完整。`
  },
  "redis-expire-eviction": {
    type: "compare",
    nodes: [
      ["过期删除", "key 到了 TTL 后属于逻辑过期数据"],
      ["删除策略", "惰性删除和定期删除共同处理过期 key"],
      ["内存淘汰", "内存达到上限时按策略挑 key 逐出"],
      ["触发时机", "一个看时间，一个看内存压力"],
      ["淘汰范围", "有的策略只淘汰带 TTL 的 key"],
      ["设计取舍", "TTL 设计和 maxmemory 策略要一起看"]
    ],
    detail: `Redis 过期删除和内存淘汰经常被混成一件事，但它们解决的问题完全不同。过期删除关注的是“这个 key 的生存时间到了没有”；内存淘汰关注的是“Redis 可用内存不够了，应该把谁赶走”。一个是基于时间语义，一个是基于容量压力。面试里如果能先把这两个问题拆开，后面就会顺很多。  

先说过期删除。给 key 设置了 TTL，并不代表一到那个时刻 Redis 就一定立刻把它删掉。Redis 主要结合惰性删除和定期删除。惰性删除是在访问 key 时发现它过期了，顺手删掉；定期删除则是后台周期性抽样扫描一部分设置了过期时间的 key，把其中已过期的清掉。这样设计是为了避免“为了一到点立刻删掉所有 key”带来过高 CPU 成本。代价是：逻辑上已经过期的数据，物理上可能会短暂残留。  

再说内存淘汰。它发生在配置了 maxmemory 且当前内存达到上限之后。此时 Redis 不是看某个 key 是否到期，而是按照策略决定淘汰谁。常见策略有 noeviction、allkeys-lru、volatile-lru、allkeys-lfu、volatile-ttl、allkeys-random 等。这里的 allkeys 和 volatile 就体现了淘汰范围差异：前者从所有 key 里选，后者只从设置了过期时间的 key 里选。  

很多故障都来自把这两套机制想当然地等同起来。比如有人觉得“我给所有缓存都设了 TTL，就一定不会顶满内存”，其实未必，因为过期 key 可能还没被及时清理；也有人觉得“用了 LRU 淘汰就不用考虑 TTL 了”，也不对，因为 TTL 还承担业务语义，比如缓存多久失效、多久需要回源刷新。TTL 决定的是数据新鲜度，淘汰策略决定的是内存压力下的生存优先级。  

工程上设计 Redis 缓存时，TTL 和 maxmemory 策略必须一起看。热点缓存、用户会话、排行榜、延迟队列、持久键值，它们对 TTL 和淘汰容忍度完全不同。比如做纯缓存层时，allkeys-lru 或 allkeys-lfu 常见；如果 Redis 里混放了不能随便丢的业务数据，就要格外谨慎，甚至不该和缓存共用实例。  

再补一个线上经验：当你发现命中率下降、内存频繁打满、慢查询或回源流量陡增时，要同时检查 TTL 分布和淘汰策略统计，不能只盯一种机制。很多缓存故障其实是“过期设计不合理”和“淘汰策略不匹配”叠加出来的。  

图解可以画成两条分支：第一条是 key 设置 TTL 后，经过惰性删除和定期删除处理；第二条是 Redis 内存接近上限，触发 maxmemory 策略，从候选 key 中做淘汰。旁边再标一句：过期不等于立删，淘汰不等于过期。这样就能把很多面试里常见的混淆点一次讲明白。`
  },
  "bloom-filter": {
    type: "structure",
    nodes: [
      ["位数组", "用很小空间记录大量元素痕迹"],
      ["多个哈希", "一个元素映射到多个位置"],
      ["判断规则", "有 0 一定不存在，全是 1 只是可能存在"],
      ["核心特性", "允许误判，不会漏判"],
      ["典型用途", "缓存穿透、去重预判、黑名单过滤"],
      ["边界限制", "不能直接删除，误判率和容量相关"]
    ],
    detail: `布隆过滤器最适合解决的问题，是“我想用很小的空间，快速判断一个元素大概率在不在集合里”。它不保存元素本身，而是把元素经过多个哈希函数映射到位数组的多个位置上，并把这些位设为 1。查询时，再对目标元素做同样的多次哈希，如果发现其中任何一位是 0，就能确定它一定没出现过；如果对应位置全是 1，只能说它可能出现过。  

这个“可能存在”的不确定性，就是布隆过滤器的关键特征：它允许误判，但不会漏判。也就是说，本来不存在的元素，可能因为别的元素把相关位都占满了而被误认为存在；但一个真实存在过的元素，不会被判断成不存在。面试里这句话非常重要，因为很多人会把误判方向说反。  

为什么它有价值？因为空间效率极高。相比 HashSet 直接存海量 key，布隆过滤器只存位图和哈希结果，特别适合做大规模存在性预判。最常见场景就是缓存穿透防护：请求来了先查布隆过滤器，如果它判断“一定不存在”，就没必要继续打缓存和数据库。除此之外，它也常用于爬虫去重、邮件黑名单、URL 去重、推荐系统曝光预判等场景。  

但它不是万能集合。第一，普通布隆过滤器不适合直接删除元素，因为多个元素可能共享同一位，删一个会影响别人；第二，误判率会随着位数组大小、哈希函数个数、已插入元素数量变化而变化；第三，如果数据规模远超预估，误判率会迅速上升。所以布隆过滤器一定要结合容量规划，而不是“随便开个 bitset 就完事”。  

项目里还可以补一句工程取舍：布隆过滤器通常不是单独存在，而是作为主存储或缓存之前的一道预判层。它帮你挡掉确定无效的请求，但对“可能存在”的请求，还得继续访问后面的真实数据源。也就是说，它解决的是流量筛选，而不是最终正确性。  

如果业务有删除需求，面试里也可以顺手提计数布隆过滤器，但要说明它空间成本更高、实现更复杂。这个补充不是必须展开很多，而是让面试官知道你理解普通布隆过滤器的局限，不会把它误用到需要强删除语义的场景。  

还有一个很实用的答法是补充“误判率怎么控制”：位数组越大、哈希函数选择越合理、预计元素数量越接近真实规模，误判率就越可控。也就是说，布隆过滤器不是一个拍脑袋组件，而是需要结合数据规模做容量设计。把这一点说出来，会让答案更像实际架构设计。  

图解很适合画一个位数组和三个哈希函数。元素写入时被打到多个 bit 位；查询时如果某一位是 0，直接判不存在；如果都为 1，则判可能存在。旁边再用红字标明“会误判、不会漏判”。这样一来，布隆过滤器的原理、优势和限制就会非常直观。`
  },
  "delayed-message": {
    type: "compare",
    nodes: [
      ["业务目标", "消息不是立刻消费而是延后一段时间处理"],
      ["消息队列能力", "利用 MQ 原生延迟或定时特性"],
      ["时间轮方案", "自己维护到期扫描和投递"],
      ["死信转发", "借助 TTL 加死信队列实现延时"],
      ["数据库任务", "小规模场景可用定时扫表补发"],
      ["选型依据", "精度、吞吐、可靠性和运维复杂度"]
    ],
    detail: `延迟消息要解决的问题很直接：消息不是现在立刻消费，而是希望过一段时间再处理。典型场景包括订单超时关单、支付超时取消、优惠券过期提醒、重试补偿、定时通知、延迟重试等。关键点不在“会不会定时”，而在于如何在延迟期间可靠保存消息，并在到期时尽量准时投递给消费者。  

实现方式通常分几类。第一类是 MQ 原生支持，比如某些消息队列提供延迟级别、定时消息或基于时间戳的投递能力。它的优点是接入简单、可靠性通常较好，缺点是粒度、延迟级别、精度和吞吐受产品特性约束。第二类是用 TTL 加死信队列实现延迟：消息先进入一个设置过期时间的队列，到期后转到真正消费队列。这种方法常见于 RabbitMQ 体系，但在大量不同延迟时间并存时，管理会比较麻烦。  

第三类是自己实现时间轮或延迟队列。比如用 Redis 的 zset、分层时间轮、堆结构、内存调度器等保存待触发任务，到期后再投递到 MQ 或直接执行。这种方式灵活，适合自定义精度和复杂调度，但也意味着你自己要负责可靠性、主从切换、重复投递、扫描性能和恢复补偿。第四类是数据库扫表，小规模业务可以用，但吞吐和准时性一般，作为核心高并发方案通常不够。  

面试里不要只说“能用 MQ 或定时任务”。更重要的是比较它们的边界：延迟精度要求高不高，消息量大不大，是否允许少量延迟漂移，是否必须持久化，失败后如何补投，消费者如何幂等。比如订单超时关闭可以允许几秒偏差，但金融风控里某些超时动作对准时性要求会更高。  

延迟消息还天然带来两个工程问题。第一，到了触发时间后可能出现瞬时堆积，所以消费者要有削峰和并发处理能力；第二，延迟消息恢复时容易重复投递，因此业务必须幂等。很多系统真正难的不是“怎么延迟”，而是“到点之后如何可靠、可控地处理”。  

如果再往架构上讲一步，还要考虑“等待中的消息如何恢复”。比如节点重启、主从切换、时间轮故障时，未到期任务不能丢；恢复后也不能因为补偿扫描把同一条消息重复投太多次。所以延迟消息方案最终拼的，往往不是功能是否存在，而是持久化和恢复机制是否成熟。  

面试里还可以顺手补一个业务判断标准：如果只是偶发的小规模延时任务，数据库扫表也许足够；如果是高并发订单超时、海量重试或大规模通知，就更应该选择 MQ 原生能力或专门的时间轮方案。把“场景大小”作为选型维度讲出来，会很有架构味。  

图解可以画成三条方案路线：原生延迟消息、TTL 加死信转发、自建时间轮。每条路线都从“消息进入”开始，经过“等待区”到“到期触发”，最后进入消费者。旁边补一列选型维度：精度、吞吐、实现成本、可靠性。这样回答能把这题从工具名堆砌，变成清晰的架构比较。`
  },
  "http-status-codes": {
    type: "structure",
    nodes: [
      ["1xx", "请求已接收，继续处理的临时状态"],
      ["2xx", "请求成功处理完成"],
      ["3xx", "需要重定向或走缓存协商流程"],
      ["4xx", "客户端请求有问题"],
      ["5xx", "服务端处理失败"],
      ["语义重点", "选对状态码比返回成功字符串更重要"]
    ],
    detail: `HTTP 状态码不是背数字题，它真正考的是你是否理解“服务端如何向客户端表达处理结果”。最基础的分类一定要知道：1xx 表示临时响应，2xx 表示成功，3xx 表示重定向或协商，4xx 表示客户端请求有问题，5xx 表示服务端处理失败。但面试里真正加分的，不是背出 200、404、500，而是能讲出常见状态码背后的语义边界。  

先看 2xx。200 是最常见的成功返回；201 常用于创建资源成功，比如创建订单、创建用户；204 表示成功但没有响应体，常见于删除成功或某些幂等更新。3xx 里，301 和 302 是重定向，304 则和缓存协商关系密切，表示资源未修改，客户端可以继续使用本地缓存。你如果能把 304 和缓存控制联系起来，状态码就不再是孤立知识点。  

4xx 的核心意思是“请求本身有问题”。400 往往表示参数错误或格式错误；401 表示未认证，通常是还没登录或 token 无效；403 表示已认证但无权限；404 表示资源不存在；405 表示方法不被允许；409 常见于资源冲突，比如重复创建；429 则用于限流场景。面试里经常有人把 401 和 403 混掉，这个点要特别稳。  

5xx 则表示服务端没把请求正确处理完。500 是通用内部错误；502、503、504 常出现在网关和代理链路里，分别偏向上游响应异常、服务暂时不可用、网关等待上游超时。如果你的项目里有 Nginx、Spring Cloud Gateway、服务网格，这几个状态码尤其值得结合网关链路去讲。  

工程上一个高频问题是“明明业务失败了，却总返回 200，再在 body 里写 code”。这种做法不是绝对不行，但会让客户端、网关、监控、缓存、重试策略失去基于协议语义做决策的能力。更成熟的做法是让 HTTP 状态码表达传输层和资源操作结果，再由业务码表达更细的业务语义。  

再补一个面试很爱问的点：状态码不是越细越好，而是要稳定、一致、团队能长期遵守。同一类错误今天返回 400、明天返回 200 包业务码、后天又改成 422，客户端和监控都会很痛苦。把状态码当成接口契约来维护，这句话很有工程味。  

如果想把答案和实际开发再贴近一点，还可以补一句“状态码会影响重试和告警策略”。比如 429 往往意味着限流后稍后重试，503 可能触发熔断或摘流，404 一般不该自动重试。也就是说，状态码不仅给人看，也是给网关、SDK 和运维系统看的。  

图解可以画成一棵状态码树，从 1xx 到 5xx 分层展开，再在 2xx、4xx、5xx 下挂典型场景：创建成功、未认证、无权限、资源不存在、限流、网关超时。最后收一句：选对状态码，本质是在给调用方、网关、监控系统提供准确语义，而不是单纯满足“接口能返回”。`
  },
  "http2-http3": {
    type: "compare",
    nodes: [
      ["HTTP/1.1", "文本协议，队头阻塞明显，常靠多连接并发"],
      ["HTTP/2", "二进制分帧、多路复用、头部压缩"],
      ["TCP 约束", "HTTP/2 仍跑在 TCP 上，丢包仍会影响同连接流"],
      ["HTTP/3", "基于 QUIC，传输层改到 UDP 之上"],
      ["连接体验", "更快建连、迁移友好、弱网表现更稳"],
      ["选型视角", "协议升级解决的是链路效率不是业务语义"]
    ],
    detail: `HTTP/1.1、HTTP/2、HTTP/3 的区别，不能只背“二进制、多路复用、基于 UDP”这些关键词，更重要的是理解每一代协议到底在解决上一代的什么痛点。HTTP/1.1 时代，请求和响应基本还是一问一答的文本协议，虽然有 Keep-Alive 能复用连接，但同一个连接上请求处理顺序仍然受限，浏览器为了并发通常要开多个 TCP 连接，这带来了额外握手和拥塞成本。  

HTTP/2 的核心改进是把应用层消息拆成二进制帧，并允许多个流在同一连接上并发交错传输，也就是多路复用。这样前端页面加载一堆 CSS、JS、图片时，不再需要像 HTTP/1.1 那样拼命多开连接。它还引入了头部压缩，减少重复 Header 开销，并支持服务端推送。不过要注意，HTTP/2 虽然缓解了应用层的队头阻塞，但底层依旧跑在 TCP 上，一旦某个 TCP 包丢失，后续数据还是要等重传，所以传输层队头阻塞并没有彻底消失。  

HTTP/3 的关键变化就在这里：它把传输基础从 TCP 换成了 QUIC，而 QUIC 又是构建在 UDP 之上的可靠传输协议。这样做不是“为了不用 TCP 很酷”，而是为了把重传、流控、拥塞控制、0-RTT、连接迁移这些能力收回到用户态协议栈中，更灵活地优化弱网和移动网络体验。HTTP/3 下，不同流之间的丢包影响可以更局部，不容易因为一个流的重传拖住整条连接。  

面试时还可以顺手讲建连体验。HTTP/1.1 上 HTTPS 通常要经历 TCP 握手再 TLS 握手；HTTP/2 大多也还是基于 TLS over TCP；HTTP/3 则借助 QUIC 把传输握手和安全握手设计得更紧密，建连延迟更低，而且连接 ID 机制让移动网络切换时连接迁移更友好。对手机弱网、切 Wi-Fi/4G 这种场景，HTTP/3 的收益尤其明显。  

但这不意味着 HTTP/3 会自动让所有业务都“快很多”。协议升级主要优化的是链路效率、弱网表现和连接管理，不会替你解决后端慢查询、接口串行调用、缓存失效这些业务问题。工程上通常还要看 CDN、负载均衡、网关、客户端 SDK 是否支持，很多系统会是 HTTP/1.1、2、3 共存。  

图解可以画三列对比：HTTP/1.1 是多请求依赖多连接；HTTP/2 是单连接多流但底层同一条 TCP；HTTP/3 是基于 QUIC 的多流并发，丢包影响更局部。最后再加一行“解决的问题”：HTTP/1.1 的连接开销、HTTP/2 的应用层并发、HTTP/3 的传输层阻塞和弱网体验。这样面试回答会非常清楚。`
  },
  "user-kernel-mode": {
    type: "flow",
    nodes: [
      ["用户态", "应用代码运行的受限环境"],
      ["内核态", "操作系统核心能力和硬件访问权限"],
      ["系统调用", "用户态请求内核提供文件、网络、进程等服务"],
      ["切换成本", "态切换会带来上下文保存和恢复开销"],
      ["隔离价值", "防止普通程序直接破坏系统资源"],
      ["工程优化", "减少频繁切换就是很多性能优化的出发点"]
    ],
    detail: `用户态和内核态的区别，本质是权限边界和执行环境的区别。应用程序大多数代码运行在用户态，这个状态权限受限，不能直接访问硬件、不能随意操作物理内存、也不能直接调度进程或中断。操作系统内核运行在内核态，拥有更高权限，可以管理 CPU、内存、磁盘、网络设备、进程调度和文件系统。之所以要分这两层，是为了隔离风险，防止一个普通程序把整台机器搞崩。  

从程序视角看，很多事情都得通过系统调用从用户态进入内核态完成。比如读文件、发网络包、创建线程、申请大块内存、epoll 等待事件，表面上是你在 Java、Go、C 里调一个 API，本质上往往要切进内核，请操作系统帮你做真正的资源操作，再把结果返回用户态。也就是说，用户态不是“啥都不能做”，而是业务逻辑能做，但涉及受保护资源时必须求助内核。  

面试时很容易被追问“为什么切换有成本”。因为态切换不仅是逻辑上换一层，而是 CPU 需要保存当前用户态上下文、切到内核栈、改变权限级别、执行内核逻辑，再恢复回来。这种开销单次可能不夸张，但如果请求路径里有大量细碎系统调用，频繁切换就会明显影响吞吐和延迟。很多高性能优化，比如零拷贝、mmap、批量 IO、用户态协议栈、协程调度，其实都和“减少不必要的用户态/内核态切换”有关。  

用户态和内核态还关系到故障隔离。用户态程序崩了，通常影响的是当前进程；内核态如果出严重问题，可能直接影响整个操作系统。也正因为如此，内核能力必须受到严格保护。你可以把它理解成：用户态是租客活动区，内核态是配电室和总闸房，不是谁都能进去乱碰。  

项目落地里，这个知识点常常用来解释性能现象。比如为什么一次网络发送不是简单内存复制，为什么大量小包发送 CPU 很高，为什么磁盘 IO 和网络 IO 优化要看系统调用次数，为什么 epoll 比轮询更节省资源。你要是能把用户态/内核态和这些具体场景连起来，答案会很有味道。  

再补一句很有工程感的话：很多中间件和高性能框架做的事情，表面上是“吞吐更高”，底层常常是在减少系统调用次数、减少数据拷贝、减少阻塞等待。把用户态和内核态这层关系想明白，很多底层优化就不再像黑魔法。  

顺着这个点再往外延一点，像零拷贝、sendfile、mmap、epoll、io_uring 这些名词，本质都和“用户态/内核态如何配合更高效”有关。你不一定要把每个都展开，但知道它们共享同一类优化目标，答案就会非常成体系。  

图解可以画成上下两层：上层是用户态应用线程，下层是内核态操作系统；中间通过系统调用、返回结果这条通道连接。旁边再标出几种典型操作：读文件、发网络、创建线程。最后补一句：很多所谓底层性能优化，归根结底都在想办法减少切换次数、减少复制、减少阻塞。`
  },
  "kubernetes-pod-deployment-service": {
    type: "flow",
    nodes: [
      ["Pod", "最小运行单元，里面装一个或多个紧耦合容器"],
      ["Deployment", "负责副本数、滚动发布和自愈管理"],
      ["Service", "给一组 Pod 提供稳定访问入口"],
      ["标签选择", "Deployment 和 Service 都靠 label 关联对象"],
      ["生命周期分工", "Pod 跑实例，Deployment 管实例，Service 管访问"],
      ["常见误区", "别把 Pod 当稳定节点，也别把 Service 当真实进程"]
    ],
    detail: `Kubernetes 里 Pod、Deployment、Service 这三个对象，常常被一起问，因为它们分别代表“跑起来”“管起来”“访问到”。理解它们的最好方式，不是把定义背成三句话，而是想象一次完整的应用发布：你先得有真正运行容器的地方，然后要有机制保证它挂了能拉起、升级能滚动，最后还得让集群内外稳定访问它。这三件事刚好对应 Pod、Deployment、Service。  

Pod 是 Kubernetes 调度的最小单位。一个 Pod 里可以放一个主容器，也可以放多个紧耦合容器，共享网络命名空间和存储卷。面试里最好强调：Pod 不是“单个容器的别名”，而是一个运行单元。很多 sidecar、日志采集、服务网格代理都会和业务容器放在同一个 Pod 里。  

但 Pod 本身是相对脆弱和临时的。Pod 掉了、节点挂了、镜像更新了，它的 IP 可能变、实例可能被重新创建，所以你通常不会手工一个个维护 Pod。Deployment 的职责就是声明“我要几个副本、用哪个镜像、怎么滚动升级、怎么回滚”。它通过 ReplicaSet 保证副本数接近你声明的目标状态，所以一个 Pod 挂了，Deployment 会拉起新的；升级时也会按策略逐步替换旧版本。  

Service 则解决“Pod 不稳定，别人怎么稳定访问它”的问题。它通过 label selector 选中一组 Pod，为它们提供一个稳定的虚拟 IP 和统一入口。调用方不需要关心今天后面是哪个 Pod、Pod IP 有没有变，只要访问这个 Service 即可。Service 常见有 ClusterIP、NodePort、LoadBalancer 等类型，面试时至少知道它是“稳定访问抽象”，不是具体进程本身。  

这三者之间靠标签连接非常重要。Deployment 创建的 Pod 会带上一组 labels，Service 再通过 selector 去挑这些 labels 对应的 Pod。也就是说，Kubernetes 很多资源关系不是硬编码绑定，而是靠声明式标签耦合。你把这点说出来，会显得对 K8s 模型更熟。  

如果想再落到运维场景，可以补一句：日常发布时你真正改动最多的是 Deployment，问题排查时最常看的是真实 Pod 状态，访问连通性问题则经常落在 Service 和 Endpoints/Selector 这一层。把“发布、运行、访问”三个日常动作和这三个对象对应起来，理解会特别稳。  

图解可以画成一条链：Deployment 在上层声明目标副本数，中间生成多个带标签的 Pod，Service 在前面选中这些 Pod 并提供稳定入口。最后再补一行职责分工：Pod 跑实例，Deployment 管生命周期，Service 管访问。这样这题就不只是术语解释，而是完整的应用运行模型。`
  },
  "ci-cd": {
    type: "flow",
    nodes: [
      ["CI", "代码提交后自动构建、测试和质量检查"],
      ["制品产出", "把通过验证的代码打成可部署产物"],
      ["CD", "自动或半自动把制品发布到目标环境"],
      ["环境推进", "开发、测试、预发、生产逐级流转"],
      ["发布控制", "灰度、回滚、审批、监控联动"],
      ["目标", "缩短交付周期并降低上线风险"]
    ],
    detail: `CI/CD 不只是“有个流水线自动执行命令”，它本质上是在把软件交付过程标准化、自动化、可追踪。CI 是 Continuous Integration，核心是代码提交后尽快集成、尽快验证；CD 则通常指 Continuous Delivery 或 Continuous Deployment，核心是让通过验证的制品能够更稳定地进入环境、上线服务。把这两层分开讲，答案会更清楚。  

先看 CI。没有 CI 时，很多问题要等到几个人代码合到一起、手工部署、甚至临近上线才暴露，比如编译不过、单测挂了、依赖冲突、代码规范问题。CI 的价值在于：每次提交或合并请求后，自动拉代码、安装依赖、编译构建、运行单元测试、静态扫描、制品打包。这样错误尽量在变更最小的时候暴露，修复成本最低。  

CD 往前再走一步：不是只证明“代码能编”，而是把“能上线”也流程化。通过 CI 的制品会进入制品库，然后按规则被部署到测试、预发、生产。这里可以有人工审批，也可以完全自动；可以做蓝绿、灰度、金丝雀发布；可以和数据库变更、配置中心、监控告警、回滚策略联动。也就是说，CD 真正解决的是“上线动作不要靠人肉记步骤”。  

面试里可以强调一个成熟交付链的几个关键要素：第一，构建出来的是不可变制品，避免同一版本在不同环境行为不一致；第二，流水线失败要尽早阻断，不把坏代码推到后面；第三，发布之后要有验证和回滚，不是部署成功就算完；第四，权限和审批边界要清楚，尤其是生产环境。  

CI/CD 也不是越自动越好。对高风险业务，生产发布可能仍然要人工确认；对早期团队，先把编译测试自动化做好，比一口气追求全自动生产部署更现实。真正成熟的思路是按风险和团队能力逐步提高自动化程度，而不是把“自动”当宗教。  

再补一个很现实的点：好的流水线不仅负责成功路径，也负责失败路径。谁能触发回滚，失败后通知谁，制品怎么追溯，上线后监控不过关是否自动终止，这些都属于 CI/CD 的一部分。把这句话说出来，答案就从“自动执行脚本”升级成了“交付治理体系”。  

如果要再往前一步，可以补一句 CI/CD 和团队协作的关系：流水线把很多“靠经验记住”的步骤固化成标准，让新成员也能按同样方式交付，这本质上是在减少人为差异和组织风险。很多团队交付效率的提升，不只是机器替人跑命令，而是流程被制度化了。  

图解可以画成一条流水线：代码提交 -> 编译测试 -> 制品入库 -> 部署测试环境 -> 自动验证 -> 灰度/审批 -> 生产发布 -> 监控与回滚。最后落一句：CI/CD 的目标不是炫技，而是让交付更快、更稳、更可追踪。`
  },
  "jwt-login": {
    type: "flow",
    nodes: [
      ["登录签发", "用户认证成功后服务端签发 JWT"],
      ["客户端保存", "前端后续请求携带 token"],
      ["服务端校验", "验证签名、过期时间和声明内容"],
      ["无状态优势", "服务端不用强依赖集中 Session 存储"],
      ["安全风险", "泄露后可被冒用，注销和续期也更复杂"],
      ["工程措施", "短期 access token 加刷新机制和黑名单策略"]
    ],
    detail: `JWT 登录方案常被说成“无状态认证”，但这句话只是开头，不是结论。它的基本流程是：用户登录成功后，服务端根据用户身份信息签发一个 token，客户端之后每次请求把这个 token 带上，服务端验证签名和声明后决定是否放行。和传统 Session 方案相比，JWT 最大的卖点是服务端不必每次都查集中 Session 才能知道用户是谁。  

JWT 一般由 Header、Payload、Signature 三部分组成。Header 说明算法，Payload 存一些声明，比如用户 ID、角色、过期时间，Signature 则用于防篡改。要注意，Payload 默认只是可读编码，不是加密内容，所以不要把密码、身份证号、敏感权限细节直接塞进去。面试里主动说出这一点，会显得你不只会讲 happy path。  

它的优势主要在扩展性和部署便利。比如多实例服务、网关校验、跨服务认证时，JWT 可以减少会话状态同步压力；网关层拿到 token 后就能做基础认证判断，不一定每次都回源认证中心。但它也带来明显问题：一旦 token 泄露，在过期前都可能被冒用；服务端很难像 Session 那样立刻“删掉这次登录状态”；权限变化、强制下线、单点登出都会复杂很多。  

所以真实项目里，JWT 很少是“签一个很长时间的 token 然后万事大吉”。更常见的是短期 access token 配合 refresh token，access token 过期快、暴露窗口小；refresh token 用于续签，并可配合设备管理、黑名单、版本号或 token 存根实现强制失效。高安全场景下，服务端甚至会保留一层状态，比如 refresh token 白名单、注销黑名单、jti 去重等，这说明所谓“无状态”在工程上往往是相对概念。  

JWT 还要特别注意存储位置和传输方式。浏览器场景里，如果放在 localStorage，要防 XSS；如果放在 Cookie，要考虑 HttpOnly、Secure、SameSite 和 CSRF。移动端和内部服务调用又是另一套权衡。面试时如果能把安全问题落到这些具体点上，可信度会很高。  

工程上再补一句也很加分：JWT 方案真正难的是“认证通过之后，身份状态如何在后续生命周期里被安全管理”。比如续签频率怎么控、权限变更何时生效、多端登录怎么处理、封禁用户如何立刻失效，这些都决定了方案是否真的可用。  

图解可以画成一条认证链：用户登录 -> 认证服务签发 JWT -> 客户端保存 -> 请求时带 token -> 网关或服务校验签名和过期时间 -> 放行业务请求。旁边再列风险与对策：泄露、过期、续签、注销、权限变更。这样回答会比“JWT 就是无状态 Session”成熟得多。`
  },
  "api-idempotency": {
    type: "structure",
    nodes: [
      ["幂等目标", "同一请求重复执行结果保持可控"],
      ["风险场景", "重试、超时、消息重复投递、前端连点"],
      ["幂等键", "请求唯一标识用于判重"],
      ["状态设计", "处理中、成功、失败要有明确记录"],
      ["实现手段", "唯一索引、去重表、Redis 锁或业务状态机"],
      ["边界意识", "幂等不是不执行，而是重复请求不产生额外副作用"]
    ],
    detail: `接口幂等性要解决的问题，是同一个业务请求因为超时重试、用户连点、网关重放、消息重复消费等原因被执行多次时，系统不能重复扣款、重复下单、重复发券、重复写账。这里要先讲清楚：幂等不是“接口只能调一次”，也不是“第二次一定什么都不做”，而是“同一业务语义的重复请求，不再产生新的副作用”。  

最典型的场景有三个。第一，客户端请求超时，不知道服务端到底成功没，于是重试；第二，用户手快连点提交按钮；第三，异步消息系统为了可靠性把同一消息投递多次。这些情况你不能靠“理论上不会发生”回避，因为分布式系统里它们非常常见。  

设计幂等的第一步，通常是定义幂等键。这个键可以是业务单号、支付流水号、客户端生成的 requestId、消息唯一 ID，也可以是“用户 ID + 业务动作 + 时间窗口”组合。关键是它要能唯一标识“这是不是同一次业务请求”。有了幂等键之后，系统才能在重复请求进来时识别出来。  

具体实现方式很多，但核心都围绕“先判重，再决定执行还是返回既有结果”。常见方式有数据库唯一索引、防重表、状态机表、Redis setnx、分布式锁、消息消费去重表等。比如创建订单时用业务单号做唯一索引；支付回调时用第三方流水号去重；消息消费时先写消费记录表，写成功才继续执行。不同方案的关键差异在于一致性要求、吞吐量和故障恢复方式。  

面试里别忘了说“处理中”状态。很多人只想到成功去重，但如果一个请求第一次刚执行到一半，第二次就进来了，系统要么返回“处理中”，要么等待已有结果，而不是直接再执行一次。也就是说，幂等设计通常不是简单布尔值，而是和业务状态流转绑在一起的。  

再往工程实践里落一步，幂等一定要和事务边界一起考虑。最怕的是“判重通过了，但业务写库失败”或者“业务成功了，幂等记录没落下”，这样重复请求又会穿透进来。所以真正稳的方案，要么把判重和业务结果放进同一事务，要么设计补偿和状态恢复机制。  

面试里如果你能再补一句“幂等键生命周期也要设计”，会更完整。比如保留多久、失败记录是否允许重试、历史幂等记录怎么清理、跨天跨月的唯一性怎么保证，这些都会直接影响系统长期运行成本和正确性。  

再往前收一句也很好用：幂等是分布式系统里对“不确定请求结果”的一种工程妥协，不是为了让系统什么都不做，而是为了让重复做不会把业务做坏。  

图解可以画成一条判重链：请求进入 -> 取幂等键 -> 查幂等记录/唯一约束 -> 未处理则执行并落状态 -> 已成功则返回已有结果 -> 处理中则拒绝或等待。旁边再补一句：真正难的不是写一个锁，而是保证幂等判断和业务落库之间的原子性。这样答案会非常扎实。`
  },
  "global-exception-handler": {
    type: "flow",
    nodes: [
      ["统一兜底", "把分散异常收敛成统一出口"],
      ["异常分类", "参数错误、业务异常、系统异常分层处理"],
      ["响应规范", "统一错误码、消息、traceId 和时间戳"],
      ["日志策略", "按严重度决定记录粒度和报警方式"],
      ["安全边界", "对外不泄露堆栈和敏感内部细节"],
      ["治理价值", "提升可观测性和客户端一致性体验"]
    ],
    detail: `后端全局异常处理的目标，不只是“少写 try-catch”，而是把异常从零散、随意、泄露细节的状态，收敛成统一的接口契约和统一的观测入口。如果没有全局异常处理，Controller 里可能各写各的返回格式，有的直接抛堆栈，有的返回 200 包业务错误，有的连日志都没打。这会让前端难接、监控难做、排障更难。  

一个成熟的全局异常处理，第一步是分类。参数校验异常、权限异常、业务异常、下游超时异常、系统未知异常，处理方式和暴露信息应该不同。比如参数错误更适合返回 400 和明确字段提示；业务异常可以返回约定的业务码；系统异常则应记录详细日志、对外返回通用错误信息，避免把内部实现和堆栈直接暴露给客户端。  

第二步是统一响应结构。通常包括错误码、错误消息、请求路径、时间戳、traceId 或 requestId。这样前端和调用方能稳定解析，日志和链路系统也能顺着 traceId 追查。同样重要的是统一状态码语义，别一边说“全局异常处理”，一边把所有错误都包成 200。  

第三步是把日志策略设计好。不是所有异常都打 error 并报警。参数错误、业务拒绝可能是 warn 或 info；系统异常、数据库不可用、线程池拒绝、下游超时等才值得 error 和告警。否则告警风暴一来，真正重要的问题会被淹没。面试里如果能把“异常处理”和“日志分级、监控告警”连起来，会很像真实项目思路。  

全局异常处理也不能代替边界判断。像线程池异步任务、消息消费、过滤器、网关层异常，并不一定都能被同一个 ControllerAdvice 或统一处理器兜住，所以系统里往往是分层异常治理：Web 层、业务层、网关层、消息消费层各有自己的统一出口。这个补充很加分，因为它说明你知道全局异常处理不是全世界唯一入口。  

再补一个设计细节：业务异常最好是“可预期、可枚举、可被客户端稳定识别”的，而系统异常更多是“先记录现场，再给用户一个安全兜底响应”。把这两类异常明确分开，前端、日志、监控和运维都会轻松很多。  

还有一个常见加分点是说清“异常处理不是吞异常”。统一处理的目标是标准化返回和记录现场，不是把错误悄悄吃掉装作没事。尤其是系统异常，如果既不报警也不打关键日志，统一处理器反而会把问题藏起来。  

图解可以画成从请求进入 Controller，到业务层可能抛出多种异常，再统一流入全局异常处理器，最终分流成参数错误、业务异常、系统异常三类响应。旁边再画日志和监控支路，说明异常不仅要返回给用户，还要进入观测系统。这样回答就非常完整。`
  },
  "nginx-reverse-proxy": {
    type: "structure",
    nodes: [
      ["反向代理", "客户端以为在访问 Nginx，实际由它转发到后端"],
      ["负载均衡", "在多台上游实例间按策略分发请求"],
      ["流量入口", "统一接 TLS、静态资源、限流和访问控制"],
      ["上游策略", "轮询、权重、ip_hash、least_conn 等"],
      ["故障隔离", "上游异常时重试、摘除、超时控制"],
      ["工程定位", "它是流量调度层，不是业务实现层"]
    ],
    detail: `Nginx 反向代理和负载均衡，核心是在客户端和后端服务之间加一层流量调度与治理入口。所谓反向代理，是客户端并不直接访问真实后端，而是先访问 Nginx，由 Nginx 再把请求转发给内部上游服务。对客户端来说，它只知道一个统一域名；对后端来说，它不用直接暴露给外部世界。  

这层代理的第一价值是屏蔽后端拓扑。后端服务可以是多台 Tomcat、多实例 Spring Boot、静态资源服务器、不同路径对应不同服务，客户端完全不需要感知这些差异。第二价值是统一做入口能力，比如 TLS 终止、跨域、静态资源缓存、访问日志、限流、黑白名单、压缩、Header 改写。也就是说，Nginx 不只是“转发请求”，而是典型的流量治理入口。  

负载均衡则是反向代理之上的关键能力。当后端有多台实例时，Nginx 需要决定这次请求发给谁。常见策略有轮询、加权轮询、ip_hash、least_conn 等。轮询适合一般场景；权重适合机器配置不一致；ip_hash 适合希望同一客户端尽量落同一台实例的情况；least_conn 更关注当前连接数负载。面试里不一定要背很多指令，但至少知道“为什么需要不同策略”。  

Nginx 还常用来做故障隔离和流量控制。比如配置上游超时、失败重试、健康检查、熔断摘除，让坏实例少接流量；再比如灰度发布时按路径、Header、Cookie、域名转发到不同上游。这也是为什么它经常出现在网关、CDN 回源、微服务入口前。  

但也要明确边界：Nginx 适合做网络层和协议层治理，不适合承载复杂业务逻辑。把太多业务判断塞进 Nginx 配置，会让维护和排查都很痛苦。它更像交通指挥层，不是业务处理层。  

如果再往项目里落地一点，可以补一句常见排查思路：访问异常时先看 Nginx access log 和 error log，再分辨问题在客户端到 Nginx、Nginx 到 upstream、还是 upstream 自己处理超时。很多 502、504 的定位，都是从这条链路拆开的。  

再补一句发布视角也很实用：很多团队把 Nginx 当成灰度入口和静态资源前置层，用它先把流量按路径、Header、Cookie 或权重切到新版本。这样它不仅是“转发器”，还是线上流量实验和风险控制的一部分。  

图解可以画成三层：客户端请求先打到 Nginx，Nginx 做 HTTPS、限流、静态资源和路由判断，再按负载均衡策略把请求分配到多台后端服务。旁边再列几个常见策略和适用场景。最后一句收束：反向代理解决的是“怎么统一进来”，负载均衡解决的是“进来之后发给谁”。`
  },
  "string-stringbuilder-stringbuffer": {
    type: "compare",
    nodes: [
      ["String", "不可变，适合常量和只读文本"],
      ["StringBuilder", "可变字符序列，单线程拼接更高效"],
      ["StringBuffer", "线程安全版本，方法多了同步开销"],
      ["不可变价值", "缓存 hash、常量池、线程安全更容易"],
      ["性能取舍", "频繁拼接别用多个 String 加号循环"],
      ["使用建议", "默认 String，频繁拼接优先 Builder"]
    ],
    detail: `String、StringBuilder、StringBuffer 的区别，表面看像“一个不可变、一个快、一个线程安全”，但面试里更重要的是把这些差异和使用场景连起来。先说 String。它是不可变对象，一旦创建，内容就不能修改。每次看起来像“修改”字符串，实际通常是在创建新对象。这种设计的好处很多：可以安全放进字符串常量池、便于缓存 hashCode、天然线程安全，也让它非常适合做配置值、参数名、URL 片段这类只读文本。  

问题是，如果你在循环里频繁用 `+` 拼接字符串，就可能不断创建新对象，带来额外内存分配和复制成本。编译器在简单场景下会帮你优化，但在复杂循环和动态拼接场景里，手动使用可变字符序列通常更合适。这时就轮到 StringBuilder。它内部维护可扩容的字符数组，append 时通常是在原有缓冲区上追加，避免频繁生成新 String 对象，所以单线程场景下拼接效率更高。  

StringBuffer 和 StringBuilder 的核心差异在于线程安全。StringBuffer 的很多方法带同步，因此在多线程同时修改同一个实例时更安全，但同步也意味着额外开销。现代 Java 业务代码里，真正需要多个线程共享并发修改同一个字符串缓冲区的场景其实并不多，所以大多数时候更常用的是 StringBuilder，而不是 StringBuffer。  

面试时最好不要把它讲成绝对化口号。比如“永远不用 String 拼接”就太僵硬。更准确的说法是：少量常规拼接、编译期常量拼接、日志参数化之外的简单场景，用 String 完全没问题；当你进入循环、批量构造 SQL、拼装大文本、生成报文时，优先考虑 StringBuilder。真正的判断标准是是否存在大量中间字符串对象。  

再往深一点，还可以补一句为什么 String 不可变这么重要。因为不可变对象更容易被共享，不必担心别人改掉内容；这对常量池、类加载、安全校验、文件路径、网络地址等基础设施都很关键。也就是说，String 的不可变不是性能缺陷，而是语言设计上的稳定性选择。  

还有一个很实际的答法是提醒“别把线程安全和可变性混为一谈”。String 安全，不是因为它做了同步，而是因为它根本不让你改；StringBuffer 安全，是因为它在改的时候做了同步。把这个因果讲清楚，答案就比单纯背结论更扎实。  

图解可以画成三个对象对比：String 每次“修改”都生成新对象；StringBuilder 在同一个可变缓冲区追加；StringBuffer 也是可变缓冲区，但外层有同步保护。最后给出一句实战结论：默认用 String 表达文本值，频繁拼接用 Builder，并发共享修改极少数场景再考虑 Buffer。`
  },
  "final-finally-finalize": {
    type: "compare",
    nodes: [
      ["final", "关键字，表示不可变约束或不可继承"],
      ["finally", "异常处理语法块，通常用于收尾逻辑"],
      ["finalize", "对象回收前的旧式回调机制"],
      ["语义层级", "一个是语言修饰符，一个是流程控制，一个是 GC 钩子"],
      ["现代建议", "谨慎用 finally，基本别再依赖 finalize"],
      ["面试重点", "别只背名字，要讲清各自职责和边界"]
    ],
    detail: `final、finally、finalize 经常被放在一起问，就是为了看你会不会把三个长得像的词讲混。最稳的回答方式，是先把它们放在不同层级上看：final 是 Java 关键字修饰能力，finally 是异常处理结构的一部分，finalize 是对象被 GC 前的旧式回调方法。它们只是名字像，职责完全不同。  

先说 final。它可以修饰变量、方法、类。修饰变量时表示引用不可再指向别处，基本类型值不能再改；修饰方法表示不能被子类重写；修饰类表示不能被继承。面试里可以顺手补一句：final 引用本身不可变，不代表引用对象内部状态一定不可变，这个点很多人会说混。  

finally 则是 try-catch-finally 结构里的收尾代码块。无论 try 里是否抛异常，通常 finally 都会执行，因此它很适合做资源释放、锁释放、连接关闭、ThreadLocal 清理这类善后动作。但也要知道边界：如果进程被强杀、System.exit、机器崩溃等极端情况，finally 也不是绝对保证执行。真正工程里更推荐用 try-with-resources 管理可关闭资源。  

finalize 则是最该谨慎讲的那个。它本意是让对象在被垃圾回收前有一次“临终回调”机会，但这个机制执行时机不确定、性能差、容易导致对象复活和资源释放不及时，所以现代 Java 基本不推荐依赖 finalize。JDK 里它已经被长期标记为不推荐使用，很多资源释放早就转向显式 close、Cleaner、PhantomReference 等更可控的方式。  

这道题真正加分的点，在于你不仅知道定义，还知道现代实践。比如：final 很常用；finally 仍然重要，但资源管理更推荐 try-with-resources；finalize 现在基本视为历史机制，不应该再作为正常资源释放手段。这样一说，答案就从“背单词差异”变成“知道今天代码该怎么写”。  

如果你想把答案收得更漂亮，可以再补一句：这三个词放一起问，真正考的不是记忆力，而是你能不能把语言层约束、异常控制流和垃圾回收机制清楚地拆开。能把层级说清楚，面试官通常会觉得你对 Java 语义是有体系感的。  

图解可以画三列：final 对应变量/方法/类修饰；finally 对应 try-catch-finally 执行流；finalize 对应对象走向 GC 时的旧式回调。再在 finalize 那列标红“不可靠、不推荐”。最后一句话收束：名字相似不代表职责接近，final 管约束，finally 管收尾，finalize 管回收钩子而且今天基本别指望它。`
  },
  "abstract-class-interface": {
    type: "compare",
    nodes: [
      ["抽象类", "更强调 is-a 关系和基础实现复用"],
      ["接口", "更强调 can-do 能力约定和解耦"],
      ["状态能力", "抽象类可持有状态，接口更偏行为规范"],
      ["继承限制", "类单继承但可实现多个接口"],
      ["设计取舍", "稳定骨架用抽象类，横向能力用接口"],
      ["现代语义", "默认方法增强了接口但没抹平本质差异"]
    ],
    detail: `抽象类和接口的区别，真正重要的不是背语法表格，而是理解它们分别适合表达什么样的设计关系。抽象类更偏向“是什么”的继承体系，也就是一组类本来就存在比较稳定的父子关系；接口更偏向“能做什么”的能力契约，适合横向给很多不相关类挂上一种能力标签。  

抽象类可以有成员变量、普通方法、抽象方法、构造方法，因此它非常适合承载一套基础状态和公共实现。比如一个支付流程父类里，可以放公共参数校验、日志模板、签名骨架，再把不同支付渠道的细节留给子类实现。这里它的价值不仅是“少写重复代码”，还在于能定义一个稳定的模板骨架。  

接口则更像一份协议。它的核心价值是解耦，让调用方依赖抽象能力，而不是依赖某个具体继承层次。比如 Cacheable、Runnable、Comparable 这些能力，本身不要求类之间有血缘关系，只要求它们遵守统一行为约定。Java 支持一个类实现多个接口，这也是接口在横向扩展能力时比抽象类更灵活的地方。  

很多人会说“Java 8 之后接口也能有默认方法，所以接口和抽象类差不多了”，这句话只说对了一小半。默认方法确实让接口可以带一点基础实现，但接口仍然不适合承载大量共享状态和复杂生命周期逻辑。它最重要的职责还是定义边界、降低耦合，而不是变成另一种继承父类。  

面试时最好不要把答案停在“一个能有变量，一个不能”这种表层差异上。更成熟的说法是：当多个实现之间存在稳定模板和共享状态时，优先考虑抽象类；当你只是希望调用方面向能力编程、方便替换实现和横向扩展时，优先考虑接口。现代 Java 设计里，接口往往是对外契约，抽象类是内部复用骨架。  

如果想再把答案说得更像真实开发，可以补一句“抽象类和接口经常组合使用”。很多成熟框架会对外暴露接口，对内再用抽象类沉淀公共实现，这样既保留扩展点，也减少重复代码。把这个组合思路说出来，会比二选一式回答更成熟。  

再补一句取舍也很有帮助：如果你一开始无法确定继承骨架会不会稳定，宁可先暴露接口，把共享实现放在普通组合对象里，也别过早做很重的继承体系。这个判断会让答案更像真实设计，而不是课本对比。  

面试里最后还可以收一句特别稳的话：抽象类解决的是“已经确认存在的公共骨架”，接口解决的是“希望长期稳定的协作边界”。一旦把“骨架”和“边界”这两个词区分开，很多设计选择都会更清楚。  

图解可以画两条设计路径：一条是抽象类向多个具体子类延伸，强调公共状态和模板方法；另一条是多个互不相关的类都实现同一个接口，强调统一能力契约。最后落一句：抽象类偏骨架复用，接口偏能力解耦，别把二者只当成语法题。`
  },
  "checked-runtime-exception": {
    type: "compare",
    nodes: [
      ["受检异常", "编译器强制显式处理或继续抛出"],
      ["运行时异常", "更多表示程序逻辑或使用方式错误"],
      ["设计意图", "一个强调可恢复场景，一个强调编码缺陷"],
      ["调用传播", "checked 会写进方法签名影响调用链"],
      ["工程取舍", "异常分类要服务可维护性而不是教条"],
      ["常见误区", "别把所有异常都包成 RuntimeException"]
    ],
    detail: `受检异常和运行时异常的区别，表面上是“编译器管不管”，本质上反映了 Java 对异常语义的不同设计意图。受检异常，也就是 checked exception，要求调用者显式处理或者继续在方法签名里抛出；运行时异常则不强制这一点，它通常表示程序员使用方式错误、前置条件不满足或代码逻辑问题。  

为什么 Java 要分这两类？因为不是所有异常都适合同样对待。像文件不存在、网络 IO 失败、外部资源不可用，这类异常往往不是程序本身写错，而是运行环境或外部依赖出现了问题，调用方有机会做降级、重试、提示用户，所以 Java 倾向于把这类异常设计成受检异常，逼着调用链正视它们。  

运行时异常则更多用于表示“不该这样写代码”或者“当前状态下这样调用不成立”。典型如空指针、数组越界、非法参数、状态不合法等。这类问题多数时候不是靠上层 catch 一下就能优雅恢复，而是应该回到代码本身修正逻辑。因此它们不要求写在方法签名里，否则整条调用链都会被异常声明污染。  

但工程实践里，也不能机械地认为“受检异常一定好、运行时异常一定坏”。很多现代项目为了避免接口层层 throws 一串异常，常常在业务层定义统一业务异常，配合全局异常处理输出标准响应；而底层资源异常、第三方调用异常则可能被封装后再往上抛。真正好的做法，是让异常分类有助于维护和定位，而不是让方法签名变成异常清单。  

面试里可以补一个很有判断力的观点：异常设计要看调用方是否真的能合理处理。如果一个异常即使暴露给上层，上层也只能记录日志后失败返回，那强行设计成 checked 反而会让代码到处 try-catch，噪音很大。反之，如果异常确实存在明确恢复路径，比如提醒重试、切换降级资源、回滚操作，那 checked 就更有价值。  

再往工程习惯里落一句也很好用：业务系统里最怕的是异常语义混乱。同一类失败一会儿抛 checked，一会儿抛 runtime，一会儿又直接返回错误码，调用方会非常痛苦。所以异常体系最重要的是一致性，而不是“把所有理论都用上”。  

如果还想再补一个追问点，可以说“很多框架层异常最后会在边界统一转换成业务异常或统一错误响应”。也就是说，异常体系并不是写完类继承树就结束了，它还和日志、监控、接口契约一起构成整条错误处理链。  

图解可以画成两条传播链：受检异常从底层方法一路显式向上声明，逼着调用者处理；运行时异常则更多在运行时冒泡，由统一异常处理或上层兜底。旁边再标一句：异常不是按心情分，关键是调用方是否应该被强制意识到它。这样答案就会很成熟。`
  },
  "java-reflection": {
    type: "flow",
    nodes: [
      ["元信息入口", "运行时获取类、方法、字段和注解信息"],
      ["动态操作", "可创建对象、调用方法、读写字段"],
      ["框架价值", "IOC、AOP、ORM、序列化都大量依赖它"],
      ["性能成本", "动态解析和访问控制比直调更重"],
      ["封装代价", "滥用会削弱类型安全和可读性"],
      ["工程原则", "用在框架边界，业务层少滥用"]
    ],
    detail: `Java 反射的核心能力，是让程序在运行时检查和操作自身结构。正常写代码时，类、方法、字段、构造器这些信息多数在编译期就确定了；而反射允许你在运行时通过 Class、Method、Field、Constructor 等元信息对象，动态地创建实例、调用方法、读取注解、修改字段值。这让 Java 具备了更强的动态性。  

反射最常见的价值，不在业务代码里“炫技式动态调用”，而在框架层。Spring 扫描注解并创建 Bean、MyBatis 根据 Mapper 接口生成代理、Jackson 根据字段和注解决定如何序列化、JUnit 动态发现测试方法，这些能力背后都离不开反射。也就是说，反射是很多“声明式开发体验”的底层基础。  

但反射不是没有代价。第一，它比直接调用多了元信息查找、访问检查、动态分派等成本，性能上通常更重；第二，它绕开了很多编译期类型检查和封装边界，如果到处用字符串写方法名、字段名，重构时很容易埋雷；第三，一些访问私有成员的操作虽然能靠 setAccessible 这类方式放行，但会削弱封装，也可能受到更严格运行环境限制。  

所以成熟项目的常见做法是：把反射压在框架或基础设施边界，不让业务代码到处直接拿反射做流程控制。业务层如果大量依赖反射，通常说明设计太动态、静态类型价值没发挥出来。你要是能主动说出“反射适合框架层，不适合当业务主逻辑常规手段”，面试官会很认可。  

另外，反射也和注解、代理、泛型元数据关系紧密。很多框架并不是单纯“看类名”，而是通过反射读取注解和签名信息，再决定注册、代理、映射和校验方式。把反射放回这些真实场景里讲，就不会显得空。  

如果要补一个非常实用的工程判断，可以再说：反射的性能问题通常不是“绝对不能用”，而是别放在高频热点路径里反复做同样解析。很多框架都会把反射结果缓存起来，本质上就是把动态能力和性能成本做平衡。  

再往维护性上说一句也很加分：一旦业务代码大量依赖反射，IDE 重构、接口收敛和静态检查的帮助都会下降，所以反射越强，越需要外围约束和测试来兜底。这样就把“动态能力”和“工程约束”两面都讲到了。  

最后还能再补一句很实在的话：反射最怕的不是写不出来，而是几年后没人敢改。因为字符串式绑定和动态调用会弱化很多编译期保护，所以一旦用了，就更需要清晰边界、缓存和测试来守住质量。  

图解可以画成一条链：运行时先拿到 Class 元信息，再向下读取字段、方法、构造器和注解，最后驱动对象创建、依赖注入、动态调用和序列化。旁边标出收益和代价：动态能力更强，但性能、封装和可维护性要付出代价。这样答案就很像真实工程判断。`
  },
  "java-serialization": {
    type: "structure",
    nodes: [
      ["序列化目标", "把对象状态转成可传输或存储的字节流"],
      ["serialVersionUID", "控制序列化兼容性版本标识"],
      ["使用场景", "缓存、RPC、持久化快照、消息传输"],
      ["兼容风险", "类结构变化可能导致反序列化失败"],
      ["安全问题", "反序列化可带来高风险攻击面"],
      ["现代取舍", "很多系统更偏 JSON、Protobuf 等明确协议"]
    ],
    detail: `Java 序列化的本质，是把对象当前状态转换成字节流，以便传输、缓存、持久化或稍后恢复。标准 Java 序列化依赖 Serializable 接口，JVM 会根据对象图把字段值写成特定格式，再在反序列化时重建对象。这个机制早年在 RMI、Session 持久化、缓存快照、对象存储等场景里很常见。  

serialVersionUID 的作用，是在反序列化时判断“当前类定义”和“当年序列化出去的类定义”是否还能兼容。你可以把它理解成序列化格式版本号。如果类结构改了、版本号对不上，就可能抛出 InvalidClassException。显式声明 serialVersionUID 可以让你更可控地管理版本兼容，而不是依赖编译器自动生成。  

面试里最好顺手说明：并不是所有字段都会被序列化。transient 字段默认不会参与标准序列化，静态字段也不属于对象实例状态。也就是说，序列化关注的是对象实例中的可持久状态，而不是类加载后的全局静态信息。  

但现代项目里，标准 Java 序列化已经不像以前那样被鼓励大量使用，原因主要有三点。第一，格式比较重，跨语言支持差；第二，类结构演进带来兼容性麻烦；第三，也是最重要的一点，反序列化安全风险很高，历史上有大量利用 gadget chain 的反序列化攻击案例。所以很多系统更倾向于显式协议，比如 JSON、Protobuf、Avro、MessagePack。  

这不代表你不用理解它。相反，理解 Java 序列化能帮助你搞清楚对象状态边界、类版本演进、缓存值兼容，以及为什么某些框架会特别强调不要直接把任意字节流反序列化成对象。面试时如果能把“serialVersionUID 管兼容”和“反序列化有安全风险”这两层一起讲出来，答案就会很完整。  

再补一个工程化视角：很多现代系统即使用 JSON 或 Protobuf，也仍然要面对“对象版本演进”和“字段兼容”问题。也就是说，学习 Java 序列化不只是记一个老机制，而是理解对象状态跨进程、跨时间传递时为什么会出现兼容和安全问题。  

如果面试官追问 serialVersionUID 到底什么时候要显式写，一个很稳的回答是：只要对象真的要持久化或跨进程传输，而且你关心后续版本兼容，就应该显式声明。否则每次编译器自动生成变化，都可能让历史数据恢复变得不可控。  

图解可以画成对象 -> 字节流 -> 存储/传输 -> 字节流 -> 对象恢复的双向链路，中间加一个 serialVersionUID 校验节点。再在旁边标红反序列化风险，说明今天很多系统会更偏向更显式、更安全的协议。这样就不只是背定义了。`
  },
  "aqs": {
    type: "structure",
    nodes: [
      ["核心抽象", "用一个 state 字段和等待队列管理同步状态"],
      ["获取失败", "线程进入 CLH 风格双向队列等待"],
      ["释放唤醒", "前驱释放后唤醒后继继续竞争"],
      ["独占共享", "既支持独占锁也支持共享同步器"],
      ["典型实现", "ReentrantLock、Semaphore、CountDownLatch 等"],
      ["设计价值", "把同步器通用框架和具体规则分离"]
    ],
    detail: `AQS，也就是 AbstractQueuedSynchronizer，是 Java 并发包里非常核心的同步器基础框架。面试问它，不是为了让你背源码细节，而是看你能不能把“锁、信号量、闭锁这些看起来不同的并发工具”，抽象成一套共用模型。AQS 的核心思想，就是用一个整数状态 state，加上一条等待队列，把线程竞争和唤醒的通用流程沉淀下来。  

你可以把它想成：具体同步器只需要定义“什么时候算获取成功、什么时候算释放成功”，剩下线程排队、挂起、唤醒、取消等待这些脏活累活都由 AQS 统一处理。比如 ReentrantLock 用 state 表示重入次数；Semaphore 用 state 表示剩余许可证数量；CountDownLatch 用 state 表示还剩多少计数。虽然语义不同，但底层竞争框架很像。  

当线程尝试获取同步状态失败时，AQS 会把线程封装成节点，挂到一个 CLH 风格的双向等待队列里，并根据情况 park 挂起；前面的线程释放资源后，再唤醒后继节点继续竞争。这里“被唤醒”不代表一定立刻成功，只是重新获得一次竞争机会。这个模型解释了为什么很多并发工具会有队列、等待、唤醒这些共性。  

AQS 还分独占模式和共享模式。独占模式下一次只能一个线程成功，比如 ReentrantLock；共享模式下多个线程可能同时成功，比如 Semaphore、CountDownLatch 某些释放逻辑。面试里能把这个区分讲出来，会很加分，因为它说明你不是只知道“有个队列”。  

真正的设计价值在于分层。AQS 负责通用的同步框架，子类通过重写 tryAcquire、tryRelease、tryAcquireShared、tryReleaseShared 等方法定义具体语义。这是非常经典的模板方法式设计。也正因为如此，理解 AQS 后，再看各种 JUC 同步器就不会觉得它们完全独立，而会看到同一套骨架下的不同规则。  

如果面试官继续追问“为什么要学 AQS”，最好的回答不是为了背源码，而是为了把很多并发工具看成同一类问题：竞争失败就排队、状态变化就唤醒、具体语义由子类决定。这个抽象一旦建立，再看并发包很多类就不会碎。  

再补一句实践层面的价值：你未必要自己写一个同步器，但理解 AQS 之后，至少能更好判断某个并发工具适不适合当前场景，也更能读懂线程卡在 park、队列等待、锁竞争这些现场。这个就是它在排障中的现实价值。  

图解可以画成一套骨架：上方是 state 状态位，中间是获取/释放逻辑，下面是一条等待队列，失败线程入队、成功释放后唤醒后继。旁边再列几个具体实现映射到 state 的不同含义。这样 AQS 这题就会从“源码八股”变成真正有抽象层次的答案。`
  },
  "countdownlatch-cyclicbarrier-semaphore": {
    type: "compare",
    nodes: [
      ["CountDownLatch", "一个线程等多个事件完成，计数到零后放行"],
      ["CyclicBarrier", "多个线程相互等待，凑齐后一起继续"],
      ["Semaphore", "控制同时进入临界区的许可证数量"],
      ["协作模型", "一个偏等待完成，一个偏会合，一个偏限流"],
      ["是否可复用", "Barrier 可循环用，Latch 一次性，Semaphore 持续可用"],
      ["使用场景", "并发编排、阶段同步、资源访问控制"]
    ],
    detail: `CountDownLatch、CyclicBarrier、Semaphore 看起来都和“线程等待”有关，但它们解决的问题完全不同。面试里最稳的方式，不是上来背 API，而是先说三者的协作模型：CountDownLatch 是“一个等多个”；CyclicBarrier 是“多个互相等到凑齐再一起走”；Semaphore 是“控制同时能进去几个”。  

CountDownLatch 内部维护一个倒计数。通常是一个主线程或某个协调线程调用 await 等待，其他工作线程每完成一个任务就 countDown 一次，等计数减到 0，等待方才继续。它非常适合“主线程等多个子任务都完成后再汇总”的场景，比如并发加载多个数据源后统一返回。要注意，它通常是一次性的，计数到零后不能自然重置复用。  

CyclicBarrier 则更像集合点。它要求一组线程都走到某个屏障位置后，才一起继续执行。比如多阶段并行计算中，每个线程都先完成本轮任务，等大家都到齐后再进入下一轮。它之所以叫 cyclic，是因为可以在一轮放行后继续用于下一轮同步。这里它强调的是“相互等待、共同推进阶段”。  

Semaphore 则完全不是“等结果”，而是“拿许可证”。它维护一定数量的 permits，线程获取到 permit 才能继续执行，用完后要 release 归还。它特别适合做并发访问控制，比如数据库连接池、限流器、固定容量资源访问、下载并发数限制等。换句话说，它关心的不是线程是否彼此同步，而是同一时间能有多少线程进入某个区域。  

三者容易混的地方在于：表面上都有 await 或 acquire 之类等待动作，但等待对象不同。CountDownLatch 等的是计数归零；CyclicBarrier 等的是同伴到齐；Semaphore 等的是许可证可用。面试如果用这个角度一说，区分会非常清楚。  

再补一个面向项目的选择原则：你如果是在做“等所有并发任务收尾”，优先想到 Latch；如果是在做“每个阶段大家都要对齐再推进”，优先想到 Barrier；如果是在做“数据库连接、外部接口、下载线程数之类的并发配额控制”，优先想到 Semaphore。这样一讲，面试官会很容易感受到你真会用。  

图解可以画三张小图并列：Latch 是一个主线程站在门口等多个子线程把计数减到零；Barrier 是多个线程跑到同一个栅栏前，凑齐后栅栏打开；Semaphore 是门口挂着固定数量通行证，谁拿到谁进去，用完归还。这样的类比非常好记，也很容易说清场景差异。`
  },
  "synchronized-lock-upgrade": {
    type: "flow",
    nodes: [
      ["无锁", "对象初始还没有被任何线程竞争"],
      ["偏向锁", "大多数时候只有一个线程反复进入同步块"],
      ["轻量级锁", "出现短时竞争，用 CAS 和栈记录尝试自旋"],
      ["重量级锁", "竞争激烈或自旋无效，升级为 OS 互斥量"],
      ["升级原因", "在不同竞争强度下平衡开销和性能"],
      ["理解边界", "升级是优化手段，不是业务层可精确控制逻辑"]
    ],
    detail: `synchronized 锁升级过程这道题，核心不是让你背对象头 bit 位，而是理解 JVM 为什么不一开始就上最重的锁。因为线程竞争强度并不总是一样：有的同步块几乎永远只有一个线程进，有的只是偶发短暂竞争，有的则是真的很多线程同时抢。JVM 为了在不同场景下平衡开销，才设计了从偏向到轻量到重量的逐步升级思路。  

最开始可以理解为无锁状态，对象还没被用于同步。偏向锁的设计假设是：很多同步块其实总是同一个线程反复进入，如果每次都做完整竞争流程太浪费，所以干脆把对象“偏向”某个线程。后续还是这个线程来进同步块时，基本不用再做昂贵竞争判断，成本很低。  

当出现第二个线程来竞争时，偏向锁就不再适合了。JVM 会撤销偏向，转向轻量级锁。轻量级锁通常会借助线程栈中的锁记录和 CAS 尝试，在用户态尽量快速完成竞争。如果竞争只是很短暂，比如另一个线程很快就释放，自旋几次可能就拿到锁，这比直接挂起唤醒线程更划算。  

如果竞争继续加剧，自旋多次也拿不到锁，JVM 就会把锁膨胀成重量级锁，底层更依赖操作系统互斥量和线程阻塞唤醒机制。重量级锁的特点是切换和调度成本更高，但在高竞争场景下更稳定，因为它不会让线程一直白白自旋耗 CPU。  

面试里最好补一句：锁升级的目标是让低竞争场景尽量轻、让高竞争场景能扛住，不是让开发者手工去“选择升级到哪一层”。而且随着 JDK 版本演进，一些实现细节和优化策略也会变化，所以比起死记某个位模式，更重要的是知道它背后的性能权衡逻辑。  

工程里再补一句也很加分：如果真的出现锁竞争严重，业务层更该关注的是缩小临界区、减少共享状态、换更合适的并发模型，而不是幻想靠理解锁升级就能自动把问题解决。JVM 优化能帮忙，但设计层面的竞争才是根因。  

如果想再把这题答得更贴近排障，可以补一句：线上看见 blocked 线程很多、CPU 空转、自旋严重时，真正关心的是竞争是否过高以及锁粒度是否过粗，而不是去猜对象头现在到底是哪一种位模式。这个角度非常务实。  

再补一句也很适合收尾：锁升级这套机制说明 JVM 已经在尽力帮你优化同步成本，所以当业务层仍然卡在锁竞争上，往往意味着问题已经不在语法层，而在共享设计本身。把这句话说出来，会显得你很有工程判断。  

图解可以画成一条升级链：无锁 -> 偏向锁 -> 轻量级锁 -> 重量级锁，每一步旁边都写上“适合什么竞争强度”和“代价是什么”。最后收一句：锁升级是 JVM 根据竞争情况自动切换同步策略，本质是在拿不同成本换不同场景下的吞吐和延迟表现。`
  },
  "java-reference-types": {
    type: "compare",
    nodes: [
      ["强引用", "默认引用方式，只要有它对象通常不会被回收"],
      ["软引用", "内存紧张时才更倾向回收，适合缓存"],
      ["弱引用", "下次 GC 就可能回收，适合辅助索引映射"],
      ["虚引用", "本身拿不到对象，只用于跟踪回收时机"],
      ["可达性层次", "引用越弱，对象越容易被 GC 回收"],
      ["应用边界", "别把引用类型当银弹缓存策略"]
    ],
    detail: `强引用、软引用、弱引用、虚引用的区别，核心在于它们对垃圾回收可达性的影响强弱不同。你可以把它们理解成对象和程序之间四种不同力度的“牵手方式”：牵得越紧，对象越不容易被回收；牵得越松，GC 越可能把它带走。  

强引用就是平时最普通的引用方式，比如 Object obj = new Object()。只要还有强引用指向对象，GC 通常就不会回收它。这是默认最常见的对象生命周期模型。软引用则比强引用弱一些，内存充足时对象通常还能留着，但当 JVM 判断内存紧张时，会更倾向于回收软引用关联对象，所以它曾经常被用来做“有就用、没了再建”的缓存。  

弱引用更弱。只要发生垃圾回收，且对象除了弱引用之外没有更强引用，通常就会被回收，不太看内存是否紧张。它更适合用在规范化映射、辅助索引、避免引用链把对象意外留住的场景。比如某些 ThreadLocalMap 的 key、WeakHashMap 的 key 就和弱引用思想有关。  

虚引用最特别。它几乎不影响对象生命周期，而且通过虚引用本身拿不到真实对象。它的主要用途不是“访问对象”，而是配合 ReferenceQueue 跟踪对象何时即将被回收，从而做一些更安全的资源清理或回收后通知。也就是说，虚引用更像一种“回收监听钩子”。  

面试时要特别避免一个误区：引用类型不是缓存策略银弹。比如软引用缓存听起来很美，但在现代大内存和不同 GC 策略下，行为可能没有你想得那么稳定；弱引用如果用在核心业务对象上则可能很快被回收。真正工程里要不要用这些引用类型，必须结合 GC 行为、访问模式和可预测性要求。  

如果想把这题答得再完整一点，可以补一句：很多同学学引用类型时只记回收时机，却忽略了它们的真实价值在于“表达不同程度的生命周期意图”。也就是说，选择哪种引用，不只是性能问题，更是在告诉 GC 和维护者“这个对象应该被留多紧”。  

再补一个工程判断也很好：如果你的缓存、对象生命周期和一致性要求非常明确，往往应该优先用显式缓存策略、淘汰策略和业务状态控制，而不是把关键正确性押在软引用、弱引用这些不那么可预测的回收行为上。  

最后收一句也很稳：引用类型是和 GC 协商对象保留强度的工具，不是业务正确性的主逻辑。把这层边界说清楚，面试官通常会认为你真的理解了它们的价值和限制。  

图解可以画一条强到弱的梯度：强引用、软引用、弱引用、虚引用，对应不同回收时机和典型用途。旁边再标上“对象是否还能直接取到”“何时更容易被回收”。这样你就能把这题讲得既清楚又有边界感。`
  },
  "spring-bean-scope": {
    type: "compare",
    nodes: [
      ["singleton", "容器里默认单例作用域"],
      ["prototype", "每次获取都创建新实例"],
      ["request/session", "Web 环境里绑定请求或会话生命周期"],
      ["application", "ServletContext 级共享实例"],
      ["生命周期差异", "创建、复用和销毁责任并不一样"],
      ["设计建议", "状态对象慎放单例，无状态服务更适合默认作用域"]
    ],
    detail: `Spring Bean 的作用域，本质上是在回答“同一个 BeanDefinition 对应的实例，到底应该活多久、被复用多少次”。很多人只记得 singleton 和 prototype，但面试里更重要的是知道这些作用域如何影响状态管理、线程安全和生命周期。  

singleton 是默认作用域，表示在同一个 Spring 容器里通常只有一个实例。大多数 Service、Repository、Controller 都是这种方式，因为它们通常设计成无状态组件，复用一个实例最省资源也最简单。要强调的是，singleton 是“容器级单例”，不是 JVM 全局单例。不同容器里还是可能各有一个实例。  

prototype 则表示每次获取 Bean 都新建一个实例。它适合那些天然带状态、短生命周期、每次使用都希望独立的数据载体或策略对象。但也要知道，Spring 对 prototype 的管理主要到创建为止，后续销毁回调通常不会像 singleton 那样由容器统一帮你善后。也就是说，你拿到 prototype 之后，它更像“Spring 帮你 new 了一次”。  

在 Web 环境中，还有 request、session、application 等作用域。request 作用域的 Bean 在一次 HTTP 请求内共享，session 作用域绑定用户会话，application 作用域一般绑定整个 ServletContext。它们特别适合封装请求上下文、会话级状态或 Web 应用全局状态，但前提是你清楚并发和生命周期边界。  

这道题真正容易出问题的，是把有状态对象错误地放进 singleton。比如把用户请求中间状态、可变缓存、非线程安全临时变量放在单例 Bean 实例字段里，就会在并发请求下串数据。面试里如果能主动提到“默认单例没问题的前提是 Bean 设计成无状态”，答案就会很扎实。  

再补一个常见实践点：如果单例 Bean 又需要拿到原型 Bean，不能想当然地以为每次调用都会自动得到新实例，通常还要借助 ObjectFactory、Provider 或方法注入等方式显式获取。这个点一说，说明你不只是记住了作用域名字，而是真知道它们在容器里怎么协作。  

图解可以画一个 BeanDefinition 向不同作用域实例分发：singleton 指向一个共享实例，prototype 指向多个新实例，request/session 则挂在各自上下文里。最后收一句：作用域不是记名词，而是在定义对象该活多久、该不该共享。`
  },
  "autowired-resource": {
    type: "compare",
    nodes: [
      ["@Autowired", "Spring 提供，默认更偏按类型注入"],
      ["@Resource", "JSR 规范注解，默认更偏按名称匹配"],
      ["候选歧义", "多个同类型 Bean 时要配合名称或限定符"],
      ["依赖语义", "构造器注入、字段注入、setter 注入都能结合用"],
      ["工程习惯", "现代 Spring 更常见 @Autowired 配合构造器"],
      ["本质理解", "两者都是声明依赖，差别在匹配规则和生态归属"]
    ],
    detail: `@Autowired 和 @Resource 的区别，不能只背“一个按类型，一个按名称”，因为实际匹配过程比这句话稍微复杂。更准确的理解是：@Autowired 来自 Spring 生态，默认更强调按类型装配；@Resource 来自 JSR 规范，默认更倾向按名称查找，再结合类型校验。它们都在做依赖注入，但匹配规则和使用习惯不同。  

@Autowired 最常见的行为是先按类型找候选 Bean。如果同类型只有一个，注入非常顺畅；如果同类型有多个，就需要进一步用字段名、参数名、@Qualifier、@Primary 等方式辅助消歧。现代 Spring 项目里，尤其推荐把 @Autowired 用在构造器注入场景，因为这样依赖更明确，也更利于测试和不可变设计。很多时候甚至不用显式写 @Autowired，单构造器 Spring 也能自动注入。  

@Resource 则更像“我想要这个名字对应的资源”。它常见情况下会先按名称匹配，比如字段名或显式指定的 name，再确认类型是否兼容。如果名称找不到，具体实现细节还会受到容器行为影响。也正因为它更偏名称语义，所以在一些需要明确资源名的老项目或和 Java 规范注解风格保持一致的项目里仍然常见。  

面试里最好主动说一点工程判断：从今天的 Spring 开发习惯看，构造器注入加 @Autowired（或单构造器省略）通常更推荐，因为依赖关系清晰、便于单元测试，也能避免字段注入带来的隐藏依赖问题。@Resource 不是不能用，而是更多看项目习惯和是否真的需要名称优先语义。  

还要注意一个误区：这两个注解不是“谁更高级”的问题，而是匹配策略不同。如果你有多个同类型 Bean，不管哪一个注解，最终都要把“到底要注入哪一个”讲清楚。否则框架只能报歧义，帮不了你做业务决定。  

工程上再补一句也很有分量：真正值得推荐的不是某个注解本身，而是“依赖要显式、边界要稳定、测试要方便”。所以很多团队最终落到构造器注入，并不是为了追时髦，而是因为这种方式最不容易藏依赖。  

如果想把这题答完整，还可以补一句“注入歧义本质是容器不知道你的业务选择”。所以当存在多个同类型 Bean 时，使用 Qualifier、Primary 或显式名称并不是麻烦，而是在把设计决策说清楚。  

图解可以画两条匹配路径：@Autowired 从类型出发，再用 Qualifier/Primary 消歧；@Resource 从名称出发，再校验类型。最后再补一句：真正成熟的注入方式，不是记口诀，而是让依赖关系足够明确、稳定、可测试。`
  },
  "filter-interceptor-aop": {
    type: "compare",
    nodes: [
      ["Filter", "Servlet 层最靠前，和容器规范绑定"],
      ["Interceptor", "Spring MVC 层，围绕 Handler 执行"],
      ["AOP", "更通用的代理增强，不局限于 Web 请求"],
      ["作用位置", "越靠前越偏协议，越靠后越偏业务"],
      ["典型用途", "编码、鉴权、日志、事务、监控各有分工"],
      ["选择原则", "按处理层级选，不要把三者混成同一件事"]
    ],
    detail: `Filter、Interceptor、AOP 经常一起被问，因为它们都能“在方法真正执行前后插点逻辑”，但三者所在层级、拦截对象和适用场景都不同。最稳的回答方式，是先按请求链路从外到内排一遍：Filter 最靠外，拦的是 Servlet 请求；Interceptor 在 Spring MVC 里，拦的是控制器调用；AOP 更抽象，拦的是被代理的 Bean 方法，不局限于 Web。  

Filter 来自 Servlet 规范，发生在进入 Spring MVC 之前。也就是说，请求一进 Web 容器就可能先经过 Filter。它特别适合处理和协议、容器、请求原始对象强相关的事情，比如字符编码、跨域、统一 request/response 包装、基础鉴权、链路 trace 初始化、访问日志、XSS 处理等。因为它离 HTTP 最近，所以拿到的是最原始的请求和响应。  

Interceptor 是 Spring MVC 提供的机制，围绕的是 Handler 执行流程。它知道将要进入哪个控制器方法，也能在方法前后以及视图渲染后插逻辑，所以比 Filter 更了解 MVC 语义。常见用途包括登录校验、权限判断、接口级访问日志、埋点、请求耗时统计、特定接口防重复提交等。它的优势是能感知 Spring 路由和方法级信息。  

AOP 则比前两者更通用。它不一定和 HTTP 请求有关，而是基于代理在 Bean 方法调用前后织入增强逻辑。事务、审计日志、方法级监控、幂等注解、缓存注解、统一重试等都很适合 AOP。它拦的是方法调用语义，而不是 Web 协议对象。所以如果你的逻辑希望在 Service、Repository、异步任务甚至定时任务里都统一生效，AOP 通常更合适。  

三者的分工不是谁替代谁，而是层次不同。Filter 更偏容器入口治理，Interceptor 更偏 Web 层请求编排，AOP 更偏业务方法横切关注点。面试里如果能主动说出“越往外越适合做协议和请求层逻辑，越往里越适合做业务语义增强”，层次会很清楚。  

还要补一个常见坑：AOP 有代理边界，自调用可能不生效；Interceptor 只对进入 Spring MVC 的请求有效，静态资源和某些容器层请求处理未必经过它；Filter 虽然早，但拿不到方法级业务信息。也就是说，选错层会让代码很别扭。  

图解可以画一条请求链：客户端 -> Filter -> DispatcherServlet -> Interceptor -> Controller/Service，其中 AOP 包裹在 Service 等 Bean 方法周围。旁边再列典型用途。最后收一句：三者都能做增强，但增强点不同，关键是按层选工具。`
  },
  "spring-boot-starter": {
    type: "structure",
    nodes: [
      ["依赖打包", "把一类能力常用依赖聚合起来"],
      ["自动配置入口", "starter 往往配合自动配置一起工作"],
      ["开箱即用", "引入后少写样板配置"],
      ["约定组合", "统一版本和推荐组件搭配"],
      ["自定义 starter", "可为团队沉淀内部能力包"],
      ["本质", "它是能力装配入口，不是魔法黑盒"]
    ],
    detail: `Spring Boot Starter 的价值，首先不是“少写几行依赖”这么简单，而是把一类技术能力的依赖组合、版本约束和默认接入方式标准化。比如你想接 Web、Redis、MyBatis、MQ，如果自己一项项挑依赖、对版本、配 Bean，不仅麻烦，还容易搭出一堆不兼容组合。Starter 的作用就是把这类常见组合打成一个能力入口。  

当你引入一个 starter 时，通常发生了两件事。第一，它把这类能力所需的常用依赖聚合进来；第二，它往往会配合自动配置，让 Spring Boot 根据类路径和配置条件自动注册默认 Bean。也就是说，starter 自己偏“把东西带进来”，自动配置偏“带进来后怎么让它跑起来”，两者通常成对出现。  

比如 spring-boot-starter-web 通常会带入 Spring MVC、Jackson、内嵌 Tomcat 等一组 Web 能力；spring-boot-starter-data-redis 会带入 Redis 访问相关基础组件；mybatis-spring-boot-starter 会把 MyBatis 和 Spring Boot 的接入桥梁一起装好。这样你面对的是“能力包”，而不是散装零件。  

Starter 还承担了团队协作和版本治理价值。一个团队如果自己维护内部 starter，就可以把统一日志组件、统一监控埋点、统一异常处理、统一鉴权客户端、统一配置约定封进 starter 里。业务项目接入后自然具备这些能力，减少每个项目复制粘贴。这个点在面试里非常加分，因为它把 starter 从“官方依赖包”提升到了“组织能力沉淀工具”。  

但要知道 starter 不是魔法。它只是把依赖和默认接入流程组织得更顺。真正排问题时，你还是得知道它带进来了哪些 jar、触发了哪些自动配置、注册了哪些 Bean、如何被你自己的配置覆盖。也就是说，starter 适合提升接入效率，但不能替代你对底层能力的理解。  

图解可以画成三层：最上层业务项目只引入 starter；中间 starter 聚合一组依赖；底层自动配置根据条件注册 Bean，最终能力可用。旁边再补一条“自定义 starter”分支，说明团队能力沉淀方式。这样回答既能讲清官方机制，也能落到企业实践。`
  },
  "mybatis-mapper-proxy": {
    type: "flow",
    nodes: [
      ["接口声明", "开发者只写 Mapper 接口和方法签名"],
      ["代理生成", "MyBatis 运行时为接口创建动态代理"],
      ["方法解析", "代理根据方法定位对应 SQL 映射"],
      ["执行链路", "通过 SqlSession 调用执行器和映射器"],
      ["结果封装", "把查询结果转回方法返回类型"],
      ["价值", "把 DAO 样板代码压缩成声明式接口"]
    ],
    detail: `MyBatis Mapper 接口之所以不用手写实现类，本质上是因为运行时动态代理已经替你把“实现类里本该写的那些转调逻辑”生成好了。你在代码里只看到一个接口方法，比如 selectById，但真正执行时，Spring 或 MyBatis 给你的并不是空接口，而是一个代理对象。这个代理知道如何把方法调用翻译成 SQL 执行。  

执行过程可以拆成几步。首先，框架扫描到 Mapper 接口后，会为它注册代理工厂；当你注入这个接口时，拿到的是代理实例。随后，当你调用某个方法，代理会根据接口全限定名、方法名和参数，去定位对应的 mapped statement，也就是 XML 或注解里定义的那条 SQL 映射。找到映射后，它再借助 SqlSession 进入 MyBatis 执行链路。  

接下来就是典型的执行流程：SqlSession 把请求交给 Executor，Executor 再经过参数处理、SQL 生成、JDBC 执行、结果集映射，最后把结果转回你接口方法声明的返回类型。也就是说，Mapper 接口方法只是一个声明式入口，真正的实现逻辑在 MyBatis 的代理和执行框架里。  

这套机制的价值非常大。它把原来大量重复的 DAO 样板代码消掉了：你不需要自己写实现类去获取连接、准备语句、执行 SQL、映射结果。你只要专注于“我需要哪些查询方法”和“SQL 应该怎么写”。这也是 MyBatis 非常适合业务 CRUD 场景的原因之一。  

当然，这种便利也有代价。方法名和 SQL 映射之间虽然看起来像静态关联，但本质是运行时绑定。如果 namespace、方法名、参数类型、返回映射不一致，问题常在运行时才暴露。面试里如果能主动说出“声明式简化了实现，但也要求映射配置保持严格一致”，会显得很稳。  

图解可以画成一条调用链：业务代码调用 Mapper 接口 -> 动态代理接住方法 -> 定位 mapped statement -> SqlSession/Executor 执行 SQL -> 结果映射返回。最后落一句：不是 Mapper 真有实现类，而是代理把方法调用翻译成了 SQL 执行流程。`
  },
  "mybatis-resulttype-resultmap": {
    type: "compare",
    nodes: [
      ["resultType", "简单场景直接映射到目标类型"],
      ["resultMap", "复杂场景显式定义字段到对象关系"],
      ["能力边界", "resultMap 支持关联、集合、嵌套映射更强"],
      ["维护成本", "resultType 简单，resultMap 更重但更精确"],
      ["典型选择", "单表简单对象优先 resultType"],
      ["核心判断", "对象结构越复杂越该显式映射"]
    ],
    detail: `MyBatis 的 resultType 和 resultMap，核心区别不是“一个简单一个复杂”这么笼统，而是它们对结果映射控制力不同。resultType 更像告诉 MyBatis：“这条查询的结果大体能直接塞进这个类型”；resultMap 则是你明确告诉 MyBatis：“数据库列和对象结构应该如何精确对应，必要时还包括嵌套对象和集合关系”。  

当查询结果和 Java 对象结构非常简单直接时，resultType 很方便。比如单表查询、列名和属性名基本一致、对象层级扁平，这时候指定一个实体类或基本类型，MyBatis 就能按规则自动映射，配置最轻。像根据 id 查一条用户记录、查一个 count 值，这类场景用 resultType 最顺手。  

但一旦对象结构复杂，resultType 就开始吃力。比如数据库列名和属性名差异较大，或者查询结果里要组装嵌套对象、关联对象、集合属性，或者你需要明确主键列、延迟加载、字段别名映射，这时 resultMap 的优势就出来了。它能把映射关系写得非常清楚，也更适合长期维护复杂查询。  

面试里可以强调一个判断标准：当“自动映射会不会产生歧义”开始变成问题时，就该优先考虑 resultMap。因为比起省几行配置，更重要的是映射行为可控、可读、可维护。尤其在多表 join、嵌套结果集、DTO 组装、历史系统字段命名混乱的场景下，resultMap 往往更稳。  

当然，resultMap 也有代价：配置更重、学习曲线更高、维护起来比 resultType 麻烦。所以工程上不是说 resultMap 永远更高级，而是要按复杂度选。简单场景没必要把配置写成一大坨；复杂场景也别为了偷懒强行用 resultType 让映射变得模糊。  

图解可以画成两条映射路径：一条是 resultType 自动从结果集到对象的简单映射；另一条是 resultMap 显式描述列 -> 属性 -> 嵌套对象/集合的复杂映射。最后收一句：映射越简单越可以自动，结构越复杂越应该显式。`
  },
  "clustered-secondary-index": {
    type: "compare",
    nodes: [
      ["聚簇索引", "叶子节点直接存整行数据"],
      ["二级索引", "叶子节点存索引列和主键值"],
      ["访问路径", "二级索引命中后常需回表查完整数据"],
      ["物理组织", "表数据按聚簇索引顺序组织"],
      ["设计影响", "主键选择会影响整表布局和二级索引体积"],
      ["优化思路", "覆盖索引可减少回表成本"]
    ],
    detail: `聚簇索引和二级索引的核心区别，不是“一个重要一个不重要”，而是它们存的东西不同、访问路径不同。以 InnoDB 为例，聚簇索引的叶子节点存放的是整行数据，因此表数据本身就是按聚簇索引顺序组织的；二级索引的叶子节点则主要存索引列值和对应主键值，不直接存完整行。  

这会直接影响查询路径。如果你通过主键查询，本质就是沿着聚簇索引树一路找到叶子节点，直接拿到整行数据，路径最短。如果通过二级索引查询，先是在二级索引树上找到匹配项，但拿到的通常只是主键值；如果查询需要的列不全在这个二级索引里，还要再根据主键回到聚簇索引里把整行取出来，这就是常说的回表。  

也正因为如此，主键设计不只是逻辑上的唯一标识，它还决定了聚簇索引的组织方式。主键过大、过长、无序，不仅会影响聚簇索引页分裂和插入局部性，还会让所有二级索引叶子节点都更胖，因为二级索引里要带主键值。面试里如果能主动说出“二级索引叶子里存主键，所以主键设计会放大到所有二级索引”，很加分。  

从性能角度看，聚簇索引适合按主键定位整行；二级索引适合按非主键条件快速筛选。真正优化时常常会进一步追求覆盖索引，也就是让查询需要的列都包含在二级索引叶子里，这样命中二级索引后就不必再回表。  

图解可以画两棵树：聚簇索引叶子节点直接挂整行记录；二级索引叶子节点挂“索引列值 + 主键值”，再用箭头指向聚簇索引完成回表。最后一句：聚簇索引决定数据怎么放，二级索引决定怎么更快找到它。`
  },
  "covering-index": {
    type: "structure",
    nodes: [
      ["命中索引", "查询条件先走索引筛选"],
      ["所需列齐全", "返回列也都在同一个索引里"],
      ["无需回表", "不用再去聚簇索引取整行"],
      ["性能收益", "减少随机 IO 和回表成本"],
      ["设计代价", "索引更宽，写入和存储成本上升"],
      ["适用场景", "高频读查询、列表页、统计页特别受益"]
    ],
    detail: `覆盖索引的本质，是“查询所需要的数据，刚好都能从索引本身拿到”，因此不需要再去主表或聚簇索引里把整行捞出来。它不是一种单独的新索引类型，而是一种查询效果：只要某条查询的过滤列、排序列、返回列都被同一个索引覆盖，就可能形成覆盖索引访问。  

为什么它快？因为在 InnoDB 里，二级索引叶子节点通常只存索引列和主键值。如果查询命中二级索引后，还需要别的列，就得回表再查一次聚簇索引。覆盖索引则把这个第二步省掉了，所以随机 IO、页访问次数和 CPU 解析成本都可能下降。对高频列表页、分页查询、后台筛选接口，收益往往很明显。  

比如你有联合索引 (user_id, status, create_time)，某个查询是按 user_id 和 status 过滤，只返回 create_time，那么这条查询就可能直接从索引叶子拿到所需结果，无需回表。面试里要强调：覆盖索引不是看有没有命中索引，而是看返回的数据列是否也在索引里。  

但覆盖索引不是越多越好。为了覆盖更多查询，你可能会把索引做得很宽，这会增加存储占用、维护成本和写入放大。每次 insert/update/delete 不仅要改数据页，还要维护更大的索引页。所以它是典型的“用空间和写成本换读性能”的优化手段。  

设计时最关键的是看热点查询。不是所有查询都值得为它量身定做覆盖索引，而是核心高频读、响应时间敏感、回表成本明显的那一批查询更值得。把这个工程取舍说出来，会比单纯讲定义更像真实调优经验。  

图解可以画两条查询路径对比：普通二级索引先命中索引，再回表；覆盖索引则在索引叶子直接返回结果。旁边再标出收益和代价。最后一句收束：覆盖索引追求的不是“索引更多”，而是“少一次回表”。`
  },
  "redis-single-thread-fast": {
    type: "structure",
    nodes: [
      ["单线程执行命令", "核心命令处理路径串行避免锁竞争"],
      ["内存操作", "绝大多数操作是内存级而非磁盘级"],
      ["高效数据结构", "跳表、哈希、压缩结构都很轻量"],
      ["IO 多路复用", "网络连接处理高效，不靠一请求一线程"],
      ["局部瓶颈", "快不代表无上限，慢命令仍会拖住全局"],
      ["工程要点", "避免 big key、慢命令和阻塞操作"]
    ],
    detail: `Redis 单线程为什么还这么快，最关键的理解不是“单线程也很厉害”，而是它快的原因主要不在“线程多不多”，而在于工作负载形态很适合单线程模型。Redis 的核心命令处理大多是内存操作，避免了复杂磁盘 IO；同时单线程串行执行命令，省掉了大量锁竞争、上下文切换和共享状态同步成本。  

很多人会误解成“Redis 完全只有一个线程”。更准确地说，传统上 Redis 的核心命令执行路径主要是单线程的，但一些后台任务、持久化、网络读写优化、bio 线程等并不一定全是一个线程。面试里最好强调：大家说的“单线程快”，指的是命令执行模型，而不是整个进程世界里绝对只有一根线程。  

Redis 快的第二个原因是数据结构和实现非常贴近内存访问。字符串、哈希、列表、集合、有序集合都有为小对象、小集合优化的内部编码，很多操作时间复杂度也很低。再配合事件驱动和 IO 多路复用，它不用像传统线程池服务器那样为每个连接都准备一个工作线程。  

但这并不意味着 Redis 没有性能边界。单线程模型的代价是：一条慢命令、一次大 key 扫描、一次阻塞式脚本、一次大范围删除，都可能拖住后面的其他请求。也就是说，Redis 的快是建立在“每条命令都足够短小、足够快”的假设上。所以工程里非常强调避免 big key、避免 KEYS 全量扫描、避免长 Lua、避免阻塞操作。  

还可以顺手讲一句为什么单线程反而适合 Redis 这类缓存中间件：因为它把绝大部分问题都转化成了快速内存读写，而不是复杂 CPU 密集计算。在线程竞争本来就不是主要瓶颈时，少一层锁和切换往往更划算。  

图解可以画成一条事件循环：客户端请求进入，IO 多路复用器分发事件，单线程顺序执行命令并直接操作内存数据结构，然后返回结果。旁边再标红慢命令、big key、阻塞操作会卡全局。这样这题既能解释“为什么快”，也能解释“为什么仍然会卡”。`
  },
  "cache-db-consistency": {
    type: "flow",
    nodes: [
      ["读写分离", "数据库是最终真相，缓存是性能加速层"],
      ["更新策略", "常见是先更新数据库再删除缓存"],
      ["并发窗口", "删除和回填之间可能出现脏数据窗口"],
      ["补偿机制", "重试删缓存、延迟双删、订阅 binlog 等"],
      ["一致性等级", "通常追求最终一致而非绝对实时一致"],
      ["核心判断", "方案选择取决于读写比例和错误容忍度"]
    ],
    detail: `缓存和数据库一致性要解决的核心矛盾是：数据库才是最终真相，但为了性能我们又把一部分数据复制到缓存里。这样一来，只要数据会变，就必然会遇到“数据库已经新了，缓存还是旧的”或者“缓存删了但又被旧值回填”的问题。面试里最重要的是先承认这件事本身就是一致性与性能的权衡，而不是幻想有一种零成本绝对完美方案。  

最常见的基础方案是 Cache Aside，也就是读时先查缓存，未命中再查数据库并回填；写时先更新数据库，再删除缓存。为什么不是先删缓存再更新数据库？因为那样中间如果有并发读，很容易把旧值重新写回缓存。先更库再删缓存是更常见也更稳的基本做法。  

但即使这样，也仍然有并发窗口。比如线程 A 更新数据库后准备删缓存，线程 B 正好在缓存缺失时读到旧数据库从库或旧快照，再把旧值回填回缓存，就会形成脏数据短暂驻留。也就是说，“先更库再删缓存”只是降低风险，不是消灭所有竞态。  

所以工程上常会加补偿措施，比如删缓存失败重试、消息队列异步补删、延迟双删、binlog 订阅驱动缓存失效、热点 key 严格串行更新等。不同业务对一致性窗口的容忍度不同：商品详情、资讯展示可以接受短暂最终一致；库存、余额、优惠券计数就要更谨慎，很多时候甚至不该过度依赖普通缓存回填模型。  

面试里最好加一句层次判断：缓存一致性多数时候追求的是最终一致，而不是每一毫秒都绝对一致。真正关键的是：一旦不一致发生，窗口有多大、能否快速收敛、业务是否能接受、有没有补偿观察手段。这样回答会比只背“延迟双删”成熟很多。  

图解可以画成一条写链路：更新数据库 -> 删除缓存；再画一条读链路：查缓存未命中 -> 查库 -> 回填缓存。中间专门标一个并发风险窗口，再接补偿机制分支。最后一句收束：缓存一致性不是追求神话级零误差，而是在性能和正确性之间把风险压到业务能接受的范围。`
  },
  "kafka-consumer-group": {
    type: "structure",
    nodes: [
      ["组内分摊", "同组消费者共同分担一个主题分区集合"],
      ["分区独占", "同一分区同一时刻只会分给组内一个消费者"],
      ["横向扩展", "增加消费者可并行消费更多分区"],
      ["广播语义", "不同消费组可各自独立消费同一主题"],
      ["重平衡", "成员变化时会重新分配分区"],
      ["工程关注", "并发度上限受分区数限制"]
    ],
    detail: `Kafka Consumer Group 的核心价值，是让一批消费者既能共享同一主题的数据，又不会把同一条消息在组内重复消费。它解决的是“怎么把一个主题的分区集合，合理分摊给多个消费者实例”。面试里最重要的一句话是：同一个消费组内，同一分区在同一时刻只会分配给一个消费者。  

因为 Kafka 的消费顺序和位点管理是按分区来的，所以 Consumer Group 的并行能力本质也受分区数限制。比如一个主题只有 3 个分区，那同一个消费组就算启动 10 个消费者实例，也最多只有 3 个能真正拿到分区工作，剩下的会空着。这也是为什么 Kafka 扩容消费能力时，经常要一起考虑分区规划。  

Consumer Group 还天然支持“组内分摊、组间广播”的语义。组内多个消费者共享工作量，适合做扩展；不同消费组彼此独立，各自维护自己的消费位点，所以同一主题完全可以同时服务于订单处理、风控审计、数据分析等多种下游。这个特点非常适合事件分发型架构。  

面试时最好别忘了讲重平衡。只要消费组成员变化、分区数变化、消费者挂掉或加入，Kafka 就可能触发 rebalance，重新分配分区。重平衡期间消费可能暂停，处理不当还会引发抖动、重复消费或吞吐下降。所以 Consumer Group 不只是“把人分组”，它还带着动态再分配的代价。  

工程上要有一个清晰判断：如果你想提升同组消费并发，先看分区数够不够；如果你想让不同业务都拿到同一批消息，就拆成不同消费组；如果你想控制重平衡影响，就得关注消费者数量变化频率、分配策略和消费处理时长。  

图解可以画一个 Topic 下多个分区，下面挂一个 Consumer Group，组内多个消费者分别领走不同分区；旁边再画另一个独立 Consumer Group，也能拿到同一组分区数据。最后收一句：Consumer Group 既是并发扩展机制，也是消费语义边界。`
  },
  "kafka-offset": {
    type: "flow",
    nodes: [
      ["位点含义", "记录某个分区消费进度的位置标记"],
      ["拉取消费", "消费者按 offset 从分区读取消息"],
      ["提交时机", "处理成功后再提交更安全"],
      ["自动手动", "可自动提交也可手动控制提交点"],
      ["失败影响", "提交过早会丢数据，提交过晚会重复消费"],
      ["核心原则", "offset 提交策略要和业务处理原子性匹配"]
    ],
    detail: `Kafka offset 可以简单理解成“某个消费组在某个分区上读到哪里了”的进度标记。它不是消息本身，而是消费位置。消费者下一次从哪里继续读、重启后从哪里恢复、是否可能重复消费，几乎都和 offset 管理方式直接相关。  

因为 Kafka 的消息是分区有序追加的，offset 也是分区维度维护的。也就是说，offset 不是整个主题一个总数字，而是“消费组 + 主题 + 分区”这组关系上的位置。面试里把这一点说清楚很重要，否则很容易把位点理解得过于粗糙。  

真正关键的是“什么时候提交 offset”。如果你在业务还没真正处理成功之前就把 offset 提交了，一旦消费者宕机，这条消息恢复后就可能被跳过，造成数据丢失；反过来，如果你处理成功了但还没来得及提交 offset 就挂了，重启后这条消息可能会再被拉一次，造成重复消费。所以 offset 提交其实是在“丢”和“重”之间做语义选择。  

Kafka 支持自动提交和手动提交。自动提交简单，但控制力较弱，很多严肃业务更倾向手动提交：消息处理成功后再提交，或者和批量处理、事务处理节奏绑定起来。这样更容易把“处理结果”和“消费进度”对应起来。也正因为如此，Kafka 的 at-least-once 语义很常见，而 exactly-once 往往需要更复杂的事务或幂等配合。  

面试里还可以主动补一句：offset 提交不是“越频繁越好”或“越晚越好”，而是要和业务处理粒度、吞吐量、可接受重复度、恢复速度一起权衡。提交太频繁会增加开销，提交太晚则失败恢复成本变高。  

图解可以画一条分区日志，从 offset 0、1、2、3 向后递增，消费者从某个位置开始拉取，处理成功后再把“已消费到哪里”提交到协调存储。旁边再画两种失败情况：先提交流失数据、后提交导致重复消费。最后收一句：offset 不是单纯进度条，而是消费语义和恢复策略的核心锚点。`
  },
  "mq-message-backlog": {
    type: "flow",
    nodes: [
      ["确认积压", "先看堆积量、消费延迟和增长速度"],
      ["生产侧突增", "活动流量、重试风暴或上游批量导入"],
      ["消费侧变慢", "消费者异常、线程不足或单条处理耗时升高"],
      ["下游瓶颈", "数据库、RPC、ES、第三方接口拖慢消费"],
      ["毒消息阻塞", "异常消息反复重试占住消费能力"],
      ["恢复方案", "扩容、限流、跳过、死信、补偿和追平计划"]
    ],
    detail: `消息队列积压的本质，是消息进入队列的速度长期大于被消费并确认的速度。排查时不要一上来只说“加消费者”，因为积压可能来自生产侧流量暴涨，也可能来自消费逻辑变慢、下游接口阻塞、分区并行度不足，甚至是一条毒消息反复失败把线程拖住。面试里最好先把现象量化：积压量是多少、增长速度是多少、消费延迟从什么时候开始上升、是否集中在某个 Topic、分区、消费组或某类消息上。

第一步看生产端。活动峰值、批量导入、重试风暴、定时任务集中执行，都可能让消息瞬间涌入。如果生产速率只是短暂尖峰，而消费端吞吐足够，系统可能只是需要时间追平；如果生产速率持续高于消费能力，就要考虑限流、削峰、拆 Topic 或扩容消费链路。这里要区分“可恢复积压”和“持续恶化积压”，前者看追平时间，后者先止血。

第二步看消费端。消费者是否频繁重启、是否出现异常堆栈、线程池是否打满、单条消息处理耗时是否变长、提交 offset 或 ack 是否失败，都要一起看。很多积压不是 MQ 自己慢，而是消费者处理一条消息要调用数据库、缓存、RPC 或第三方接口，下游一慢，消费吞吐就被拖下来。这个时候盲目加消费者，可能把下游压得更死。

第三步看并行度。Kafka 的消费并发受分区数限制，RocketMQ 也要看队列数量和消费模式。如果分区或队列数量不足，增加消费者实例不会线性提升吞吐；如果单分区消息必须保序，扩容空间更有限。遇到这种场景，要考虑按业务 key 拆分、增加分区、批量消费、异步化处理，或者先临时关闭非核心耗时逻辑。

第四步处理异常消息。某些消息因为字段脏、依赖缺失、业务状态不满足而一直失败，重试会持续占用消费能力。工程上通常要设置最大重试次数，超过后转入死信队列或异常表，避免一小批坏消息拖垮整个消费组。后续再用补偿任务或人工修复处理这些异常数据。

图解可以画成“发现积压 -> 定位来源 -> 判断瓶颈 -> 保护下游 -> 追平队列 -> 补偿复盘”的流程。项目里还要补监控：堆积量、消费延迟、生产 QPS、消费 QPS、失败率、重试次数、死信数量和下游耗时。最后收一句：积压处理的优先级是先止血，再扩容，再补偿，最后复盘容量和告警阈值。`
  },
  "tcp-reliable": {
    type: "structure",
    nodes: [
      ["序列号", "给字节流编号，支持排序和去重"],
      ["确认应答", "ACK 告诉发送方哪些数据已到达"],
      ["重传机制", "超时或重复 ACK 触发重新发送"],
      ["滑动窗口", "批量在途数据提升传输效率"],
      ["流量控制", "按接收方窗口限制发送速度"],
      ["拥塞控制", "按网络拥塞情况动态调整发送速率"]
    ],
    detail: `TCP 的可靠传输不是靠某一个开关保证的，而是一组机制叠在一起完成的。它要解决的问题是：IP 层只负责尽力投递，包可能丢、可能乱序、可能重复，也可能因为网络拥塞变慢。TCP 在此之上把数据抽象成有序字节流，通过编号、确认、重传、窗口和控制算法，让应用层看到的更像一条可靠连接。

序列号是基础。TCP 不是按“第几个包”简单编号，而是按字节流编号。接收方根据序列号判断数据顺序，能发现缺口，也能丢弃重复数据。比如收到 1-1000 和 2001-3000，却没收到 1001-2000，接收方就知道中间缺了一段，不能随便交给应用层当作完整数据。

ACK 确认负责反馈进度。接收方会告诉发送方自己已经连续收到了哪里，发送方据此判断哪些数据可以从发送缓冲区移除。为了提高效率，ACK 通常是累计确认，不是每个字节都单独确认。这样既减少网络开销，也能让发送方根据确认推进窗口。

重传机制负责补丢失。发送方发出数据后会启动重传计时器，如果超时还没收到确认，就会重发；如果连续收到多个重复 ACK，也可能触发快速重传，提前判断某个段丢了。这里要注意，重传不是越频繁越好，超时时间要动态估算 RTT，否则会把正常延迟误判成丢包，加剧网络压力。

滑动窗口解决效率问题。如果 TCP 发一个包等一个 ACK，吞吐会很差。窗口允许发送方同时发送多段未确认数据，ACK 回来后窗口向前滑动。流量控制使用接收窗口告诉发送方“我还能接多少”，避免接收方缓冲区被打爆；拥塞控制则根据网络状态调整拥塞窗口，慢启动、拥塞避免、快重传、快恢复都是为了不把网络压垮。

图解可以画成一条发送端到接收端的字节流：每段数据带序列号，接收方返回 ACK，发送方窗口向前滑动；中间丢一段后触发超时或快速重传；旁边再标出接收窗口和拥塞窗口共同限制实际发送量。最后收束：TCP 可靠不是“不丢包”，而是丢了能发现、乱了能排序、重复能去重、慢了能调速。`
  },
  "tcp-udp": {
    type: "compare",
    nodes: [
      ["连接模型", "TCP 面向连接，UDP 无连接"],
      ["可靠性", "TCP 负责确认和重传，UDP 不内建保证"],
      ["顺序语义", "TCP 提供有序字节流，UDP 保留报文边界"],
      ["传输成本", "TCP 状态更多，UDP 头部小、延迟低"],
      ["拥塞控制", "TCP 内建控制，UDP 需要应用自律"],
      ["场景选择", "可靠业务用 TCP，实时容忍丢包可用 UDP"]
    ],
    detail: `TCP 和 UDP 的区别，不能只背“TCP 可靠、UDP 不可靠”。更准确地说，TCP 在传输层提供连接管理、可靠投递、有序字节流、流量控制和拥塞控制；UDP 只提供很薄的一层报文发送能力，不保证到达、不保证顺序、不保证去重，但保留报文边界，头部开销小，延迟和控制自由度更高。

连接模型上，TCP 通信前要通过三次握手建立连接，双方维护序列号、窗口、拥塞状态、重传队列等信息。连接结束还要挥手释放状态。UDP 没有连接建立过程，应用把报文交给 UDP，它加上端口信息就往外发。这个差异决定了 TCP 更重，但也给了它管理可靠性的基础。

可靠性上，TCP 会给字节编号，通过 ACK、重传、滑动窗口保证接收方按序拿到数据。UDP 不做这些，报文丢了就丢了，乱序就乱序，重复就重复。如果业务需要可靠性，就要在应用层自己做确认、重试、序列号、超时和去重。很多实时音视频或游戏协议选择 UDP，不是因为它永远更好，而是它们宁愿丢一帧，也不愿为了重传旧数据阻塞最新画面。

数据语义也不同。TCP 是字节流，没有天然消息边界，所以应用层要处理粘包和拆包，比如用长度字段、分隔符或固定协议头。UDP 是数据报，一次 send 对应一个报文，接收端能感知报文边界，但单个报文太大容易被分片，分片丢失会导致整个报文不可用。

性能和场景选择要结合业务。HTTP/1.1、HTTP/2、数据库连接、文件传输通常依赖 TCP，因为它们更看重完整和有序。DNS、语音视频、直播、部分游戏、QUIC 则常用 UDP 或基于 UDP 自建可靠机制，因为它们更看重延迟、连接迁移或自定义拥塞控制。QUIC 也说明了一个点：UDP 本身不可靠，但可以作为更灵活的承载层。

图解可以画成左右对比：TCP 一侧有握手、状态、ACK、重传、窗口；UDP 一侧是应用报文直接发送，旁边标注轻量、低延迟、应用自控。最后回答时收一句：选 TCP 还是 UDP，不是看谁高级，而是看业务更怕数据丢失，还是更怕延迟和阻塞。`
  },
  "get-post": {
    type: "compare",
    nodes: [
      ["语义", "GET 获取资源，POST 提交或触发处理"],
      ["幂等", "GET 应该安全幂等，POST 通常不保证"],
      ["参数位置", "GET 多在 query，POST 多在 body"],
      ["缓存", "GET 更容易被浏览器和代理缓存"],
      ["安全", "二者都不天然安全，敏感数据依赖 HTTPS 和规范处理"],
      ["接口设计", "按资源语义选择，而不是按参数长短乱用"]
    ],
    detail: `GET 和 POST 的区别，核心应该从 HTTP 语义讲，而不是只说“GET 参数在 URL，POST 参数在 body”。GET 的语义是获取资源，应该是安全的、幂等的，也就是说一次请求和多次请求不应该改变服务器资源状态。POST 的语义更偏向提交数据、创建资源或触发处理，通常不承诺幂等，重复提交可能产生多条记录或多次业务动作。

参数位置只是常见表现。GET 请求通常把过滤条件、分页参数、查询关键字放在 URL query 里，方便分享、收藏、缓存和日志分析。POST 通常把复杂对象、表单、JSON 或文件放在 body 里，适合提交较大或结构化的数据。但协议并不是说 GET 绝对不能有 body，而是现实中很多中间件、网关、浏览器和服务框架对 GET body 支持不一致，所以工程上不建议依赖它。

缓存行为也不同。GET 因为语义上是读取资源，更容易被浏览器、CDN、代理缓存，配合 Cache-Control、ETag、Last-Modified 可以减少请求成本。POST 默认不容易被缓存，因为它可能改变状态。当然，是否缓存最终还是由响应头和中间件策略决定，不是方法名单独决定一切。

安全性是面试里的常见误区。有人说 POST 比 GET 安全，这是不准确的。GET 参数暴露在 URL、浏览器历史、服务器访问日志和 Referer 里，所以不适合放密码、token 等敏感信息；POST body 不会直接出现在地址栏，但如果没有 HTTPS，同样可能被窃听。真正的安全依赖 HTTPS、认证授权、CSRF 防护、输入校验和敏感字段脱敏。

接口设计时要按业务语义选。查询列表、获取详情适合 GET；创建订单、提交表单适合 POST；更新资源可用 PUT/PATCH；删除资源可用 DELETE。对于支付、下单、发券这类接口，即使用 POST，也要设计幂等键，防止用户重复点击、网关重试或网络超时导致重复处理。

图解可以画成两列：GET 一列标“读取、幂等、query、可缓存、适合查询”；POST 一列标“提交、可能改变状态、body、默认不缓存、适合创建或动作”。最后收束：GET 和 POST 的第一差异是语义，其次才是参数位置和缓存行为。`
  },
  "context-switch": {
    type: "flow",
    nodes: [
      ["调度触发", "时间片用完、阻塞、唤醒或更高优先级任务到来"],
      ["保存现场", "保存寄存器、程序计数器和栈指针等状态"],
      ["切换任务", "调度器选择下一个线程或进程运行"],
      ["恢复现场", "加载新任务上下文继续执行"],
      ["缓存影响", "进程切换可能影响页表、TLB 和 CPU cache"],
      ["性能观察", "频繁切换会带来 CPU 空耗和吞吐下降"]
    ],
    detail: `上下文切换指 CPU 从当前任务切到另一个任务时，操作系统保存当前执行现场，再恢复另一个任务执行现场的过程。这里的“上下文”包括寄存器、程序计数器、栈指针、线程状态、内核调度信息等；如果是进程切换，还可能涉及地址空间、页表、TLB 和缓存局部性的变化。它不是业务代码显式调用的函数，而是操作系统调度多任务运行的基础动作。

触发原因很多。线程时间片用完、主动阻塞等待 IO、锁竞争进入等待、系统调用让出 CPU、更高优先级任务被唤醒，都可能导致调度器介入。调度器会把当前任务的现场保存到内核维护的数据结构里，再选择下一个可运行任务，把它的现场加载回 CPU，最后从它上次停下的位置继续执行。

上下文切换有成本，第一部分是直接成本：进入内核、保存和恢复寄存器、更新调度队列、切换内核栈等都要花 CPU 时间。第二部分是间接成本：CPU cache 里原来热的数据可能换了，TLB 也可能失效，新的任务需要重新把数据加载进缓存。进程切换通常比线程切换更重，因为进程之间地址空间隔离更强，页表和内存上下文变化更多。

这也是为什么线程不是越多越好。线程很多但都在抢同一颗 CPU，系统会把大量时间花在切换上，而不是实际执行任务。在线上表现可能是 CPU 使用率很高，但业务吞吐没有上升，甚至响应时间变差。锁竞争、过小线程池任务粒度、频繁阻塞唤醒、过度异步化，都可能制造大量不必要的切换。

排查时可以看 vmstat、pidstat、top、perf、线程栈和运行队列长度。比如 voluntary context switch 多，可能是线程主动等待 IO 或锁； involuntary context switch 多，可能是时间片竞争激烈。结合 CPU、负载、线程数和响应时间一起看，才能判断是不是上下文切换拖慢了系统。

图解可以画成“任务 A 运行 -> 保存 A 现场 -> 调度器选择 B -> 恢复 B 现场 -> B 继续运行”的时序。旁边标出成本来源：内核切换、寄存器保存、缓存失效、TLB 影响。最后回答时收一句：上下文切换让多任务成为可能，但过多切换会把 CPU 从做业务变成搬现场。`
  },
  "virtual-memory": {
    type: "structure",
    nodes: [
      ["虚拟地址", "进程看到的是独立连续的地址空间"],
      ["页表映射", "MMU 将虚拟地址转换成物理地址"],
      ["内存隔离", "进程之间不能随意访问彼此内存"],
      ["按需加载", "页面真正访问时再调入物理内存"],
      ["页面置换", "内存紧张时把冷页换出或回收"],
      ["故障表现", "缺页、抖动、swap、OOM 都和它有关"]
    ],
    detail: `虚拟内存解决的核心问题，是让每个进程都以为自己拥有一片独立、连续、完整的地址空间，而不必直接面对真实物理内存的碎片、大小和共享复杂度。程序里看到的地址是虚拟地址，真正访问内存时，CPU 通过 MMU 和页表把虚拟地址翻译成物理地址。这个抽象让操作系统可以实现隔离、保护、按需加载和页面置换。

第一层价值是隔离。每个进程有自己的虚拟地址空间，同一个虚拟地址在不同进程里可以映射到不同物理页。进程 A 不能随便读写进程 B 的内存，除非通过共享内存等受控机制。这样既提升安全性，也让程序开发更简单，不需要关心别的进程把物理内存占在哪里。

第二层价值是地址抽象。程序可以认为自己拥有从低地址到高地址的一整片空间，代码段、堆、栈、共享库、mmap 区域都按虚拟地址组织。物理内存可以是不连续的，操作系统通过页表把它们拼成进程看到的连续空间。TLB 则缓存常用地址翻译结果，减少每次访问都查页表的成本。

第三层价值是按需加载。程序启动时不一定把所有代码和数据都加载进内存，只有真正访问某个页面时，如果页表发现它还不在物理内存，就触发缺页中断，由操作系统把页面从磁盘文件、匿名页或 swap 中准备好，再恢复执行。正常的缺页是机制的一部分，不一定是错误。

第四层是内存回收和置换。当物理内存紧张时，操作系统会回收文件页、换出匿名页，或者触发 OOM 选择进程杀掉。过度依赖 swap 会导致性能急剧下降，因为磁盘速度远慢于内存，系统可能进入频繁换入换出的抖动状态。线上看到延迟飙升、系统 load 高、swap in/out 增多，就要警惕内存压力。

图解可以画成三层：进程虚拟地址空间、页表映射、物理内存页框。一次访问从虚拟地址出发，先查 TLB，未命中查页表，页面不存在时触发缺页中断。最后收束：虚拟内存不是单纯“把磁盘当内存”，而是一套地址抽象、隔离保护和内存管理机制。`
  },
  "distributed-id": {
    type: "compare",
    nodes: [
      ["唯一性", "全局不能重复，是最底线要求"],
      ["趋势递增", "对数据库索引和排序更友好"],
      ["本地生成", "减少中心节点依赖，提高吞吐"],
      ["号段模式", "数据库批量发号，本地消耗一段"],
      ["雪花算法", "时间戳、机器号、序列号组合生成"],
      ["边界风险", "时钟回拨、机器号冲突和容量规划要兜底"]
    ],
    detail: `分布式 ID 要解决的是多节点并发生成主键时，既不能重复，又要尽量高性能、可排序、易存储、可追踪。单机自增在一个数据库里很好用，但服务拆分、分库分表、多机房部署之后，多个节点同时发号，如果没有统一规则，就可能冲突。面试里要先说需求维度：全局唯一、趋势递增、性能高、可用性好、长度可控、最好能携带时间或业务信息。

UUID 最简单，本地即可生成，不依赖中心服务，冲突概率极低。但它通常比较长，而且无序，作为数据库主键会让 B+Tree 插入更随机，页分裂和索引膨胀更明显。它适合对可读性和索引局部性要求不高的场景，不一定适合高频写入的核心业务表。

数据库自增最直观，但在分布式高并发下容易成为瓶颈和单点。号段模式是改进：服务一次从数据库取一段 ID，比如 1000 个，之后在本地内存里快速发号，用完再取下一段。它减少了数据库访问频率，性能更好，但要处理服务重启导致号段浪费、数据库不可用时无法续段、多个业务号段隔离等问题。

Redis 自增也常见，INCR 性能高、语义简单，但依赖 Redis 的可用性和持久化策略。如果 Redis 故障恢复后丢失最近计数，就可能产生重复；如果跨机房使用，还要考虑延迟和一致性。它更适合对 Redis 依赖已经很强、并且能接受相应运维成本的系统。

雪花算法是面试高频方案，通常由时间戳、机器 ID、序列号组成。它本地生成、趋势递增、性能高，还能从 ID 中大致反推出时间。但它对机器 ID 分配和时钟很敏感：机器号重复会冲突，时钟回拨可能生成比过去更小甚至重复的 ID，同一毫秒序列号耗尽也要等待下一毫秒。生产里要有机器号注册、时钟回拨保护、监控告警和容量评估。

图解可以画成一个方案对比表：UUID 简单但无序，数据库自增简单但中心化，号段模式性能好但有续段风险，Redis 自增快但依赖中间件，雪花算法本地高性能但怕时钟和机器号。最后收束：分布式 ID 没有绝对最佳，核心是按业务选择唯一性、排序性、可用性和运维复杂度之间的平衡。`
  },
  "rbac": {
    type: "structure",
    nodes: [
      ["用户", "具体登录主体，可绑定多个角色"],
      ["角色", "一组权限的集合，承接组织职责"],
      ["权限", "菜单、按钮、接口、资源动作和数据范围"],
      ["授权关系", "用户-角色、角色-权限通常都是多对多"],
      ["鉴权执行", "后端按用户权限判断是否允许访问"],
      ["扩展治理", "角色膨胀、数据权限、缓存失效和审计"]
    ],
    detail: `RBAC 是基于角色的访问控制，核心是把“用户直接拥有大量权限”改成“用户拥有角色，角色拥有权限”。这样做的价值是解耦授权关系：员工岗位变化时，调整角色即可；某个岗位新增一个接口权限时，改角色权限即可，不需要逐个修改每个用户。面试里要先把用户、角色、权限三者关系说清楚，再讲为什么它能降低权限维护成本。

用户是具体登录主体，角色是一组职责或岗位能力的抽象，权限则是能访问什么资源、执行什么动作。权限不只包括前端菜单，还包括按钮、接口、数据范围、字段级脱敏等。真正安全的系统一定以后端接口鉴权为准，前端菜单隐藏只能改善体验，不能作为安全边界。

数据模型上，用户和角色通常是多对多，角色和权限也是多对多。常见表包括 user、role、permission、user_role、role_permission。权限本身可以设计成资源加动作，比如 order:read、order:update，也可以按菜单树组织。复杂系统还会引入租户、部门、岗位、组织层级、资源归属等维度。

鉴权流程可以按请求链路讲：用户登录后拿到身份标识，后端根据用户 ID 查询角色和权限，或从 token、缓存中取权限快照；请求进入接口时，框架或拦截器判断当前用户是否具备访问该资源的权限；如果涉及数据权限，还要在 SQL、查询条件或领域服务里限制只能看到自己部门、自己创建或授权范围内的数据。

RBAC 的问题是角色膨胀。业务越复杂，角色可能越来越多，最后出现“销售主管华东临时版”这类难维护角色。解决方式包括角色分层、权限模板、角色继承、ABAC 属性控制、数据权限单独建模、临时授权设置有效期等。权限变更还要考虑缓存失效，否则用户被收回权限后仍能短时间访问。

图解可以画成三层：用户层、角色层、权限层，中间两组多对多关系；旁边再画请求鉴权链路：登录身份 -> 权限解析 -> 接口校验 -> 数据范围过滤 -> 审计记录。最后收一句：RBAC 解决的是授权关系的可维护性，但真正落地还要补后端强校验、数据权限、缓存失效和审计追踪。`
  },
  "pagination-design": {
    type: "compare",
    nodes: [
      ["offset 分页", "实现简单，但深分页会扫描和丢弃大量行"],
      ["游标分页", "基于上一页最后位置继续查，性能更稳定"],
      ["排序稳定", "必须有唯一且稳定的排序条件"],
      ["索引配合", "where 和 order by 要尽量走联合索引"],
      ["总数成本", "count 可能很贵，必要时异步或近似"],
      ["一致性问题", "翻页期间新增删除会导致重复或漏数据"]
    ],
    detail: `分页设计看起来简单，真正线上容易出问题的是深分页性能、排序稳定性和数据变化带来的重复漏读。最常见的 offset 分页是 limit offset, size，比如第 1000 页每页 20 条，数据库可能要先扫描并丢弃前 19980 条，再返回后 20 条。页数越深，丢弃成本越高，尤其当排序字段没有合适索引时，还可能触发 filesort 和大量回表。

offset 分页的优点是简单，适合后台管理、页数不深、数据量不大、需要跳页的场景。它的缺点是深分页慢，而且在翻页过程中如果有新增或删除，用户可能看到重复数据或漏数据。比如第一页看完后有新记录插入到最前面，第二页按 offset 查时，原第一页的最后一条可能又被推到第二页。

游标分页，也叫 seek pagination，是基于上一页最后一条记录继续查。比如按 create_time 和 id 倒序排序，下一页条件写成 create_time 小于上一页最后时间，或者 create_time 相等时 id 小于上一页最后 id。这样数据库可以沿着索引继续向后扫，不需要跳过大量行，性能更稳定。它的缺点是不方便任意跳到第 N 页，也不天然返回精确总页数。

排序条件必须稳定。只按 create_time 排序，如果同一秒有很多记录，翻页边界可能不确定，所以通常要加唯一字段做兜底排序，比如 order by create_time desc, id desc。对应索引也要配合，比如 (user_id, create_time, id) 或 (status, create_time, id)，让 where 和 order by 尽量走同一棵联合索引，减少排序和回表成本。

总数也是成本点。很多接口喜欢返回 total，但大表 count 可能很贵，尤其条件复杂时。可以根据场景选择精确 count、缓存 count、异步统计、只返回 hasMore，或者在搜索系统里使用近似总数。不要为了一个不关键的总页数拖慢核心列表接口。

图解可以画两条路径：offset 分页从索引头部扫过大量记录再丢弃，游标分页从上次最后一条继续向后读取。旁边标注三条设计原则：稳定排序、联合索引、限制深跳页。最后回答时补一句：分页不是只写 limit，而是要保证性能稳定、结果顺序稳定、数据变化时体验可接受。`
  },
  "sort-stability": {
    type: "compare",
    nodes: [
      ["稳定定义", "相等元素排序后仍保持原相对顺序"],
      ["不稳定定义", "相等元素顺序可能被交换"],
      ["多关键字排序", "先按次关键字排，再稳定按主关键字排"],
      ["常见稳定", "冒泡、插入、归并通常稳定"],
      ["常见不稳定", "选择、快速、堆排序通常不稳定"],
      ["工程判断", "是否需要稳定取决于相等元素的业务含义"]
    ],
    detail: `排序稳定性说的是：如果两个元素的排序关键字相等，排序完成后它们的相对顺序是否保持不变。保持不变就是稳定排序，可能改变就是不稳定排序。这里的“相等”不是对象完全相同，而是当前比较器认为它们相等。比如学生按成绩排序，两个学生成绩都是 90 分，如果排序后他们仍保持原来的先后顺序，这次排序就是稳定的。

稳定性最典型的价值在多关键字排序。假设要按成绩从高到低排序，成绩相同再按姓名排序。可以先按姓名排一遍，再用稳定排序按成绩排一遍。第二次排序会调整不同成绩之间的顺序，但同成绩的人仍保留姓名顺序。数据库里的 order by score desc, name asc 本质也是在定义多关键字顺序，只是由数据库执行。

常见稳定排序包括冒泡排序、插入排序和归并排序，前提是实现时相等元素不乱交换。常见不稳定排序包括选择排序、快速排序和堆排序。比如选择排序每次把最小元素和当前位置交换，可能把相等元素的相对顺序打乱；快速排序分区时也可能让相等元素跨区移动；堆排序不断把堆顶和末尾交换，也不保证相等元素顺序。

但稳定性不是越稳定越高级，而是看业务需不需要。纯数字排序只关心数值大小，相等元素之间没业务差别，稳定性通常无所谓。可如果元素是对象，排序关键字只是其中一个字段，相等元素的原顺序可能携带了上一次排序、录入顺序、优先级或业务含义，这时稳定性就很重要。

面试里还要补复杂度和实现取舍。归并排序稳定，时间复杂度 O(n log n)，但通常需要额外空间；快速排序平均很快，原地性好，但不稳定且最坏情况可能退化；插入排序稳定，适合小规模或基本有序数据。Java 里对象数组排序使用的 TimSort 是稳定排序，基本类型数组排序则不需要保留对象相对顺序，策略也不同。

图解可以画四个带标签的元素：A(90)、B(80)、C(90)、D(70)。按分数排序后，如果 A 仍在 C 前面，就是稳定；如果 C 跑到 A 前面，就是不稳定。最后收束：稳定性关注的不是大小是否排对，而是相等关键字背后的原始顺序有没有被保留。`
  },
  "service-discovery": {
    type: "flow",
    nodes: [
      ["服务注册", "实例启动后上报地址和元数据"],
      ["健康检查", "注册中心剔除异常实例"],
      ["服务发现", "调用方获取可用实例列表"],
      ["负载均衡", "按策略选择一个实例调用"],
      ["变更推送", "实例上下线及时通知调用方"],
      ["容错治理", "重试、熔断、权重、灰度"]
    ],
    detail: `服务注册与发现解决的是微服务环境里“调用方如何找到服务实例”的问题。单体系统里服务地址固定，直接配置 IP 和端口就能调用；微服务里实例会动态扩缩容、发布重启、故障下线，地址经常变化。如果还靠手写配置，维护成本高，也容易调用到已经下线的实例。注册中心就是用来管理服务实例地址和状态的组件。

服务注册是实例启动后把自己的服务名、IP、端口、版本、权重、机房、健康状态等元数据上报给注册中心。常见注册中心有 Nacos、Eureka、Consul、etcd、ZooKeeper 等。注册后，实例通常要通过心跳或租约续期告诉注册中心自己还活着。如果心跳超时，注册中心会把实例标记为不健康或剔除。

服务发现是调用方按服务名获取可用实例列表。发现方式有客户端发现和服务端发现。客户端发现是调用方从注册中心拿列表，在本地做负载均衡，比如 Ribbon、Spring Cloud LoadBalancer。服务端发现是调用方请求网关或负载均衡器，由它转发到后端实例，比如 Kubernetes Service、云负载均衡。两种方式各有取舍：客户端发现灵活，服务端发现对调用方更简单。

注册中心还要处理变更。实例上线、下线、权重调整、灰度版本发布，都要及时通知调用方。可以轮询拉取，也可以长轮询或 watch 推送。调用方通常会缓存实例列表，避免每次调用都访问注册中心。缓存带来可用性，也带来一致性延迟，所以要处理短时间调用到已下线实例的情况，配合重试、超时和熔断。

健康检查很关键。仅仅进程存在不代表服务可用，还要看端口、依赖、线程池、数据库连接等。健康检查太严格可能误剔除，太宽松又会保留坏实例。优雅下线也很重要：实例发布前先从注册中心摘除或标记不接流量，等待已有请求处理完，再关闭进程，避免发布期间大量失败。

图解可以画成链路：服务实例启动后注册到注册中心，注册中心维护实例列表和健康状态；消费者订阅服务列表，发生调用时本地负载均衡选择实例；实例下线时注册中心推送变更，消费者更新缓存。旁边列治理能力：权重、灰度、就近访问、重试、熔断、超时。最后一句：服务发现不是简单存 IP，而是动态服务治理的入口。`
  }
};

const detailProfiles = [
  {
    test: /Java|JVM|Spring|MyBatis/,
    focus: "对象状态、运行时机制、框架代理、异常边界和排查入口",
    evidence: "日志、单元测试、线程栈、JFR/GC 日志、最小复现和配置对照",
    risk: "不要只背 API 名称，要说清触发条件、默认行为和失效边界"
  },
  {
    test: /MySQL|Redis/,
    focus: "读写路径、索引或数据结构、并发控制、一致性窗口和性能成本",
    evidence: "explain、慢日志、锁等待、命中率、内存曲线、QPS、P99 和错误率",
    risk: "不要把数据库或缓存讲成万能组件，要主动补上写放大、热点和补偿手段"
  },
  {
    test: /消息队列|MQ|分布式/,
    focus: "链路状态、失败重试、幂等、顺序、积压、注册发现和最终一致性",
    evidence: "TraceId、消费延迟、重试次数、死信数量、下游耗时和最终数据状态",
    risk: "不要只说引入中间件，要说明重复、丢失、乱序、超时和恢复策略"
  },
  {
    test: /网络/,
    focus: "协议状态、系统调用、内核资源、时序变化和故障表现",
    evidence: "抓包、连接状态、RTT、重传率、超时日志、网关日志、监控指标和最小压测",
    risk: "不要只背定义，要能把一次请求、一次调度或一次内存访问的路径讲出来"
  },
  {
    test: /操作系统/,
    focus: "进程线程、内存管理、调度开销、内核资源和系统调用路径",
    evidence: "top、vmstat、pidstat、perf、dmesg、缺页次数、swap、上下文切换和内存曲线",
    risk: "不要只背定义，要能把一次调度、一次内存访问或一次系统调用的路径讲出来"
  },
  {
    test: /项目|工程/,
    focus: "业务目标、容量边界、数据模型、权限一致性、灰度降级和可观测性",
    evidence: "接口访问日志、审计日志、权限变更记录、灰度记录、数据核对、告警和回滚记录",
    risk: "不要只画架构图，要把异常分支、兜底开关和上线验证一起讲完整"
  },
  {
    test: /算法/,
    focus: "问题抽象、输入规模、状态定义、复杂度、稳定性和边界样例",
    evidence: "空输入、重复元素、极值、无解、顺序变化和复杂度推导",
    risk: "不要只背模板，要解释为什么当前数据特征适合这个算法"
  }
];

function cleanText(text) {
  return String(text ?? "")
    .replace(/\*\*/g, "")
    .replace(/`/g, "")
    .replace(/\[(.*?)\]\(.*?\)/g, "$1")
    .replace(/<[^>]+>/g, "")
    .replace(/\s+/g, " ")
    .trim();
}

function stripTerminalPunctuation(text) {
  return cleanText(text).replace(/[。！？!?]+$/g, "");
}

function section(raw, title) {
  const match = raw.match(new RegExp(`\\n## ${title}\\s+([\\s\\S]*?)(?=\\n## |$)`));
  return match?.[1]?.trim() ?? "";
}

function sentenceFrom(text, fallback) {
  const clean = cleanText(text);
  return stripTerminalPunctuation(clean.split(/[。！？!?]/).map((item) => item.trim()).find(Boolean) || fallback);
}

function extractAnswerPoints(raw) {
  const answer = section(raw, "面试回答");
  const bullets = answer
    .split("\n")
    .map((line) => line.trim())
    .filter((line) => /^[-*]\s+/.test(line) || /^\d+\.\s+/.test(line))
    .map((line) => cleanText(line.replace(/^[-*]\s+/, "").replace(/^\d+\.\s+/, "")))
    .filter((line) => line.length > 2);

  if (bullets.length >= 3) {
    return bullets.slice(0, 6);
  }

  return cleanText(answer)
    .split(/[。；;]/)
    .map((item) => item.trim())
    .filter((item) => item.length > 4)
    .slice(0, 6);
}

function getDetailProfile(category) {
  return detailProfiles.find((profile) => profile.test.test(category ?? "")) ?? {
    focus: "核心概念、运行机制、适用边界、失败表现和工程取舍",
    evidence: "日志、指标、测试、压测、链路追踪和最小复现",
    risk: "不要停留在定义，要把原因、过程、影响和解决方案串起来"
  };
}

function inferVisualType(question) {
  const text = `${question.title} ${question.category} ${(question.tags ?? []).join(" ")}`;

  if (/区别|对比| vs |VS|和.*区别|GET|POST|TCP.*UDP/.test(text)) return "compare";
  if (/握手|调用|发送|消费|注册|发现|生成|保证|传输|提交|切换|映射|分页|排查|处理/.test(text)) return "flow";
  if (/系统|设计|权限|RBAC|项目|模型|方案/.test(text)) return "scenario";
  return "structure";
}

function compactNodeLabel(text, fallback) {
  const clean = cleanText(text || fallback);
  const first = clean.split(/[，,：:；;。]/).map((item) => item.trim()).find(Boolean) ?? clean;

  if (first.length <= 12) {
    return first;
  }

  return `${first.slice(0, 12).trim()}…`;
}

function makeGeneratedNodes(question, raw) {
  const points = extractAnswerPoints(raw);
  const summary = question.summary || sentenceFrom(section(raw, "一句话结论"), "先抓住问题背景，再讲清机制和边界");
  const base = points.length >= 4 ? points : [
    "问题背景",
    summary,
    "核心机制",
    "关键风险",
    "工程处理",
    "面试收束"
  ];
  const labels = base.slice(0, 6).map((point, index) => compactNodeLabel(point, ["问题背景", "核心机制", "关键风险", "工程处理", "验证闭环", "面试收束"][index]));

  while (labels.length < 6) {
    labels.push(["问题背景", "核心机制", "关键风险", "工程处理", "验证闭环", "面试收束"][labels.length]);
  }

  return labels.map((label, index) => [
    label,
    [
      summary,
      "把机制拆成可解释的链路",
      "说明什么时候会失效或变慢",
      "结合项目给出处理动作",
      "用日志、指标或测试验证",
      "最后补一句取舍和边界"
    ][index]
  ]);
}

function makeGeneratedDetail(question, raw) {
  const title = String(question.title ?? question.slug).replace(/[？?]$/g, "");
  const category = question.category ?? "";
  const summary = stripTerminalPunctuation(question.summary || sentenceFrom(section(raw, "一句话结论"), "先给结论，再解释机制、边界和落地方式"));
  const points = extractAnswerPoints(raw);
  const profile = getDetailProfile(category);
  const primary = points[0] ?? summary;
  const secondary = points[1] ?? profile.focus;
  const tertiary = points[2] ?? profile.risk;
  const tagText = (question.tags ?? []).length ? question.tags.join("、") : category || "这个知识点";

  return {
    type: inferVisualType(question),
    nodes: makeGeneratedNodes(question, raw),
    detail: `${title} 这道题不能只按定义回答，因为面试官通常不是在确认你会不会背概念，而是在看你能不能把它放回真实系统的链路里。可以先用一句话定调：${summary}。有了这个结论，再往下拆“为什么需要它、它靠什么机制工作、什么时候会出问题、项目里怎么验证”，答案就会比简单罗列更稳定。  

第一层要讲背景。${tagText} 相关问题通常都不是孤立出现的，而是和流量、并发、数据规模、调用链路或资源限制绑在一起。回答时先说明它解决的矛盾：一边是业务希望简单、快速、稳定地完成请求，另一边是系统内部存在状态变化、失败重试、性能成本和一致性边界。把这个矛盾讲出来，后面再解释机制才不会像背书。  

第二层要讲核心机制：${primary}。这里最好把动作讲成链路，而不是只说名词。比如谁先发生、谁依赖谁、哪个状态会改变、失败时会留下什么痕迹。如果能把输入、处理、输出和副作用串起来，面试官继续追问时，你也能沿着链路往下展开，而不是被单点概念卡住。  

第三层要讲边界和代价：${secondary}。任何方案都有适用范围，真正成熟的回答要主动说明它在哪些条件下有效，哪些条件下会失效或成本变高。比如数据量变大、并发提升、节点故障、网络抖动、下游变慢、配置不一致，都可能让原本简单的方案暴露问题。这里可以补一句 ${profile.risk}。  

第四层要落到工程排查。项目里遇到类似问题，不要只说“改配置”或“加组件”，而是先看信号：${profile.evidence}。能用证据定位问题，说明你知道这件事在线上会长什么样；能说出验证方式，说明你不是只会写 happy path。  

最后用一句话收束：${tertiary}。回答模板可以是“先给结论，再讲机制，再补边界，最后落到项目验证”。图解时适合把它画成六个节点：问题背景、核心机制、关键风险、工程处理、验证闭环、面试收束。这样既能覆盖概念，也能展示你对真实系统复杂度的判断。`
  };
}

function ensureDetailMinimum(data, question, raw) {
  if (countDetailChars(data.detail) >= 1000) {
    return data;
  }

  const profile = getDetailProfile(question.category ?? "");
  const summary = stripTerminalPunctuation(question.summary || sentenceFrom(section(raw, "一句话结论"), "先给结论，再解释机制、边界和落地方式"));
  const supplement = `如果还要把答案讲得更像项目经验，可以用验证闭环收束：先说明这个问题在什么条件下出现，再说线上能从哪些信号确认，最后讲处理后怎么证明风险已经下降。对这道题来说，可以围绕“${summary}”展开，再结合 ${profile.evidence} 做交叉验证。这样回答不会停在概念层，而是把发现问题、定位原因、采取动作、验证结果串成一条完整链路。最后再补一句边界：方案要和业务容忍度匹配，不能只追求理论正确，还要考虑性能、复杂度、维护成本和失败后的恢复路径。`;

  let detail = `${data.detail.trim()}\n\n${supplement}`;

  if (countDetailChars(detail) < 1000) {
    detail = `${detail}\n\n进一步展开时，可以把答案再压到一个判断框架里：先问它解决的是定位效率、吞吐能力、一致性还是可维护性；再问代价落在哪里，是更多存储、更复杂链路、更高写入成本，还是更严格的运维要求；最后问上线后怎么证明它有效。只要能把“问题、机制、代价、验证”四件事讲完整，这道题就不再是孤立概念，而是一次可落地的工程选择。`;
  }

  return {
    ...data,
    detail
  };
}

function walk(dir) {
  return fs.readdirSync(dir, { withFileTypes: true }).flatMap((entry) => {
    const fullPath = path.join(dir, entry.name);

    if (entry.isDirectory()) {
      return walk(fullPath);
    }

    return entry.isFile() && entry.name.endsWith(".md") ? [fullPath] : [];
  });
}

function getQuestions() {
  return walk(questionRoot)
    .map((file) => {
      const raw = fs.readFileSync(file, "utf8");
      const { data } = matter(raw);

      return {
        file,
        slug: data.slug,
        title: data.title,
        category: data.category,
        summary: data.summary,
        tags: Array.isArray(data.tags) ? data.tags : [],
        order: typeof data.order === "number" ? data.order : 999
      };
    })
    .sort((a, b) => a.order - b.order || String(a.title).localeCompare(String(b.title), "zh-CN"));
}

function countDetailChars(markdown) {
  return markdown.replace(/\s/g, "").length;
}

function upsertSection(raw, title, body) {
  const sectionRegex = new RegExp(`\\n## ${title}\\s+[\\s\\S]*?(?=\\n## |$)`);
  const section = `\n## ${title}\n\n${body.trim()}\n`;

  if (sectionRegex.test(raw)) {
    return raw.replace(sectionRegex, section);
  }

  const insertBefore = raw.indexOf("\n## 深挖理解") !== -1 ? "\n## 深挖理解" : "\n## 记忆钩子";
  const index = raw.indexOf(insertBefore);

  if (index === -1) {
    return `${raw.trimEnd()}\n${section}`;
  }

  return `${raw.slice(0, index).trimEnd()}\n${section}${raw.slice(index)}`;
}

function updateVisuals(slug, data) {
  const visuals = JSON.parse(fs.readFileSync(visualPath, "utf8"));
  const current = visuals[slug] ?? {};

  visuals[slug] = {
    ...current,
    type: data.type,
    nodes: data.nodes.map(([label, detail], index) => ({
      label,
      detail,
      tone: index === 0 ? "main" : /风险|丢|不可|异常|拒绝|旧值|回收|故障/.test(`${label}${detail}`) ? "warn" : "safe"
    })),
    prompt: `站内图解：${data.nodes.map(([label]) => label).join(" -> ")}。要求用中文短标签画成${data.type}图，突出顺序、结构和风险落点。`
  };

  fs.writeFileSync(visualPath, `${JSON.stringify(visuals, null, 2)}\n`, "utf8");
}

function readProgress(total) {
  if (fs.existsSync(progressPath)) {
    return JSON.parse(fs.readFileSync(progressPath, "utf8"));
  }

  return {
    batchSize,
    total,
    completedSlugs: [],
    batches: []
  };
}

function writeProgress(progress) {
  fs.writeFileSync(progressPath, `${JSON.stringify(progress, null, 2)}\n`, "utf8");
}

const requestedBatch = Number.parseInt(process.argv.find((item) => item.startsWith("--batch="))?.split("=")[1] ?? "1", 10);
const questions = getQuestions();
const batchStart = (requestedBatch - 1) * batchSize;
const batch = questions.slice(batchStart, batchStart + batchSize);
const progress = readProgress(questions.length);
const completed = new Set(progress.completedSlugs);
const results = [];

for (const question of batch) {
  const raw = fs.readFileSync(question.file, "utf8");
  const data = ensureDetailMinimum(details[question.slug] ?? makeGeneratedDetail(question, raw), question, raw);
  const nextRaw = upsertSection(raw, "详细讲解", data.detail);
  fs.writeFileSync(question.file, nextRaw, "utf8");
  updateVisuals(question.slug, data);
  completed.add(question.slug);
  results.push({
    slug: question.slug,
    file: question.file,
    detailChars: countDetailChars(data.detail),
    visualNodes: data.nodes.length
  });
}

progress.completedSlugs = Array.from(completed);
progress.lastUpdated = new Date().toISOString();
progress.currentBatch = requestedBatch;
progress.nextBatch = requestedBatch + 1;
progress.batches = [
  ...(progress.batches ?? []).filter((item) => item.batch !== requestedBatch),
  {
    batch: requestedBatch,
    startIndex: batchStart + 1,
    endIndex: batchStart + batch.length,
    completedAt: progress.lastUpdated,
    slugs: results.map((item) => item.slug),
    minDetailChars: Math.min(...results.map((item) => item.detailChars)),
    maxDetailChars: Math.max(...results.map((item) => item.detailChars))
  }
].sort((a, b) => a.batch - b.batch);

writeProgress(progress);

console.log(JSON.stringify({
  batch: requestedBatch,
  processed: results.length,
  completedTotal: progress.completedSlugs.length,
  nextBatch: progress.nextBatch,
  results
}, null, 2));
