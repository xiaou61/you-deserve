# 题目质检 Batch 12：Java 111-120

审查时间：2026-05-03  
范围：排序后的第 111-120 道题，`content/questions/java/stream-api.md` 至 `content/questions/java/threadlocal-inheritable.md`  
审查口径：事实准确性、表达清晰度、内容深度、面试可用度、是否存在生成模板污染。  

## 总体结论

这一批 Java 字符串、锁、线程池和 ThreadLocal 题基础答案大多可用，`thread-pool-core-parameters.md`、`synchronized-reentrantlock.md`、`string-equals-hashcode.md` 的正文质量较好。主要问题仍是模板化扩写和若干拼接/截断：

- `thread-interrupt.md`、`thread-pool-rejection-policy.md`、`threadlocal-inheritable.md` 存在重复 `## 图解提示`。
- `string-stringbuilder-stringbuffer.md` 有一句“频繁用  拼接字符串”，缺失 `+` 符号，影响阅读。
- `string-intern.md` 存在列表拼接，如“调用 intern() 时： - 如果池中已有...”，需润色。
- 10 篇都命中通用模板，线程/字符串题后半段仍出现“数据量或并发量扩大 10 倍”“灰度、压测和回滚策略”等不贴题表达。

## 逐题记录

| 序号 | 文件 | 结论 | 严重度 | 问题与建议 |
|---:|---|---|---|---|
| 1 | `content/questions/java/stream-api.md` | 基础正确，流水线机制可补 | P2 | filter/map/sorted/collect/groupingBy/reduce、并行流风险和副作用都讲对。建议补中间操作惰性、终止操作触发执行、短路操作、stream 不能复用、原集合修改风险、`Collectors.toMap` key 冲突处理、parallelStream 使用公共 ForkJoinPool 的风险。后半段模板偏重。 |
| 2 | `content/questions/java/string-equals-hashcode.md` | 正文扎实，可保留 | P2 | equals/hashCode 契约、HashMap/HashSet 查找流程、可变字段作为 key 的风险讲得很好。建议补 `Objects.equals/hash`、Lombok 生成策略、继承场景 getClass vs instanceof、实体 ID 为空时 equals 设计。图解节点截断。 |
| 3 | `content/questions/java/string-intern.md` | 核心正确，版本细节和拼接需补 | P2 | intern 返回常量池引用、不要盲目优化、常量池也占内存都正确。建议补 JDK 7 后字符串常量池移到堆、`new String("abc")` 对象数量的条件性、编译期常量折叠、运行期拼接、intern 后可能增加 GC 压力。`详细讲解` 有列表拼接问题。 |
| 4 | `content/questions/java/string-stringbuilder-stringbuffer.md` | 主体正确，有明显缺字 | P1 | String 不可变、StringBuilder 单线程高效、StringBuffer 同步安全都正确。但正文出现“频繁用  拼接字符串”，缺失 `+`，属于可见表达错误。建议补 JDK 9+ 字符串拼接可能用 `invokedynamic`、StringBuilder 扩容成本、StringBuffer 现实场景很少。图解节点截断。 |
| 5 | `content/questions/java/synchronized-reentrantlock.md` | 正文质量较好 | P2 | 自动释放、显式 unlock、可中断、公平锁、Condition、AQS、选择建议都完整。建议补 `tryLock(timeout)`、Condition 多条件队列示例、`synchronized` monitor wait set、ReentrantLock 公平锁吞吐下降、死锁排查入口。后半段模板仍需清理。 |
| 6 | `content/questions/java/thread-interrupt.md` | 概念正确，追问占位 | P2 | 协作式中断、阻塞方法抛 `InterruptedException` 并清标记、捕获后恢复中断标记都正确。建议补 `Thread.interrupted()` 与 `isInterrupted()` 区别、NIO/LockSupport 对中断的表现、线程池 shutdownNow、不要吞中断、业务循环退出示例。重复 `图解提示`。 |
| 7 | `content/questions/java/thread-pool-core-parameters.md` | 内容很强，可作为优质稿 | P2 | core/max/queue/keepAlive/threadFactory/rejection、任务流转、队列选择、Executors 风险、参数估算、监控指标都讲得扎实。建议补 `prestartCoreThread`、`allowCoreThreadTimeOut`、不同队列对 maxPoolSize 的影响再明确一点。主要问题是后半段仍有模板化追问。 |
| 8 | `content/questions/java/thread-pool-rejection-policy.md` | 方向正确，业务兜底可加强 | P2 | 四种拒绝策略和 CallerRuns 反压讲对。建议补自定义 RejectedExecutionHandler、拒绝计数/日志/告警、不同业务对丢弃的可接受性、CallerRuns 对上游线程的阻塞风险、不要无限重试。重复 `图解提示`，图解节点 `DiscardOldestPolic…` 截断。 |
| 9 | `content/questions/java/thread-states.md` | 基础准确，dump 排查可补 | P2 | 6 种线程状态、RUNNABLE 不等于正在跑、BLOCKED/WAITING 差异正确。建议补 `TIMED_WAITING` 常见来源、`parking to wait for` 与 LockSupport/AQS、BLOCKED 只针对 monitor 锁、线程 dump 中 deadlock/blocked owner 信息如何看。后半段模板泛化。 |
| 10 | `content/questions/java/threadlocal-inheritable.md` | 主体正确，替代方案可补 | P2 | 新建子线程继承、线程池复用导致脏数据、任务结束 remove 都准确。建议补 `TransmittableThreadLocal` 或任务包装器、MDC/TraceId 透传、InheritableThreadLocal 只在 Thread 创建时复制、父子后续修改不自动同步、线程池禁用/清理策略。重复 `图解提示`，图解节点截断。 |

## 优先修复建议

1. 先修 `string-stringbuilder-stringbuffer.md` 的缺失 `+` 符号，这是可见表达错误。
2. 修复 `string-intern.md` 的列表拼接，并补 JDK 版本差异和常量折叠边界。
3. 合并 3 篇重复 `图解提示`，清理所有省略号截断节点。
4. 线程相关题统一补专项追问：interrupt 的中断标记差异、线程池拒绝后的业务兜底、线程状态在 dump 中怎么定位、InheritableThreadLocal 在线程池中的上下文污染。
5. `thread-pool-core-parameters.md` 正文质量较高，建议优先保留正文，只清理模板段和加强图解。
