---
title: "Kafka 幂等生产者是什么？"
slug: "kafka-idempotent-producer"
category: "消息队列"
tags: ["Kafka", "幂等生产者", "消息重复", "可靠性"]
difficulty: "hard"
route: "Java 后端上岸路线"
scene: "二面加分"
order: 2610
summary: "幂等生产者通过 Producer ID 和序列号避免生产者重试导致的单分区重复写入。"
---

## 一句话结论

Kafka 幂等生产者用于避免生产者重试时在同一分区写入重复消息，核心依赖 Producer ID 和序列号。

## 通俗解释

你寄作业时每一页都有编号。即使快递员重复送了一次，老师看到编号重复就不会收两份。

## 面试回答

生产者发送消息时可能因为网络超时重试。如果 broker 其实已经写入成功，只是响应丢了，重试就可能造成重复。

幂等生产者会为每个生产者分配 ID，并对每个分区维护序列号。broker 可以根据序列号识别重复请求，避免重复写入。

它提升了生产端可靠性，但不等于业务端完全 exactly once，消费端仍然要做幂等。

## 常见追问

### 幂等生产者解决跨分区重复吗？

主要解决单生产者、单分区维度的重复写入问题，跨分区和业务处理仍需额外设计。

### 开了幂等生产者就不用消费幂等了吗？

不是。消费者重试、offset 提交失败等仍可能导致重复消费。

## 易错点

- 不要把生产者幂等等同于业务幂等。
- 它主要解决生产重试导致的重复写入。
- 可靠性还需要 acks、重试、副本等配置配合。

## 记忆钩子

**幂等生产者给每条消息页码，重复页不再收。**
