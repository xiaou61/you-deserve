---
title: "分布式选主是什么？"
slug: "distributed-leader-election"
category: "分布式系统"
tags: ["分布式", "选主", "一致性", "高可用"]
difficulty: "hard"
route: "Java 后端上岸路线"
scene: "二面加分"
order: 2100
summary: "分布式选主是在多个节点中选出一个主节点负责协调任务，常用于高可用和任务调度。"
---

## 一句话结论

分布式选主是在多个节点中选出一个 leader，由它负责协调写入、调度或管理，leader 故障后再选出新的 leader。

## 通俗解释

一个小组需要组长安排任务。组长在时大家听组长的，组长离线后要重新选一个，避免多人同时乱指挥。

## 面试回答

选主常用于：

- 主从架构故障切换。
- 分布式任务调度，只允许一个节点执行。
- 集群元数据管理。
- 分布式锁和协调场景。

常见实现可以依赖 ZooKeeper、etcd、Redis 或数据库。强一致要求高的场景更常用 ZooKeeper、etcd 这类协调组件。

关键问题是避免脑裂，也就是网络分区后出现多个 leader。

## 常见追问

### 什么是脑裂？

脑裂是集群因为网络分区等原因出现多个节点都认为自己是主节点，可能导致数据冲突。

### 为什么选主要依赖一致性组件？

因为选主本质要让集群对“谁是主”达成一致，需要可靠的协调机制。

## 易错点

- 不要只说“抢锁”而不考虑锁失效和脑裂。
- leader 故障检测不能太敏感，否则容易频繁切换。
- 选主后的任务执行也要做幂等。

## 记忆钩子

**选主就是选组长，最怕两个组长同时发号施令。**
