---
title: "API 网关在微服务里有什么作用？"
slug: "api-gateway"
category: "分布式系统"
tags: ["网关", "微服务", "限流", "鉴权"]
difficulty: "medium"
route: "Java 后端上岸路线"
scene: "项目高频"
order: 560
summary: "从统一入口、路由转发、鉴权、限流、日志和灰度发布理解网关。"
---

## 一句话结论

API 网关是微服务系统的统一入口，负责路由转发、鉴权、限流、日志、跨域、灰度等通用能力。

## 通俗解释

网关像小区门卫。外面的人不用知道每栋楼在哪，先到门卫处登记，门卫检查身份，再告诉你去几号楼。

这样每个服务就不用重复处理所有通用入口逻辑。

## 面试回答

API 网关常见作用：

- 统一入口，屏蔽内部服务地址。
- 路由转发，把请求转发到对应服务。
- 认证鉴权，拦截非法请求。
- 限流熔断，保护后端服务。
- 日志监控，记录请求链路。
- 协议转换、跨域处理、灰度发布。

网关不能承担所有业务逻辑。它适合做通用入口能力，复杂业务仍应放在对应服务里。

## 常见追问

### 网关会不会成为单点？

会有这个风险，所以网关本身要多实例部署，前面再接负载均衡，并做好监控和限流。

### 鉴权放网关就够了吗？

网关可以做统一鉴权，但核心服务也要有必要的权限兜底，避免内部调用绕过网关。

## 易错点

- 不要把网关写成大杂烩业务服务。
- 不要忽略网关自身高可用。
- 不要只说“转发请求”，要补充鉴权限流监控。

## 记忆钩子

**网关是门卫：统一入口、查身份、控人流、记日志。**
