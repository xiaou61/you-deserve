---
title: "Linux 文件描述符 fd 是什么？"
slug: "linux-file-descriptor"
category: "操作系统"
tags: ["Linux", "文件描述符", "网络连接", "操作系统"]
difficulty: "medium"
route: "Java 后端上岸路线"
scene: "OS 高频"
order: 3480
summary: "文件描述符是进程访问文件、Socket、管道等内核资源的整数句柄，数量不足会导致打开文件失败。"
---

## 一句话结论

文件描述符是进程用来引用内核资源的整数句柄，文件、Socket、管道在 Linux 里都可以通过 fd 操作。

## 通俗解释

它像寄存柜号码牌。东西放在柜子里，你手里拿的是号码牌，凭号码牌让系统帮你找到对应资源。

## 面试回答

Linux 中进程打开文件、建立网络连接、创建管道时，内核会返回一个文件描述符。应用后续读写时不是直接操作资源本身，而是通过 fd 让内核找到对应对象。

后端项目里常见相关问题：

- 连接数太多导致 fd 不够。
- 文件或连接没关闭导致 fd 泄漏。
- `Too many open files` 报错。
- 高并发网络服务需要关注 fd 限制。

可以通过 `ulimit`、`lsof`、`/proc/<pid>/fd` 等方式查看和排查。

## 常见追问

### 为什么 Socket 也是文件描述符？

Linux 把很多资源抽象成“文件”来统一处理，Socket 也可以通过 fd 进行读写。

### fd 泄漏怎么排查？

看进程 fd 数是否持续上涨，再用 `lsof` 或 `/proc/<pid>/fd` 观察是哪类资源没有释放。

## 易错点

- 不要把 fd 只理解成普通文件。
- 连接没关也会占 fd。
- 调大限制只是止血，泄漏仍要修。

## 记忆钩子

**fd 是资源号码牌，文件和连接都靠它找。**

## 图解提示

适合画结构图：进程 fd 表 -> 0/1/2 标准输入输出错误 -> 文件/Socket/管道。
