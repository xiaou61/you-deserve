---
title: "页面置换算法是什么？"
slug: "page-replacement"
category: "操作系统"
tags: ["操作系统", "虚拟内存", "页面置换", "LRU"]
difficulty: "medium"
route: "Java 后端上岸路线"
scene: "一面基础"
order: 1790
summary: "页面置换是在内存页不够时选择淘汰哪个页，常见算法有 FIFO、LRU、Clock 等。"
---

## 一句话结论

页面置换算法是在物理内存不够时，决定把哪个内存页换出到磁盘，为新页面腾空间。

## 通俗解释

内存像书桌，磁盘像书架。桌子放不下新书时，要决定把哪本书放回书架，页面置换算法就是这个选择规则。

## 面试回答

虚拟内存让程序看到更大的地址空间，但真正物理内存有限。当访问的页面不在内存中，会发生缺页，需要把页面调入内存。

如果内存没有空闲页，就要选择一个页面换出去。常见算法：

- FIFO：先进先出，最早进来的先淘汰。
- LRU：最近最少使用，淘汰最长时间没访问的页。
- Clock：近似 LRU，用访问位降低实现成本。

算法目标是尽量减少缺页次数，提高内存利用率。

## 常见追问

### LRU 为什么常见？

因为它利用了程序局部性原理：最近用过的数据，近期可能还会再用。

### 页面置换和缓存淘汰像不像？

很像，Redis、操作系统页缓存、应用缓存都会遇到“空间不够淘汰谁”的问题。

## 易错点

- 不要把页面置换和进程调度混为一谈。
- LRU 思想好，但精确实现成本可能较高。
- 页面频繁换入换出会导致性能严重下降。

## 记忆钩子

**桌子放不下，就按规则把旧书放回书架。**
