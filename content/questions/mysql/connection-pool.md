---
title: "数据库连接池为什么重要？"
slug: "connection-pool"
category: "MySQL"
tags: ["MySQL", "连接池", "性能", "HikariCP"]
difficulty: "medium"
route: "Java 后端上岸路线"
scene: "项目高频"
order: 3340
summary: "连接池复用数据库连接，控制并发访问数据库的数量，避免频繁建连和连接打爆数据库。"
---

## 一句话结论

数据库连接池通过复用连接降低建连成本，并用最大连接数控制应用对数据库的并发压力。

## 通俗解释

它像出租车站。乘客不用每次造一辆车，直接复用已有出租车；但车太多也会把道路堵死。

## 面试回答

连接池主要解决：

- 复用连接，减少频繁创建和关闭连接的开销。
- 控制最大连接数，避免应用把数据库连接打满。
- 管理连接健康，剔除失效连接。
- 提供等待队列和超时机制。

常见参数包括最大连接数、最小空闲连接、连接超时、空闲超时、最大生命周期。参数不是越大越好，要结合数据库承载能力、接口耗时和应用实例数计算。

## 常见追问

### 连接池开得越大 QPS 越高吗？

不一定。连接太多会增加数据库上下文切换、锁竞争和资源消耗，反而变慢。

### 连接泄漏是什么？

代码拿了连接没有归还，连接池可用连接越来越少，最终请求等待或超时。

## 易错点

- 不要只调大连接池解决慢 SQL。
- 总连接数要按实例数一起算。
- 连接超时和慢 SQL 要配合排查。

## 记忆钩子

**连接池是出租车站，车够用就好，不是越多越好。**

## 图解提示

适合画结构图：应用线程 -> 连接池 -> MySQL，展示最大连接数和等待队列。
