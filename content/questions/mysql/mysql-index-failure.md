---
title: "MySQL 索引为什么会失效？"
slug: "mysql-index-failure"
category: "MySQL"
tags: ["MySQL", "索引", "SQL优化", "Explain"]
difficulty: "medium"
route: "Java 后端上岸路线"
scene: "一面/二面高频"
order: 120
summary: "从函数计算、隐式转换、最左前缀、范围查询和低选择性字段理解索引失效。"
---

## 一句话结论

索引失效通常是因为查询条件破坏了索引的有序性，或者优化器判断使用索引不划算，所以最终没有走预期索引。

## 通俗解释

索引像一本排好序的目录。你按目录规则查，速度就很快；但如果你把每个目录项都先加工一遍再查，或者只给了后半截信息，目录就帮不上忙了。

比如索引列是手机号，你写 `where left(phone, 3) = '138'`，数据库要先对每一行手机号做函数计算，原本排好序的索引就很难直接使用。

## 面试回答

常见索引失效场景包括：

1. 对索引列使用函数或表达式，比如 `date(create_time)`。
2. 发生隐式类型转换，比如字符串字段用数字条件查询。
3. 联合索引不满足最左前缀原则。
4. 范围查询后面的联合索引字段可能无法继续用于定位。
5. 使用 `like '%xxx'` 这种左模糊匹配。
6. `or` 两边不是都有可用索引，可能导致整体不走索引。
7. 字段区分度太低，优化器认为全表扫描更划算。

最终是否走索引要用 `EXPLAIN` 看执行计划，而不是只凭感觉判断。

## 常见追问

### 索引失效是不是一定会全表扫描？

不一定。可能走其他索引，也可能走索引扫描但效率不高。面试里要区分“不走预期索引”和“完全全表扫描”。

### 怎么避免函数导致索引失效？

尽量把计算放到条件右侧，或者增加冗余字段、生成列、函数索引等。比如不要写 `date(create_time) = '2026-04-26'`，可以改成时间范围查询。

## 易错点

- 不要把所有慢查询都归因于索引失效。
- 不要忽略优化器成本选择，索引存在不代表一定会用。
- 不要只背规则，最好能配合 `EXPLAIN` 说明。

## 记忆钩子

**索引像目录：别改目录项，别跳开头，别只给后半截。**
