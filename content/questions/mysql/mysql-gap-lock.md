---
title: "MySQL 间隙锁是什么？解决什么问题？"
slug: "mysql-gap-lock"
category: "MySQL"
tags: ["MySQL", "InnoDB", "间隙锁", "事务"]
difficulty: "hard"
route: "Java 后端上岸路线"
scene: "二面高频"
order: 1560
summary: "间隙锁锁住索引记录之间的范围，主要用于防止幻读，但也可能带来锁等待和死锁。"
---

## 一句话结论

间隙锁锁的是索引记录之间的范围，不是某一条具体记录，主要用于在可重复读隔离级别下防止幻读。

## 通俗解释

记录锁像占住一个座位，间隙锁像把两个座位之间的过道也拦住，别人不能在这个范围里插新座位。

## 面试回答

InnoDB 在可重复读隔离级别下，为了解决当前读场景中的幻读问题，会使用 next-key lock，它可以理解为记录锁加间隙锁。

间隙锁锁住的是一个范围，防止其他事务在这个范围内插入新记录。

比如查询 `id between 10 and 20 for update`，数据库可能不仅锁住已有记录，还会锁住相关索引间隙，避免其他事务插入新的满足条件的数据。

间隙锁能增强一致性，但也可能导致锁范围变大，引发锁等待甚至死锁。

## 常见追问

### 这题最容易被追问的边界是什么？

重点说清索引失效、锁范围扩大、主从延迟、统计信息过期、深分页和 DDL 风险。不要只给结论，要说明哪些条件下结论成立，哪些条件下会退化或需要换方案。

### 怎么证明自己不是在背模板？

用EXPLAIN、慢日志、optimizer trace、锁等待、事务状态、redo/undo/binlog 和复制延迟做验证。能说出具体信号、反例或排查入口，答案就从概念层进入实战层。

### 和相近方案怎么区分？

可以拿索引优化、SQL 改写、分页改造、读写分离、分区分库或归档策略对比，从查询性能、锁影响、一致性、扩展性和回滚难度几个维度选择。

### 面试官继续深挖时怎么展开？

先围绕“InnoDB 在可重复读隔离级别下，为了解决当前读场景中的幻读问题，会使用 next-key lock，它可以理解为记录锁加间隙锁。”讲清主链路，再补“补：普通一致性快照读通常不加 gap lock”。如果能把边界条件、异常分支和验证闭环连起来，就能接住二面、三面的追问。

## 易错点

- 间隙锁锁的是范围，不是具体行。
- 它和隔离级别、索引、当前读关系很大。
- 线上死锁排查要看具体 SQL 和索引使用情况。

## 详细讲解

MySQL 间隙锁是什么？解决什么问题？ 这道题现在要从“能背出来”修到“能接住追问”。核心结论是：间隙锁锁住索引记录之间的范围，主要用于防止幻读，但也可能带来锁等待和死锁。回答时先把它放回MySQL语境，说明它解决什么矛盾，再围绕执行计划、索引事务、锁范围、统计指标和数据一致性展开。这样能避免泛泛而谈，也能让面试官听出你知道这题的真实边界。

第一步是拆清问题背景。记录锁像占住一个座位，间隙锁像把两个座位之间的过道也拦住，别人不能在这个范围里插新座位。在MySQL题里，背景不能写成空泛的工程套话，而要落到把 SQL、优化器、索引结构、事务隔离和存储引擎行为串起来。如果只说“看场景”或“加组件”，读者很难知道下一步该检查什么；如果能把输入、状态、依赖和输出说清，答案就有了主线。

第二步是讲机制。可以围绕这些点展开：InnoDB 在可重复读隔离级别下，为了解决当前读场景中的幻读问题，会使用 next-key lock，它可以理解为记录锁加间隙锁。；间隙锁锁住的是一个范围，防止其他事务在这个范围内插入新记录。；比如查询 id between 10 and 20 for update，数据库可能不仅锁住已有记录，还会锁住相关索引间隙，避免其他事务插入；间隙锁能增强一致性，但也可能导致锁范围变大，引发锁等待甚至死锁。。每个点都要回答三个问题：它在链路里的位置是什么，它改变了什么状态，失败或边界条件下会留下什么现象。把这三件事讲清楚，追问从概念跳到实战时就不会断。

第三步是补边界。这里尤其要主动说明索引失效、锁范围扩大、主从延迟、统计信息过期、深分页和 DDL 风险。间隙锁锁的是范围，不是具体行。；它和隔离级别、索引、当前读关系很大。；线上死锁排查要看具体 SQL 和索引使用情况。很多八股答案的问题不是方向错，而是没有说适用范围；一旦遇到数据规模变化、异常分支或版本差异，原来的结论就可能不成立。

第四步是补专项深度：补：普通一致性快照读通常不加 gap lock。这部分适合放在面试回答后半段，用来证明你不仅知道标准答案，也知道高频追问会往哪里挖。

最后收束成一套回答节奏：先给结论，再讲机制，再补边界，最后说验证方式。对这题来说，验证可以看EXPLAIN、慢日志、optimizer trace、锁等待、事务状态、redo/undo/binlog 和复制延迟；方案选择可以和索引优化、SQL 改写、分页改造、读写分离、分区分库或归档策略对比。这样既保留八股题的清晰度，也能把答案讲成可落地、可排查、可复盘的经验。

## 图解提示

适合画一张结构图：分析SQL条件 -> InnoDB 在可重复读隔离级别下 -> 间隙锁锁住的是一个范围 -> 明确一致性边界 -> 观察运行指标 -> 制定回滚方案。画面重点突出：MySQL 间隙锁是什么？解决什么问题？ 不是孤立概念，要把核心机制、边界风险、异常处理和验证证据放在同一张图里。

## 记忆钩子

**记录锁占座，间隙锁封路。**
