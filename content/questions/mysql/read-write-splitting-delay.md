---
title: "读写分离里的主从延迟怎么处理？"
slug: "read-write-splitting-delay"
category: "MySQL"
tags: ["MySQL", "读写分离", "主从延迟", "一致性"]
difficulty: "hard"
route: "Java 后端上岸路线"
scene: "数据库进阶"
order: 3350
summary: "读写分离提升读扩展能力，但主从延迟会导致刚写完读不到，需要按场景走主库、延迟检测或缓存兜底。"
---

## 一句话结论

读写分离会带来主从延迟，刚写完马上读的强一致场景要走主库或做一致性兜底。

## 通俗解释

它像老师刚在黑板改了答案，后排同学拍照上传到群里需要几秒。你立刻看群相册，可能还是旧答案。

## 面试回答

读写分离中，写请求进主库，读请求走从库。主库数据通过复制同步到从库，如果复制有延迟，就可能出现写后读不到。

常见处理方式：

- 写后短时间内读主库。
- 对强一致接口强制读主。
- 根据主从延迟监控动态切换。
- 关键数据使用缓存或版本号校验。
- 页面提示“处理中”，接受短暂最终一致。

不同业务容忍度不同。订单支付结果、余额这类不能随便读旧；浏览量、列表搜索可以接受短暂延迟。

## 常见追问

### 主从延迟怎么发现？

通过数据库复制延迟指标、心跳表、监控告警和业务侧不一致告警发现。

### 所有读都走主库不就解决了吗？

会失去读写分离扩展能力。应该只让强一致读走主库，普通读继续走从库。

## 易错点

- 不要只讲读写分离的好处，不讲延迟问题。
- 不要所有场景都强行读从库。
- 主从切换时也要考虑数据一致性。

## 记忆钩子

**读写分离怕“刚写就读”，强一致就读主。**

## 图解提示

适合画时序图：写主库 -> 复制延迟 -> 读从库旧数据 -> 强一致请求改读主库。
