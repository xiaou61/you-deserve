---
title: "如何手写 LRU 缓存？"
slug: "lru-cache"
category: "数据结构与算法"
tags: ["算法", "LRU", "哈希表", "双向链表"]
difficulty: "hard"
route: "Java 后端上岸路线"
scene: "手撕高频"
order: 600
summary: "用哈希表 + 双向链表实现 O(1) 查询、更新和淘汰。"
---

## 一句话结论

LRU 通常用哈希表 + 双向链表实现。哈希表负责 O(1) 定位节点，双向链表负责维护最近使用顺序。

## 通俗解释

LRU 像桌面只放有限本书。你刚看过的书放最前面，很久没看的书慢慢被挤到最后。桌面满了，就把最后那本最久没看的拿走。

哈希表让你快速找到书，链表让你快速调整书的位置。

## 面试回答

设计思路：

1. 哈希表 `Map<key, Node>` 存 key 到链表节点的映射。
2. 双向链表按最近使用顺序排列，头部最新，尾部最旧。
3. `get` 时，如果 key 存在，把节点移动到头部。
4. `put` 时，如果 key 存在，更新值并移动到头部；如果不存在，插入头部。
5. 超过容量时，删除尾部节点，并从 Map 中移除。

这样 `get` 和 `put` 都可以做到 O(1)。

## 常见追问

### 这题最容易被追问的边界是什么？

重点说清空输入、重复元素、极值、无解、越界、负数、溢出和复杂度退化。不要只给结论，要说明哪些条件下结论成立，哪些条件下会退化或需要换方案。

### 怎么证明自己不是在背模板？

用边界样例、时间复杂度、空间复杂度、单调性、递归深度和反例做验证。能说出具体信号、反例或排查入口，答案就从概念层进入实战层。

### 和相近方案怎么区分？

可以拿暴力解、排序、哈希、双指针、二分、堆、动态规划或图遍历对比，从正确性、复杂度、边界样例和实现难度几个维度选择。

### 面试官继续深挖时怎么展开？

先围绕“哈希表 Map 存 key 到链表节点的映射。”讲清主链路，再补“补 LinkedHashMap 必须使用 accessOrder=true，并重写 removeEldestEntry；列出四个函数 addFirst/removeNode/moveToHead/removeLast”。如果能把边界条件、异常分支和验证闭环连起来，就能接住二面、三面的追问。

## 易错点

- 不要只用数组或普通队列，删除中间节点会慢。
- 不要忘记更新已有 key 时也要移动到头部。
- 不要忘记容量超限时同步删除 Map。

## 详细讲解

如何手写 LRU 缓存？ 这道题现在要从“能背出来”修到“能接住追问”。核心结论是：用哈希表 + 双向链表实现 O(1) 查询、更新和淘汰。回答时先把它放回算法语境，说明它解决什么矛盾，再围绕输入规模、状态定义、边界样例和复杂度推导展开。这样能避免泛泛而谈，也能让面试官听出你知道这题的真实边界。

第一步是拆清问题背景。LRU 像桌面只放有限本书。你刚看过的书放最前面，很久没看的书慢慢被挤到最后。桌面满了，就把最后那本最久没看的拿走。 哈希表让你快速找到书，链表让你快速调整书的位置。在算法题里，背景不能写成空泛的工程套话，而要落到先给暴力思路，再说明为什么当前数据特征允许优化，最后落到状态、指针、堆、哈希或递归栈这些可验证结构。如果只说“看场景”或“加组件”，读者很难知道下一步该检查什么；如果能把输入、状态、依赖和输出说清，答案就有了主线。

第二步是讲机制。可以围绕这些点展开：哈希表 Map 存 key 到链表节点的映射。；双向链表按最近使用顺序排列，头部最新，尾部最旧。；get 时，如果 key 存在，把节点移动到头部。；put 时，如果 key 存在，更新值并移动到头部；如果不存在，插入头部。。每个点都要回答三个问题：它在链路里的位置是什么，它改变了什么状态，失败或边界条件下会留下什么现象。把这三件事讲清楚，追问从概念跳到实战时就不会断。

第三步是补边界。这里尤其要主动说明空输入、重复元素、极值、无解、越界、负数、溢出和复杂度退化。不要只用数组或普通队列，删除中间节点会慢。；不要忘记更新已有 key 时也要移动到头部。；不要忘记容量超限时同步删除 Map。很多八股答案的问题不是方向错，而是没有说适用范围；一旦遇到数据规模变化、异常分支或版本差异，原来的结论就可能不成立。

第四步是补专项深度：补 LinkedHashMap 必须使用 accessOrder=true，并重写 removeEldestEntry；列出四个函数 addFirst/removeNode/moveToHead/removeLast。这部分适合放在面试回答后半段，用来证明你不仅知道标准答案，也知道高频追问会往哪里挖。

最后收束成一套回答节奏：先给结论，再讲机制，再补边界，最后说验证方式。对这题来说，验证可以看边界样例、时间复杂度、空间复杂度、单调性、递归深度和反例；方案选择可以和暴力解、排序、哈希、双指针、二分、堆、动态规划或图遍历对比。这样既保留八股题的清晰度，也能把答案讲成可落地、可排查、可复盘的经验。

## 图解提示

适合画一张流程图：识别题型约束 -> 拆清缓存语义 -> 双向链表按最近使用顺序排列 -> 处理边界样例 -> 分析复杂度 -> 准备反例验证。画面重点突出：如何手写 LRU 缓存？ 不是孤立概念，要把核心机制、边界风险、异常处理和验证证据放在同一张图里。

## 记忆钩子

**LRU 两件套：Map 负责找得快，双链表负责挪得快。**
