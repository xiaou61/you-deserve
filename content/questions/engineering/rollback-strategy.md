---
title: "上线后出问题怎么回滚？"
slug: "rollback-strategy"
category: "工程化"
tags: ["工程化", "发布", "回滚", "稳定性"]
difficulty: "medium"
route: "Java 后端上岸路线"
scene: "项目上线追问"
order: 1780
summary: "回滚要提前设计版本、配置、数据库和数据兼容策略，不是事故发生后才临时想。"
---

## 一句话结论

回滚是上线异常时快速恢复服务的手段，要提前准备应用版本回滚、配置回滚、数据库兼容和数据补偿方案。

## 通俗解释

回滚像开车走错路时掉头。能不能安全掉头，取决于你是不是提前留了路口，而不是撞墙后才想办法。

## 面试回答

常见回滚策略：

- 应用回滚：保留上一版本镜像或包，快速切回。
- 配置回滚：配置中心保留历史版本，可一键恢复。
- 灰度发布：先让少量流量验证，出问题只影响小范围。
- 数据库兼容：新旧版本都能兼容同一段时间，避免表结构变更导致无法回滚。
- 数据补偿：对已经产生的错误数据做修复脚本或补偿任务。

真正难的是数据库和数据回滚，所以上线前要做兼容设计和回滚预案。

## 常见追问

### 数据库变更怎么支持回滚？

尽量采用向前兼容方案，比如先加字段不删字段，代码稳定后再清理旧字段。

### 灰度发布和回滚有什么关系？

灰度能减少问题影响面，发现问题后只需要回滚少量实例或流量。

## 易错点

- 不要只说重新部署旧包。
- 数据库结构和已写入数据最容易让回滚复杂化。
- 回滚预案要在上线前准备。

## 记忆钩子

**回滚不是后悔药，是上线前先铺好的退路。**
