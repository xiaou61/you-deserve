---
title: "Nginx 反向代理和负载均衡是什么？"
slug: "nginx-reverse-proxy"
category: "工程化"
tags: ["Nginx", "反向代理", "负载均衡", "部署"]
difficulty: "medium"
route: "Java 后端上岸路线"
scene: "项目高频"
order: 910
summary: "理解客户端访问 Nginx，由 Nginx 转发到后端服务并分摊流量。"
---

## 一句话结论

Nginx 反向代理是客户端访问 Nginx，由 Nginx 转发请求到后端服务；负载均衡是在多个后端实例之间分摊请求。

## 通俗解释

Nginx 像餐厅前台。客人只找前台，不直接找厨师。前台根据哪个厨师空闲，把订单分给后厨。

客人不需要知道后厨有几个人，也不需要知道具体谁做菜。

## 面试回答

反向代理的作用：

- 隐藏后端服务地址。
- 统一入口和域名。
- 做 TLS 终止、静态资源处理、压缩等。
- 转发请求到后端服务。

负载均衡的作用：

- 把请求分摊到多个服务实例。
- 提升吞吐和可用性。
- 后端实例异常时可以摘除。

常见策略包括轮询、权重、最少连接、IP hash 等。

## 常见追问

### 正向代理和反向代理区别？

正向代理代理客户端，比如访问外网；反向代理代理服务端，客户端不知道真实后端。

### Nginx 挂了怎么办？

要多实例部署，前面可用云负载均衡或 Keepalived 等方案保证高可用。

## 易错点

- 不要把反向代理和负载均衡完全等同。
- 不要忽略健康检查和高可用。
- 不要让客户端直接依赖后端实例地址。

## 记忆钩子

**Nginx 是前台：统一接客，分给后厨。**
