---
title: "服务器磁盘满了怎么排查？"
slug: "linux-disk-full"
category: "工程化"
tags: ["Linux", "磁盘", "日志", "排障"]
difficulty: "easy"
route: "Java 后端上岸路线"
scene: "线上排障"
order: 3160
summary: "磁盘满要先定位分区和大目录，再检查日志、临时文件、删除未释放和 inode 耗尽。"
---

## 一句话结论

磁盘满排查先看哪个分区满，再找大目录和大文件，同时检查日志滚动、已删除未释放文件和 inode 是否耗尽。

## 通俗解释

它像宿舍柜子塞满了。先看是哪个柜子满，再看是衣服太多、纸箱没扔，还是柜门卡住导致空位用不了。

## 面试回答

常见排查步骤：

- `df -h` 看哪个分区空间满。
- `du -sh *` 或按目录逐层定位大文件。
- 检查应用日志、Nginx 日志、临时文件、上传文件。
- 用 `df -i` 看 inode 是否耗尽。
- 检查删除但仍被进程占用的文件。

处理时不要直接乱删，要确认文件归属、是否可恢复、是否需要先压缩或迁移。长期方案是日志滚动、过期清理、磁盘监控和告警。

## 常见追问

### 文件删了为什么空间没释放？

如果文件仍被进程打开，目录项删了但进程还持有文件句柄，空间要等进程关闭句柄后才释放。

### inode 满是什么现象？

磁盘空间看起来还有，但因为小文件太多，inode 用完，系统无法创建新文件。

## 易错点

- 不要盲目删除未知目录。
- 不要只看 `df -h`，还要看 inode。
- 日志必须有滚动和保留策略。

## 记忆钩子

**磁盘满三查：空间、大文件、inode。**

## 图解提示

适合画流程图：df 定分区 -> du 找目录 -> 查日志/临时文件 -> 查 inode -> 查 deleted 文件。
