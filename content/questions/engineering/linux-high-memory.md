---
title: "线上内存持续上涨怎么排查？"
slug: "linux-high-memory"
category: "工程化"
tags: ["Linux", "内存", "JVM", "排障"]
difficulty: "hard"
route: "Java 后端上岸路线"
scene: "线上排障"
order: 3150
summary: "内存上涨要区分堆内、堆外、缓存和容器限制，再结合 GC、dump 和监控定位。"
---

## 一句话结论

内存持续上涨不能只看一个指标，要区分 JVM 堆内存、堆外内存、系统缓存、线程栈和容器内存限制。

## 通俗解释

它像水桶水位上涨。可能是桶里水真的变多，也可能是旁边海绵吸水、外壳变小，不能只盯着水面。

## 面试回答

排查路径可以这样说：

- 先看机器或容器总内存、RSS、swap、OOM 记录。
- 看 JVM 堆使用、GC 次数和 Full GC 后是否回落。
- 如果堆内存不回落，考虑对象泄漏，导出 heap dump 分析。
- 如果堆不高但进程内存高，排查堆外内存、DirectBuffer、线程数、native 内存。
- 容器环境要看 memory limit，避免 JVM 感知不准确或限额过小。

项目回答里要强调：先保留现场，再扩容止血，最后用 dump 和监控找根因。

## 常见追问

### Full GC 后内存不降说明什么？

可能有大量对象仍被引用，存在内存泄漏或缓存无上限，也可能关注的不是堆内存指标。

### dump 会不会影响线上？

可能会。大堆 dump 会造成停顿和磁盘压力，生产环境要评估时机，必要时先在副本或低峰处理。

## 易错点

- 不要把 Linux buff/cache 直接当成内存泄漏。
- 不要只看堆，堆外同样可能出问题。
- 不要在高峰期随意 dump 大内存进程。

## 记忆钩子

**内存排障先分桶：堆内、堆外、缓存、容器。**

## 图解提示

适合画结构图：总内存拆成 heap、direct memory、thread stack、native、page cache、container limit。
