---
title: "Docker 多阶段构建有什么用？"
slug: "docker-multi-stage-build"
category: "工程化"
tags: ["Docker", "镜像", "多阶段构建", "部署"]
difficulty: "medium"
route: "Java 后端上岸路线"
scene: "上线部署"
order: 3130
summary: "多阶段构建把编译环境和运行环境分离，减少最终镜像体积并降低安全暴露面。"
---

## 一句话结论

Docker 多阶段构建可以先在构建阶段编译产物，再只把运行需要的文件复制到最终镜像，从而减小体积、减少依赖和风险。

## 通俗解释

它像做蛋糕。厨房里需要烤箱、模具、面粉，但送到顾客手里只需要成品蛋糕，不需要把整个厨房搬过去。

## 面试回答

多阶段构建常见做法：

1. 第一阶段使用 Maven、Gradle、Node 等完整构建环境。
2. 执行编译、测试、打包。
3. 第二阶段使用更小的运行时基础镜像。
4. 只复制 jar、静态资源或可执行文件。

好处包括：最终镜像更小、构建依赖不进入生产镜像、镜像安全扫描风险更少、部署拉取更快。

## 常见追问

### Java 项目为什么也需要多阶段构建？

因为 Maven 仓库、源码、构建工具不一定需要出现在生产镜像里，最终通常只需要 JRE 和 jar 包。

### 镜像越小一定越好吗？

不是绝对。还要考虑可观测、排查工具、基础镜像安全维护和团队熟悉度。

## 易错点

- 不要把源码和构建缓存都塞进生产镜像。
- 不要忘记 `.dockerignore`。
- 不要为了极致小牺牲排查和兼容性。

## 记忆钩子

**构建像厨房，运行只交成品。**

## 图解提示

适合画流程图：源码 -> 构建镜像编译 -> 产物 jar -> 运行镜像 -> 部署。
