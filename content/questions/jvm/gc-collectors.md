---
title: 常见垃圾收集器怎么选？
slug: jvm-gc-collectors
category: JVM
tags:
  - Java
  - JVM
  - GC
  - 垃圾收集器
difficulty: medium
route: Java 后端上岸路线
scene: 二面高频
order: 230
summary: 从吞吐量、低延迟和默认选择理解 Serial、Parallel、G1、ZGC 等收集器。
---

## 一句话结论

垃圾收集器没有绝对最好，主要看目标：追求吞吐量可以看 Parallel，通用服务端常见 G1，追求低延迟可以关注 ZGC。

## 通俗解释

GC 像打扫房间。有人喜欢一次彻底大扫除，打扫时别人先别进来；有人喜欢边用边打扫，每次停顿短一点。不同收集器就是不同打扫策略。

## 面试回答

常见收集器可以这样理解：

- Serial：单线程收集器，简单，适合小内存或客户端场景。
- Parallel：并行收集器，关注吞吐量，适合后台计算任务。
- CMS：曾经用于低停顿场景，但已经逐渐退出历史舞台。
- G1：面向服务端的通用收集器，把堆划分为多个 Region，目标是可预测停顿。
- ZGC：低延迟收集器，适合大内存、低停顿要求场景。

回答时不要只背名字，要说选择维度：吞吐量、停顿时间、堆大小、业务类型和 JDK 版本。

## 常见追问

### JVM 题最容易讲得太空的地方在哪？

就是只讲术语，不讲观测。像「常见垃圾收集器怎么选」这种题，如果不顺手补一句会看什么日志、火焰图、JFR 或诊断命令，内容就容易飘。

### 排查时第一眼看什么？

先确认是 CPU、内存、停顿还是线程卡死，再决定去看 GC 日志、jstack、堆外内存、类加载还是编译行为。JVM 排查最怕一上来就盲打命令。

### 为什么同一个结论在不同线上环境会不一样？

因为堆大小、GC 选择、对象分配模式、容器限制和流量形态都在变。答题时主动提版本差异和运行参数，通常会显得更稳。

### 怎么讲得像真处理过线上问题？

别只背定义，可以直接说：如果 常见垃圾收集器怎么选 相关指标异常，我会先看哪几个信号，再判断是短期抖动还是结构性问题，最后再决定调参数、改代码还是换收集器。

## 易错点

- 不要说某个 GC 永远最好。
- 不要脱离 JDK 版本谈收集器。
- 不要把并行和并发混为一谈。

## 详细讲解

常见垃圾收集器怎么选？ 核心结论是：从吞吐量、低延迟和默认选择理解 Serial、Parallel、G1、ZGC 等收集器。回答时先把它放回JVM语境，说明它解决什么矛盾，再围绕运行时结构、参数开关、日志指标、排障工具和版本差异展开。

第一步是拆清问题背景。GC 像打扫房间。有人喜欢一次彻底大扫除，打扫时别人先别进来；有人喜欢边用边打扫，每次停顿短一点。不同收集器就是不同打扫策略。在JVM题里，背景不能写成空泛的工程套话，而要落到把内存区域、类加载、JIT、GC 或 safepoint 放到真实运行时链路里解释。如果只说“看场景”或“加组件”，读者很难知道下一步该检查什么；如果能把输入、状态、依赖和输出说清，答案就有了主线。

第二步是讲机制。可以围绕这些点展开：Serial：单线程收集器，简单，适合小内存或客户端场景。；Parallel：并行收集器，关注吞吐量，适合后台计算任务。；CMS：曾经用于低停顿场景，但已经逐渐退出历史舞台。；G1：面向服务端的通用收集器，把堆划分为多个 Region，目标是可预测停顿。。每个点都要回答三个问题：它在链路里的位置是什么，它改变了什么状态，失败或边界条件下会留下什么现象。把这三件事讲清楚，追问从概念跳到实战时就不会断。

第三步是补边界。这里尤其要主动说明堆外内存、容器限制、GC 停顿、类加载泄漏、JIT 退化和版本默认值变化。不要说某个 GC 永远最好。；不要脱离 JDK 版本谈收集器。；不要把并行和并发混为一谈。很多八股答案的问题不是方向错，而是没有说适用范围；一旦遇到数据规模变化、异常分支或版本差异，原来的结论就可能不成立。

第四步是补专项深度：补 Shenandoah、Epsilon 的定位，CMS 在 JDK 9 废弃/JDK 14 移除的版本边界，G1 默认版本差异，ZGC/Shenandoah 对 JDK 和平台的要求，以及选择前必须看 P99、分配速率、Full GC、CPU 的证据链。这部分适合放在面试回答后半段，用来证明你不仅知道标准答案，也知道高频追问会往哪里挖。

最后收束成一套回答节奏：先给结论，再讲机制，再补边界，最后说验证方式。对这题来说，验证可以看GC 日志、JFR、jcmd、jstack、jmap、NMT、CodeCache、Safepoint 和线程状态；方案选择可以和GC 参数、内存拆分、对象生命周期分析、类加载排查或运行时采样对比。这样既保留八股题的清晰度，也能把答案讲成可落地、可排查、可复盘的经验。

## 图解提示

适合画一张运行机制图：先放内存区或执行入口 -> 再标关键对象/线程 -> 展开 Serial：单线程收集器，简单，适合小内存或客户端场景 -> 补一个常见误判点 -> 标观测信号 -> 最后接排查动作。画的时候让“现象”和“底层位置”能对应起来。

## 记忆钩子

**选 GC 先问目标：要吞吐、要低停顿，还是要通用稳定。**
