---
title: 类加载过程有哪些阶段？
slug: jvm-class-loading
category: JVM
tags:
  - Java
  - JVM
  - 类加载
  - 双亲委派
difficulty: medium
route: Java 后端上岸路线
scene: 一面/二面高频
order: 220
summary: 按加载、验证、准备、解析、初始化理解类从字节码到可用的过程。
---

## 一句话结论

类加载大致包括加载、验证、准备、解析、初始化五个核心阶段，最终让 class 文件变成 JVM 中可使用的 Class 对象。

## 通俗解释

类加载像新人入职。先把简历拿进来，这是加载；核验身份和材料，这是验证；分配工位，这是准备；把通讯录里的名字对应到真人，这是解析；最后正式入职培训，这是初始化。

## 面试回答

类加载过程常见阶段是：

1. 加载：通过类名获取二进制字节流，并生成 Class 对象。
2. 验证：确保字节码格式、语义、安全性符合 JVM 要求。
3. 准备：为类变量分配内存并设置默认初始值。
4. 解析：把符号引用转换成直接引用。
5. 初始化：执行类构造器 `<clinit>`，给静态变量赋代码中的初始值，执行静态代码块。

面试里还经常追问双亲委派模型。它的核心是类加载器收到加载请求后，先交给父加载器尝试加载，父加载器加载不了再由自己加载。这样可以避免核心类被随意替换。

## 常见追问

### JVM 题最容易讲得太空的地方在哪？

就是只讲术语，不讲观测。像「类加载过程有哪些阶段」这种题，如果不顺手补一句会看什么日志、火焰图、JFR 或诊断命令，内容就容易飘。

### 排查时第一眼看什么？

先确认是 CPU、内存、停顿还是线程卡死，再决定去看 GC 日志、jstack、堆外内存、类加载还是编译行为。JVM 排查最怕一上来就盲打命令。

### 为什么同一个结论在不同线上环境会不一样？

因为堆大小、GC 选择、对象分配模式、容器限制和流量形态都在变。答题时主动提版本差异和运行参数，通常会显得更稳。

### 怎么讲得像真处理过线上问题？

别只背定义，可以直接说：如果 类加载过程有哪些阶段 相关指标异常，我会先看哪几个信号，再判断是短期抖动还是结构性问题，最后再决定调参数、改代码还是换收集器。

## 易错点

- 不要把准备阶段的默认值和代码赋值混为一谈。
- 不要只背阶段名，要能解释每一步做什么。
- 双亲委派不是所有场景都绝对遵守，比如 SPI 等场景会有打破。

## 详细讲解
类加载过程有哪些阶段？ 核心结论是：按加载、验证、准备、解析、初始化理解类从字节码到可用的过程。回答时先把它放回JVM语境，说明它解决什么矛盾，再围绕运行时结构、参数开关、日志指标、排障工具和版本差异展开。

第一步是拆清问题背景。类加载像新人入职。先把简历拿进来，这是加载；核验身份和材料，这是验证；分配工位，这是准备；把通讯录里的名字对应到真人，这是解析；最后正式入职培训，这是初始化。在JVM题里，背景不能写成空泛的工程套话，而要落到把内存区域、类加载、JIT、GC 或 safepoint 放到真实运行时链路里解释。如果只说“看场景”或“加组件”，读者很难知道下一步该检查什么；如果能把输入、状态、依赖和输出说清，答案就有了主线。

第二步是讲机制。可以围绕这些点展开：加载：通过类名获取二进制字节流，并生成 Class 对象。；验证：确保字节码格式、语义、安全性符合 JVM 要求。；准备：为类变量分配内存并设置默认初始值。；解析：把符号引用转换成直接引用。。每个点都要回答三个问题：它在链路里的位置是什么，它改变了什么状态，失败或边界条件下会留下什么现象。把这三件事讲清楚，追问从概念跳到实战时就不会断。

第三步是补边界。这里尤其要主动说明堆外内存、容器限制、GC 停顿、类加载泄漏、JIT 退化和版本默认值变化。不要把准备阶段的默认值和代码赋值混为一谈。；不要只背阶段名，要能解释每一步做什么。；双亲委派不是所有场景都绝对遵守，比如 SPI 等场景会有打破。很多八股答案的问题不是方向错，而是没有说适用范围；一旦遇到数据规模变化、异常分支或版本差异，原来的结论就可能不成立。

第四步是补专项深度：补主动/被动使用触发条件、父类初始化顺序、数组和编译期常量不触发初始化、ClassNotFoundException 与 NoClassDefFoundError 的区别。这部分适合放在面试回答后半段，用来证明你不仅知道标准答案，也知道高频追问会往哪里挖。

最后收束成一套回答节奏：先给结论，再讲机制，再补边界，最后说验证方式。对这题来说，验证可以看GC 日志、JFR、jcmd、jstack、jmap、NMT、CodeCache、Safepoint 和线程状态；方案选择可以和GC 参数、内存拆分、对象生命周期分析、类加载排查或运行时采样对比。这样既保留八股题的清晰度，也能把答案讲成可落地、可排查、可复盘的经验。

JVM 题真正好用的答法，一定会把概念和观测工具绑在一起。除了说明 按加载、验证、准备、解析、初始化理解类从字节码到可用的过程，还可以顺手补一句你会看哪些信号，例如 GC 日志、JFR、jstack、NMT、堆外内存曲线或暂停时间分布。用户真正需要的不是再听一遍术语定义，而是知道出问题时第一眼该看哪里。

## 图解提示

适合画一张运行机制图：先放内存区或执行入口 -> 再标关键对象/线程 -> 展开 加载：通过类名获取二进制字节流，并生成 Class 对象 -> 补一个常见误判点 -> 标观测信号 -> 最后接排查动作。画的时候让“现象”和“底层位置”能对应起来。

## 记忆钩子

**类加载像入职：拿材料、验身份、分工位、对人名、正式上岗。**
