---
title: "TCP 为什么需要三次握手？"
slug: "tcp-three-way-handshake"
category: "计算机网络"
tags: ["网络", "TCP", "三次握手", "计网"]
difficulty: "easy"
route: "Java 后端上岸路线"
scene: "一面高频"
order: 70
summary: "从双方收发能力确认和历史连接请求两个角度理解三次握手。"
---

## 一句话结论

TCP 三次握手是为了确认客户端和服务端的发送、接收能力都正常，并同步初始序列号，避免历史连接请求造成错误连接。

## 通俗解释

建立 TCP 连接像两个人打电话确认信号：

1. 客户端说：你能听到我吗？
2. 服务端说：我能听到你，你能听到我吗？
3. 客户端说：我也能听到你。

到这里，双方才都确认“我能发、我能收、对方也能发、对方也能收”。

## 面试回答

三次握手过程是：

1. 客户端发送 SYN，进入 SYN_SENT。
2. 服务端收到后返回 SYN + ACK，进入 SYN_RCVD。
3. 客户端收到后发送 ACK，双方连接建立。

三次握手的核心目的有两个：

- 确认双方的发送和接收能力。
- 交换并确认初始序列号，保证后续可靠传输。

如果只有两次握手，服务端收到客户端的 SYN 并回复后，就可能认为连接已经建立。但客户端不一定收到了服务端的响应，服务端就会浪费资源。更糟的是，历史延迟的 SYN 抵达服务端时，也可能误建立连接。

## 常见追问

### 为什么不是四次？

三次已经能完成双方收发能力确认和序列号同步。四次不是不能做，而是多余。

### 第三次握手可以携带数据吗？

理论上第三次 ACK 可以携带数据，但第一次和第二次通常不能携带普通应用数据，因为连接还没有正式建立。

## 易错点

- 不要只说“为了安全”，要具体说确认收发能力和同步序列号。
- 不要把三次握手和四次挥手的状态混在一起。
- 不要忽视历史连接请求这个经典原因。

## 记忆钩子

**三次握手的本质：你听得到我，我听得到你，双方都确认。**
