---
title: "TCP 和 UDP 有什么区别？"
slug: "tcp-udp"
category: "计算机网络"
tags: ["TCP", "UDP", "网络"]
difficulty: "easy"
route: "Java 后端上岸路线"
scene: "一面高频"
order: 1240
summary: "从连接、可靠性、顺序、开销和适用场景区分 TCP 与 UDP。"
---

## 一句话结论

TCP 面向连接、可靠、有序，开销更大；UDP 无连接、不保证可靠和顺序，开销更小，适合实时性要求高的场景。

## 通俗解释

TCP 像打电话，先建立连接，确认对方听到再说。UDP 像广播，直接喊出去，不保证每个人都听到。

## 面试回答

区别：

- TCP 面向连接，UDP 无连接。
- TCP 保证可靠传输，UDP 不保证。
- TCP 保证数据顺序，UDP 不保证。
- TCP 有流量控制和拥塞控制，UDP 更轻量。
- TCP 适合文件传输、网页、数据库连接等；UDP 适合直播、游戏、DNS、语音视频等。

## 常见追问

### UDP 一定不可靠吗？

UDP 本身不保证可靠，但应用层可以自己实现确认、重传等机制，比如 QUIC 就基于 UDP 实现可靠传输。

### DNS 为什么常用 UDP？

查询通常短小，UDP 开销低、速度快。响应过大或区域传送等场景也可能使用 TCP。

## 易错点

- 不要说 UDP 完全没用。
- 不要说 TCP 一定更快。
- 不要忽略应用层可以基于 UDP 做可靠机制。

## 记忆钩子

**TCP 像电话，UDP 像广播。**
