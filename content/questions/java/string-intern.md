---
title: "String intern 是什么？"
slug: "string-intern"
category: "Java 基础"
tags: ["Java", "String", "intern", "字符串常量池"]
difficulty: "hard"
route: "Java 后端上岸路线"
scene: "二面加分"
order: 2280
summary: "intern 会尝试把字符串放入字符串常量池，并返回常量池中的引用。"
---

## 一句话结论

`intern()` 会返回字符串常量池中对应字符串的引用，如果常量池中没有，则尝试把当前字符串加入常量池。

## 通俗解释

字符串常量池像公共字典。相同的词没必要每个人都复印一份，大家指向字典里的同一个词条即可。

## 面试回答

Java 为了复用字符串，会维护字符串常量池。字面量字符串通常会进入常量池。

调用 `intern()` 时：

- 如果池中已有相同内容的字符串，返回池中引用。
- 如果没有，则把字符串放入池中或记录引用，再返回池中引用。

它可以减少重复字符串内存占用，但不应该在业务里盲目调用，因为常量池本身也占内存，滥用可能带来性能和内存问题。

## 常见追问

### new String("abc") 创建几个对象？

可能涉及常量池中的 "abc" 和堆上的 String 对象，具体还要看常量池里是否已有该字面量。

### intern 适合什么场景？

适合大量重复字符串且生命周期可控的场景，但需要压测和内存评估。

## 易错点

- 不要把 intern 当成默认优化手段。
- 字符串常量池也会占用内存。
- 不同 JDK 版本实现细节可能有差异，面试重点讲原理即可。

## 记忆钩子

**intern 就是去公共字典里找同一个词条。**
