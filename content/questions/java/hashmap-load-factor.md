---
title: "HashMap 负载因子为什么默认是 0.75？"
slug: "hashmap-load-factor"
category: "Java 基础"
tags: ["Java", "HashMap", "负载因子", "集合"]
difficulty: "medium"
route: "Java 后端上岸路线"
scene: "一面/二面高频"
order: 1830
summary: "0.75 是空间利用率和哈希冲突概率之间的折中，太小浪费空间，太大冲突增多。"
---

## 一句话结论

HashMap 默认负载因子 0.75 是空间利用率和查询性能之间的折中，太小浪费内存，太大容易冲突。

## 通俗解释

储物柜不能刚放一半就换大柜，也不能塞到快爆才换。0.75 就像柜子用到七八成时扩容，比较均衡。

## 面试回答

负载因子决定 HashMap 什么时候扩容：

```text
threshold = capacity * loadFactor
```

默认容量 16、负载因子 0.75 时，阈值是 12。元素超过阈值后会扩容。

如果负载因子太小，扩容更频繁，空间浪费更多。如果负载因子太大，数组利用率更高，但哈希冲突概率上升，链表或红黑树上的查找成本可能增加。

所以 0.75 是一个经验折中。

## 常见追问

### 可以自己设置负载因子吗？

可以，但大多数业务不建议随意改。更常见的是根据预估数据量设置合适初始容量。

### 初始容量为什么重要？

容量设置太小会频繁扩容，扩容需要迁移元素，成本较高。

## 易错点

- 负载因子不是越大越好。
- 扩容阈值不是固定值，而是容量乘负载因子。
- 优化 HashMap 常先设置初始容量，而不是乱改负载因子。

## 记忆钩子

**0.75 是柜子七八成满就换大柜。**
