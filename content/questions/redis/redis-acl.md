---
title: Redis ACL 解决什么问题？
slug: redis-acl
category: Redis
tags:
  - Redis
  - ACL
  - 安全
difficulty: medium
route: Java 后端上岸路线
scene: 进阶追问
order: 4530
summary: Redis ACL 用于按用户控制命令、key 范围和权限，提升访问安全性。
---
## 一句话结论

Redis 6 引入 ACL 后，不再只能依赖单一 requirepass，可以为不同客户端分配不同权限。

## 通俗解释

ACL 像门禁卡，不同人员只能进不同房间、使用不同设备。

## 面试回答

可以从这几层回答：

- 可以创建多个用户并设置密码。
- 可限制允许执行的命令类别或具体命令。
- 可用 key pattern 限制访问 key 范围。
- 适合多应用共享 Redis 或需要最小权限控制的场景。

## 常见追问

### Redis ACL 和 requirepass 有什么区别？

requirepass 更像全局密码，ACL 可以按用户限制命令、key pattern、频道和密码，更适合多应用共享实例。

### 怎么禁止危险命令？

可以在 ACL 中去掉危险命令权限，例如 `-FLUSHALL`、`-CONFIG`、`-KEYS`，也可以按命令类别控制。

### key pattern 能完全保证多租户隔离吗？

不能。它依赖 key 命名规范，强隔离场景更适合独立实例或独立集群。

### 密码轮换怎么做更稳？

先给用户增加新密码，发布客户端使用新密码，确认连接稳定后再删除旧密码，并观察 ACL LOG 和连接错误率。

## 易错点

- 所有服务共用 default 用户和同一个密码。
- 只做网络隔离，不做命令权限约束。

## 详细讲解

Redis ACL 解决什么问题 这类题如果只背命令或数据结构名字，面试官一般两句就能把答案追空。更稳的讲法是先把它放回真实请求链里：请求从哪里进来，Redis 在中间承担什么职责，命中以后省掉了什么成本，失效以后又会把压力推给谁。先用一句话压住主题，比如 Redis ACL 用于按用户控制命令、key 范围和权限，提升访问安全性，然后马上往读写路径和状态变化上落。

主线部分要尽量讲成“数据怎么流动”，而不是“组件会什么”。可以围绕“可以创建多个用户并设置密码”往下展开，说明 key 是怎么设计的，命令是在什么时机发出的，客户端和服务端各自看到了什么状态，哪些结果是即时生效，哪些又要依赖复制、淘汰、TTL 或集群路由。Redis 题只要能把这条路径讲清楚，内容就已经比单纯背 API 厚很多。

真正容易翻车的地方通常不在 happy path，而在边界被放大以后。这里最好主动把 所有服务共用 default 用户和同一个密码 这类风险摆出来，再补一句它在线上会表现成什么现象，比如热点打满单核、TTL 抖动、主从短暂不一致、锁误删、缓存穿透后数据库被打穿，或者集群迁移时客户端不断重试。读者复习时最有价值的，往往就是这些“会怎么坏”的细节。

继续深挖时，问题通常会落到“Redis ACL 和 requirepass 有什么区别？”和“怎么禁止危险命令？”这两个方向上。这时不要只说“看监控”，而是直接讲证据：会看哪些 key 模式、TTL 分布、命中率、慢日志、错误码、主从延迟、slot 迁移状态、客户端重试次数，必要时再配合业务日志和对账结果。能把观察信号说清楚，才像真的排过线上问题。

Redis 题还有一个很值钱的层次，是把取舍说出来。很多方案不是“能不能做”，而是“这样做会把复杂度和风险转移到哪里”。比如为了换吞吐量牺牲一部分一致性，或者为了更低延迟接受一段时间的数据不新鲜；再比如为了省一次数据库访问，引入了更复杂的过期、补偿或旁路读逻辑。把这些取舍讲出来，答案就从知识点变成工程判断。

Redis ACL 解决什么问题 真正像项目经验的讲法，通常是“现象 -> 判断 -> 动作 -> 指标回稳”。先说你在什么症状下会怀疑它，再说如何缩小到缓存层，接着说明最小止血动作是什么，例如隔离热点、降级旁路、限流、延迟重试、预热回补或人工修复，最后补一句靠哪些指标判断问题真的被收住。这样读者拿去答项目追问时会更顺手。

最后收口时，不要把 Redis ACL 解决什么问题 讲成孤立的 Redis 技巧，而要把它放回整个系统：它解决了什么矛盾，最怕哪类边界，验证抓手是什么，替代方案为什么没选。只要这四件事讲顺，面试官基本就能判断你不是只会背名词，而是知道它在系统里到底怎么活。

## 图解提示

适合画一张 Redis 数据流图：请求进入 -> 执行 可以创建多个用户并设置密码 -> 再经过 可限制允许执行的命令类别或具体命令 -> 状态写回或返回客户端 -> 补上 所有服务共用 default 用户和同一个密码 -> 最后标 Redis ACL 和 requirepass 有什么区别 对应的观测点。画的时候让“命令、状态、风险”三件事能一一对应。

## 记忆钩子

**Redis ACL 做最小权限：用户、命令、key 范围一起控。**
