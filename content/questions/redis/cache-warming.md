---
title: "缓存预热是什么？什么时候需要？"
slug: "cache-warming"
category: "Redis"
tags: ["Redis", "缓存预热", "高并发", "性能"]
difficulty: "easy"
route: "Java 后端上岸路线"
scene: "缓存高频"
order: 3360
summary: "缓存预热是在流量到来前提前把热点数据加载进缓存，避免冷启动时请求打爆数据库。"
---

## 一句话结论

缓存预热是在系统启动、活动开始或大流量到来前，提前把热点数据加载进缓存，减少冷启动压力。

## 通俗解释

它像饭店开门前先备菜。客人来了再洗菜切菜就太慢，提前准备好才能扛住饭点。

## 面试回答

缓存预热常见场景：

- 秒杀活动开始前加载商品和库存信息。
- 首页热门内容提前缓存。
- 系统重启后恢复热点配置和字典数据。
- 大促前预加载高频查询结果。

预热方式可以是启动任务、定时任务、运营触发、消息触发。要注意控制速度，避免预热本身打爆数据库，也要设置过期时间和版本。

## 常见追问

### 所有数据都要预热吗？

不需要。只预热高频、核心、可预测的热点数据，否则成本太高。

### 预热失败怎么办？

要有重试、告警和降级策略，必要时活动入口限流。

## 易错点

- 不要全量无脑预热。
- 预热也要限速。
- 预热数据要考虑过期和更新。

## 记忆钩子

**饭点前先备菜，流量前先热缓存。**

## 图解提示

适合画流程图：活动配置 -> 预热任务 -> 查数据库 -> 写 Redis -> 流量进入读缓存。
