---
title: 缓存击穿为什么常用互斥锁解决？
slug: cache-breakdown-mutex
category: Redis
tags:
  - Redis
  - 缓存击穿
  - 互斥锁
  - 热点 key
difficulty: medium
route: Java 后端上岸路线
scene: 缓存高频
order: 3370
summary: 热点 key 过期瞬间大量请求回源会打爆数据库，互斥锁让只有一个请求重建缓存。
---
## 一句话结论

缓存击穿是热点 key 失效瞬间大量请求同时打到数据库，互斥锁方案让只有一个请求去查库并重建缓存。

## 通俗解释

它像食堂热门菜刚卖完，所有人都冲进后厨催。互斥锁就是只让一个人去催菜，其他人在窗口等一会。

## 面试回答

处理流程：

1. 请求查询缓存未命中。
2. 尝试获取互斥锁。
3. 获取成功的请求查数据库并写回缓存。
4. 获取失败的请求短暂等待后重试缓存。
5. 如果数据库也没有数据，要缓存空值防穿透。

互斥锁可以用 Redis `SET NX EX` 实现，但要设置过期时间，避免锁不释放。热点 key 还可以配合逻辑过期、永不过期加异步刷新等方案。

## 常见追问

### Redis 题最容易在哪个边界讲虚？

最常见的是只讲命令，不讲热点、TTL、内存和主从/集群边界。像「缓存击穿为什么常用互斥锁解决」这种题，如果不把这些现实约束补出来，就很像背命令手册。

### 排查时你会先看哪些指标或命令？

通常先看命中率、慢日志、INFO、内存碎片、热点 key、迁移状态、复制延迟和错误码，再决定是数据结构选错了，还是流量形态变了。

### 为什么本地 demo 看起来都正常，线上却会出事？

因为本地没有热点集中、没有大 key、没有主从切换、也没有 TTL 抖动。一到线上，这些边界会同时出现。

### 怎么把这题讲得更像做过线上缓存？

别只说 Redis 会什么，最好顺手补一句：如果 缓存击穿为什么常用互斥锁解决 出问题，我会先看哪几个指标、怎么止血、之后怎么防再发。这样就很像真实经验。

## 易错点

- 锁必须有过期时间。
- 等待请求不要无限阻塞。
- 数据库空结果也要处理。

## 详细讲解

缓存击穿为什么常用互斥锁解决 这类题如果只背命令或数据结构名字，面试官一般两句就能把答案追空。更稳的讲法是先把它放回真实请求链里：请求从哪里进来，Redis 在中间承担什么职责，命中以后省掉了什么成本，失效以后又会把压力推给谁。先用一句话压住主题，比如 热点 key 过期瞬间大量请求回源会打爆数据库，互斥锁让只有一个请求重建缓存，然后马上往读写路径和状态变化上落。

主线部分要尽量讲成“数据怎么流动”，而不是“组件会什么”。可以围绕“请求查询缓存未命中”往下展开，说明 key 是怎么设计的，命令是在什么时机发出的，客户端和服务端各自看到了什么状态，哪些结果是即时生效，哪些又要依赖复制、淘汰、TTL 或集群路由。Redis 题只要能把这条路径讲清楚，内容就已经比单纯背 API 厚很多。

真正容易翻车的地方通常不在 happy path，而在边界被放大以后。这里最好主动把 锁必须有过期时间 这类风险摆出来，再补一句它在线上会表现成什么现象，比如热点打满单核、TTL 抖动、主从短暂不一致、锁误删、缓存穿透后数据库被打穿，或者集群迁移时客户端不断重试。读者复习时最有价值的，往往就是这些“会怎么坏”的细节。

继续深挖时，问题通常会落到“Redis 题最容易在哪个边界讲虚？”和“排查时你会先看哪些指标或命令？”这两个方向上。这时不要只说“看监控”，而是直接讲证据：会看哪些 key 模式、TTL 分布、命中率、慢日志、错误码、主从延迟、slot 迁移状态、客户端重试次数，必要时再配合业务日志和对账结果。能把观察信号说清楚，才像真的排过线上问题。

Redis 题还有一个很值钱的层次，是把取舍说出来。很多方案不是“能不能做”，而是“这样做会把复杂度和风险转移到哪里”。比如为了换吞吐量牺牲一部分一致性，或者为了更低延迟接受一段时间的数据不新鲜；再比如为了省一次数据库访问，引入了更复杂的过期、补偿或旁路读逻辑。把这些取舍讲出来，答案就从知识点变成工程判断。

缓存击穿为什么常用互斥锁解决 真正像项目经验的讲法，通常是“现象 -> 判断 -> 动作 -> 指标回稳”。先说你在什么症状下会怀疑它，再说如何缩小到缓存层，接着说明最小止血动作是什么，例如隔离热点、降级旁路、限流、延迟重试、预热回补或人工修复，最后补一句靠哪些指标判断问题真的被收住。这样读者拿去答项目追问时会更顺手。

最后收口时，不要把 缓存击穿为什么常用互斥锁解决 讲成孤立的 Redis 技巧，而要把它放回整个系统：它解决了什么矛盾，最怕哪类边界，验证抓手是什么，替代方案为什么没选。只要这四件事讲顺，面试官基本就能判断你不是只会背名词，而是知道它在系统里到底怎么活。

## 图解提示

适合画一张时序图：客户端尝试加锁 -> Redis 执行 请求查询缓存未命中 -> 业务线程持锁执行业务 -> 释放时用 Lua 校验 token -> 补上 锁必须有过期时间 -> 最后标 Redis 题最容易在哪个边界讲虚 对应的日志和指标。这样画能把“谁拿锁、谁删锁、哪里会误删”一眼看清。

## 记忆钩子

**热点失效别一起冲，一个人重建，其他人等。**
