---
title: 缓存穿透、击穿、雪崩有什么区别？
slug: redis-cache-penetration-breakdown-avalanche
category: Redis
tags:
  - Redis
  - 缓存
  - 高并发
  - 系统设计
difficulty: medium
route: Java 后端上岸路线
scene: 二面高频
order: 60
summary: 用请求如何打到数据库来区分三类缓存问题。
---
## 一句话结论

缓存穿透是查不存在的数据，缓存击穿是热点 key 失效，缓存雪崩是大量 key 同时失效或 Redis 整体不可用。它们的共同结果都是请求打到数据库，但原因不一样。

## 通俗解释

把缓存想成数据库门口的前台：

- 穿透：有人一直问“根本不存在的人住哪”，前台查不到，只能每次都问后面的数据库。
- 击穿：所有人都问同一个大明星，刚好这位大明星的信息过期了，瞬间全挤到数据库。
- 雪崩：前台大片资料同一时间过期，或者前台直接停工，所有请求都冲向数据库。

## 面试回答

缓存穿透通常是请求查询不存在的数据，缓存和数据库都没有结果。如果不处理，每次请求都会绕过缓存打到数据库。常见方案是缓存空值、布隆过滤器、参数校验。

缓存击穿是某个热点 key 在高并发下突然过期，大量请求同时访问数据库。常见方案是互斥锁、逻辑过期、热点数据不过期。

缓存雪崩是大量 key 同一时间过期，或者 Redis 集群不可用，导致大量请求直接打到数据库。常见方案是过期时间加随机值、缓存预热、多级缓存、限流降级、Redis 高可用。

## 常见追问

### 布隆过滤器能彻底解决穿透吗？

不能。布隆过滤器有误判，且容量超过设计值后误判率会上升，所以还要配合空值缓存、限流和参数校验。

### 互斥锁解决击穿有什么坑？

要设置锁过期时间，获取锁后再双重检查缓存，失败请求要等待、降级或返回旧值，避免锁本身成为瓶颈。

### 雪崩只靠 TTL 随机化够吗？

不够。TTL 抖动只能解决集中失效，还要考虑 Redis 故障、高可用、限流、熔断、降级和数据库保护。

### 这些方案如何验证？

看缓存命中率、DB QPS、热点 key 访问量、接口 P99、错误率和降级命中量，并用压测模拟 key 过期和 Redis 异常。

## 易错点

- 不要把击穿和雪崩混在一起。击穿通常是单个热点 key，雪崩是大面积问题。
- 不要只说“加锁”，还要考虑锁粒度、超时和兜底。
- 不要忽视限流和降级，缓存事故本质上是系统保护问题。

## 详细讲解

缓存穿透、击穿、雪崩有什么区别 这类题如果只背命令或数据结构名字，面试官一般两句就能把答案追空。更稳的讲法是先把它放回真实请求链里：请求从哪里进来，Redis 在中间承担什么职责，命中以后省掉了什么成本，失效以后又会把压力推给谁。先用一句话压住主题，比如 用请求如何打到数据库来区分三类缓存问题，然后马上往读写路径和状态变化上落。

主线部分要尽量讲成“数据怎么流动”，而不是“组件会什么”。可以围绕“用请求如何打到数据库来区分三类缓存问题”往下展开，说明 key 是怎么设计的，命令是在什么时机发出的，客户端和服务端各自看到了什么状态，哪些结果是即时生效，哪些又要依赖复制、淘汰、TTL 或集群路由。Redis 题只要能把这条路径讲清楚，内容就已经比单纯背 API 厚很多。

真正容易翻车的地方通常不在 happy path，而在边界被放大以后。这里最好主动把 不要把击穿和雪崩混在一起。击穿通常是单个热点 key，雪崩是大面积问题 这类风险摆出来，再补一句它在线上会表现成什么现象，比如热点打满单核、TTL 抖动、主从短暂不一致、锁误删、缓存穿透后数据库被打穿，或者集群迁移时客户端不断重试。读者复习时最有价值的，往往就是这些“会怎么坏”的细节。

继续深挖时，问题通常会落到“布隆过滤器能彻底解决穿透吗？”和“互斥锁解决击穿有什么坑？”这两个方向上。这时不要只说“看监控”，而是直接讲证据：会看哪些 key 模式、TTL 分布、命中率、慢日志、错误码、主从延迟、slot 迁移状态、客户端重试次数，必要时再配合业务日志和对账结果。能把观察信号说清楚，才像真的排过线上问题。

Redis 题还有一个很值钱的层次，是把取舍说出来。很多方案不是“能不能做”，而是“这样做会把复杂度和风险转移到哪里”。比如为了换吞吐量牺牲一部分一致性，或者为了更低延迟接受一段时间的数据不新鲜；再比如为了省一次数据库访问，引入了更复杂的过期、补偿或旁路读逻辑。把这些取舍讲出来，答案就从知识点变成工程判断。

缓存穿透、击穿、雪崩有什么区别 真正像项目经验的讲法，通常是“现象 -> 判断 -> 动作 -> 指标回稳”。先说你在什么症状下会怀疑它，再说如何缩小到缓存层，接着说明最小止血动作是什么，例如隔离热点、降级旁路、限流、延迟重试、预热回补或人工修复，最后补一句靠哪些指标判断问题真的被收住。这样读者拿去答项目追问时会更顺手。

最后收口时，不要把 缓存穿透、击穿、雪崩有什么区别 讲成孤立的 Redis 技巧，而要把它放回整个系统：它解决了什么矛盾，最怕哪类边界，验证抓手是什么，替代方案为什么没选。只要这四件事讲顺，面试官基本就能判断你不是只会背名词，而是知道它在系统里到底怎么活。

## 图解提示

适合画一张缓存访问图：请求先查缓存 -> 未命中时回源数据库 -> 回填或删缓存发生在何处 -> 热点/并发放大问题出现在哪一步 -> 补上 不要把击穿和雪崩混在一起。击穿通常是单个热点 key，雪崩是大面积问题 -> 最后标 布隆过滤器能彻底解决穿透吗。这样画最容易讲清楚“为什么数据库会突然被打穿”。

## 记忆钩子

**穿透是假人，击穿是明星，雪崩是前台集体下班。**
